# A15 probe — deliberate break, removed before merge

This file exists for one push only, to prove the new `ID cross-references`
lint step actually fails a PR rather than passing vacuously. It is deleted in
the next commit on this branch.

The line below names a row that does not exist and never will:

**Z9** is the blocker here, and this run is blocked on Z9 until it lands.
