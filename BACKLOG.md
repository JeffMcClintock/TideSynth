# TIDE Synth — Backlog

Ordered. A weekly agent obeys the **NEXT block below first**; only when its
platform's NEXT item is ineligible does it fall back to the topmost item that
is not blocked and matches its machine's platform role. One item, then stop.

## NEXT — per-platform priority, obeyed before file order

| Platform | Take | Why |
|---|---|---|
| win | **C12f** — and first, someone with the authority to touch `SynthEditLib` should fix [#111](https://github.com/JeffMcClintock/TideSynth/issues/111) | **This box does not build `main` as of 2026-08-17, and the break is not TIDE's.** `SynthEditLib/SeAudioMaster.cpp:548,805` fail under MSVC on `MidiIn` — `class MidiIn* midiInModule` at `SeAudioMaster.h:609` plus `dynamic_cast<class MidiIn*>`, both accepted by Clang, both rejected by MSVC, from [SynthEditLib#16](https://github.com/JeffMcClintock/SynthEditLib/pull/16) (`e14970e`). Filed with a suggested one-line fix as [#111](https://github.com/JeffMcClintock/TideSynth/issues/111) and **not fixed**, because `SynthEditLib` is GATED and this is not a carve-out stage — the same STEP 1 / STEP 5 contradiction the linux box recorded in [#87](https://github.com/JeffMcClintock/TideSynth/issues/87) hours earlier. **Two `platform:*` breaks in GATED shared code in one day; that pattern wants a ruling from Jeff, not a third run's judgement.** **C12c is IN-REVIEW** ([SynthEdit#41](https://github.com/JeffMcClintock/SynthEdit/pull/41) + [SynthEditLib#18](https://github.com/JeffMcClintock/SynthEditLib/pull/18)), so this row moves on: **C12f** is the remaining large stage and the one that takes `${EDITOR_DIR}` to zero and unblocks C6 — ten entries, 6,298 lines, atomic, because `PatchManager`/`PatchParameter`/`PatchParameter_host_generated`/`UG2` are a strongly-connected component. **Measure against the new baseline: 935/935, 92/92, and 21 dangling edges** — not the 904/904 and 45 this row carried before; the ninja edge count rose from upstream growth in `SynthEditLib`, and the dangling total is what C12c left. Use [scripts/dangling_private_includes.py](scripts/dangling_private_includes.py) and compare deltas from that one script. **A run taking C12f before #111 is fixed must do what C12c did** — verify on top of `d96edbb` and say so — or it cannot build at all. **Do not take C12d** (`linux`: its only real risk is a GNU-ld rescan group invisible to MSVC). **Do not take C6** — `BLOCKED(C12f)`. **Fallback: P3** — `win`, and none of C12's files includes `afxres.h`, so C12 does not worsen it. **Do not fall back to S1b/S5/S7/S8** (GATED-in-full) or **A12/B1** (`.github/workflows/**`, which the bot token structurally cannot push). |
| mac | **S11 restore check — measure `/tmp/tide-persist3.rpp` reopening before writing any code** | **2026-08-17 (session end): the rack is now IN the saved project.** Blob parameters are base64'd into presets ([GMPI#3](https://github.com/JeffMcClintock/GMPI/pull/3) + [SynthEditLib#17](https://github.com/JeffMcClintock/SynthEditLib/pull/17), **merge together**): saved VST state went 250 → 1503 bytes with the document visible in the `.rpp`. Non-stateful parameters are now skipped — required, because TIDE's `controllerPtr` blob holds a raw pointer and restoring it blanked the editor (A/B-proven). **Next: reopen a saved project and see whether the rack comes back** — the reader decodes base64 now, so measure before coding. Then the faded swap and preset retention. Small items: Windows/Linux clipboard for Copy link (**D6**), win box's two **U2e** items. **Build note (also in Claude's memory):** this box uses `GMPI_SDK_FOLDER_OVERRIDE`, `GMPI_UI_FOLDER_OVERRIDE` and `SYNTHEDITLIB_FOLDER_OVERRIDE` pointed at the local checkouts per Jeff — the local clones are the truth, and FetchContent will not re-pull once `_deps` is populated. **Do not fall back to E2a** without the check its row demands, nor to **S1b/S5/S7/S8** (GATED-in-full) or **A12/B1** (`.github/workflows/**`). |
| linux | **Two `platform:linux` issues, [#87](https://github.com/JeffMcClintock/TideSynth/issues/87) and [#88](https://github.com/JeffMcClintock/TideSynth/issues/88) — but a scheduled run cannot take either.** Next takeable item: **P10** | **The linux box does NOT build `main` as of 2026-08-17, and both breaks are in GATED paths, which is the whole problem with this row.** [#87](https://github.com/JeffMcClintock/TideSynth/issues/87): `SynthEditLib/modules/se_sdk3_hosting/ModuleView.cpp:621-633` carries a U2d debug trace whose own comment says *"TEMPORARY … do not commit"*, committed via [SynthEditLib#12](https://github.com/JeffMcClintock/SynthEditLib/pull/12). Its `#include <cstdio>`/`<cstdarg>` sit **inside `namespace SE2`**, creating `SE2::std`, so GCC rejects every later `std::` in the file (30+ errors from `:696`); it is invisible on mac/win only because those toolchains already included both headers, making the lines no-ops. **It also `fopen`s `/tmp/tide-skin-debug.log` unconditionally in Release, in the shared library — constraints 3 and 4, in SynthEdit's code as well as TIDE's.** One revert fixes both. [#88](https://github.com/JeffMcClintock/TideSynth/issues/88): `SynthEditWayland` fails to link on `doDialogConnectUg`/`doDialogPatchManager` — **C12e** added `Dialogs_editor2.cpp` to `SynthEditCL` and `SynthEdit2.vcxproj` but missed `SynthEditWayland` and `SynthEditJuce`, neither of which is generated on Windows where C12e was verified. One line each. **STEP 1 says a broken build outranks everything and a scheduled run must fix it; STEP 5 says `SynthEditLib` and both those app folders are GATED. Those two rules contradict each other here, and the run that found this obeyed STEP 5.** Until Jeff or an interactive session clears them, the linux box can build TIDE only with a local workaround for #87. **S3 is IN-REVIEW** ([SynthEdit#32](https://github.com/JeffMcClintock/SynthEdit/pull/32)); its remaining half is **S3g**, which is GATED and NEEDS-JEFF. **P10** is the next thing this box can actually finish — `any`, ALLOWED (`SE16/SynthEditSem/`), minutes. **Do not take C12d** despite its `linux` mark until #88 is fixed: its whole Accept is *"the Linux link verified — `SynthEditCL`, `SynthEditWayland` and `SynthEditJuce` all link under GCC"*, and two of those three cannot link today for reasons that have nothing to do with C12d. **The screening below still stands.** Previous pointer follows. **[#53](https://github.com/JeffMcClintock/TideSynth/issues/53) is DONE — merged 2026-08-14 ([GMPI_Wrappers#3](https://github.com/JeffMcClintock/GMPI_Wrappers/pull/3), `49ff927`), so this row drops back to the item behind it.** The linux box configures and builds again: verified against merged `main` with `GMPI_Wrappers` **fetched from GitHub rather than a local override**, so it is what a fresh clone gets — configure RC=0, and `--target TIDE_VST3` links 298/298. **S3** is the item: `TideApp.cpp` stubs `doDialogConnectUg`, `doDialogPatchManager` and `doDialogBuildCodeSkeleton` with `assert(false)`, which compiles out in release. Its target `SE16/SynthEditSem/TideApp.cpp` is **ALLOWED**, and S2 supplied the evidence its row was missing — finding A6 of [docs/sandbox-audit.md](docs/sandbox-audit.md) shows live filesystem-write sites sitting behind those stubs (`CUG.cpp:2833-2968`: two `create_directory`, a `copy_file`, an `fopen(…,"w")`), so "silently falls through" is measured now, not suspected. **The screening behind this pointer stands and is worth re-reading before picking anything else:** `S1b`, `S5`, `S7` and `S8` all read as `TODO`/`any`/unblocked but are entirely GATED work; `A4`, `A10`, `A12` and `B1` are all `.github/workflows/**` the bot token cannot push. Full reasoning in the 2026-08-14 linux journal entry. |
| any | **A4, A10, A14, A15 and P9 all went DONE on 2026-08-16** — every one of their PRs had merged, so this row's premise has changed and it needs re-pointing by whoever next takes an `any` item | **A4's auto-merge tier is not merely merged, it is proven live**, which is what its row required before DONE: `auto-merge.yml` merged [#62](https://github.com/JeffMcClintock/TideSynth/pull/62), [#63](https://github.com/JeffMcClintock/TideSynth/pull/63) and [#64](https://github.com/JeffMcClintock/TideSynth/pull/64) as `tide-rack-bot` within seconds of each lint run, and left [#65](https://github.com/JeffMcClintock/TideSynth/pull/65) — a `.github/workflows/**` change its allowlist denies — for Jeff. Merge one, leave one alone, from real traffic. **The walls that remain are unchanged:** **A12** and **B1** are `.github/workflows/**` the bot token cannot push (`repo` scope, deliberately no `workflow`), and **A7** was re-pointed from `BLOCKED(A4)` to `NEEDS-JEFF` this run rather than left to become falsely claimable — see its row. **A9** is the topmost `any` row a scheduled run can complete today |

Status: `TODO` · `DOING` · `IN-REVIEW` · `DONE` · `BLOCKED` / `BLOCKED(<id>)` ·
`NEEDS-JEFF` · `WONTFIX`

- `IN-REVIEW` = work complete, PRs open (linked in the row). A run never sets
  `DONE` on its own fresh work; `DONE` is set when every linked PR has merged,
  by Jeff or by a later run that observes it.
- `BLOCKED(<id>)` = blocked until `<id>` is DONE. **Eligibility lives in this
  column alone** — section prose never overrides it.
- `NEEDS-JEFF` rows should carry a `Default in effect:` and a `Decide-by:` line
  (template in [docs/decisions.md](docs/decisions.md)), so an unanswered
  question cannot silently become the answer.
- `WONTFIX` = considered and declined, with the reasoning in the row. Do not
  re-file it.

Platform: `any` · `win` · `mac` · `linux`

New items state three things, tersely: **Scope** (files/dirs), **Accept** (a
command or observable that proves it done), **Size** (fits one session, or
names its stages). Anything longer than a few lines lives in `docs/<id>.md`
with the row pointing at it. An agent that cannot state an item's acceptance
check before starting must skip it and mark it `NEEDS-SPEC` rather than guess.

Update this file at the end of every run. An item left in `DOING` with no
matching JOURNAL entry means a previous run died — reset it to `TODO` and note
that in the journal (claims younger than 24h are presumed live; see the run
prompt).

---

## Carve-out — APPROVED 2026-08-08, C0 cleared

Stages are sequential. Each must leave SynthEdit, SynthEditCL and TIDE all
building before the next starts. See [docs/carve-out.md](docs/carve-out.md).

| ID | Status | Plat | Item |
|---|---|---|---|
| C6 | BLOCKED(C12f) | any | **Re-pointed from `BLOCKED(C5)` to `BLOCKED(C12f)` on 2026-08-14 (windows), and that is a correction, not bookkeeping.** C5 merged the same day, so by the status column alone — which is where eligibility lives — this row became eligible, and the next run to read it would have done the one thing C5's journal entry, C12's row and [docs/carve-out.md](docs/carve-out.md) all say must not happen: move `EditorLib/CMakeLists.txt` into the public repo while it still points at private files. The prose warnings were already there and would not have stopped anyone, because prose never overrides the status column. The real blocker is the last C12 sub-stage. Move `EditorLib/CMakeLists.txt` itself into `SynthEditLib`. |
| C7 | BLOCKED(C6) | any | Repoint TIDE at public `SynthEditLib` only. Prove it with a clean-clone CI build that has no access to the private repo. **Before claiming a stranger can build TIDE, grep `SynthEditLib` for `#include`s of headers that live in `SE16/SynthEdit2/`.** C2 found one — `SynthEditLib/modules/se_sdk3_hosting/ModuleViewStruct.cpp:11` included `"cpu_accumulator.h"` while that header was still private, and it compiled only because that `.cpp` is on *EditorLib's* source list and so gets `../SynthEdit2` on its include path. A public file that its own repo cannot compile. C2 closed that one by accident; each remaining one is invisible until a clean-clone build. |
| C9 | TODO | any | **`se_build_number.h` blocks C4 and C5 — a private-repo header that public files include.** Found during C3, 2026-08-13. `SE16/se_build_number.h` holds SynthEdit's product version (`SE_MAJOR_VERSION`/`SE_MINOR_VERSION`/`SE_BUILD_NUMBER`), lives at the **private** repo's root, is bumped by the `[Build-Machine]` on every release, and is greped for the version string by three release workflows (`SynthEdit_cmake_mac.yml:153`, `SynthEdit_cmake_win.yml:206`, `SynthEdit_store_win.yml:266`). Five `SynthEdit2/` files include it as `"../se_build_number.h"` — and unlike this stage's other relative includes it **genuinely resolves relative to the file**, so it does not survive a move to `SynthEditLib`. C3 escaped only by luck: its one includer, `SynthEditDocBase.cpp`, never used the macros, so the fix was deleting a dead line. **The other four are live uses and cannot be deleted:** `ModuleFactory_Editor.cpp` (2) and `SkinMgr.cpp` (2) are **C4**; `Application.cpp` (1) is **C5**; `ExportAsPlugin.cpp` (1) stays private and is fine. **Options, none free:** (a) move the header into `SynthEditLib` — but the three workflows read it at the SE16 path, and `.github/workflows/**` is off-limits to a scheduled run (the bot token deliberately lacks `workflow` scope), so this needs Jeff; (b) add SE16's root to the include path — leaks a private path into the public repo and **fails at C7**, whose whole test is a clean clone with no access to SE16; (c) decouple — give `SynthEditLib` its own version header, or pass the version in as a compile definition, leaving SynthEdit's build-number file where its workflows expect it. ~~**(c) looks right and is the one that needs a decision, not just work.** Settle it before C4 starts, or C4 will settle it by accident.~~ **Resolved 2026-08-13 by Jeff: option (c).** `SynthEditLib` gets its own version header (or a compile definition), decoupled from `se_build_number.h`, which stays exactly where SynthEdit's three release workflows expect it. C4 implements it for `ModuleFactory_Editor.cpp` and `SkinMgr.cpp`, C5 for `Application.cpp`. Ruling and reasoning: [docs/decisions.md](docs/decisions.md). **C4 half implemented 2026-08-13** ([SynthEditLib#6](https://github.com/JeffMcClintock/SynthEditLib/pull/6), [SynthEdit#15](https://github.com/JeffMcClintock/SynthEdit/pull/15)): the mechanism now exists and is proven — `SynthEditLib/se_version.h` defaults `SE_APP_BUILD_NUMBER` to 0, and `EditorLib/CMakeLists.txt` injects SynthEdit's value with a `file(READ)` + `string(REGEX MATCH)` on `se_build_number.h`, `PRIVATE` to EditorLib. **C5 reuses it verbatim for `Application.cpp` — one `#include` swap and one macro rename, no new machinery.** This row stays TODO until that lands. **One thing C6 must not miss:** the injection block reads a private-repo path, so it cannot travel with `EditorLib/CMakeLists.txt` when C6 moves that file — a comment in the block says so, and the injection then belongs in each SynthEdit application's own build. |
| C10 | BLOCKED(C6) | any | **Re-home the moved editor files from `SynthEditLib`'s root into a subfolder.** C2 chose root for 16 files; C3 deliberately kept root for 27 more (reasoning in [SynthEditLib#5](https://github.com/JeffMcClintock/SynthEditLib/pull/5)) — root is already an include dir in all three build systems, so both moves cost **zero** include-path edits, which is what keeps a large move's breakage unambiguous. That trade gets worse as C4/C5 land and ~120 editor files sit beside the DSP engine in one flat root. **Blocked on C6 on purpose:** once `EditorLib/CMakeLists.txt` lives in `SynthEditLib`, one `target_include_directories` line there covers every CMake consumer at once, instead of the same edit being smeared across C3/C4/C5 and several build systems — including the macOS/iOS ones no Windows run can verify. The move itself is a `git mv`. Non-CMake consumers still need checking by hand: `SynthEdit2.vcxproj` and the SynthEditMac Xcode project. |
| C12 | BLOCKED(C12f) | any | **Scoped and split 2026-08-14 (windows) into six sub-stages — C12a–C12f below. This row is now the umbrella, not a claimable item**; its acceptance check (zero `${EDITOR_DIR}` entries) is satisfied exactly when C12f lands, which is what the blocker names. Plan, measurements and the reasoning behind the split: [docs/c12-remaining-editor-files.md](docs/c12-remaining-editor-files.md). **Three of this row's own assumptions were wrong in C12's favour, all measured rather than read off the stage list:** the 41 entries are **closed under inclusion** — they pull in no private header any stage lacks, so C12 opens **zero** new dangling includes where C4 opened 20 and C5 opened 10; **four of the 41 are dead or duplicate** and are delisted rather than moved (`GuiPin.h` has no includer anywhere and three of its own includes survive only in the dormant SE15 repo; `Module_Info_Plugin` is a deprecated VST2 class whose only construction site is commented out at `ModuleFactory_Editor.cpp:1320`; `resource.h` already exists in **both** repos, 361 lines each, identical in every `ID_*` constant); and the real dangling count from the public repo into the 41 is **43, not the 114 a naive scan reports** — 71 of those are `resource.h` hits from public files that resolve to their own repo's copy by the own-directory-first rule. **One risk this row did not name, and it is the only part a Windows box cannot verify:** `platform_editor.cpp` is the factory that breaks a `SynthEditLib`↔`EditorLib` mutual static-archive dependency, and three CMakeLists carry GNU-ld rescan-group comments about it — so C12d is marked `linux`. Neither `SynthEdit2.vcxproj`/`.filters` nor the SynthEditMac Xcode project lists any of the 41 (checked, not assumed), and none of the 41 includes `afxres.h`, so C12 needs no non-CMake build edits and does not worsen **P3**. Original row follows. **The carve-out stage list does not cover the files it claims to, and C7 cannot pass until something does.** Found 2026-08-14 by the C5 run, by counting rather than reading: [docs/carve-out.md](docs/carve-out.md) stages C3/C4/C5 as "the rest", and `EditorLib/CMakeLists.txt`'s own comment said *"C3-C5 convert the rest"*. After C5 there are still **41 `${EDITOR_DIR}` entries** on EditorLib's source list — ~21 units: `CLine2`, `commandMgr`, `Control`, `CPlugin`, `Ctl_Combo`, `Ctl_Keyboard2`, `Ctl_Slider`, `Ctl_Text`, `Dialogs_editor`, `GuiPin`, `IGuiHost`, `InterfaceObject_editor`, `legacyExternalApp`, `ModuleDragAndDropManager`, `Module_Info_Plugin`, `PatchManager`, `PatchParameter`, `PatchParameter_host_generated`, `platform_editor`, `resource.h`, `SuspendDSP`, `UG2`, `ui_msg_target`. **C7's whole test is a clean clone with no access to SE16**, so every one of these is a C7 blocker. **Do this before C6, not after** — C6 moves `EditorLib/CMakeLists.txt` into the public repo, and doing that while it still points at 41 private files puts a source list a stranger cannot satisfy into the public repo: the `cpu_accumulator.h` shape C2 hit, at 41× the size. **Also absorb the three headers C5 measured as dangling and on no stage's list:** `IMidiDriver.h`, `ParseSynthEditArgs.h`, `ISEAppManaged.h` (`SynthEditApp.h` is the fourth and is **C11**, not this row — its exclusion is deliberate). **Scope** the remaining `${EDITOR_DIR}` entries in `SE16/EditorLib/CMakeLists.txt`, plus `SynthEdit2.vcxproj`/`.filters` and the SynthEditMac Xcode project for any that those list. **Accept** zero `${EDITOR_DIR}` entries remain, and SynthEdit, SynthEditCL, SynthEdit2 (WinUI3) and TIDE all build with tests green — plus the dangling-private-include count measured before and after from git refs, as C4 and C5 both did. **Size** larger than any single stage so far (~3× C5); name its own sub-stages rather than attempting it in one session. The C5 journal entry carries the measurement method and the re-createable script. |
| C12c | IN-REVIEW | any | **Both PRs were merged by Jeff within minutes of being opened (07:21 and 07:24 UTC, 2026-08-17), so the code has landed — but the run that wrote this row does not set `DONE` on its own fresh work; the next run to read this may flip it.** **DONE 2026-08-17 (windows) — IN-REVIEW: [SynthEdit#41](https://github.com/JeffMcClintock/SynthEdit/pull/41) + [SynthEditLib#18](https://github.com/JeffMcClintock/SynthEditLib/pull/18), which must merge together.** Accept met on every clause: twelve entries gone (**25 -> 13** `${EDITOR_DIR}`), **21 dangling edges closed, 42 -> 21** measured before and after with [scripts/dangling_private_includes.py](scripts/dangling_private_includes.py) and **zero new opened**, full build + tests green. **The build baseline has moved: 935/935 and 92/92, not 904/904** -- upstream growth in `SynthEditLib` (tearout-window-layout), not this stage, which moves TUs rather than adding or removing them. **One qualification, stated in both PR bodies and not to be lost: `SynthEditLib` `main` does not compile on Windows today** ([#111](https://github.com/JeffMcClintock/TideSynth/issues/111), `SeAudioMaster.cpp` vs MSVC, from SynthEditLib#16), so the numbers were measured with the twelve files on top of `d96edbb` -- the merge before the offending commit -- rather than on top of `main`. The same failure reproduces on pristine `main` with a single-TU control and none of C12c present, which is what makes them independent; **this branch has not been proven to build against `main` as it stands.** Original row follows. **Move the independent leaves.** `CLine2`, `commandMgr`, `SuspendDSP`, `legacyExternalApp`, `ModuleDragAndDropManager` (`.cpp` + `.h` each) plus header-only `ui_msg_target.h` and `IGuiHost.h` — twelve entries, 1,316 lines. **Deliberately excludes `InterfaceObject_editor` and `platform_editor`**, which look like leaves and are not; they are C12d. **Scope** those twelve entries plus the `git mv`. **Accept** twelve entries gone; **21** dangling edges close — the largest reduction of any sub-stage, and more than C5 closed in total; full build + tests green; count measured from git refs. **Size** one session. |
| C12d | TODO | linux | **Move the InterfaceObject factory — the one sub-stage MSVC cannot check.** `InterfaceObject_editor.{cpp,h}` and `platform_editor.cpp`, three entries, 319 lines. `platform_editor.cpp` is 16 lines of nothing but `new_InterfaceObjectA/B/C`, and it is the seam that lets `SynthEditLib` call into the editor layer without a compile-time dependency: `SynthEditCL/CMakeLists.txt:57-61`, `SynthEditJuce/CMakeLists.txt:92-96` and `SynthEditWayland/CMakeLists.txt:150-153` each carry a comment saying the two are *mutually-referencing static archives* and **GNU ld needs a rescan group**. Moving the provider into `SynthEditLib` puts it in the same archive as the code expecting it, which plausibly makes the rescan group redundant — and *plausibly* is the problem, because MSVC's linker is indifferent either way, so a Windows run sees green whichever way it lands and the failure surfaces as a Linux link error. **Marked `linux` for that reason alone; the code is platform-neutral.** **Scope** three entries plus the `git mv`. **Accept** three entries gone **and the Linux link verified** — `SynthEditCL`, `SynthEditWayland` and `SynthEditJuce` all link under GCC. If the rescan group is now redundant, prove it by building once without it rather than reasoning it away; if it is still needed, leave it and record why. Windows/macOS green as usual. Closes 0 dangling edges — its value is the entry count and the link topology, not the count. **Size** one session, on the Linux box. |
| C12f | TODO | any | **The patch cluster — the stage that finishes C12.** `PatchManager`, `PatchParameter`, `PatchParameter_host_generated`, `UG2`, `CPlugin` — `.cpp` and `.h` each, ten entries, **6,298 lines, 64% of C12 by volume**. **Must move in one commit:** the first four are a genuine strongly-connected component (`PatchManager` ↔ `PatchParameter` ↔ `PatchParameter_host_generated`, and `PatchManager` ↔ `UG2`), so there is no split that leaves both halves whole; `CPlugin` hangs off `PatchManager` and rides along. **Scope** those ten entries plus the `git mv`. **Accept** **zero `${EDITOR_DIR}` entries remain** — this is the stage that satisfies C12's own top-level check — plus 14 dangling edges closed, full build, all three suites green, SynthEdit2 (WinUI3) links, and the count measured from git refs. **Also re-check `resource.h` here** if this is the last stage to move a `.cpp` out of `SynthEdit2`: those TUs switch from the private copy to the public one, a no-op only while the constants stay identical — they do today, and that is the whole basis for C12a being safe. **Size** the largest single stage of the carve-out, larger than C3, C4 or C5. Still one session, because it is a mechanical `git mv` of a closed set with no include-path, `.vcxproj` or Xcode edits — but do not combine it with anything else. |

---

## Process hardening — from the process review, 2026-08-09

The findings, red-team verdicts, and the reasoning behind the ordering are in
[docs/process-review-2026-08-09.md](docs/process-review-2026-08-09.md). The
sequence is the decision: A1 gates everything, and cadence (A7) comes last.
Rejected recommendations are listed in that doc — do not re-file them.

| ID | Status | Plat | Item |
|---|---|---|---|
| A7 | NEEDS-JEFF | — | **Re-pointed from `BLOCKED(A4)` to `NEEDS-JEFF` on 2026-08-16 (macos), and that is a correction, not bookkeeping.** A4 went DONE this run, so by the status column alone — which is where eligibility lives — this row would have become claimable, and the next run to read it would have taken an item whose own text says the work is *"per-box manual steps (Jeff or an interactive session on each box)"*: editing three machines' scheduled-task cron. A scheduled run cannot do that for the two boxes it is not running on, and doing it for its own box only would leave the fleet staggered wrong. The blocker chain (A1, A6, A4) is now **fully cleared** — nothing technical is outstanding, so this is purely Jeff's to schedule. **Default in effect:** weekly, as today. **Decide-by:** no deadline; the A6 digests are the evidence to review it against. **A6 and A1 both landed 2026-08-13 — the remaining blocker is A4 alone, unblocked but not yet built.** **Raise cadence to 2 staggered runs per box per week.** Also requires A1 and A4 (the red team's ordering: cadence multiplies journal growth, PR volume, and the secret-exposure window linearly, so the drains come first — this row's blocker chain encodes A6, and A1/A4 are NEEDS-JEFF/BLOCKED upstream of it). The stagger is no longer load-bearing — the pushed-DOING claim protocol is what prevents collisions — so weekly is a historical artifact. The task-cron edits are per-box manual steps (Jeff or an interactive session on each box). Start at 2×, review after a month against the A6 digests. |
| A16 | TODO | any | **A `git add`+`git commit` in a shared working copy can silently commit fewer files than were staged.** Filed 2026-08-15, same session as A14, different failure shape: mid-C11, `git add <5 paths>` followed immediately by `git commit` produced a commit containing only 1 of the 5 files. The working tree held all five, correctly, throughout — nothing was lost or overwritten, and A14's authorship check would have passed cleanly, since the short commit was correctly authored as `tide-rack-bot`. **A14 does not catch this**: it asserts *who* wrote a commit, not that the commit contains *what was staged*. The most likely cause is a race on `.git/index` from a concurrent git operation in the other session sharing this checkout — plausible but not proven; no reliable repro was attempted, since recovering the missing content and moving on was the higher priority in the moment. **Cheapest workable guard:** immediately after any commit in a shared checkout, `git show --stat HEAD` and eyeball it against the file list just staged, before doing anything else — this is what caught it this session, entirely by habit, not by an automated check. A script version would compare `git diff --cached --name-only` captured before the commit against `git show --stat HEAD`'s file list after it, and fail loudly on a mismatch. **Scope** a `scripts/check-commit-completeness.py` in the shape of `check-commit-authorship.py`, plus a STEP 3/4 line in the run prompt. **Accept** a deliberately short commit (stage N files, commit N-1 of them via a race simulated with a background `git reset` mid-commit, if that's even reproducible) is detected before push. **Size** unknown until the repro is attempted — this is the part A14 didn't need, since authorship races are trivial to simulate and index-write races may not be. |
| A12 | TODO | any | **A halted box is invisible to the fleet, and nothing escalates it.** Filed 2026-08-13 from A11's mac half. STEP 0.7's assertions can put a box into a state that **only an interactive session can clear** — the check that detects the gap also forbids fixing it (*"do not 'fix' it by carrying on as whoever you are"*), and STEP 0.7 fires before STEP 1/2 so the run can never claim the item that would repair it. On 2026-08-13 the mac box was in exactly that state; it was cleared within minutes only because Jeff happened to be at the keyboard. **Unattended, it would have burned a run per week indefinitely, each producing an identical journal entry that nobody is prompted to read.** The same shape applies to any STEP 0.7 failure (missing/expired token file, revoked credential) — note the bot token **expires 2026-11-07**, which will halt all three boxes at once and is the obvious first real instance. **Scope:** A6's fleet watchdog digest is the natural place — it already runs and already reports; it should flag a box that halted or has not completed a run in N days, distinct from a box that ran and did nothing. Likely also worth a line in [docs/weekly-run-prompt.md](docs/weekly-run-prompt.md) telling a halted run to label its PR so the digest can find it cheaply. **Accept:** a box halted at STEP 0.7 appears in the next digest as halted, named, with the failing assertion — demonstrated by a deliberately induced halt (e.g. a temporarily bogus token path on one box), not by reasoning. **Size:** one session. **Note the digest workflow lives in `.github/workflows/`, so a scheduled run cannot edit it** — same wall as A3/A5/A6 and C9(a). Needs Jeff or an interactive session. |
| A17 | NEEDS-JEFF | any | **May a scheduled run repair a build break whose cause is in a GATED path?** Raised by Jeff 2026-08-17 after the same standoff hit two boxes in two days — [#87](https://github.com/JeffMcClintock/TideSynth/issues/87) (linux) and [#111](https://github.com/JeffMcClintock/TideSynth/issues/111) (windows) — where STEP 1 ("a broken build outranks all backlog work") and STEP 5 (SynthEditLib and the two SE16 folders are GATED) point opposite ways and both runs obeyed STEP 5. **Full `PROPOSED:` entry, options and the six proposed bounds: [docs/decisions.md](docs/decisions.md).** Recommended default is (b), build-break repair only, minimal restoration, its own PR. **Default in effect:** today's rule — file the issue and stop. **Decide-by:** before the next scheduled run on any box; the linux box already has two of these waiting and will meet the same wall. Pairs with **A18**, which is the enforcement half — the review this rule would lean on is not actually enforced in `SE16`. |
| A18 | NEEDS-JEFF | any | **`SE16`'s `master` is unprotected — the "everything is a reviewed PR" guarantee is convention there, not a rule.** Measured 2026-08-17: `SynthEditLib` and `TideSynth` both carry an active **"Agent PRs only"** branch ruleset; `SynthEdit` carries none and `master` reports `protected=false`, because private repos on the current plan cannot have rulesets (`/rulesets` answers *"Upgrade to GitHub Pro or make this repository public"*). The bot holds write access, so nothing mechanical stops a run pushing straight to the repo that contains the commercial product — every PR opened there so far ([#41](https://github.com/JeffMcClintock/SynthEdit/pull/41), #20, #15) was voluntary compliance with the run prompt. **This matters most exactly where A17 would relax the gate:** two of the three GATED paths (`EditorLib/`, `SynthEdit2/`) live in the unprotected repo. **Options:** (a) upgrade the plan so the same ruleset covers `SE16` — prevention, a few dollars a month; (b) detection instead — have the A6 watchdog digest flag any `tide-rack-bot` commit on `SE16/master` that did not arrive as a merge commit; (c) accept convention, knowing it is convention. **Default in effect:** (c), today. **Decide-by:** with A17, since A17's whole premise is that a human reviews the result. Jeff's call — it is a billing decision and a risk-appetite decision, not a technical one. |

---

## Ready now — no dependency on the above

| ID | Status | Plat | Item |
|---|---|---|---|
| N1 | TODO | any | **Carry the TIDE Rack rename through the tree** — PLAN naming ruling, 2026-08-08, reaffirmed 2026-08-09. Product is **TIDE Rack**, **TIDE Synth is the organisation**; domain and repo name stay. **The prose half is DONE** (website 2026-08-08, `README.md` 2026-08-09); what remains is the build-system half, which needs decisions rather than edits. **Do it after C7, not during** — C2–C7 touch the same build files and nothing has shipped under either name, so a rename now only doubles the conflict surface. Forms are settled and must never be mixed in one filename: display `TIDE Rack`, shipped files `TIDE-Rack`, CMake targets `TIDE_Rack`. **Do not start with a global search-and-replace** — "TIDE" is a product name, an org name, a CMake target, a fixed-width 4-char vendor code and a filename prefix, and they do not all move together. Depends on **P5** or supersedes it. Reasoning, the per-surface cost and the open sub-questions: [docs/n1-tide-rack-rename.md](docs/n1-tide-rack-rename.md). |
| P7d | TODO | any | **`GMPI-plugins` cannot link a GUI plugin on macOS.** `Undefined symbols … "_OBJC_CLASS_$_UTType", referenced from: … DrawingFrameMac.mm.o`. `gmpi_ui/backends/MacFileDialog.h:8` imports `<UniformTypeIdentifiers/UniformTypeIdentifiers.h>` and uses `UTType`; `GMPI-plugins`' link line never adds the framework. `SynthEdit/EditorLib/CMakeLists.txt:166` does add it, which is why SynthEdit's own macOS build is fine and this stayed invisible. Pre-existing — nothing in P7 caused it. **Not fixed, on purpose:** `GMPI-plugins` is on neither the ALLOWED nor the GATED list, so it is GATED by default and this is the scope question, named per that rule. The P7 audit worked around it at configure time with `-DCMAKE_MODULE_LINKER_FLAGS="-framework UniformTypeIdentifiers"`, touching no file. **The scope question for Jeff:** the requirement arguably belongs in `gmpi_ui`, declaring the framework it actually needs, rather than in every consumer's link line — that would fix `GMPI-plugins` and any other consumer at once, and it is an ALLOWED path. Accept: `ninja GainGui_VST3` links on macOS with no extra `-D` flags. Size: minutes once the scope is ruled. |
| P8 | DONE-PENDING-CI | win | **Fixed 2026-08-09 on the Windows box, at Jeff's direction, straight to `SE16` master (`4baddfbb4`) — see [docs/decisions.md](docs/decisions.md).** One line at `SynthEdit2/EditorWindowHelper.cpp:294`: `currentContainerView.universalFactory` → `.get()`. `UniversalFactory` derives from `gmpi::api::IUnknown` (`SynthEditLib/Shared/DrawingFrame2_cpu.h:19`) and `renderContainerThumbnail` **borrows** rather than takes the factory, so `.get()` matches what the other three callers already pass and what `DrawingFrame2_win.h:270` does when it hands the same factory out. **Verified:** `MSBuild SynthEditStore.sln -t:SynthEdit2 -p:Configuration=Release -p:Platform=x64` now links, producing `x64\Release\SynthEdit2\SynthEdit2.exe`. **This was bigger than "does not compile from clean" — it was failing the SynthEdit Store release pipeline.** Actions run [31297103293](https://github.com/JeffMcClintock/SynthEdit/actions/runs/31297103293) died on `master` at "Build SynthEditStore (Release x64)" on exactly this error, skipping changelog, MSIX signing, bootstrapper and FTP upload — so nothing shipped. **Left as DONE-PENDING-CI rather than DONE:** the local Release link is proof the error is gone, but the CI job goes on to sign and upload, and nobody has watched a green run yet. Whoever sees one first should flip this to DONE. **Also unblocks the link-stage verification C1b and C2 each had to leave open** — both could compile their new TUs but neither could reach SynthEdit2's link. A Debug build of the same project failed on an unrelated `x64\Debug\pch.pch: Invalid argument` intermediate-file error; not investigated, and not this bug. Original finding below. |
| ~~P8~~ | *(was)* | win | **`SynthEdit2` (the WinUI3 app) does not compile from clean.** Not TIDE, and pre-existing — found during C1b and **A/B-confirmed unrelated** (fails identically with the C1b vcxproj change reverted): `SynthEdit2/EditorWindowHelper.cpp(294)` passes `std::unique_ptr<UniversalFactory>` where `se_cl::renderContainerThumbnail` (`EditorScreenshot/ContainerThumbnail.h:35`) now takes `gmpi::api::IUnknown*` — the thumbnail API moved and the WinUI3 caller never caught up. Matters to TIDE because "leave SynthEdit building" is the standing rule and **C1b's link-stage verification of SynthEdit2 is blocked behind it** (the new `ExportAsPlugin.cpp` ClCompile entry compiles clean; the app cannot reach its link step). Same shape as P6 on macOS: SynthEdit's own bug, in SynthEdit's own files — check with Jeff before touching. |
| S1b | TODO | any | **Compile the module scan out — stages (b) and (c); (a) is done.** ~~Blocked on C0~~ — unblocked 2026-08-08. **(a) DONE:** TIDE no longer compiles `SynthEdit2/SynthEditApp.cpp`, which had been putting a second `LoadOrScanModuleData()` call site, a filesystem-watcher thread and the whole licensing surface into the shipping plugin (`SE16` `40b6008ee`). **(b) and (c):** `ScanFolder`, `LoadModuleData`, `Module_Info3` and an imported `dlopen`/`dlsym`/`dlclose` are still in the Release binary. Needs a new `TIDE_NO_EXTERNAL_MODULES` through `EditorLib/CMakeLists.txt` and a real split of `ModuleFactory_Editor.cpp`. **Do not attempt this with an existing flag** — `SE_EXTERNAL_SEM_SUPPORT` guards two lines and removes none of it (measured), and `GMPI_IS_PLATFORM_JUCE` is a trap. **Recommend riding along with C4:** these are the same files C3 and C4 move, so standalone means doing it twice. Accept: constraint 7 verifiable from the Release binary — none of those symbols present. Measurements: addendum B1–B5 of [docs/module-enumeration.md](docs/module-enumeration.md); row history in [docs/s1b-compile-the-scan-out.md](docs/s1b-compile-the-scan-out.md). |
| X4 | WONTFIX | any | **Closed 2026-08-08 by Jeff: leave the `GIT_TAG origin/main` pins alone. Do not re-file this.** `FetchContent` is not how the SynthEdit-family dependencies are meant to be developed against — they change daily and the intended dev path is the `*_FOLDER_OVERRIDE` variables, which are the normal path on a dev box, not an optimisation. CI clones fresh, so `origin/main` there is real. **The thing to watch for instead is a *missing* override** — that asymmetry, not the pin, is what caused X3. **CMake already announces it:** every dependency prints `Using local <X> folder` or `Fetching <X> from github` at configure time. An unexpected "Fetching" on a SynthEdit-family repo means you are building against whatever `main` looked like when the tree was first configured; confirm with `git log -1` in `build/_deps/<dep>-src` before believing your tree. Full reasoning and the original finding: [docs/x4-fetchcontent-pins.md](docs/x4-fetchcontent-pins.md). |
| S5 | TODO | any | **`getFolderInfo` is undefined behaviour in TIDE.** `TideApp::InitInstance` never calls `CSynthEditAppBase::InitInstance`, so `refreshFolderLocations()` never runs and `m_folder_settings` stays empty; `getFolderInfo` (`SE16/SynthEdit2/Application.cpp:167`) then evaluates `m_folder_settings[0]->current_folder` on an empty vector. Reachable via `CSynthEditAppBase::ShortenFilename` (`SE16/SynthEdit2/SynthEditAppBase.cpp:238`). `ResolveFilename` is unaffected. Fix in `Application.cpp` (guard the empty case) — that is EditorLib, so check carve-out ordering before moving it. |
| S9 | WONTFIX | mac | **Moot 2026-08-15 — S10 ruled retire, not revive.** This row's own scope was surgical cleanup of `TIDE/Plugins` wiring *inside* `SE_IOS_APP.xcodeproj`; deleting the whole project ([SynthEdit#22](https://github.com/JeffMcClintock/SynthEdit/pull/22)) makes that cleanup unnecessary rather than incomplete, exactly as this row itself predicted ("S9 is moot if the answer is retire"). Original row follows. **GATED remainder of S6 — remove the now-dangling `TIDE/Plugins` wiring from `SE_IOS_APP.xcodeproj`.** Filed 2026-08-13 when S6 deleted the files. Three things to remove, all in the **shared, GATED** `SE16/SE_IOS_APP/SE_IOS_APP.xcodeproj/project.pbxproj`, all belonging to the `SeAudioUnitMacOS` target: (a) the Run Script phase at `:2064` (`cp -r "$SRCROOT/TIDE/Plugins" …`); (b) the six `.sem` group/file references (`Oscillator`, `EnveloperAdsr`, `PatchMemory`, `Converters`, `SubControlsXp`, `ControlsXp`, ids `D5F5219525E5F21600E75519` and siblings) — navigator-only, cosmetic, they just show red; (c) **the real ones — the `CpResource` build-file entries for files *inside* those bundles, which are genuine Resources-phase inputs** and are what now errors. **Do not do this without reading S10 first** — if that project is retired rather than revived, this row is moot and the right move is deleting the whole `.xcodeproj`. **Accept:** `SeAudioUnit macOS` builds with no `TIDE/Plugins`-related error (it will still fail on `SE_DSP_CORE` — that is S10, not this). **Size:** minutes, once scope is ruled. **Needs Jeff** — GATED path, and a scheduled run may not edit it. |
| S7 | TODO | any | **Enforce the no-user-skins ruling — PLAN constraint 8, decided 2026-08-07.** The static chain says a TIDE build writes skins to the user's disk today: `CContainer.cpp:97` calls `SkinMgr::Instance()->getSkin(L"default3")` (also `SynthEditDocBase.cpp:386`), and `SkinMgr`'s **constructor** (`SE16/SynthEdit2/SkinMgr.cpp:27-30`) points its folder at `<CommonDocuments>\SynthEdit Projects\skins\`, where `setSkinFolder` (`:47+`) **recursively copies the built-in skins on first use**. So the first container a TIDE instance constructs probably spews the shared skin set over the user's drive — verify at runtime, then make TIDE load its default look from plugin resources only, with no skin folder, scan, or write reachable. `SkinMgr` is EditorLib (C4, GATED) — if the fix cannot be confined to TIDE-side code, do what can be and file the gated remainder naming the exact lines. Overlaps S2's audit; whoever takes either first should check the other's scope. |
| S8 | TODO | any | **TIDE ships modules constraint 2 forbids, and shows SynthEdit licensing copy.** Measured from the macOS Release binary 2026-08-08 (addendum B3 of [docs/module-enumeration.md](docs/module-enumeration.md)): `ug_soundcard_in`, `ug_soundcard_out` and `ug_midi_out` are compiled in *and registered* — each has its `se_static_library_init_*` and `__GLOBAL__sub_I_*` static initialiser — so the module browser offers a **Sound Out** module that opens the soundcard directly, in a plugin whose whole premise is that the DAW owns I/O. `ug_soundcard_out.cpp:10` registers it under Input-Output with the help text *"Sends audio to your speakers… Non-registered SynthEdit limited to 2 output channels"*, which is also wrong for a free unlicensed plugin. Confirms A1's prediction; also confirmed the modern SEM modules are absent (`OscillatorNaive`: zero symbols). Both arms of `initialise_synthedit_modules` are in `SynthEditLib/UgDatabase.cpp:1063-1155`, so there is **no TIDE-side fix** — ~~GATED on C0~~ **unblocked 2026-08-08, C0 approved.** The `SE_EXTRA_STATIC_FILE_CPP` hook (A2) can only *add* modules, not suppress these. Pairs with S1b; whoever takes either should take both. |

| S3g | TODO | any | **GATED remainder of S3 — TIDE still offers menu items for dialogs it does not have.** Filed 2026-08-17 (linux) after S3 closed the TIDE-side half. A loud stub is a backstop; the menu entry is the actual defect, because the user is being offered a command that cannot work. **Three entries, all in GATED paths, all reached from TIDE's module context menu:** (a) `SynthEditLib/MfcDocPresenter.cpp:1274` adds **"Connect..."** when `viewType == CF_STRUCTURE_VIEW` → `POPUP_MENU_CONNECT` → `CUG.cpp:2057` → `CUG::OnConnectDialog()` (`:2633`) → `doDialogConnectUg`; (b) `SynthEditLib/MfcDocPresenter.cpp:1276` adds **"Build Code Skeleton..."** → `POPUP_MENU_DEBUG_CODE` → `CUG.cpp:2034`'s `VO_Notify(OM_SHOW_CODE_SKELETON_DIALOG)`, which TIDE does not handle, so it is a silent no-op **that no stub guards** — S3's loud report does not cover this one; (c) the patch manager, `SynthEditLib/CUG_with_patches.cpp:33` (`HC_CopyPatch`) and `:142` (`ID_PATCH_MGR`) → `DoPatchCopy()` (`:162`) → `doDialogPatchManager`. **The shape is already settled by precedent, which is why this is small once ruled:** `MfcDocPresenter.cpp:1264` already consults `GetLicenseState()` — a per-app runtime query, resolved at link time, added by **C11** for exactly this "the shared file must not know the app's type" problem — to grey out "Selection to Prefab". An equivalent per-app capability query is the fix; an `#ifdef` is not, since EditorLib compiles once for every app. **Accept** with TIDE built, the module context menu offers none of the three, and S3's stderr report never fires because nothing can reach it. **Size** minutes of code; the cost is the scope ruling, not the work. **Also tidy while there:** `SE16/SynthEditCL/CLApp.cpp:26` still defines the dead `doDialogBuildCodeSkeleton` that S3 deleted from `TideApp.cpp`; `SynthEditCL/` is GATED by default too. **Needs Jeff** — every path named here is GATED or GATED-by-default, and the `SynthEditLib` ALLOWED/GATED contradiction the 2026-08-17 macOS run raised as a process note is unresolved and lands squarely on this row. |
| B1 | TODO | any | **Partly mitigated 2026-08-16 (interactive, Jeff's credentials) — the symptom is gone, the row is not done.** `build.yml` now has a `guard` job that checks for a root `CMakeLists.txt` and gates the matrix on it, so pre-C7 the three platforms are **skipped** rather than each dying at Configure. That removes the six FAILURE rows every PR was carrying (3 platforms × 2 events — the push and PR runs are both deliberate, see the workflow's `concurrency` comment). Worth being precise about what those rows were costing: **nothing gated on them** — this repo has no branch protection and no required status checks, and A4's auto-merge tier reads `lint`, not this — so their only effect was to make `gh pr checks` permanently red and train every reader to skim past it. A check list that is always red measures as little as a run that always reports success, which is this row's other half. The gate skips rather than passing, because a green matrix would claim a clean-clone build succeeded; and it is keyed on the file itself, like the issue-filing suppression, so it lifts by itself when C7 lands. **What remains for this row, unchanged:** TIDE's top-level CMake still has to be authored (wants C7's shape settled), and `continue-on-error` at job level still has to go, or green here will still mean nothing. Note the diagnosis below was also briefly mis-stated as covering `verify.yml` — it does not: `verify.yml` runs no CMake at all, downloads a published engine, and passes. Only `build.yml` was ever failing. Original row follows. **Bigger than this row implies — measured 2026-08-14 (windows), in passing during the C12 scoping run.** `build.yml` does **not** currently fail for the reason it and this row both claim. All three platforms die at the **Configure** step with `CMake Error: The source directory "…/TideSynth" does not appear to contain CMakeLists.txt` — because **`TideSynth` has no top-level `CMakeLists.txt` at all**; the repo root holds only markdown, `docs/`, `scripts/`, `tests/`, `tools/` and `website/`. So the run never gets as far as the missing private dependency, and the honest-failure state this row asks for cannot be reached by removing toolchain noise — it needs **TIDE's top-level CMake to be authored**, which probably wants C7's shape settled first. Note also the workflow's own header: `continue-on-error` at job level means every run reports success, so nothing should treat it as a gate until this lands. **Still needs Jeff or an interactive session either way** — the file is `.github/workflows/**`, which the bot token structurally cannot push. Original row follows. **CI that builds nothing yet but is correct.** `.github/workflows/build.yml` exists as a skeleton and is expected to fail until C7. Get it to the point where it fails for exactly one honest reason (missing private dependency) rather than for syntax or toolchain errors. |
| D5 | DONE | — | **DONE 2026-08-16 — Jeff did it during this session, and it is verified rather than taken on trust.** The page now renders as **"Jef [TIDE Rack]"** with the browser title **"Buy Jef [TIDE Rack] a Coffee"**; before, it was plain *"Jef"* with nothing naming the product. Checked by loading <https://ko-fi.com/tiderack> in a real browser — **the only way to check it**, per this run's own finding that Ko-fi 403s unfamiliar user-agents even for handles that do not exist. So the Accept (*"the page names TIDE Rack"*) is met, and the website's *"Donate to TIDE Rack on Ko-fi"* link now lands somewhere that agrees with it. **One optional nicety deliberately not re-filed:** the bio still reads *"I'm a dude in New Zealand"* and does not name SynthEdit Ltd, which this row listed as *ideally*, not as its Accept. Not worth a row; mention it only if the about pane ever quotes the bio. Original row follows. **The Ko-fi page does not identify itself as TIDE Rack's, which defeats the point of D2 one hop later.** Found 2026-08-16 by the D2 run, by opening it rather than assuming: <https://ko-fi.com/tiderack> renders as *"Buy Jef a Coffee"*, display name **"Jef"**, bio *"I'm a dude in New Zealand"*. Nothing on it says TIDE Rack, TIDE Synth or SynthEdit. **Why this is not cosmetic:** D2's whole justification is making the identity trail legible on TIDE's own terms, and the website's link text is *"Donate to TIDE Rack on Ko-fi"* — so a user who clicks it lands on a stranger's tip jar and meets exactly the surprise the credit exists to prevent. It is also the page that will be linked from the in-plugin about pane ([docs/about-pane.md](docs/about-pane.md)), so it gets more exposed, not less. **Scope** Ko-fi account settings only — page title, display name, a line of bio, ideally naming SynthEdit Ltd to match the credit. **No repo change, and not an agent's to make**: it needs the account password. **Accept** the page names TIDE Rack. **Size** minutes, in a browser. **Default in effect:** the page stays as it is, and the donation link keeps landing somewhere that looks unrelated. **Decide-by:** before v0.1 ships anything that links it. |
| D4 | WONTFIX | any | **WONTFIX 2026-08-17 — this row's central measurement is FALSE today, and acting on it would have broken the build.** It claims *zero* call sites for `gmpi::browse_to`; re-measured this session there are **two live ones** — `SynthEditLib/SkinMgr.cpp:111` (`SkinMgr::EditSkin`, the "open this skin's folder" command) and `SynthEditLib/MfcDocPresenter.cpp:1106` (the skin-folder context command). Deleting `browseto.mm` would produce an undefined symbol, not a saving "at no functional cost". The positive control was run too (`gmpi::open_url`, 5+ call sites), so a zero would have been distinguishable from a broken grep. The row was filed 2026-08-16 and the C-series carve-out has been moving files into `SynthEditLib` throughout, so the callers most likely arrived with a move after the original grep. **The row's real INTENT — getting an AppKit dependency and the sandbox-hostile `activateFileViewerSelectingURLs:` out of builds that should not have them — is delivered by D3 instead**: `browse_to` compiles to a no-op off macOS, while macOS keeps the behaviour it actually uses. **Do not re-file.** **The generalisable lesson: re-run a row's own measurement before acting on its conclusion, especially a "delete this, nothing uses it" row in a tree being actively carved up.** Original row follows. **`browseto.mm` is compiled into every Apple build and never called.** Found 2026-08-16 by the D1 run: grepping `SynthEdit`, `SynthEditLib`, `gmpi_ui` and `GMPI_Wrappers` finds **zero** call sites for `gmpi::browse_to` (`gmpi::open_url` has real ones — `SynthEdit2/Application.cpp:389` and the Wayland Help menu — so the grep is not simply missing them). Dropping it from `SE16/EditorLib/CMakeLists.txt:162` removes an AppKit dependency and one sandbox-hostile API (`activateFileViewerSelectingURLs:`, i.e. reveal-in-Finder) from every Apple build including TIDE's, at **no functional cost**. Reduces D3's surface to one file rather than two. **Scope** one line of `SE16/EditorLib/CMakeLists.txt`; optionally the now-unreferenced `helpers/browseto.{h,mm}` in `gmpi_ui`, which is ALLOWED and shared, so leave it unless asked. **Accept** SynthEdit, SynthEditCL and TIDE all still build on macOS with the entry removed. **Size** minutes. **GATED path — same file and gate as D3; do them together.** |
| P3 | TODO | win | **Remove the MFC dependency from the two shared-core files that have one.** **Paths updated 2026-08-13: one of the two has moved.** `CContainer.cpp:8` is now `SynthEditLib/CContainer.cpp:8` — C3 moved it to the **public** repo unaltered, so once [SynthEditLib#5](https://github.com/JeffMcClintock/SynthEditLib/pull/5) merges the hard MFC requirement is public, which is the thing this row was filed to prevent. `MfcDocPresenter.cpp:4` is still `SE16/SynthEdit2/` and moves under **C4**, so the second half is best done before or with C4. Both include `afxres.h` under `#ifdef _WIN32`. That header ships only with Visual Studio's MFC component, so a contributor with VS Build Tools (no MFC) cannot build TIDE at all — see the "MFC trap" section of [docs/building.md](docs/building.md). Both files are on the carve-out list (C3 and C4), so this lands in the public repo as a hard MFC requirement unless it is removed first. Find out what they actually need from `afxres.h` — it is likely only `ID_*`/`IDR_*` resource constants, which could move to the local `resource.h`. Verify SynthEdit and SynthEditCL still build. |
| P11 | TODO | any | **Building `TIDE_VST3` alone ships a plug-in that cannot build its DSP graph, and the error blames the user.** Found 2026-08-16 (windows) by the host-verification session, which hit it before it hit anything in U2. **The mechanism:** on Windows the VST3 resolves its built-in `SE *` GUI modules through the *installed module database*, whose TIDE entry is `C:\Program Files\Common Files\SynthEdit\modules\TIDE.gmpi`. That file is written by the **`TIDE` target's** post-build copy, not by **`TIDE_VST3`'s** — so `cmake --build . --target TIDE_VST3` alone leaves whatever `TIDE.gmpi` was there before, and the two halves disagree. **The symptom:** a modal *"Export failed: required module is missing from the module database: &lt;name&gt;. The module scan is incomplete - this installation is broken. Re-scan modules (or repair the modules folder), then export again."* — `SE Background Image` at instantiate, then `SE List Entry` / `SE Text Entry` on each GUI-control insert. **DSP-only modules (`Moog Filter`) export clean**, which is the tell that isolates it to the GUI half rather than to a broken modules folder. Building the `TIDE` target as well cleared every instance. **Why it is worth a row:** the message names the *user's installation* as broken and tells them to re-scan modules, for what is actually a half-built tree — so the first person to hit this on a fresh box will chase the wrong thing, and a scheduled run would report it as an environment failure. **Options:** (a) make the two targets' module-database outputs consistent, or have `TIDE_VST3` refresh `TIDE.gmpi` too; (b) leave the build alone and fix the diagnostic to name the mismatch it can actually detect; (c) document it in [docs/building.md](docs/building.md) as a build-both-targets rule. **(b) plus (c) is probably the cheap correct pair** — (a) is a build-graph change with wider blast radius, and the diagnostic is wrong regardless of whether the build changes. **Scope** `SE16/SynthEditSem/CMakeLists.txt` and whichever file raises the export diagnostic — check the GATED line before reaching. **Accept** a tree built with `--target TIDE_VST3` alone either loads without the dialog, or produces a message that names the stale `TIDE.gmpi` rather than the user's install. **Size** minutes for (c), one session for (b). **Mac finding, 2026-08-16 (interactive U2d session): the mac build has NO counterpart to the win post-build module-DB copy at all** — `/Library/Audio/Plug-Ins/GMPI/TIDE.gmpi` sat 3.5 months stale (May 7 build, pre-P5 identity, zero `ContainerViewPanel` symbols) until refreshed by hand; the fresh `TIDE.gmpi` lands only in `build/SynthEditSem/Release/`. Note the stale DB was RULED OUT as U2d's cause (see that row), so this stays exactly what P11 says it is: a build/install trap with a misleading symptom. Documented meanwhile in [docs/building.md](docs/building.md). |
| U2 | TODO | mac | **Triaged on mac the same day (2026-08-16, interactive session, minutes after the Windows re-test above landed) — root causes named, split into U2a/U2b/U2c/U2d; the merged two-box picture is in [docs/u2-triage-2026-08-16.md](docs/u2-triage-2026-08-16.md).** The two boxes' findings interlock. **(2) is mac-only and is FIXED** — the mac backend received wheel events and dropped them (`DrawingFrameMac.mm:813/818`, both dispatch lines commented out as TODO), which is exactly why Windows never reproduced it; fix mirrors the Windows path, live-verified in REAPER 7.45, IN-REVIEW as **U2a** ([gmpi_ui#7](https://github.com/JeffMcClintock/gmpi_ui/pull/7)). **(1) is not a code defect on either platform**: placement is click-to-arm → click-to-place by design (`SynthEditGui.cpp`'s OM_DRAG_NEW_MODULE path → `ViewBase::DragNewModule` → drop on the next `onPointerDown`), and the mac session PROVED the designed gesture works — browser click then canvas click placed a List Entry at the exact click point; what both boxes reproduced is that press-drag-release does not place, which is a UX decision recorded in the doc (default in effect: the design stands). **(4) is root-caused to a line, and it also explains the Windows "every later module landing on that same point"**: `TopView::centerPos` defaults `{0,0}` (`ViewBase.h`), TIDE never calls `setCenter`/`setPanZoom`, and `AddModule(moduleId, view->getCenter())` inserts at that same corner — **U2c**, one line in ALLOWED `TideApp.cpp`. **(3) is cross-platform and is the real remaining unknown** — the placed control draws only its ResizeAdorner around a degenerate rect (Jeff identified the adorner live; Windows saw it at literal zero size); standing hypothesis is missing skin/panel resources (`TIDE_VST3.vst3` stages NO Resources at all while the panel pipeline is skin-driven, `ContainerView.cpp:25` → SkinMgr), with P11 ruled out by the Windows re-test — **U2d**, and it gates whether the rack can display anything. The Windows guess "(3)+(4) one geometry cause" was half right: adjacent but distinct. This row stays as the umbrella until the children land. Original row follows. **Re-tested on Windows the same day (2026-08-16, interactive session) and three of the four reproduce — so this row is NOT mac-only, and its own root-cause guess is wrong.** REAPER 7.78/win64 rev 608e49, x64, a Release `TIDE_VST3` off `origin/master`. **(1) drag-drop places nothing — reproduces.** **(2) scroll wheel — does NOT reproduce; the wheel scrolls the module list and the canvas normally on Windows.** **(3) placed control draws wrong — reproduces, and worse: on Windows it does not draw at all, only its selection/resize adorner does, collapsed onto a zero-size rect at the canvas origin, with every later module landing on that same point** (`Text Entry` identical). **(4) the §6 canvas offset/dead-strip — reproduces**, canvas anchored right+bottom with dead grey filling top and left, translating with the right edge on resize. **This kills the row's own "(1)/(2) smell like one event-routing cause"**: (1) reproduces and (2) does not, so they are two causes, not one. **Start triage from (3)+(4) as one geometry cause, with (1) separate.** The mac's "~10 px glyph stack" and Windows' "zero-size" are the same defect at two magnitudes — the model is right on both (properties pane fully correct, Appearance=Combo Box) and only the panel geometry is wrong. Also cleared as a confound: the Windows box first threw *"Export failed: required module is missing from the module database"*, which is the separate **P11** build trap and **not** part of this row — (3) still reproduces after P11 is fixed. **The `Plat` cell above still reads `mac` and now understates this row; it wants `any`.** It was left alone deliberately: [scripts/check-backlog-diff.py](scripts/check-backlog-diff.py) fails any run that changes `Plat` on an existing row, by design, so that cell needs Jeff or a deliberate human edit rather than a silent one here. Original row follows. **First-render defects of the panel view in a host — filed 2026-08-16 by the interactive session that closed U1a's bar (b).** Environment: REAPER 7.45/macOS-arm64, the U1a build of TIDE_VST3.vst3 (universal, ad-hoc signed). The rack renders and survives instantiate → UI open → insert → select → window resize; these four do not: **(1) drag-and-drop from the module browser onto the canvas places nothing** — two synthetic drag profiles (instant and stepped-slow) and Jeff's real mouse all failed, while **double-click inserts fine**, so it is the drop path, not insertion; **(2) the mouse scroll wheel does nothing anywhere in the plugin UI** (real hardware); **(3) a placed List Entry control draws as a ~10 px stack of glyphs** instead of a usable combo box, while its properties pane is fully correct (pins, parameters, Appearance=Combo Box) — the model is right, the panel geometry is not; **(4) the §6 canvas offset/dead-strip layout survives in the panel view** — the gridded canvas floats offset inside the pane with dead grey around it and re-anchors oddly on window resize. Moog Filter showing no panel is correct panel-view semantics, not a fifth defect; module browser and properties pane otherwise work. **Triage before splitting** — (1)/(2) smell like one event-routing cause and (3)/(4) like one geometry/DPI cause, and U1c cannot be costed until that triage happens. **Scope** unknown until triaged; start in the view-host glue TIDE shares with SynthEdit (`SE16/SynthEditSem/` is ALLOWED — check the GATED line before reaching deeper). **Accept** for this row: a triage note naming root cause(s) and the split; per-defect accepts land on the split rows. **Size** triage fits a session; fixes unknown. |
| U2e | IN-REVIEW | any | **Decision made and shipped the same morning (2026-08-17, interactive, Jeff directing) — IN-REVIEW: [SynthEdit#30](https://github.com/JeffMcClintock/SynthEdit/pull/30) (stacked on [#28](https://github.com/JeffMcClintock/SynthEdit/pull/28)) + [SynthEditLib#13](https://github.com/JeffMcClintock/SynthEditLib/pull/13), and the mac Accept is MET: a placed List Entry draws as a real combo box with styled title in REAPER, browser curation intact, no crash.** The scheduled run's trace below reached the same mechanism from source alone, hours before this session shipped the fix — convergent, not duplicated. The shipped answer to its decision question: stage `ControlsXp.xml` into `Contents/Resources` from SynthEditLib's copy (single source of truth, no drift; P6's rule keeps data out of `MacOS/`) and merge it in `TideApp::InitInstance` **into already-registered classes only** — a plain `RegisterPluginsXml` grew insertable phantom browser entries (observed live: Keyboard (MPE) placed as an empty adorner), because `Module_Info3_internal` never marks XML-only entries unavailable and the browser filters only `!isDllAvailable()`. **Win follow-ups before DONE:** the staging equivalent (win `getResource` reads the exe resource first, then the same bundled file) and the combo re-verify there. Earlier row follows. **The pin-delivery trace this row demanded is DONE — 2026-08-17 (macos, scheduled) — and it names a root cause this row had wrong: [docs/u2e-pin-delivery-trace.md](docs/u2e-pin-delivery-trace.md).** **`Module_Info` for `SE List Entry` has ZERO GUI pin descriptions in TIDE, so the handler pass is not skipped, it is never requested.** `GMPI_REGISTER_GUI`/`REGISTER_PLUGIN2` (`mp_sdk_gui.h:12`, `mp_sdk_audio.h:26`) register a constructor by id and nothing else (`UgDatabase.cpp:242` — no `ScanXml`); the pin metadata lives in `ControlsXp.xml:262-283`, which `modules/plugin_helper.cmake:236` copies into the `.sem` bundle for **the module scan S1a removed**. Both initial-value paths in `ViewBase::ConnectModules` gate on `moduleInfo->gui_plugs` (`ViewBase.cpp:711`→`:742` and `:778`→`:799`), so zero `setPin` calls are made, so `ModuleView.cpp:1404`’s `notifyPin` never fires, so `ListEntryGui::onSetAppearance` (`ListEntryGui.cpp:49` — the only `widgets.push_back` site) never runs, so `widgets` is empty at `initialize`/`measure`/`arrange`: the three recorded SIGSEGVs, in `Refresh`’s own call order. **Verified from the shipping binary with positive controls** (`master`, Release, BUILD SUCCEEDED 2026-08-17): `SE List Entry` id-string ×2, embedded `<Plugin id=…>` XML ×**0**; `SE Background Image`/`SE Patch Point in`/`PatchAutomator` (all `withXml`) ×2 each. Only 29 modules in the whole TIDE binary carry XML. **This is a wall in front of TIDE’s whole fixed module set (constraint 7), not a List Entry defect** — every future classic control lands in the same state. **The row’s skin-bitmap/ImageCache suspicion is NOT the gate** and drops to second hypothesis; nothing in the widget-construction path is reached to fail. Pin IDs were ruled out too (`ListEntryGui.cpp:32-40` seeds id 10 and auto-increments to 11-16, matching the XML exactly). **Next step is mechanical, and the trace doc costs the three fix options:** embed the XML in TIDE’s build and `RegisterPluginXml` it from `TideApp::InitInstance` (ALLOWED, cannot regress the scanning editor) is the recommendation; swapping ControlsXp to `withXml` is tidier but changes commercially-shipped scanning behaviour no mac box can test. **No code landed this run and that is deliberate:** the acceptance check is a GUI observable, the run was unattended, and the fix stacks on the still-open [SynthEdit#28](https://github.com/JeffMcClintock/SynthEdit/pull/28). **UNVERIFIED beyond the binary artifact — nobody has yet seen a List Entry draw.** Original row follows. **First pass landed 2026-08-16 (macos, interactive, same evening) — crash-free placeholders, and the remaining gap is one sentence: the pin-update handlers that build the widgets never run in TIDE's SDK3 hosting.** PRs: [SynthEdit#28](https://github.com/JeffMcClintock/SynthEdit/pull/28) (TideApp seeds `GmpiResourceManager::resourceFolders[Image]` — the base InitInstance's seeding was dropped by the S1a override, so skin-image URIs resolved against an EMPTY map; real prerequisite, but proven insufficient alone) + [SynthEditLib#13](https://github.com/JeffMcClintock/SynthEditLib/pull/13) (the third empty-widgets guard, `arrange()`). **Verified in REAPER on the final build: no crash through instantiate → insert → select; a placed List Entry draws as a right-sized selectable 100×20 placeholder; Background Image renders alongside.** The controls stay in the module list — visible placeholder + stderr breadcrumb beats a dead host or an invisible module. **Why the gate is pin delivery, argued from defaults rather than instrumentation:** widgets build in `onSetAppearance()` (handler registered in the ctor via `initializePin`, gated only by `currentAppearance == pinAppearance`, ctor default -2) — had the handlers fired even once with default values, ACM_PLAIN would have built a ListWidget; the vector is empty, so the handler pass never happens. **Next step, directed:** trace how `ModuleView`'s Sdk3 path delivers initial pin values (the fake plugs `Ctl_Combo::Export` writes) and why the handler pass is skipped — the same wiring full SynthEdit exercises when these controls work there. Original row follows. **The classic SDK3 control GUIs crash when their widgets never build — the layer between U2d's registrations and a usable List Entry.** Split from U2d, 2026-08-16, with three distinct SIGSEGV stacks from one hour of REAPER testing on mac: `ClassicControlGuiBase::initialize` (`widgets.back()` on an empty vector — crash address -16 is the tell: empty `back()` with 16-byte `shared_ptr` elements), `ListEntryGui::measure` (`widgets[0]`, null at 0x0), and `ListEntryGui::arrange` after both were guarded. **Mechanism:** the ControlsXp widget layer builds its widgets during pin-init (`widgets.push_back` in the appearance callback, `ListEntryGui.cpp:88-151`) and every later method assumes that succeeded — 47 unguarded `widgets[` sites in `ListEntryGui.cpp` alone, so guarding call-sites is whack-a-mole (proven: the crash just moved twice). **The fix is upstream: make widget-building succeed in the plugin context, or fail into a placeholder widget** — the suspicion is skin-bitmap/font loading (`BitmapWidget::Load` → ImageCache → GmpiResourceManager) failing in the VST3, which is also where the "skins" thread from the triage doc re-enters one layer down. **Repro:** re-add `ControlsXp/ListEntry*.cpp`, `TextEntry*.cpp` + deps (`ClassicControlGuiBase`, `sharedLegacyWidgets/*`, `shared/ImageCache+ImageMetadata+FontCache`) to the module list in `SynthEditSem/CMakeLists.txt` (the exact list is in that file's U2d comment), build, insert List Entry in REAPER. **Guards already landed** ([SynthEditLib#12](https://github.com/JeffMcClintock/SynthEditLib/pull/12)) keep initialize/measure survivable; arrange and the pin callbacks are NOT guarded. **Scope** `SynthEditLib/modules/ControlsXp` + `sharedLegacyWidgets` + the resource-loading path; ALLOWED (public repo), but it is Jeff's control-rendering architecture — design intent beats patching. **Accept** a placed List Entry draws as a usable combo box in a host on both platforms, and a control whose resources are missing degrades visibly (placeholder) rather than crashing or vanishing. **Size** a real session; the crash stacks and repro make it a directed one. |
| S11 | TODO | any | **BLOCKER FIXED 2026-08-17 (Jeff chose base64, codec in GMPI): [GMPI#3](https://github.com/JeffMcClintock/GMPI/pull/3) + [SynthEditLib#17](https://github.com/JeffMcClintock/SynthEditLib/pull/17) — MUST MERGE TOGETHER.** **The saved VST state grows from 250 bytes of base64 to 1503 and the `.rpp` now contains the rack document inside the `<Preset>`.** Codec at **`GMPI/Core/base64.h`** (lowest layer needing it — both ends of the round-trip are in `Hosting`); `SynthEditLib/Base64.h` is now a thin forwarder, so one implementation, four call sites unchanged. **SECOND HALF WAS NOT OPTIONAL:** with encoding alone **the editor rendered blank** — TIDE's `controllerPtr` is a blob holding a **raw pointer** marked `persistant="false"`, harmless while blobs serialised as `"0"` but fatal once they round-trip, since restoring it hands the plug-in a dangling pointer from a previous run. Fixed by honouring the existing flag (`if (!parameter.info->is_stateful) continue;`); **proved mine by A/B — stash the change and the editor renders again.** **LESSON: fixing a lossy round-trip can resurrect things that were only safe while broken — audit what relied on the loss.** **REMAINING FOR THIS ROW:** confirm the RESTORE side rebuilds the rack on load (place a module, save, reopen, look). The reader decodes base64 now, so it may already work — measure first. A saved test project is left at `/tmp/tide-persist3.rpp` for exactly that. **BLOCKER IDENTIFIED 2026-08-17 — exactly one thing stands between today's work and a rack that survives reload: BLOB PARAMETERS ARE NOT ENCODED IN THE PRESET.** Measured with a full instrument patched and playing: the saved VST state is **250 bytes of base64 before the save and 250 after the reopen** — the same number this row recorded originally. The document now reaches the processor (MIDI notes play), but it is never written into DAW state. **Both halves are already flagged in the source:** **writer** — `gmpi_processor::getPresetUnsafe()` writes every parameter as `SetAttribute("val", parameter.valueReal())`, the *double* accessor, so a blob serialises as `val="0"` and the bytes are dropped (exactly the two-parameter all-zero `<Preset>` this row dissected); **reader** — `GmpiParameter::setFromXml` contains verbatim `case gmpi::PinDatatype::Blob: break; // TODO uuencode or hex`. **Blobs have never round-tripped through a preset — TIDE is simply the first plug-in whose entire state IS a blob.** **DECISION NEEDED FROM JEFF (it is a persisted format, so it is permanent): base64 or hex, and where the codec lives** — `SynthEditLib/Base64.h` exists but **GMPI is the lower layer and cannot depend on SynthEditLib**, so GMPI needs its own encoder (or the pair moves down into GMPI). Both sites are one function each; the work is small, the format choice is not. **Deliberately not implemented unsupervised:** the rest of today's fixes were behaviour, this one is compatibility — a preset written by the first implementation must stay readable by every later one. **DESIGN ANSWERED BY JEFF 2026-08-17 — the three questions below are settled, so this row is specified and no longer needs his input to start:** **(1) A new document implies a rebuild of the DSP graph, and SynthEdit already handles that — fade-out → teardown → reconstruction → fade-up.** So restore drives the existing mechanism rather than inventing one. **(2) A host preset change MAY modify the rack** — treat it as loading a brand-new document; anything can change. **(3) All state lives in the preset** — which settles "plug-in state vs a referenced user file" in favour of the preset, and is what makes (2) coherent. **BLOCKED IN PRACTICE BY S12, discovered while reading the path ruling (1) points at: there is no DSP graph to rebuild** — TIDE's processor is a silent stub. The restore path here can be written and will be correct, but its DSP half cannot be exercised or verified until S12 lands, so **take S12 first**. Original row follows. **TIDE never saves or restores the user's rack — the patch is lost the moment a project is reloaded.** Found 2026-08-17 (macos, interactive) while measuring U1c's `rackMode`-on-load follow-up; the follow-up turned out to be unanswerable because **nothing loads a document at all**. **Evidence, three cheap steps:** (1) the saved VST state is **250 bytes of base64** for a project containing a placed List Entry in a rack (`GetTrackStateChunk` via ReaScript) — a document with a module in it cannot fit; (2) decoded, the `.rpp`'s VST block is literally `<Preset><Param id="1" val="0"/><Param id="0" val="0"/></Preset>` — `controllerPtr` and `chunk`, both zero; (3) **save → close → reopen → the module is gone**, the plug-in draws an empty rack (rails present, because rack mode is set at document creation). Verified visually. **Mechanism, so this is actionable:** TIDE's XML already declares the parameter this needs — `<Parameter id="1" name="chunk" ignorePatchChange="true" datatype="blob"/>` in `SynthEditSem/SynthEdit.cpp` — and **nothing ever writes or reads it**; `TideApp::InitInstance` unconditionally calls `createNewDocument()` + `OnNewDocument()`, so every instance starts empty by construction. `MpController`/`DawPreset` serialise *parameter values*, which is exactly the two-param XML observed. The document already has serialisers: `CSynthEditDocBase::ExportXml` / `ImportXml`. **Why this is an architecture difference, not a regression:** in a normal SynthEdit-exported plug-in the document IS the product — baked in at export, so the chunk need only carry knob values. TIDE inverts that: the document is what the user edits at runtime, so it must ride in the state, and nothing before TIDE needed that. **Scope** `SE16/SynthEditSem/` (ALLOWED) for the save/restore hooks; the serialisers are in `SynthEditLib`. **Accept** place a module, save the host project, reopen it, and the module is still there — the same three-step test above, passing. **Questions that need Jeff before the design settles:** does restoring a document have to rebuild the DSP graph (and if so, is that safe mid-session)? should a host patch/preset change reload the rack, or only project load? and does the rack belong in the plug-in state at all, or in a user-visible file the state merely references? **Size** a real session, probably more; do not start it at the end of one. **This blocks the R-series** — a synthesiser that cannot save its patch is not shippable, which turns "there is nothing to ship" into one concrete thing to build. |
| S12 | IN-REVIEW | any | **Every PR linked in this row has merged (checked 2026-08-17, windows) and it is still NOT `DONE`, deliberately** -- the row itself names remaining work (the faded swap, preset retention) and the same day's mac journal entry says S11's restore has one open blocker. A run flipping IN-REVIEW on merged PRs alone would have called this landed; read the row, not just the links. Original row follows. **S12(a) CLOSED FROM UPSTREAM 2026-08-17: with every local override cleared and both GMPI fixes merged ([#1](https://github.com/JeffMcClintock/GMPI/pull/1), [#2](https://github.com/JeffMcClintock/GMPI/pull/2)), a MIDI note still plays — peak 0.5103 (−5.8 dB) with a note, 0.0001 stopped, identical to the override build.** A fresh clone now hears MIDI. **The bot has write access to GMPI** (it had `push=true` everywhere else and `push=false` there alone; invitation accepted via `gh api -X PATCH user/repository_invitations/<id>`), so **the patch-file route is retired** — `docs/patches/README.md` marks both files superseded and says to open a PR instead; **the `.patch` files stay because JOURNAL entries link to them and the journal is immutable.** **NEAR-MISS WORTH KEEPING: `cmake` succeeded against STALE deps.** Clearing the overrides does not make FetchContent re-pull an already-populated dependency, so the first "upstream" build compiled the old checkouts and would have passed as a false verification; fixed with `git fetch && git reset --hard origin/main` in each `_deps/*-src`. **Grep the fetched source for the change you are verifying — a green build proves nothing about which code was compiled.** Also: `SE_LOCAL_BUILD=TRUE` needed restoring after `--fresh` for the second time today (see docs/building.md), and a polluted test instance (modules dropped into the rack by failed UI batches) caused one spurious silent reading — **rebuild the rig in a fresh tab before debugging the product.** **S12(a) DONE 2026-08-17 — A MIDI NOTE PLAYS A TIDE RACK.** Measured **peak 0.5103 (−5.8 dB) while the note sounds, 0.0001 with the transport stopped**, so audio follows the notes rather than droning (the comparison Jeff's VCA correction made possible). **Root cause was NOT the two processor instances** — it was that `gmpi_processor`'s pin-initialisation loop seeded parameter-backed pins for `Float32`/`Int32`/`Bool` while **`Blob` fell through to `default: assert(false)`**, so a newly created processor started with an empty blob pin and was never told otherwise (blobs only reach the processor when they CHANGE). Hosts create processors at will — `restartComponent`, offline/anticipative processing, state restore — and REAPER's second instance was receiving all the MIDI with no document. **Fix is generic and in GMPI; the bot cannot push that repo (403), so the patch is filed at [docs/patches/gmpi-seed-blob-pins.patch](docs/patches/gmpi-seed-blob-pins.patch)** (72 lines) — **Jeff must apply it**, and until then `GMPI_SDK_FOLDER_OVERRIDE` on the mac box points at the local checkout. **Lesson: a `default: assert(false)` is a to-do list.** The same switch pattern bit twice today — `sendParameterToProcessor` was missing its Blob case this morning; fixing one arm without grepping every switch over `PinDatatype` in that file left the initialise path broken for six hours. **S12(a) SOLVED 2026-08-17 — the blocker is TWO PROCESSOR INSTANCES, and MIDI goes to the one that never receives a document.** One instrumented run settles it: `onMidiMessage proc[0x12b0b0800] prepared=1` ×31 (the editor's instance, which gets every `PREPARE` and is the only one running `subProcess`) versus **`onMidiMessage proc[0x12ba36800] prepared=0` ×77** — a second processor that receives the host's MIDI, never gets a document, and never processes audio. **Cause is a flaw in the S12 design, not a host quirk:** `serviceDocumentSync` pushes the document **only when the XML changes** (dedupe on `lastPushedDspXml`), so **any processor created afterwards starts empty and stays empty** — and a host may create one at any time (re-instantiation after `restartComponent`, offline/anticipative processing, state restore). **FIX DIRECTION — prefer the generic one:** a newly created processor should be **seeded with the current value of every parameter, blobs included**; the chunk parameter already persists in DAW state, so the same delivery that restores a saved project should seed a mid-session instance. **Check `gmpi_processor`'s construction path first** — if it seeds pins from `patchManager` but skips blobs, this is a small generic fix beside this morning's blob-transport work rather than a TIDE-specific patch; re-pushing on a timer is the hack to avoid. **RETRACTION:** the previous entry's "MIDI delivery stops across a document rebuild" is **wrong** — MIDI never stopped; the **transport** had stopped, because clicking in the editor to place modules halts playback. The pointer evidence was real, the conclusion was not. **Missing control: confirm the transport is running while measuring** (`reaper.defer`'s first line reports `play_state`). **S12(a) CHOICE (ii) BUILT 2026-08-17 — Jeff ruled "MIDI-in will eventually be a rack module with a patch-point of its own, perhaps more like a MIDI-CV", so the runtime feeds it: [SynthEditLib#16](https://github.com/JeffMcClintock/SynthEditLib/pull/16) (29 lines).** `SeAudioMaster` remembers a registered `MIDI In` module (symmetric with `audioInModule`/`audioOutModule`) and `MidiIn()` delivers with the **same `AddMidiEvent` call `UIoManager::OnMidiData` makes**, so the module behaves identically in standalone and plug-in; the VST-input route is untouched and now null-guarded. **VERIFIED:** the module registers in a plug-in (`RegisterIoModule: midiIn=1`, pointer stored). **NOT VERIFIED, because a SECOND DEFECT surfaced: MIDI delivery stops across a document rebuild.** Instance pointers made it unambiguous — every delivery went to `SeAudioMaster[0x11ce0e2a0]` (the graph before the rebuild) while registration landed on `[0x11e39a370]` (the graph after), which then received **nothing** despite the transport running for 240 sampled frames. **NEXT PROBE:** log in TIDE's `onMidiMessage` and in `SynthRuntime::MidiIn` across a document change and compare instance pointers on both sides — the cheapest way to see whether the processor stops receiving, or stops forwarding, or forwards to a stale generator. **Method notes:** (1) log the instance pointer whenever "registered" and "not receiving" seem contradictory; (2) read the log's ORDER — the registration line was simply LAST, and a `tail` overturned a wrong conclusion; (3) **CRLF**: a line-based Python rewrite turned a 29-line change into a 5754-line diff on `SeAudioMaster.*` (the same trap as the GMPI patch); use the byte-safe `b'\r\n'`-aware edit helper. **S12(a) CONTRACT WORKED OUT 2026-08-17 (Jeff ruled: synthesise container IO, least disruptive to SynthEdit). Read from the IMPORTER, so it is exact:** **(1) container IO plug** — any `<Plug>` with a `Direction` attribute is one (that is literally how `ug_base::Setup` identifies it): `<Plug Direction="0" Datatype="2"/>` = MIDI **in**, since `EPlugDataType{DT_ENUM=0, DT_TEXT=1, DT_MIDI2=2, DT_DOUBLE=3, DT_BOOL=4, DT_FSAMPLE=5}`. **(2) IO Mod tie** (only honoured when the module has `UGF_IO_MOD`): `<Plug Direction="1" Datatype="2" TiedTo="<containerHandle>" TiedToPinIdx="<n>"/>` → `up->TiedTo = p2; p2->TiedTo = up;`. **(3) connections:** `<Line From="<handle>" To="<handle>" FromPin="i" ToPin="j"/>`, pins default 0, handles via `HandleToObject`. **CONSTRAINT THAT SHAPES THE FIX — the browser's "MIDI In" module CANNOT be the endpoint in a plug-in.** `modules_internal/MidiIn.h` is `class MidiIn : public MpBase2, public ISpecialIoModule`; it self-registers via `AudioMaster()->RegisterIoModule(this)` in `open()`, which in the standalone reaches `UIoManager` (fed from a MIDI device) but in the plug-in reaches **`SynthRuntime::RegisterIoModule(ISpecialIoModule*) { return 1; } // nothing special to do in plugin`** — so it registers and is never fed. Its Audio pins confirm it is a source only (`MIDI Data` out, `Activity` out, `MPE Mode` in — **no MIDI input pin**, so nothing can be routed into it either). **The classic plug-in path is therefore the only one available and is exactly the ruling:** host → `vst_in` → **synth container's DT_MIDI2 plug** → **IO Mod** inside → the user's MIDI-CV 2 etc. **REMAINING CHOICE IS UX, AND IT IS JEFF'S: (i)** synthesise container plug + tied IO Mod at export and let users patch from the IO Mod (already in TIDE's module list; **no SynthEdit change**), or **(ii)** keep "MIDI In" as the user-facing source and teach `SynthRuntime` to feed registered MIDI modules like `UIoManager` does (nicer UX, but it is the SynthEdit change the ruling was avoiding). **S12(a) ROOT CAUSE FOUND 2026-08-17 — suspect (a) REFUTED, and the real break is one layer lower: TIDE's rack container exposes NO MIDI plug and NO audio plugs.** **(a) is wrong:** `MpeConverter::processMidi` opens with `if (isMidi2Message(msg)) { sink(msg, timestamp); return; }` — **MIDI 2.0 passes through by design**, so the UMP packets TIDE forwards are exactly what SE accepts; nothing needs translating. **The chain probe (four points, one run) shows the break:** `SetupVstIO: synthModule=1 plugs=3` / `synth plug: type=0`, `type=4`, `type=0` / `AudioMaster::MidiIn len=8 [40 90 3c] audioIn=1` / `vst_in sink: size=8 **inUse=0** mo=1`. With `EPlugDataType = DT_ENUM 0, DT_TEXT 1, DT_MIDI2 2, DT_DOUBLE 3, DT_BOOL 4, DT_FSAMPLE 5`, those three plugs are **ENUM/BOOL/ENUM** — no DT_MIDI2, no DT_FSAMPLE. `SetupVstIO` scans exactly those for MIDI and audio, finds neither, connects nothing, and the MIDI is sent into an unconnected plug. **This also explains the asymmetry:** audio works NOT via container plugs but because **Sound Out is a special IO module** (`ug_soundcard_out` calls `RegisterIoModule` at `Open()` and takes the host's buffers directly); MIDI has no such registration, so it depends on plumbing that does not exist. **Fix direction:** give the inner rack container real plugin IO — a `DT_MIDI2` input plug wired to the patch's MIDI In module (plus `DT_FSAMPLE` outputs if audio should ever leave via the container rather than Sound Out's registration); in SE terms that is an **IO Mod**, and `exportDspXml`'s synthetic outer container is the natural place to synthesise it. **Design question for Jeff: should TIDE instead make the patch's "MIDI In" module a registering special IO module — the symmetric counterpart of Sound Out?** **Build trap recorded:** the first probe run logged nothing because `cmake --fresh` had cleared `SYNTHEDITLIB_FOLDER_OVERRIDE`, so the build compiled SynthEditLib fetched from GitHub and local edits were invisible — `strings` the installed binary for the probe's log path before believing a silent probe. **S12(a) MEASURED 2026-08-17 — MIDI reaches the processor but NOT the graph.** (1) **Proven arriving:** a temp probe in `onMidiMessage` logged 42 messages with `prepared=1` during a looping C4 — note-on `0x90`, note-off `0x80`, note `0x3c` — all forwarded to `rack.MidiIn`; **they are 8-byte MIDI 2.0 UMP packets** (leading `0x40`), not MIDI 1.0 bytes. (2) **Audio path proven alive:** oscillator straight to Sound Out clipped the master at +10 dB. (3) **A COMPLETE instrument is silent:** per Jeff, an osc wired straight to Sound Out drones at default pitch regardless of MIDI and proves nothing — so the patch was rebuilt properly (MIDI In → MIDI-CV 2; Pitch → Phase Dist Osc; **Gate → VCA Volume**; Osc → VCA Signal; VCA → Sound Out) and the measured peak is **−156.7 dB = digital silence** through the note. **Two suspects, in order: (a) MIDI FORMAT** — GMPI delivers UMP while `SeAudioMaster::MidiIn` may want MIDI 1.0; `se_vst3`'s `SeProcessor` does explicit translation (`midi2data`/`midi2size`, advertises `kMIDIProtocol_2_0`) that TIDE's one-line forward does not — compare those call sites first; **(b) CONTAINER PLUMBING** — `SetupVstIO` connects VST Input's "MIDI Out" to *the synth container's* MIDI plug, and S12 wraps the flat rack in a **synthetic outer container**, so MIDI may land on the outer container and never reach the inner rack holding the MIDI In module. **Method note:** a tight Lua `Track_GetPeakInfo` loop measures NOTHING — it blocks REAPER's main thread and re-reads one frozen snapshot (9.3M "samples", transport position never advancing); use `reaper.defer`, one sample per main-thread cycle. **Also:** TIDE's set has **no MIDI Monitor** (Diagnostic = 1 kHz Tone + DAW Sample Rate only). **FIRST SOUND 2026-08-17 — the thin-slice Accept is met and HEARD (Jeff: "i can hear it!"): place 1 kHz Tone, place Sound Out, drag the cable — track meter −6.0 dB, master green.** The loop is live end to end: edit → export → chunk param → processor → rebuild → audio. IN-REVIEW: [SynthEditLib#15](https://github.com/JeffMcClintock/SynthEditLib/pull/15) + [SynthEdit#40](https://github.com/JeffMcClintock/SynthEdit/pull/40) (diagnostics stripped). **The three finds that unlocked it:** (1) the module pruning was `ExportXml_Pt2`'s DESIGN for top-level containers under any target but `SAT_SYNTHEDIT_DSP` — TIDE now exports the editor's own runtime format; (2) **the AsyncRestart swap path is unreachable in the plugin runtime** (`eRuntimeState::resetting` is a case label nothing enters) — `documentPending_` now forces `prepareToPlay` to rebuild and the processor re-calls it per document arrival, synchronous and fine at rack scale; (3) **no browser unhide needed** — "VST Output" is SetupVstIO's internal plumbing, and plain **Sound Out** is the user sink: `ug_soundcard_out` registers as `ISpecialIoModuleAudioOut` at Open and takes whatever buffers the shell provides (device in the standalone, host buffers in TIDE). IO Mod refuses master-level instantiation (needs a parent), which closes that path. **Remaining before this row is DONE:** MIDI-note path (wired `onMidiMessage`→`rack.MidiIn`, untested), the faded document swap (replaces the synchronous rebuild without changing transport), preset retention across edits, and a save/reopen re-check now that the chunk carries real documents (S11's restore half). **GMPI patch still unapplied** — main lacks "ppc3" while GMPI_Wrappers main requires it; builders need the local branch or the patch from [docs/patches/](docs/patches/gmpi-blob-param-transport.patch). Original row follows. **IN-PROGRESS 2026-08-17, built to its last step — the rack HAS an engine; the tone is one export gate away.** Four-repo stack: GMPI branch `tide/mac/blob-param-transport` (**bot cannot push there — 403; patch filed at [docs/patches/gmpi-blob-param-transport.patch](docs/patches/gmpi-blob-param-transport.patch) for Jeff**), [GMPI_Wrappers#4](https://github.com/JeffMcClintock/GMPI_Wrappers/pull/4), [SynthEditLib#14](https://github.com/JeffMcClintock/SynthEditLib/pull/14), [SynthEdit#39](https://github.com/JeffMcClintock/SynthEdit/pull/39) (WIP, temp diagnostics tagged `TEMP S12 diag` deliberately in). **Working and verified in REAPER:** (1) generic blob-parameter transport controller→processor — did not exist in the wrapper at all; the unused "ppc3" framing is now sent (holder hook), carried (VST3 binary message) and consumed (Blob PinSet event); (2) the document travels editor → chunk blob param → processor pin (three-point log); (3) **an empty document builds and RUNS** (`prepared SR=48000 BS=512`); (4) a wired-document push while running takes the AsyncRestart fade/teardown/rebuild **without crashing** — the `#if 0 // editor only` pendingDspXml hook is enabled and working. **Shape lessons encoded in TideApp::exportDspXml:** synthetic outer container (SetupVstIO wants main→synth-child; the flat rack IS the master) with the PatchManager moved to the outer (BuildPatchManager runs on main unconditionally). **The frontier: placed modules are pruned from the export** — `<Modules/>` empty while the cable's `<Line>` survives. Fixed first cause: `EXP_PLUGIN` → `doExport()` drops `excludeFromVst` modules (Sound Out + all Diagnostics) → flags now 0 (TIDE self-hosts editor semantics). **Still pruned with flags 0; prime suspect `hasDspModule()` false — the module set's DSP-side registrations never ran in TIDE, the exact U2d pattern on the DSP half. NEXT PROBE (one build): in exportDspXml log `name / doExport() / hasDspModule()` per master-child.** **Build note:** the build cache sets `GMPI_SDK_FOLDER_OVERRIDE` + `GMPI_WRAPPER_FOLDER_OVERRIDE` to local checkouts until the GMPI patch and GMPI_Wrappers#4 land on main (CPM fetches main). After the tone: remove diagnostics, then S11's restore rides this same wire (the chunk already persists in DAW state). Original row follows. **MAPPED 2026-08-17 after Jeff's pointer (SynthEdit builds the graph for its own use and swaps the DSP smoothly; the SynthEdit VST3 target is similar and can rebuild under certain conditions) — scope is now KNOWN and this row is start-ready:** **(1) the plugin-side graph host exists** — `SynthRuntime` (`SynthEditLib/SynthRuntime.cpp`, sibling of the standalone's `SynthRuntime_editor`) owns `SeAudioMaster` and builds the whole graph from XML; today it reads the exporter-baked `dsp.se.xml` bundle resource (line 59), and **the cache check `if (!currentDspXml.RootElement())` is the injection point** — a pre-seeded document skips the bundle read entirely. **(2) the smooth swap is real and complete** (`SynthRuntime.cpp:330-395`): AsyncRestart → retain presets → `Close()` → new `SeAudioMaster` → background `rebuildDsp` thread → fade-up — **and a `pendingDspXml` document-swap hook already exists behind `#if 0 // editor only` inside that very path**, disabled because an exported plug-in's document never changes; TIDE is the product it was sketched for. **(3) a complete working reference exists:** `se_vst3`'s `SeProcessor` (`adelayprocessor.cpp`, 1502 lines) — `prepareToPlay`/`reInitialise`, MIDI translation, queue servicing, `ProcessorStateMgrVst3`, `setState`/`getState` as `DawPreset` chunk — Jeff's "all state lives in the preset" is that target's existing design. **(4) the editor can emit the DSP XML at runtime:** `dsp.se.xml` is just `<Document><DSP>` + `MasterContainer->ExportXml(element, target)` — fifteen copyable lines at `ExportAsPlugin.cpp:1204-1220` (skip the Release `Scramble`). **So S11 and S12 are ONE mechanism:** document XML rides in the preset; a preset carrying a new document sets `pendingDspXml` and triggers AsyncRestart; the rebuild thread builds the new graph while audio fades. **One fork needs Jeff's one-word ruling — A (recommended): keep the GMPI wrapper**, grow TIDE's processor a `SynthRuntime`, document travels through the already-declared `chunk` blob parameter bound to a DSP pin (`BlobInPin` exists in sdk3; GMPI-Core equivalent is a named unknown) — all of this week's controller/editor wiring survives; **or B: rebase onto `se_vst3`'s `SeProcessor`/`SeController`** — processor ready-made, but `controllerPtr`, TideApp attachment and editor hosting all get redone against Steinberg classes. **Named unknowns for either:** `BundleInfo` calls inside `SynthRuntime` (`latencyConstraint`, resource folder) against a bundle lacking the exporter's resources; in-process queue wiring TideApp ↔ runtime. **Start with a fresh session; thin-slice accept unchanged** (1 kHz Tone → Sound Out → host meter moves). Original row follows. **TIDE makes no sound. The audio processor is a stub that writes silence, and nothing ever builds a DSP graph from the user's rack.** Found 2026-08-17 (macos, interactive) while reading the DSP path Jeff's S11 ruling pointed at. **The evidence is four lines of TIDE's own code:** `SynthEditSem/SynthEdit.cpp`'s `class SynthEdit final : public Processor` is the ONLY DSP class TIDE has, and its `subProcess` body is `// TODO: Signal processing goes here.` followed by `*left = 0.0f; *right = 0.0f;`. **`TideApp` never starts a synth runtime** — no `prepareToPlay`, no `StartBackgroundProcessing`, no generator — and **nothing anywhere instantiates the placed modules as DSP**, even though their DSP counterparts are registered and linked (`ug_oscillator2` and the rest). So the rack is an editor plus a silent audio stub: documents and views are real, the running graph does not exist. **Not a regression and nothing broke** — the stub has carried a `// TODO` since the prototype, and every session since has been building the editor, which is what constraint 1 is about. **Why it outranks S11:** a rack that forgets your patch is a bad synthesiser; a rack that makes no sound is not a synthesiser. **This is the real v0.1 gate and blocks the R-series ahead of S11.** **Scope** unknown and probably large: building a DSP graph from the document inside a plug-in is what SynthEdit's *export* does at build time, and TIDE needs it at runtime — the existing pieces to study are `SynthRuntime_editor` (the standalone app's graph builder, including the fade-out/teardown/rebuild/fade-up Jeff cited) and whatever the exporter emits. **Accept** place an oscillator in the rack, wire it to Sound Out, play a MIDI note in a host, and hear it — with a meter reading, not an assertion. **Size** genuinely unknown; scope it before costing it. **Process note worth keeping:** six sessions of host verification missed both this and S11 because every test drove the UI and **none ever played a note and looked at a meter, or saved and reopened a project**. Two findings from one missing habit; add both to how a host check is done. |
---

## After the carve-out

| ID | Status | Plat | Item |
|---|---|---|---|
| M1 | BLOCKED | mac | AU + AUv3 targets building on macOS. |
| M2 | BLOCKED | mac | **Rescoped 2026-08-15, following S10's retire ruling — same status, different shape.** There is no existing iOS project to fix; S10 confirmed all four of `SE_IOS_APP.xcodeproj`'s targets were already broken and deleted it. Jeff's own direction for S10 — *"lean on a generic AUv3 iOS backend for gmpi_ui as much as possible"* — sets M2's shape too: author a **new, generic AUv3 wrapper** alongside `GMPI_Wrappers/wrapper/`'s existing `VST3`/`AU2`/`CLAP`/`Standalone` siblings (checked — no `AUv3` one exists there yet), usable by any GMPI plugin, not a TIDE-specific rebuild of the retired scaffolding. Still `BLOCKED` — nothing here unblocks it, this only corrects what "done" now means. Original row follows. iOS AUv3 build; validate under real sandbox rules. This is the constraint that validates the whole design. |
| M3 | BLOCKED | mac | `auval` clean. |
| X1 | BLOCKED | linux | VST3 + CLAP on Linux, GCC 13+. See the Linux toolchain memory for WSL specifics. |
| X2 | BLOCKED | linux | Zero-warning build at `-Wall -Wextra`. |
| V1 | BLOCKED(E2a) | any | Patch state survives host save/reload — the v0.1 acceptance test in [PLAN.md](PLAN.md), **amended 2026-08-13: rack-first, and the acceptance test itself needs the oscillator/envelope/output prefabs E2a builds before it can even be attempted.** The circularity this created against the old E2 (BLOCKED(V1)) is resolved by the split — E2a is unblocked, the rest of E2 stays behind V1 as before. |
| V2 | BLOCKED | any | Host-automatable parameters without a panel view (design note 4 in [docs/design-notes.md](docs/design-notes.md)). **This premise is now stale too — panel view (the rack) is TIDE's default view as of the 2026-08-13 pivot, not something being avoided. Design note 4 needs re-reading against that before this row is trusted; not done here, flagged in passing while splitting E2.** |
| E2a | TODO | any | **Split out of E2, 2026-08-13, at Jeff's direction — the minimal slice V1's acceptance test needs, unblocked.** Build exactly three prefabs: oscillator, envelope, output — each a Container wrapping the real DSP module with its patch points exposed on the panel, per constraint 1's rack model. **Check this before building anything — it may already be a blocker:** S8 measured the current Release binary and found the modern SEM oscillator (`OscillatorNaive`) has **zero symbols** — not compiled in at all. If no suitable oscillator primitive is currently registered, this row cannot produce a real oscillator prefab until S8 (or at least the part of it that gets a usable oscillator primitive registered) lands first. Verify what's actually in the binary before assuming either way. **Also unclear, flagged rather than assumed:** whether placing and cabling prefabs in the rack needs U1's UX work first. U1's original scope was breadcrumb/structure-view navigation for *opening* a Container to edit it — placing and cabling a *closed* prefab in the rack is a different surface (SynthEdit's rack-mode UI itself), and nobody has audited what that UI currently supports. Each prefab needs its own E1 test case. |
| E2 | BLOCKED(V1) | any | **The rest of the first Eurorack module set, beyond E2a's three V1-critical prefabs.** Split 2026-08-13 — see E2a for the part that's unblocked. Everything else here still waits on V1: "no point authoring a fuller module library for a plugin that cannot yet keep its patch across a host save" holds for the broader set even though the minimal V1-critical prefabs couldn't wait for V1 themselves. PLAN "The Eurorack rack", 2026-08-09. Define the naming and I/O conventions for a module Container, then build the rest of a first curated set, each with its own E1 test case. Two dependencies that are not obvious: constraint 7 means the **primitives** a Container is built from must be statically registered — see **S8**, which is about the wrong ones being compiled in today — and a Container is only openable-and-editable if U1's UX can actually navigate into it, which is what the breadcrumb bar is for. Carried over from the superseded `tide-rack` repo, where this was EP-002. |
| E3 | BLOCKED | any | **Curated starter racks** — ready-to-play preset patches shipped with the plugin, treated as a deliverable rather than a demo. PLAN calls this the friendly surface, and it is the Blocks trait the product is explicitly copying. Blocked on **E2**. Two things it needs that nothing else forces: a rack has to survive load without a filesystem path (constraint 3), and it should be the thing a user sees *first*, which is a product decision about default state, not a file format. Carried over from the superseded `tide-rack` repo, where this was EP-003. |

---

## Release & distribution — blocked on V1 (nothing to ship yet)

The plan is [docs/distribution.md](docs/distribution.md). R1 is the only item
with lead time and the only one Jeff can do now; the rest wait for a v0.1 that
passes V1. CI automation additionally waits on C7 — the interim path (build on
each box, `gh release upload`) is in the doc.

| ID | Status | Plat | Item |
|---|---|---|---|
| R1 | RESOLVED | — | **Signing identities and accounts.** **(a) Decided 2026-08-13 by Jeff: TIDE ships under the existing `SynthEdit Limited` identity, deliberately — a second Azure certificate profile is not affordable right now.** So both platforms sign under `SynthEdit Limited`/`SynthEditCertificateProfile` as already configured; no new credential, no R7-style hardening needed for a profile that doesn't exist. Compensating branding decided the same day: **"TIDE Synth — by SynthEdit Ltd"**, placed subtly (about pane, footer, or installer credit — not the plugin name itself, constraints 1 and 5 still rule out anything dialog- or splash-shaped). Placement is unscoped — filed as **D2**. (b) **Confirmed** — Apple Developer ID is `Developer ID Application: SynthEdit Limited (36SNPLRFK3)`, and notarization runs on the app-specific-password route (`APPLE_ID` / `APPLE_ID_PASSWORD` / `APPLE_TEAM_ID`), matching `SynthEdit_cmake_mac.yml:223-244` so the recipe ports without rewriting `notarytool`. An App Store Connect API key would be scoped and revocable where the app-specific password is tied to Jeff's Apple ID and grants far more than notarization — worth switching before anything consumes it, not urgent while nothing does. (c) App Store listing for the iOS AUv3 (needed by M2, not v0.1) — **not started.** (d) **Done early, ahead of C7** — 8 Actions secrets and 4 variables now exist on the public repo: secrets `AZURE_TENANT_ID`, `AZURE_CLIENT_ID`, `AZURE_CLIENT_SECRET`, `APPLE_CERT_P12_BASE64`, `APPLE_CERT_PASSWORD`, `APPLE_ID`, `APPLE_ID_PASSWORD`, `APPLE_TEAM_ID`; variables `AZURE_CODESIGN_ENDPOINT`, `AZURE_CODESIGN_ACCOUNT`, `AZURE_CODESIGN_PROFILE`, `APPLE_SIGNING_IDENTITY`. The non-secret half is deliberately in *variables* — those values are on every binary TIDE ships, and hiding them only makes failures harder to read. Nothing consumes any of them yet. **Two follow-ups, filed as R7.** Detail on the identities, and why the non-secret half is deliberately in variables: [docs/r1-signing-identities.md](docs/r1-signing-identities.md). |
| R7 | TODO | any | **Harden the signing credentials now that they exist (follow-up to R1(d)).** Two things, neither urgent while no workflow consumes them. **(1) The exposure is same-repo branches, not fork PRs.** GitHub withholds secrets from fork-triggered `pull_request` runs, so `build.yml`'s bare `pull_request:` trigger is safe. But it also runs on `push: branches: ["main", "tide/**"]`, and same-repo runs get full secret access — and pushing `tide/**` branches is exactly what the weekly agents do. So today a workflow edit on any agent branch would execute with read access to all 8 credentials, ungated. Fix: create a `release` environment with Jeff as a required reviewer, move the secrets into it (`gh secret set <NAME> --env release`), delete the repo-level copies. Only a job declaring `environment: release` can then reach them, and it pauses for approval. **(2) Drop the two long-lived credentials.** `AZURE_CLIENT_SECRET` → OIDC federated credentials scoped to `repo:JeffMcClintock/TideSynth:ref:refs/tags/*`, which removes the stored secret entirely *and* means even an approved non-tag run cannot sign; `azure/trusted-signing-action@v0` takes the same inputs as the `ArtifactSigning@1` task either way. `APPLE_ID_PASSWORD` → App Store Connect API key (`.p8` base64 + key id + issuer id). Do this before **R5** wires anything up, or R5 will have to be rewritten. |
| R2 | BLOCKED | win | **Windows installer.** Inno Setup, modelled on `SE16/SynthEdit2/installer/SynthEdit2.iss`. Installs `TIDE-Rack.vst3` to `Common Files\VST3`; sign the .vst3 and the installer via Azure Trusted Signing. Also produce `TIDE-Rack-Windows.zip` for users who refuse installers. Installer title and shortcuts use the display form **TIDE Rack**; every filename uses **TIDE-Rack** — see the naming table in [docs/distribution.md](docs/distribution.md). Constant asset names — the version lives in the tag and the version resource, per the doc. |
| R3 | BLOCKED | mac | **macOS pkg.** AU → `Components`, VST3 → `VST3`; Developer ID sign, **notarize and staple** — model on `SE16/SynthEdit_cmake_mac.yml`. AUv3 is deliberately absent: it ships in the container app with M2. |
| R4 | BLOCKED | linux | **Linux tarball.** `TIDE-Rack-Linux.tar.gz` with the VST3 and CLAP bundles and a short `install.sh` targeting `~/.vst3` / `~/.clap`. No signing. |
| R5 | BLOCKED | any | **Release workflow.** Tag `v*` → build + sign every platform → one GitHub Release with constant-name assets + `SHA256SUMS.txt`. Runs on tag push only, never on PRs, so fork PRs never see signing secrets. Needs C7 for public CI builds; until then the same release page is fed by `gh release upload` from the platform boxes. |
| R6 | BLOCKED | any | **Website Downloads section.** Replace the "nothing to download yet" card with static `releases/latest/download/<asset>` permalinks — no JS, no version maintenance, keeps the "no cookies, no scripts, no tracking" footer honest. iOS is a plain text App Store link (Apple's badge image would be the page's first external resource — do not use it). |
**The publisher name TIDE ships under is SynthEdit Limited — decided, not
inherited, as of R1(a) 2026-08-13.** `APPLE_SIGNING_IDENTITY` is
`Developer ID Application: SynthEdit Limited (36SNPLRFK3)`, and
`SynthEditCertificateProfile` almost certainly carries the same subject on
Windows. So a user installing TIDE sees *SynthEdit Limited* in the macOS
Gatekeeper prompt and the Windows UAC dialog — deliberately: a second Azure
certificate profile is not affordable right now, and PR #14's website
stripped every SynthEdit mention so TIDE stands on its own there, which is
why a compensating credit was filed as **D2** — "TIDE Synth — by SynthEdit
Ltd", placed on TIDE's own terms rather than only surfacing in the OS
install dialog.

---

## Done

Landed rows live in **[BACKLOG-DONE.md](BACKLOG-DONE.md)** — moved there by A8,
2026-08-12. Look there for precedent: how a past stage was verified, what a
previous run measured.

**As part of STEP 4:** an `IN-REVIEW` row whose PRs have all merged becomes
`DONE` and moves to that file, with the merge date in the `Done` column. Move
the row verbatim; do not rewrite it on the way out.
