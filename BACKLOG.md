# TIDE Synth — Backlog

Ordered. A weekly agent obeys the **NEXT block below first**; only when its
platform's NEXT item is ineligible does it fall back to the topmost item that
is not blocked and matches its machine's platform role. One item, then stop.

## NEXT — per-platform priority, obeyed before file order

| Platform | Take | Why |
|---|---|---|
| win |**P11** — its Windows half, and it is the only thing on this platform a scheduled run can actually take. Re-pointed 2026-08-22 (macos) because **R2 merged** ([#276](https://github.com/JeffMcClintock/TideSynth/pull/276)) and this cell still named it. **P3 is this platform's only own-boxed row and it is GATED** — both its files (`SynthEditLib/CContainer.cpp`, `SE16/SynthEdit2/MfcDocPresenter.cpp`) are on the GATED list, so it needs Jeff. **What P11 still wants is Windows-only:** the mac half was re-measured and corrected 2026-08-22 ([#277](https://github.com/JeffMcClintock/TideSynth/pull/277)), and the answer there turned out to be a domain mismatch rather than a missing install step — **do not assume the Windows mechanism is the same one**, it was described from a Windows box and the two platforms resolve modules differently. The takeable piece is option (b): make the export diagnostic name the stale `TIDE.gmpi` instead of telling the user their installation is broken. **Note the artifact names moved under N1a** — the row's `TIDE.gmpi` / `TIDE_VST3.vst3` are now `TIDE-Rack.gmpi` / `TIDE-Rack.vst3`, so re-measure before quoting its paths.**|**R2 merged, so this cell was pointing at finished work.** Windows is the only box that can see P11's symptom: the export dialog that blames the user's installation. The mac half turned out to be a *different* mechanism, so this is not a re-run of that measurement.|
| mac |**E9** — sample-rate changes, and it is `SynthEditSem/SynthEdit.cpp`, TIDE's own and ALLOWED. Re-pointed 2026-08-22 (macos, scheduled run) after six items landed and left every cell in this block stale. **The spec is already answered** (2026-08-19, Jeff directing): TIDE prepares with NOTHING before a document exists, and `open()` installs the silence writer. A `preparedSampleRate` guard already logs `TIDE: rack built for N Hz`; what is missing is acting on it. **This platform's own-boxed row is R8 and it is NOT takeable** — no plugin bundle has a `CFBundleIdentifier` (it is present and EMPTY, from CMake's stock plist template), and fixing it needs a naming decision from Jeff plus a GMPI change, which is PR-GATED. **Do not take:** **S27** and **E1c** were both measured down to a decision this run and now want Jeff or a Linux box, not more mac measurement; **S35**, **E10**, **S16**, **S22**, **S5**, **S7**, **S3g** are GATED; **B1** and **R5** are `.github/workflows/**`, which this token cannot push (`repo` scope only, verified 2026-08-22). **R3a** is `BLOCKED(M1)`.**|**Six items landed from this box on 2026-08-22 and every cell here went stale at once.** R8 is mac's own row and is blocked on a naming decision; S27 and E1c were measured down to decisions this run. E9 is the largest live thing left that this box can both change and verify.|
| linux |**R4** — the Linux tarball, and every one of its blockers cleared today. Re-pointed 2026-08-22 (macos, scheduled run); this cell still named S23, which that box already measured as not reproducing. **Why R4 is now genuinely clear:** **R4a** merged, so `FORMATS_LIST` builds a `.clap` and the tarball has the second artifact its Accept names; and **[GMPI#6](https://github.com/JeffMcClintock/GMPI/pull/6) + [#274](https://github.com/JeffMcClintock/TideSynth/pull/274) both merged together**, so the VST3 bundle directory and the `.so` inside it finally agree — a tarball built before that would have shipped something no host loads ([#271](https://github.com/JeffMcClintock/TideSynth/issues/271)). **Two small things to do on the way, neither worth its own row.** **(1)** Run `sh tests/s31_kill_named_test.sh` — one command. S31 shipped `scripts/kill-named.sh` 7/7 on macOS, but **Linux is the only platform its bug is real on**, because BSD `pkill` excludes ancestors by default and GNU procps does not; a mac box cannot verify it however carefully it tries. If it fails, reopen S31. **(2)** **E1c** wants one render: the naive oscillator with pitch pinned to 5 V. Its stated hypothesis is already refuted — `osc_naive_sine` and `voice_midi_note` use the SAME module and land 50 dB apart — and that one case decides whether the variable is the undriven pitch input. `verify.yml` is `ubuntu-24.04`, so this box is the only one that can run it.**|**R4's blockers all cleared today** — R4a gave it a `.clap`, and GMPI#6 + #274 gave it a VST3 bundle a host will actually load. The two side-tasks are one command and one render, and Linux is the ONLY box that can do either: S31's bug does not exist on BSD, and `verify.yml` is ubuntu-only.|
| any |**E9** if a mac or Linux box is idle — it is `SynthEditSem/SynthEdit.cpp`, TIDE's own, and the spec is settled. Re-pointed 2026-08-22 (macos); this cell named C7b and C15, both archived DONE, which is exactly the failure A20 and A27 were written to catch and which the check still cannot see because they are named as *history* rather than as a take-target. **The `any` queue is 26 rows and most of it is not takeable by a scheduled run** — **S5**, **S7**, **S3g**, **S22**, **S16**, **E10**, **S34**, **S35** and **S1b** are GATED; **B1** is `.github/workflows/**`; **S8** is MOOT since Jeff ruled Oscillator HD; **N1** is an umbrella whose children have all landed. **What is left and real:** **E9** (above), **P11**'s Windows half, **S23**, **S25** and **S33**. **S33 is PR-GATED but small** — `GmpiParameter::setBlob` is a stub returning `true`, so every blob a processor is told to store is discarded; a GMPI PR proposing it is in scope for any box.**|**This cell named two archived rows, which is the A20/A27 failure mode surviving in a form the check cannot see** — they are cited as history, not as a take-target. Re-pointed with the takeable set spelled out, because most of the 26-row `any` queue is GATED and a run should not have to re-derive that.|

Status: `TODO` · `DOING` · `IN-REVIEW` · `DONE` · `BLOCKED` / `BLOCKED(<id>)` ·
`NEEDS-JEFF` · `WONTFIX`

- `IN-REVIEW` = work complete, PRs open (linked in the row). A run never sets
  `DONE` on its own fresh work; `DONE` is set when every linked PR has merged,
  by Jeff or by a later run that observes it.
- `BLOCKED(<id>)` = blocked until `<id>` is DONE. **Eligibility lives in this
  column alone** — section prose never overrides it.
- `NEEDS-JEFF` rows should carry a `Default in effect:` and a `Decide-by:` line
  (template in [docs/decisions.md](docs/decisions.md)), so an unanswered
  question cannot silently become the answer.
- `WONTFIX` = considered and declined, with the reasoning in the row. Do not
  re-file it.

Platform: `any` · `win` · `mac` · `linux`

New items state three things, tersely: **Scope** (files/dirs), **Accept** (a
command or observable that proves it done), **Size** (fits one session, or
names its stages). Anything longer than a few lines lives in `docs/<id>.md`
with the row pointing at it. An agent that cannot state an item's acceptance
check before starting must skip it and mark it `NEEDS-SPEC` rather than guess.

Update this file at the end of every run. An item left in `DOING` with no
matching JOURNAL entry means a previous run died — reset it to `TODO` and note
that in the journal (claims younger than 24h are presumed live; see the run
prompt).

---

## Carve-out — APPROVED 2026-08-08, C0 cleared

Stages are sequential. Each must leave SynthEdit, SynthEditCL and TIDE all
building before the next starts. See [docs/carve-out.md](docs/carve-out.md).

| ID | Status | Plat | Item |
|---|---|---|---|

| C7 | DONE | any | **DONE — [#165](https://github.com/JeffMcClintock/TideSynth/pull/165) and [#250](https://github.com/JeffMcClintock/TideSynth/pull/250) both merged; flipped 2026-08-21 (linux) as STEP 4 bookkeeping.** **ALL FIVE STAGES ARE NOW COMPLETE — C7a (archived), C7b (DONE), C7c (RESOLVED by removal), C7d (DONE), C7e (IN-REVIEW on [#250](https://github.com/JeffMcClintock/TideSynth/pull/250), linux green).** The carve-out's goal — *a stranger can clone this repo and build TIDE* — was proven on all three platforms separately: macOS by the clean-clone test (C7e), Linux by a fresh `git clone` on the linux box, Windows by CI. What remained was CI proving it, and the Linux half of that now does. **Moves with C7e**: this row is IN-REVIEW for exactly the same reason and should flip to DONE with it. **On closing, C10 and the release track R2-R6 unblock.** **Previous text follows.** **SCOPED AND SPLIT 2026-08-19 (linux) — C7 is not one session, and its content is not what this row said.** Measured on the merged defaults with a from-scratch tree; the inventory and the five stages are in **[docs/c7-clean-clone.md](docs/c7-clean-clone.md)**. **The four things actually in the way are separate, and three of them are nothing to do with `SynthEditLib`:** (1) TIDE's own CMake listed four private include paths and **four of the five were DEAD** — that is C7a, done; (2) TIDE's own source lives in the private repo (`SE16/SynthEditSem/`, `SE16/TideModules/`) — C7b, and its blast radius is **three files**; (3) TIDE links `EditorScreenshot`, which is `SE16/EditorScreenshot/` and on NEITHER STEP 5 list — C7c, needs a ruling; **C13 merged mid-run and the count is now 7 → 1** — the survivor is `SynthEditApp.h`, the same header TIDE itself includes, so C14 closes both consumers. (4) **`SynthEditLib` cannot configure standalone** — it consumes `${GMPI_SDK}`/`${GMPI_UI_SDK}`/`${VST3_SDK}` and never sets them, so a stranger needs a root `CMakeLists.txt` in THIS repo playing SE16's role — C7d, the piece nobody had named, and the one that switches TideSynth's CI matrix from skipped to running **with no `.github/workflows/**` edit** (B1's guard job gates on exactly that file existing). **Two measurements worth not re-deriving:** TIDE's own sources have exactly **TWO** private includes — `SynthEditGui.cpp` -> `ContainerThumbnail.h` and `TideAppStubs.cpp:31` -> `SynthEditApp.h` — and **everything except `SynthEdit` is already a public repo** (`gh repo view --json isPrivate`, all five checked). **Take C7b next.** Previous text follows. | **ELIGIBLE as of 2026-08-19 (linux): C6 merged.** `EditorLib/CMakeLists.txt` is in the public repo and the merged trees build 935/935 with ctest 67/67, verified rather than inferred. **C7's starting point is already measured — do not re-derive it.** Configuring the public `EditorLib` standalone, with no SE16 anywhere, is **RC=0**; a standalone *build* stops only on `GmpiUiDrawing.h`, `RawView.h` and `Hosting/message_queues.h`, with **ZERO** `SE16`/`SynthEdit2` references in the log. So what stands between the public repo and a clean-clone build is the external GMPI/gmpi_ui SDKs, not anything private. **The one private dependency that DOES remain is the include directory SE16 re-adds for EditorLib** — the 7 public-file includes resolving only in `SynthEdit2` (`ISEAppManaged.h`, `IMidiDriver.h`, `ParseSynthEditArgs.h`, `SynthEditApp.h`). C6 made that visible; closing it is C7's and C11's, and it is the real content of the clean-clone test. Repoint TIDE at public `SynthEditLib` only. Prove it with a clean-clone CI build that has no access to the private repo. **Before claiming a stranger can build TIDE, grep `SynthEditLib` for `#include`s of headers that live in `SE16/SynthEdit2/`.** C2 found one — `SynthEditLib/modules/se_sdk3_hosting/ModuleViewStruct.cpp:11` included `"cpu_accumulator.h"` while that header was still private, and it compiled only because that `.cpp` is on *EditorLib's* source list and so gets `../SynthEdit2` on its include path. A public file that its own repo cannot compile. C2 closed that one by accident; each remaining one is invisible until a clean-clone build. **The version of this row that [#165](https://github.com/JeffMcClintock/TideSynth/pull/165) left on `main`, kept verbatim because that PR merged while this branch was open:** **ELIGIBLE as of 2026-08-19 (linux): C6 merged.** `EditorLib/CMakeLists.txt` is in the public repo and the merged trees build 935/935 with ctest 67/67, verified rather than inferred. **C7's starting point is already measured — do not re-derive it.** Configuring the public `EditorLib` standalone, with no SE16 anywhere, is **RC=0**; a standalone *build* stops only on `GmpiUiDrawing.h`, `RawView.h` and `Hosting/message_queues.h`, with **ZERO** `SE16`/`SynthEdit2` references in the log. So what stands between the public repo and a clean-clone build is the external GMPI/gmpi_ui SDKs, not anything private. **The one private dependency that DOES remain is the include directory SE16 re-adds for EditorLib** — the 7 public-file includes resolving only in `SynthEdit2` (`ISEAppManaged.h`, `IMidiDriver.h`, `ParseSynthEditArgs.h`, `SynthEditApp.h`). C6 made that visible; closing it is C7's and C11's, and it is the real content of the clean-clone test. Repoint TIDE at public `SynthEditLib` only. Prove it with a clean-clone CI build that has no access to the private repo. **Before claiming a stranger can build TIDE, grep `SynthEditLib` for `#include`s of headers that live in `SE16/SynthEdit2/`.** C2 found one — `SynthEditLib/modules/se_sdk3_hosting/ModuleViewStruct.cpp:11` included `"cpu_accumulator.h"` while that header was still private, and it compiled only because that `.cpp` is on *EditorLib's* source list and so gets `../SynthEdit2` on its include path. A public file that its own repo cannot compile. C2 closed that one by accident; each remaining one is invisible until a clean-clone build. **UPDATED 2026-08-19 (windows) on landing C13: the 7 is now 1.** `ISEAppManaged.h`, `IMidiDriver.h` and `ParseSynthEditArgs.h` are in the public repo; the only public file still reaching into `SynthEdit2` is `ApplySynthEditConfig.cpp` → `SynthEditApp.h`, and that one is **C14**, not C7's and not C11's — C11 is DONE and closed a different call site. **So C7 is eligible by status but cannot PASS until C14 lands**, because the clean-clone test is precisely what that one include fails. Take C14 first, or take C7 knowing its first act is to do C14. |
| C7b | DONE | any | **DONE — [#186](https://github.com/JeffMcClintock/TideSynth/pull/186) + [SynthEdit#69](https://github.com/JeffMcClintock/SynthEdit/pull/69) both merged; flipped 2026-08-20 (linux) per STEP 4.** **IN-REVIEW 2026-08-20 (macos) — [#186](https://github.com/JeffMcClintock/TideSynth/pull/186) + [SynthEdit#69](https://github.com/JeffMcClintock/SynthEdit/pull/69), which MUST merge together; branch `tide/mac/C7b-tide-source`.** **TIDE's own source is in the public repo:** `SynthEditSem/` (16 files) and `TideModules/` (11) now live in TideSynth, and SE16 consumes them through a `TIDESYNTH_FOLDER_OVERRIDE` + `FetchContent` pair that mirrors `SYNTHEDITLIB_FOLDER_OVERRIDE` line for line — one pattern to learn. **Accept met exactly as written:** `git ls-files` in SE16 shows **zero** `SynthEditSem/` or `TideModules/` entries, and SE16's own build still produces `TIDE.gmpi` **and** `TIDE_VST3.vst3` at **943 objects — identical to the pre-move baseline measured in the same tree** — with `ctest` **86/86**. `TIDE_STANDALONE` also runs and seeds 6 prefabs from the moved `TideModules`, and the bundle stages all 6. **Only ONE of the three `../` paths actually broke**, not three: both `../TideModules/prefabs` entries still resolve because the two folders moved *together*, so only `../SynthEdit2` needed changing — it is now `${SE16_SYNTHEDIT2_DIR}`, set by SE16's root before `add_subdirectory`, and empty when TideSynth builds standalone. **Taken on mac rather than linux**, which the NEXT block nominated: linux has not run since 2026-08-19 and left no branch or claim, the row is platform `any`, and it gates C7e → C7 → C10 → R2–R6. **The residual is filed as C16:** `TideAppStubs.cpp:31` still includes the private `SynthEditApp.h` for two member definitions, so a *standalone* TideSynth build still cannot compile this target — expected, and C7d/C7e's business, not a regression. **Previous text follows.** **Move TIDE's own source into the public repo.** **Scope:** `SE16/SynthEditSem/` (16 files) and `SE16/TideModules/` (10) move into `TideSynth`; `SE16` consumes them through a `TIDESYNTH_FOLDER_OVERRIDE` + `FetchContent` pair mirroring `SYNTHEDITLIB_FOLDER_OVERRIDE` exactly (`SE16/CMakeLists.txt:114-127`); fix the three `../` paths in `SynthEditSem/CMakeLists.txt` that stop resolving after the move (`../SynthEdit2` once, `../TideModules/prefabs` twice). **Accept:** `git ls-files` in `SE16` shows **zero** `SynthEditSem/` or `TideModules/` entries, AND `SE16`'s own build still produces `TIDE.gmpi` and `TIDE_VST3.vst3` at the same object count with ctest green. **Size:** one session. **The blast radius is three files** — `grep -rl` over `SE16`'s build files finds `CMakeLists.txt` (one `add_subdirectory` at `:409`), `SynthEditSem/CMakeLists.txt` itself, and `se_gmpi/vst3/CMakeLists.txt` where both hits are comments. No `.vcxproj`, no `.pbxproj`, no `.sln`. **Both folders are ALLOWED paths**, so this needs no ruling. Does NOT depend on C13/C14. |
| C16 | DONE | any | **DONE — [#187](https://github.com/JeffMcClintock/TideSynth/pull/187) + [SynthEdit#70](https://github.com/JeffMcClintock/SynthEdit/pull/70) both merged; flipped 2026-08-20 (linux) per STEP 4.** **IN-REVIEW 2026-08-20 (macos) — [#187](https://github.com/JeffMcClintock/TideSynth/pull/187) + [SynthEdit#70](https://github.com/JeffMcClintock/SynthEdit/pull/70), stacked on C7b's and merged after them; branch `tide/mac/C16-tideappstubs`.** **The three symbols were DEAD and the fix is a deletion, not an interface.** This row expected C14's treatment — narrow the two member definitions to something public. Measuring first showed there was nothing to narrow: **neither `theApp` nor `isMoonbaseEnabled()` nor `licenseIsActive()` is referenced anywhere in `EditorLib` or `SynthEditLib`.** C11 had already replaced that path with `ILicenseState`, and `GetLicenseState()` returning `nullptr` — in this same file — is what EditorLib actually calls. The file's own header comment still described the pre-C11 world, which is what made the row expect harder work. Every surviving caller of those members is desktop-app code (`SynthEditMac/`, `SynthEdit2/`) that links the real `SynthEditApp.cpp` and never this file. **So `#include "SynthEditApp.h"` and all three symbols are deleted.** One include replaced it — `ILicenseState.h`, which had been arriving transitively through the private header and lives in the **public** `SynthEditLib`, which is the whole point. **Accept met:** `SE16_SYNTHEDIT2_DIR` is deleted from both `SE16/CMakeLists.txt` and `SynthEditSem/CMakeLists.txt`, and `scripts/dangling_private_includes.py` goes **3 → 2** with `SynthEditApp.h` gone; **the two survivors are `tinyxml/tinyxml.h`, which resolves in the PUBLIC `SynthEditLib`** (on TIDE's include path via `SYNTHEDITLIB_DIR`) and is only reported because `--public` was pointed at TideSynth alone. **So TIDE's own source has zero real dependencies on the private repo.** **Verified:** 943 objects — identical to C7b's baseline, so nothing stopped being built — `TIDE.gmpi` + `TIDE_VST3.vst3`, `ctest` **86/86**, and `TIDE_STANDALONE` runs and seeds 6 prefabs. **Unblocks C7d.** **Previous text follows.** **The last private include in TIDE's own source: `SynthEditSem/TideAppStubs.cpp:31` → `SynthEdit2/SynthEditApp.h`.** Filed 2026-08-20 (macos) on landing C7b, which moved that file into the public repo and so turned this from an internal reference into a cross-repo one. **It is C14's twin and wants the same treatment.** C14 narrowed `ApplySynthEditConfig.cpp` to an interface; this file is harder in one way and easier in another. *Harder:* it defines **two member functions of the concrete class** — `SynthEditApp::isMoonbaseEnabled()` and `SynthEditApp::licenseIsActive()`, both `return false` — and a member definition needs the complete type, so a forward declaration is not enough. *Easier:* the third symbol, `SynthEditApp* theApp = nullptr;`, needs **only a forward declaration** — a pointer definition does not require the complete type, and a global's mangled name carries no type in the Itanium ABI, so the symbol is unchanged. **Scope** whatever interface EditorLib actually needs from those two members — read C11's ruling first (*“narrow it to an interface. TIDE needs no licensing.”*), and note `TideAppStubs.cpp`'s own comment explains the stubs were **already dead on their first term** at their one TIDE-reachable caller (`MfcDocPresenter.cpp:1244` reads `theApp && …`, and `theApp` is always null in a TIDE process). **Accept** `SE16_SYNTHEDIT2_DIR` can be deleted from `SE16/CMakeLists.txt` and `SynthEditSem/CMakeLists.txt`, and TIDE's sources contain **zero** includes resolving only in SE16. **Why it matters:** it is the last thing between C7b and a TideSynth that configures on its own, so it gates **C7d** and therefore C7e. **Size** small, but it sits on the licensing seam like C14 did. |
| C7c | RESOLVED | any | **ANSWERED BY JEFF 2026-08-20, and the answer is neither option this row offered: TIDE drops the breadcrumb bar, so `EditorScreenshot` stays private and nothing moves.** *“let's remove the breadcrumb bar from TIDE, it's a bit redundant in a product where you seldom dig deeper than 1 level in.”* **C7e is unblocked from this direction** — its clean-clone test no longer needs an `EditorScreenshot` a stranger cannot clone. **The removal itself is [U3](#), not this row**, because the code forces two questions the ruling does not settle: the crumbs are the only way back *up* a level, and the About pane's only entry point is anchored to the crumb strip. See [decisions.md](docs/decisions.md) 2026-08-20. **Previous text follows.** **`EditorScreenshot` — TIDE links it, and it is on neither STEP 5 list.** TIDE's targets `target_link_libraries(... EditorScreenshot)` (`SynthEditSem/CMakeLists.txt`) and `SynthEditGui.cpp` includes `ContainerThumbnail.h`; the directory is `SE16/EditorScreenshot/`, 13 files, and it is GATED by default. It is what draws the breadcrumb thumbnails (BACKLOG U1b). **The question:** does `EditorScreenshot` become public — the whole folder, or just `ContainerThumbnail` + `ScreenshotRenderer` + the platform `ScreenshotFactory` — or does TIDE get a thumbnail-free path instead? `Default in effect:` nothing moves and C7e cannot pass, because a stranger's clone has no `EditorScreenshot` to link. `May proceed meanwhile:` C7b and C7d, both of which are identical under every answer. `Decide-by:` before C7e. G3 is the precedent — the P4 crash fix lived entirely in two repos on neither list, and filing the ruling question took one day to answer. |

| C7d | DONE | any | **DONE — [#188](https://github.com/JeffMcClintock/TideSynth/pull/188) merged; flipped 2026-08-20 (linux) per STEP 4. Confirmed by consequence, not just by the merge: `build.yml`'s guard job keys on a root `CMakeLists.txt`, and the matrix has now actually run on all three platforms.** **IN-REVIEW 2026-08-20 (macos) — branch `tide/mac/C7d-root-cmake`.** **TideSynth configures and builds on its own.** A root `CMakeLists.txt` now fetches the SDKs and adds SynthEditLib, EditorLib and TIDE — modelled on `SE16/CMakeLists.txt` and kept recognisably similar so a change there is easy to mirror. **Accept met:** `cmake -B build -G Ninja` **rc=0**, `cmake --build` **rc=0 / 460 edges / 456 objects**, producing **`TIDE.gmpi`, `TIDE_VST3.vst3` and `TIDE_STANDALONE.app`**, and the standalone runs and seeds 6 prefabs. **Zero references to SE16** in either the configure or the build log. **One thing the row did not predict, and it cost the first configure:** TIDE's `FORMATS_LIST` is `GMPI VST3 STANDALONE`, so it ships neither AU nor CLAP — but `GMPI_Wrappers` (fetched by `SynthEditSem`) configures its **AU2 and CLAP wrappers unconditionally**, and CMake needs those SDK sources to exist even for a target nothing links. The AudioUnit and CLAP fetches are copied from SE16 verbatim for that reason alone. **What is deliberately NOT here:** `se_vst3`/`se_gmpi`/`se_au` (SynthEdit's own engine), `EditorScreenshot` (dropped by U3), `SynthEditCL`, `SynthEditWayland`, `tests` and the desktop apps. **This turns B1's CI matrix ON** — the guard job keys on this file existing, so all three platforms start building on `tide/**` pushes and PRs. **macOS is proven; Windows and Linux are unproven and may go red on first contact** — that is the mechanism working, and `build.yml` files a platform issue automatically. **Unblocks C7e.** **Previous text follows.** **The superproject this repo does not have.** **Scope:** a root `CMakeLists.txt` in `TideSynth` carrying the override-or-fetch block for `GMPI`, `gmpi_ui`, `GMPI_Wrappers`, the VST3 SDK and `SynthEditLib`, then `add_subdirectory` for `SynthEditLib`, `EditorLib` and TIDE. Model on `SE16/CMakeLists.txt:1-230`, TIDE's subset only. **Why it is needed at all:** `SynthEditLib/CMakeLists.txt` consumes `${GMPI_SDK}`, `${GMPI_UI_SDK}` and `${VST3_SDK}` and **never sets them** — measured, they come from SE16's root. **Accept:** `cmake` + `ninja` in a fresh clone of this repo alone, with `SE16` absent from the disk, produces `TIDE.gmpi`. **Size:** one session, after C7b. **Bonus, and it is not small:** `build.yml`'s guard job (B1) gates the three-platform matrix on a root `CMakeLists.txt` existing, so this turns CI from *skipped* to *running* **without any `.github/workflows/**` edit** — which the fleet's token cannot make. **Cannot PASS before C14 lands**: C13 merged 2026-08-19 and took the dangling count 7 → 1, so `SynthEditApp.h` is the only header a clean clone still fails. |
| C7e | DONE | any | **DONE — [#250](https://github.com/JeffMcClintock/TideSynth/pull/250) merged; flipped 2026-08-21 (linux) as STEP 4 bookkeeping.** **THE APT-GET IS PUSHED — [#250](https://github.com/JeffMcClintock/TideSynth/pull/250), by Jeff, 2026-08-21, and LINUX IS GREEN FOR THE FIRST TIME.** The step that has failed every Linux run since the matrix first fired now passes: `Install Linux deps: success` → **`Configure: success`** → `Build: success`, and *"File a platform issue on failure"* reports **skipped**, which is the mechanism confirming it rather than a human reading a log. **Four packages, not three** — `libxext-dev libharfbuzz-dev libdbus-1-dev libpng-dev`; `libpng` is in the probe list at `VST3/CMakeLists.txt:261` and passed before only because the runner image happens to ship it. Strictly additive: `libxcb1-dev` and everything else already installed is untouched, unlike the recipe in [docs/ci/linux-build-deps.md](docs/ci/linux-build-deps.md), which replaces it. **Nothing was hiding behind the fail-fast probe** — the measured set was complete, which was the open risk. **The clause reads *"three platforms run rather than skip, and pass, on a PR"*, and windows + linux both pass on #250 while macOS is QUEUED — so this is IN-REVIEW rather than DONE, and the honest reading is recorded rather than glossed.** **What the macOS half rests on instead:** (1) **#250 cannot affect macOS** — it changes one file, and its only non-comment change is inside a step gated `if: matrix.platform == 'linux'`, so the macOS job is byte-identical to `main`'s; (2) macOS **passed** on the two most recent runs that completed it (`32411882991`, `32411834158`), both of which were runs where **linux failed and macOS succeeded**; (3) macOS completes only ~5% of runs on this repo for queue reasons unrelated to TIDE — filed as **S30**. **So all three platforms are proven, but not in one run, and that is a CI-capacity fact rather than a TIDE fact.** Whether that satisfies the clause as written is Jeff's call, which is why this is not flipped to DONE unilaterally. **Previous text follows.** **THE LINUX CLAUSE IS NOW VERIFIED END TO END — 2026-08-20 (linux, scheduled run), branch `tide/linux/issue-190`, working [#190](https://github.com/JeffMcClintock/TideSynth/issues/190) under STEP 1 rather than taking this row.** **The clean clone builds on Linux too:** `git clone` of the public URL, 158 files, `cmake -S . -B build -DCMAKE_BUILD_TYPE=Release` **rc=0**, `cmake --build build --config Release --parallel` **rc=0 with zero error lines**, producing **`TIDE.gmpi` (7,378,536 bytes)**, **`TIDE_VST3.so` (8,494,704)** and a correct **`TIDE_VST3.vst3/Contents/x86_64-linux/`** bundle — 350 objects, **zero `SE16`** in the configure log, build log or `CMakeCache.txt`. So all three platforms are now proven, and **the only thing standing between C7e and closed is the `apt-get`.** **The package set was MEASURED, not inferred, and the mac run's list was one package short of the failure chain.** `pkg_check_modules(REQUIRED)` fails fast, so the chain was walked by restoring one `.pc` at a time: **`xext` (`VST3/CMakeLists.txt:257`) → `harfbuzz` (`:260`) → `dbus-1` (`:264`) → rc=0**. Three packages to add to `build.yml`'s existing list — **`libxext-dev libharfbuzz-dev libdbus-1-dev`** — plus **`libpng-dev`**, which passes today only because the runner image ships it. **Verified `.pc`→package mapping, the ready-to-paste step and the full evidence are in [docs/ci/linux-build-deps.md](docs/ci/linux-build-deps.md)**, which sits beside `verify.yml` for the same reason: a run may not write `.github/workflows/**`. **Relaxing the X11 probe is now measurably the wrong move**, not just the inelegant one — `TIDE_VST3.so` takes **5 `XShm*`**, **50 `hb_*`** and **27 `dbus_*`** undefined symbols, so an optional probe ships a plugin that fails to link, not one with a lesser editor. The Wayland trio genuinely is optional: without it configure prints *"Wayland support off … X11 editor only"* and *"STANDALONE skipped"*, then succeeds. Status stays **NEEDS-JEFF** — unchanged, and for the same reason. **Previous text follows.** **HALF PROVEN, HALF BLOCKED ON JEFF — 2026-08-20 (macos), branch `tide/mac/C7e-clean-clone`.** **The clean-clone build is PROVEN**, by the literal test rather than a worktree: `git clone` of `https://github.com/JeffMcClintock/TideSynth` into an empty directory with **no sibling repos and no SE16 anywhere**, then `cmake -B build -G Ninja` **rc=0** and `cmake --build` **rc=0, zero error lines**, producing **`TIDE.gmpi`, `TIDE_VST3.vst3` and `TIDE_STANDALONE.app`** — and `lipo` says the plugin is a **universal `x86_64 arm64`** binary, not a single-arch dev build. The standalone runs, enriches its XML and seeds its prefabs. **Zero `SE16` paths** in the configure log, the build log, `CMakeCache.txt` or `build.ninja`. **What is NOT met is the CI clause**, and it is the one the row is written in: *“build.yml's three platforms run rather than skip, and pass, on a PR.”* They **run** now (C7d) and **windows passes**, but **linux fails** — and not for any carve-out reason. `GMPI_Wrappers/wrapper/VST3/CMakeLists.txt:249-263` hard-requires six pkg-config modules on Linux (`x11`, `xext`, `fontconfig`, `freetype2`, `harfbuzz`, `libpng`, `dbus-1`) and the ubuntu runner has none of them. **`pkg_check_modules(REQUIRED)` fails fast, so the log names only `xext` — fixing that alone moves the failure to the next probe**, the same shape as CoreMidiDriver.h earlier today. Full package list and a ready-to-paste step are on [#189](https://github.com/JeffMcClintock/TideSynth/issues/189). **Status is NEEDS-JEFF because the remaining step is an `apt-get` in `.github/workflows/build.yml`, which the fleet's token structurally cannot push** — not because anything is undecided. `Default in effect:` C7 stays open and C10/R2–R6 stay blocked. `May proceed meanwhile:` nothing else in C7 — C7a/b/c/d are all done. `Decide-by:` whenever Jeff next touches CI. **Previous text follows.** **The proof.** CI green on the public three-platform matrix from a checkout with no access to `SynthEdit`. **Accept:** `build.yml`'s three platforms **run rather than skip**, and pass, on a PR. Needs C7b, C7c, C7d and **C14** (C13 merged 2026-08-19). This is the clause carve-out.md calls *"the real proof"* and it is the only thing that closes C7. |
| A31 | DONE | any | **DONE — [#218](https://github.com/JeffMcClintock/TideSynth/pull/218) merged; flipped 2026-08-21 (macos) per STEP 4, PR states verified rather than assumed.** **BUILT AND MEASURED 2026-08-20 (macos, scheduled run), branch `tide/mac/A31-same-job-habit`, [#218](https://github.com/JeffMcClintock/TideSynth/pull/218).** Both halves shipped, and the granularity decision is measured rather than argued: **(b)** the filing-time habit is in the run prompt (STEP 3) — grep freshly-fetched `origin/main:BACKLOG.md` for the file you are about to name, fetch-fresh per A19's id-recheck precedent because your branch's copy predates a concurrent run's filing. **(a)** `check-id-refs.py` now FAILS when two LIVE rows cite the same `basename:line`. Measured against the real tree before building, and the numbers chose the design: same-FILE matching fires on **14** groups of live rows, all legitimate co-citations (`CMakeLists.txt` alone is cited by 14 rows) — unusable; same-`file:line` on live rows fires on **ZERO** today and **catches the real C15/C16 pair**, which both cite line 31 of `SynthEditSem/TideAppStubs.cpp` verbatim (spelled out here rather than as a citation, because this row is live and umbrella C7 legitimately cites that line — the check catching its own author's first draft is the fifth false-alarm measurement, and the reword is the correct fix); live-vs-DONE/archived pairing fires on **6**, and all six are legitimate (umbrella C7 vs its own landed splits, S3g vs archived S3, E6/E7 follow-ups) — a **100%% false-alarm tier, deliberately not checked**, per this row's own bar. Keyed on basename:line so C15's `SE16/SynthEditSem/...` spelling and C16's `SynthEditSem/...` spelling meet. Verified: selftest **32 cases, 0 failed** (7 new, each a measured tier), proven able to fail (flipped expectation → rc=1); positive control reconstructs C15+C16 both-live on the real tree → rc=1 naming every live citing row; real tree → rc=0. **Like A23 it fires once both rows are on one branch** — the both-live window C15/C16 actually had — and the habit covers filing time. **Previous text follows.** **Two runs can allocate DIFFERENT ids for the SAME job, and A23's check cannot see it.** Found 2026-08-20 (macos) on taking C15 and discovering every clause of its Accept was already met — by **C16**, which this box had filed and shipped hours earlier for the identical file, the identical three symbols and the identical include-path deletion. **A23 solved the neighbouring problem:** it detects *one id, two rows*, which is what happened with the two A17s. This is *two ids, one job*, and the duplicate-ID check is blind to it by construction. **Same root cause though:** a run allocates and files from a branch cut off `main`, where a concurrent run's row is unmerged and therefore invisible — C15 came from the windows box landing C14, C16 from the macos box landing C7b. **Cost this time was small** — C15 closed as already-done in minutes — but only because the second row's work happened to land first. Filed the other way round, a run would have spent a session re-doing finished work and then discovered the conflict at merge. **What might actually work, and none of it is free:** (a) a lint that flags two rows whose text names the same `path:line`, which is cheap but only catches rows that cite a location; (b) a STEP 2/STEP 4 habit — before filing a row, grep `BACKLOG.md` for the file you are about to name, which costs one command and catches the case a lint cannot parse; (c) accept it and rely on the taker noticing, which is what happened here. **Recommended: (b), and (a) only if it can be done without false alarms** — A23's own lesson was that the naive version was red on day one. **Scope** whichever of the above. **Accept** filing a row for work another open PR already covers is caught before the row lands, or the habit that catches it is written into the run prompt. **Size** minutes for (b); small for (a). |
| A32 | DONE | any | **DONE — [#220](https://github.com/JeffMcClintock/TideSynth/pull/220) merged; flipped 2026-08-21 (macos) per STEP 4, PR states verified rather than assumed.** **BUILT 2026-08-20 (macos, scheduled run), branch `tide/mac/A32-umbrella-advisory`, [#220](https://github.com/JeffMcClintock/TideSynth/pull/220), stacked on A31's branch (same file).** Exactly as this row specced: `check-id-refs.py` prints an **ADVISORY** naming live rows whose `X[a-z]` children are all closed (archived, or closed-status in the live file), and **never sets a non-zero exit**. On the real tree it names **E2 alone** — the U2 half of this row's own measurement is moot because U2 was archived when it closed — and rc stays 0 with the advisory printed. C7 is correctly silent (C7e is live), verified by positive control: flipping C7e to DONE in a copy makes C7 join the report with all five children listed. Selftest **38 cases, 0 failed** (6 new: U2's shape, C7's shape, closed-in-live-file children, childless rows, closed umbrellas, child-of-child), proven able to fail (flipped expectation → rc=1). The report's own text explains the E2 false-positive class inline, which is what this row's Accept asked for. **E2 itself was deliberately not re-specced** — which module children are intended is a product call (E17's design language just resolved), noted for whoever takes E2. **Previous text follows.** **An umbrella row whose splits have ALL landed keeps reading TODO, and nothing notices.** Found 2026-08-20 (macos) on taking U2 — the last `mac`-marked row in the queue — and finding its Accept had been met on **2026-08-16**: the triage doc shipped and all five splits (U2a–U2e) were archived with merged PRs. **Four days stale, and it was the only mac work on offer**, so a run looking for mac work took it and found nothing to do. **The obvious lint is “flag any live row all of whose `X[a-z]` splits are archived”, and it must NOT fail the build. Measured against the real tree before proposing it:** it fires on **two** rows — **U2**, correctly, and **E2**, wrongly. E2's a/b/c splits are all done, but E2 is legitimately open because its remaining module stages have not been filed yet; the row is a live umbrella with unfiled children. **One real, one false — a 50% false-positive rate**, which is exactly the shape A23 and A27 were both nearly shipped with. It correctly does **not** fire on C7, whose splits include open ones. **Scope** an ADVISORY report — `--show`-style output, or a line in the lint summary that never sets a non-zero exit — rather than a gate. **Accept** the report names U2-shaped rows; a row with unfiled children like E2 appearing in it is understood as advisory rather than a defect; `lint` stays green. **Size** minutes, on data `check-id-refs.py` already collects. **Worth doing because the failure is silent and self-concealing:** the queue looks like it has work in it, and only someone who takes the row finds out it does not. **E2 also wants re-speccing while someone is here** — an umbrella with all children done and more intended is indistinguishable, from the outside, from one that is finished. |
| C10 | DONE | any | **DONE — [SynthEditLib#29](https://github.com/JeffMcClintock/SynthEditLib/pull/29) + [SynthEdit#71](https://github.com/JeffMcClintock/SynthEdit/pull/71) + [#240](https://github.com/JeffMcClintock/TideSynth/pull/240) merged; flipped 2026-08-21 (macos) per STEP 4, PR states verified rather than assumed.** **BUILT AND MEASURED 2026-08-20 (macos, scheduled run), branches `tide/mac/C10-rehome-editor`: [SynthEditLib#29](https://github.com/JeffMcClintock/SynthEditLib/pull/29) + [SynthEdit#71](https://github.com/JeffMcClintock/SynthEdit/pull/71) (must merge together; this repo's branch is bookkeeping only — its two cross-repo references turned out to be comments).** **104 files moved** — EditorLib's compiled root-level entries that C2–C5/C12 added; the 3 root files it compiles that predate the carve-out stay (`CancellationAnalyse.{cpp,h}`, `SafeMessageBox.h`). One `target_include_directories(EditorLib PUBLIC ${SYNTHEDITLIB_DIR}/EditorLib)` covers every CMake consumer of the bare includes (52 SE16 files include these headers bare); `CpuMeterGui.cpp` — compiled by the DSP target, which does not link EditorLib — carries explicit `EditorLib/` prefixes instead. SE16 side: the six literal vcxproj/.filters entries, 4 AdditionalIncludeDirectories configs, both xcconfigs' HEADER_SEARCH_PATHS, the pbxproj FuzzyMatch ref, and the one literal `.mm` include (the same file the carve-out broke once already). **Verified on mac, before vs after on the same tree:** Ninja full build 1072/1072 rc=0 → rebuild 207/207 rc=0; **compile-graph edges 941 = 941** (base graph reconstructed from a detached pre-move worktree, identical metric — no target silently dropped); ctest **67/67 → 67/67**; `TIDE_STANDALONE` runs and seeds 6 prefabs; SynthEditMac `xcodebuild` SUCCEEDED before and after. **Stated unverified:** `SynthEdit2.vcxproj` (WinUI3/MSBuild — win box or the Store pipeline) and `SynthEditWayland` (linux); both changes are mechanical path/include-dir rewrites, and the win box's next run should build them. **Previous text follows.**  **ELIGIBLE as of 2026-08-19 (linux): C6 merged.** `EditorLib/CMakeLists.txt` is in the public repo and the merged trees build 935/935 with ctest 67/67, verified rather than inferred. **Re-home the moved editor files from `SynthEditLib`'s root into a subfolder.** C2 chose root for 16 files; C3 deliberately kept root for 27 more (reasoning in [SynthEditLib#5](https://github.com/JeffMcClintock/SynthEditLib/pull/5)) — root is already an include dir in all three build systems, so both moves cost **zero** include-path edits, which is what keeps a large move's breakage unambiguous. That trade gets worse as C4/C5 land and ~120 editor files sit beside the DSP engine in one flat root. **Blocked on C6 on purpose:** once `EditorLib/CMakeLists.txt` lives in `SynthEditLib`, one `target_include_directories` line there covers every CMake consumer at once, instead of the same edit being smeared across C3/C4/C5 and several build systems — including the macOS/iOS ones no Windows run can verify. The move itself is a `git mv`. Non-CMake consumers still need checking by hand: `SynthEdit2.vcxproj` and the SynthEditMac Xcode project. |

---

## Process hardening — from the process review, 2026-08-09

The findings, red-team verdicts, and the reasoning behind the ordering are in
[docs/process-review-2026-08-09.md](docs/process-review-2026-08-09.md). The
sequence is the decision: A1 gates everything, and cadence (A7) comes last.
Rejected recommendations are listed in that doc — do not re-file them.

| ID | Status | Plat | Item |
|---|---|---|---|
| A7 | WONTFIX | — | **ANSWERED BY JEFF IN SESSION 2026-08-17: the 7x/week cadence is DELIBERATE, and the extra same-day firings are Jeff starting runs manually. "not a scheduling problem."** So this row closes: it asked to raise cadence from 1x to 2x, and the fleet has been running 7x on purpose the whole time. **Nothing to schedule, nothing to decide, and no run should re-file it.** What the measurement was still good for, and what remains true: the task description reads *"Weekly ... (Sat 02:00)"* while `cronExpression` is `0 6 * * *`, so **the description is simply stale** — cosmetic, Jeff's to correct if he cares, not a defect. The **documents** that assume 1x are the part worth knowing about: `docs/weekly-run-prompt.md` is named for a weekly cadence, and **A8 sized journal rotation against "192 KB across 37 entries in six days" on a 1x assumption** — that sizing is the one thing here that could actually bite, and it is A8's to revisit, not this row's. **Also settled by the same answer: the concurrent-session collisions are expected**, being Jeff's manual runs overlapping, not a fleet defect — though the observation stands on its own that **the claim protocol protects a pushed DOING mark and cannot protect an uncommitted working tree** (A14/A16 are that shape). **Original text follows.** **Measured 2026-08-17 (macos): this box runs 7x/week, not 1x.** The `tidesynth-weekly-macos` task reports `cronExpression: 0 6 * * *` ("At 06:03 AM, every day") while its own description still reads *"Weekly ... (Sat 02:00)"* — the cron is what runs, and the two have disagreed silently. **So this row is not "should we go 1x -> 2x"; it is "we are at 7x, what did we intend?"** A7's red-team reasoning was that cadence multiplies journal growth, PR volume and the credential-exposure window linearly — that has been happening unmeasured, and A8 sized journal rotation against a 1x assumption. It is also the same silent-staleness class the bootstrap redesign was built to kill, except it lives in the **cron**, which the bootstrap deliberately holds nothing about — no run can observe its own cadence, so no run had ever reported it. **It plausibly bears on the collisions too:** three sessions shared these working trees on 2026-08-17, and A14/A16 both traced to concurrent sessions in one checkout; the claim protocol protects a *pushed DOING mark* and cannot protect an uncommitted working tree. **Deliberately not changed by any run** — cron is standing configuration and this row is Jeff's. **Still NEEDS-JEFF, but the question has changed.** (Also observed: a firing at 20:11 local that a 06:00 daily cron does not explain; cause unknown — manual trigger, catch-up, or otherwise — recorded as an observation, not a diagnosis.) **Previous text follows.** **Re-pointed from `BLOCKED(A4)` to `NEEDS-JEFF` on 2026-08-16 (macos), and that is a correction, not bookkeeping.** A4 went DONE this run, so by the status column alone — which is where eligibility lives — this row would have become claimable, and the next run to read it would have taken an item whose own text says the work is *"per-box manual steps (Jeff or an interactive session on each box)"*: editing three machines' scheduled-task cron. A scheduled run cannot do that for the two boxes it is not running on, and doing it for its own box only would leave the fleet staggered wrong. The blocker chain (A1, A6, A4) is now **fully cleared** — nothing technical is outstanding, so this is purely Jeff's to schedule. **Default in effect:** weekly, as today. **Decide-by:** no deadline; the A6 digests are the evidence to review it against. **A6 and A1 both landed 2026-08-13 — the remaining blocker is A4 alone, unblocked but not yet built.** **Raise cadence to 2 staggered runs per box per week.** Also requires A1 and A4 (the red team's ordering: cadence multiplies journal growth, PR volume, and the secret-exposure window linearly, so the drains come first — this row's blocker chain encodes A6, and A1/A4 are NEEDS-JEFF/BLOCKED upstream of it). The stagger is no longer load-bearing — the pushed-DOING claim protocol is what prevents collisions — so weekly is a historical artifact. The task-cron edits are per-box manual steps (Jeff or an interactive session on each box). Start at 2×, review after a month against the A6 digests. |

| A30 | DONE | any | **DONE — [#198](https://github.com/JeffMcClintock/TideSynth/pull/198) merged; flipped 2026-08-20 (linux) per STEP 4.** **BUILT AND MEASURED 2026-08-20 (linux, scheduled run), branch `tide/linux/A30-lessons-digest`, [#198](https://github.com/JeffMcClintock/TideSynth/pull/198) — third item this session, continued at Jeff's direction.** **[docs/lessons.md](docs/lessons.md) is generated from BOTH journal files by [scripts/extract-lessons.py](scripts/extract-lessons.py)**, so rotation no longer takes a lesson out of circulation, and rotation step 4 regenerates it. One line added to the prompt's reading list — **that half is the one wanting Jeff's eye**, as this row said. **Accept met:** **599 lessons from 152 entries, 0 dropped**, and the digest is **56 KB against the 223 KB of Learned sections it distils — 4.1x smaller**. **The literal scope would have backfired, and the measurement is the useful part.** This row says *"rotation copies each entry's Learned bullets into"* the digest; copied verbatim that is **223 KB today, 24% of the whole corpus**, i.e. **worse than the 192 KB that triggered A8 in the first place** — the identical trap A24 measured and rejected for a 7-day floor. What makes the cheap version work is the journal's own convention: every Learned bullet opens with a bold claim, so the claim alone is a lesson rather than a title. **Two costs, stated not hidden:** the working is dropped (a line says what was learned and where to read why), and **the file grows ~3.5 KB/day, ~100 KB/month** — affordable at 56 KB now, not by October, and the pruning lever (dropping the archive from `SOURCES`) is Jeff's call, not a run's. **One bug worth not repeating:** the first cut read only markdown list items and silently kept **72 of 152** entries — it looked complete while dropping 80. Three Learned shapes exist in this journal and only one is a list. `--check` is the regression guard; it fails when the digest is stale. **Rotation ages out the lessons, not just the narrative — found 2026-08-20 (macos) while doing A24.** Every journal entry ends with a **Learned** section, and those bullets are the durable part: *“a control built from a real name has a shelf life”*, *“`-ffp-contract` is invisible in a fast-math discussion”*, *“open the artifact”*. Rotation moves them into `JOURNAL-<YYYY>-<MM>.md` with their entry, and **no run reads the archive** — the prompt points at `JOURNAL.md` only. So a lesson is load-bearing for about a day and then silently stops being read. **A24 bounded the window at one date, which is the most that is affordable** (7 days measures 112 entries / 651 KB), so the window cannot be widened to fix this — the lessons have to leave the rotating file. **Scope** a standing, append-only digest — `docs/lessons.md` — that rotation copies each entry's Learned bullets into, plus one line in the run prompt's reading list. **Accept** rotating an entry out leaves its Learned bullets readable without opening an archive, and the digest is measurably smaller than the entries it distils. **Size** small, but it changes what every run reads, so it wants Jeff's eye on the prompt half. **Not merely a nice-to-have:** twice today a run re-derived something an earlier entry had already recorded, and this session is the shortest possible distance from those entries. |
| A12 | DONE | any | **DONE — [#262](https://github.com/JeffMcClintock/TideSynth/pull/262) merged 2026-08-21, at Jeff's explicit direction in session (*"then merge 262"*), which overrides STEP 5's "do NOT merge the PR" for this one PR. All three platform builds were green on the head before the merge-of-main; `lint` and `guard` passed on the merged commit. **Previous text follows.** **IN-REVIEW 2026-08-21 (linux, scheduled run), branch `tide/linux/A12-halted-box-digest` ([#262](https://github.com/JeffMcClintock/TideSynth/pull/262)). The detection half is built and self-testing; one clause of Accept is NOT met and the row says which.** **The wall this row records was wrong, and that is the main finding:** it says *"the digest workflow lives in `.github/workflows/`, so a scheduled run cannot edit it — Needs Jeff or an interactive session."* `watchdog.yml:39` only ever runs `python3 scripts/watchdog-digest.py --repo-root .`, so **all of the logic lives in a script a run may freely edit** and no workflow change was needed. **Built:** `check_halted_boxes()` classifies each box as *alive* / **QUIET** (>=3 days silent) / **LIKELY HALTED** (>=7 days) / **NO ENTRY EVER FOUND**, and states the discriminator in the digest itself — a box that ran and found nothing to do still writes and pushes a journal entry, so it reads as alive; a box halted at STEP 0.7 cannot push anything, so silence is the only symptom it can emit. Plus `check_credential_expiry()`, a countdown to the bot token's **2026-11-07** expiry — the one fleet-wide halt that is known in advance, and which would otherwise arrive as three simultaneous silences with no cause. **Fixed a false alarm the old check shipped with, and this is the measured before/after on the real repo:** it read `JOURNAL.md` only, so a box whose last entry had been ROTATED into the archive read as `no entry found` — indistinguishable from a box that never ran. Before: `windows: no entry found`. After: `windows: alive -- last entry 2026-08-20 (1 day ago)`. **The windows box was fine the whole time.** **Verification:** `python3 scripts/watchdog-digest.py --selftest` — four fixtures, all passing, covering alive / quiet / likely-halted / rotated-but-alive. Full digest still builds end-to-end (`--dry-run`, rc=0, 8 sections). **WHAT IS NOT MET, stated plainly:** Accept asks for the halted box to appear *"with the failing assertion"*, and **that is not obtainable from this repo by any means.** A run that fails STEP 0.7 holds no credential it is permitted to use — filing an issue or pushing a branch to report the failure is exactly what the step forbids — so the assertion cannot leave that machine. The digest now says so in place, and prints the three commands to run at that keyboard. **If Jeff wants the assertion itself surfaced, that needs a channel outside GitHub auth and is a separate item.** Accept also says *"demonstrated by a deliberately induced halt... not by reasoning"*; the self-test is a **fixture**, not an induced halt on a real box, and should not be read as one. **Previous text follows.** **A halted box is invisible to the fleet, and nothing escalates it.** Filed 2026-08-13 from A11's mac half. STEP 0.7's assertions can put a box into a state that **only an interactive session can clear** — the check that detects the gap also forbids fixing it (*"do not 'fix' it by carrying on as whoever you are"*), and STEP 0.7 fires before STEP 1/2 so the run can never claim the item that would repair it. On 2026-08-13 the mac box was in exactly that state; it was cleared within minutes only because Jeff happened to be at the keyboard. **Unattended, it would have burned a run per week indefinitely, each producing an identical journal entry that nobody is prompted to read.** The same shape applies to any STEP 0.7 failure (missing/expired token file, revoked credential) — note the bot token **expires 2026-11-07**, which will halt all three boxes at once and is the obvious first real instance. **Scope:** A6's fleet watchdog digest is the natural place — it already runs and already reports; it should flag a box that halted or has not completed a run in N days, distinct from a box that ran and did nothing. Likely also worth a line in [docs/weekly-run-prompt.md](docs/weekly-run-prompt.md) telling a halted run to label its PR so the digest can find it cheaply. **Accept:** a box halted at STEP 0.7 appears in the next digest as halted, named, with the failing assertion — demonstrated by a deliberately induced halt (e.g. a temporarily bogus token path on one box), not by reasoning. **Size:** one session. **Note the digest workflow lives in `.github/workflows/`, so a scheduled run cannot edit it** — same wall as A3/A5/A6 and C9(a). Needs Jeff or an interactive session. |

---

## Ready now — no dependency on the above

| ID | Status | Plat | Item |
|---|---|---|---|
| N1 | TODO | any | **COSTED AND SPLIT 2026-08-21 (linux, scheduled run, fourth item at Jeff's direction), branch `tide/linux/N1-cost-and-split` ([#265](https://github.com/JeffMcClintock/TideSynth/pull/265)). This row is now an UMBRELLA — take N1a or N1b, not N1.** **The count changes the shape of the job, and bucket C is the finding:** the live surface is **21 references in 11 files**; live reference docs are ~28 more; and **220 references sit in `JOURNAL*`, `BACKLOG-DONE.md` and `docs/lessons.md`, none of which may be rewritten** — they record measurements of a binary that really was called `TIDE_VST3.so` the day they were taken, and this project preserves superseded text verbatim. **So a global search-and-replace is not merely risky here, it is wrong on 91% of its hits** — which is the concrete reason behind the row's own warning. **Two flagged unknowns are now answered:** `OUTPUT_NAME` alone is **not** enough, because `gmpi_plugin.cmake:774` sets `MACOSX_BUNDLE_BUNDLE_NAME "${GMPI_PLUGIN_PROJECT_NAME}"` — the project name reaches the macOS bundle name directly; and **no `OUTPUT_NAME` is set anywhere today**, so the dashed convention is an addition, not an edit. **Two flagged hazards do NOT apply:** `TIDE.xml` / `TIDE.rc` do not exist, so `gmpi_plugin()`'s `${PROJECT_NAME}.xml`/`.rc` paths are out of play; and **`build.yml` names no TIDE target or artifact** (comments only), so no workflow change and no `workflow` scope is needed. **The prose half is verified done, not assumed:** `SynthEditSem/SynthEdit.cpp:396` ships `name="TIDE Rack" vendor="TIDE Synth"` and every fixture records the host seeing `"VST3i: TIDE Rack (TIDE Synth)"`; `getVendor4charCode()` still returns the fixed-width `"TIDE"` and must not change. **Nothing user-visible is waiting on N1.** Full working, per-file counts and the bucket A list: [docs/n1-tide-rack-rename.md](docs/n1-tide-rack-rename.md). **Previous text follows.** **Carry the TIDE Rack rename through the tree** — PLAN naming ruling, 2026-08-08, reaffirmed 2026-08-09. Product is **TIDE Rack**, **TIDE Synth is the organisation**; domain and repo name stay. **The prose half is DONE** (website 2026-08-08, `README.md` 2026-08-09); what remains is the build-system half, which needs decisions rather than edits. **Do it after C7, not during** — C2–C7 touch the same build files and nothing has shipped under either name, so a rename now only doubles the conflict surface. Forms are settled and must never be mixed in one filename: display `TIDE Rack`, shipped files `TIDE-Rack`, CMake targets `TIDE_Rack`. **Do not start with a global search-and-replace** — "TIDE" is a product name, an org name, a CMake target, a fixed-width 4-char vendor code and a filename prefix, and they do not all move together. Depends on **P5** or supersedes it. Reasoning, the per-surface cost and the open sub-questions: [docs/n1-tide-rack-rename.md](docs/n1-tide-rack-rename.md). |
| N1a | DONE | any | **DONE — [#268](https://github.com/JeffMcClintock/TideSynth/pull/268) merged; flipped 2026-08-22 (linux) as STEP 4 bookkeeping. **Verified on Linux for the first time in the same run: `main` builds rc=0, 0 error lines, and the renamed targets exist** (`TIDE_Rack`, `TIDE_Rack_VST3`, `TIDE_Rack_STANDALONE`) emitting `TIDE-Rack.gmpi`, `TIDE-Rack.so` and `TIDE-Rack`. **One gap the mac/windows verification could not see, filed as [#271](https://github.com/JeffMcClintock/TideSynth/issues/271):** the Linux VST3 *bundle directory* is still target-named, so `TIDE_Rack_VST3.vst3/Contents/x86_64-linux/TIDE-Rack.so` has a bundle and a payload that disagree — and `gmpi_plugin.cmake`'s own comment says a host requires them to match. Linux is the only platform that assembles that path by hand; mac (`BUNDLE_EXTENSION`) and Windows (`SUFFIX`) both pick up `OUTPUT_NAME` for free. **Previous text follows.** **THE WINDOWS LINK BREAK THIS ROW CAUSED IS FIXED, 2026-08-22 (macos, scheduled run), same branch `tide/mac/N1a-rename` — and the mechanism is worth reading before anyone sets `OUTPUT_NAME` again.** `OUTPUT_NAME "TIDE-Rack"` renames **three** artifacts per target, not one: the module, the linker PDB and the import library. All three format targets build into the SAME directory on Windows and **only the module is disambiguated, by its extension** (`.gmpi` / `.vst3` / `.exe`) — so `TIDE_Rack`, `TIDE_Rack_VST3` and `TIDE_Rack_STANDALONE` raced for one `Release\TIDE-Rack.pdb` and MSBuild's parallel link died with `LNK1201: error writing to program database`, a message that names disk space, path and privilege and means none of them. **macOS and Linux were GREEN on the identical commit** — neither emits a PDB, neither emits an import library for a MODULE, and on macOS each artifact is a bundle so even the paths differ; that is exactly why the end-to-end REAPER verification below could not have caught it. **Fixed** by keying the two side-channel names off the TARGET name, which is unique by construction: `PDB_NAME` / `COMPILE_PDB_NAME` / `ARCHIVE_OUTPUT_NAME` = `${SUB_PROJECT_NAME}`, alongside the unchanged `OUTPUT_NAME`. That also **restores the pre-rename convention rather than inventing one** — the PDBs were `TIDE.pdb` / `TIDE_VST3.pdb`, i.e. target-named. **Verified that this cannot affect macOS, by construction rather than by re-rendering:** the branch was configured twice, with and without the fix, and the ENTIRE generated build system under `build/SynthEditSem` diffed with the build-directory name normalised away — **three files differ and every difference is a PDB filename**, all three `link.txt` byte-identical. The baseline column of that diff is also the proof of the diagnosis: three targets, one PDB name, obtained with no Windows machine. **The Accept was then re-run against this tree's own binary**, not the previously installed one: `cmake --build` does not copy the VST3 into `~/Library/Audio/Plug-Ins`, so it was installed deliberately and the identity checked by hash (installed == built, `009060be…5fe816`), with Jeff's stale same-ID `TIDE_VST3.vst3` parked for the duration and **restored afterwards**. `--control` PASS; `v1-rack` −6.3/−17.0, `v1-rack-midi` −6.3/−17.0, `v3-midi-pitch` −6.2/−21.1, `v3-midi-gate` −6.3/−21.2, `v1-rack-uncabled` **silence** — all five identical to 2026-08-21. **THE WINDOWS LINK IS VERIFIED, by CI rather than by argument** — there is no MSVC on this box, so the run stayed up for the PR's own job instead of declaring victory on a mechanism. [Run 32492249466](https://github.com/JeffMcClintock/TideSynth/actions/runs/32492249466) on `276e150`: **`windows` red → GREEN** on a one-block change, with **`linux` green on both ends as the control**, plus `guard` and `lint`. That is the A/B this platform cannot run locally. `macos` is still queued behind **S30** (~5% of runs complete there) and **carries no information about this change** — it was green on the previous head, the change sets Windows-only properties, and the local build here was rc=0 with the artifacts checked; waiting on it would be waiting on a 5%-likely event to confirm something already measured, the same call C7e made on 2026-08-21. **No `platform:win` issue was needed.** **Previous text follows.** **DONE AND VERIFIED IN REAPER 2026-08-21 (macos), branch `tide/mac/N1a-rename`, [#268](https://github.com/JeffMcClintock/TideSynth/pull/268). Every Accept clause measured, and the artifact was made to PROVE it was the one under test.** **Shipped names are now dashed:** `TIDE-Rack.vst3` / `TIDE-Rack.gmpi` / `TIDE-Rack.app`, via `OUTPUT_NAME` in the format loop; targets keep the underscore (`TIDE_Rack`, `TIDE_Rack_VST3`, `TIDE_Rack_STANDALONE`), exactly as [docs/distribution.md](docs/distribution.md) requires. All 21 references updated across 11 files, comments included so none of them lie. **THE ROW'S SCOPE WAS ONE CLASS SHORT, and it fails SILENTLY — the most important finding here.** Two loops in `SynthEditSem/CMakeLists.txt` built target names by hand (`set(_tide_target TIDE)` / `TIDE_${_fmt}`, at what were `:347` and `:393`) rather than from `PROJECT_NAME`, and **every use of `_tide_target` sits behind an `if(TARGET ...)` guard** — so after the rename the bodies were simply skipped: **no `tide_render` link, no resource staging, no error**. It surfaced only as `fatal error: 'TidePathTracer.h' file not found`; had `TiDEPanelGui.cpp` not needed that header, the build would have gone green while shipping bundles with **zero prefabs**. All four now derive from `${PROJECT_NAME}`, with a comment saying why. **Identity is UNCHANGED, checked before renaming rather than after:** the plugin's `id`, `name` and `vendor` come from the `<Plugin>` element in `SynthEditSem/SynthEdit.cpp`, not from `PROJECT_NAME`, so hosts still resolve saved sessions. What does follow `PROJECT_NAME` is the STANDALONE's bundle id (`gmpi_plugin.cmake` sets `com.gmpi.standalone.${PROJECT_NAME}`) — a developer tool, so its preferences container resets; noted, not a product risk. `${PROJECT_NAME}.xml` is not used here (no `HAS_XML`, no `TIDE.xml`), so distribution.md's other warning does not bite. **Measured, before and after, same box:** baseline `v1-rack.rpp` = peak **−6.3 dBFS**, rms **−17.0**, 2 patch cables; after the rename, **identical**. And because the old `TIDE_VST3.vst3` was still installed with the same plugin ID, that alone proves nothing — so it was **moved aside and the render repeated**, giving the same numbers with only `TIDE-Rack.vst3` present. All five fixtures then pass under that isolation: `v1-rack` −6.3/−17.0, `v1-rack-midi` −6.3/−17.0, `v3-midi-pitch` −6.2/−21.1, `v3-midi-gate` −6.3/−21.2, and `v1-rack-uncabled` **silence** (the negative control). `--control` PASSes. **Jeff's original bundle was restored afterwards.** **One housekeeping item for Jeff, not done unilaterally:** `~/Library/Audio/Plug-Ins/VST3/TIDE_VST3.vst3` (16 Aug) is now stale — the build no longer produces that name — and sits beside the new `TIDE-Rack.vst3`, so a DAW scan will list both. Deleting it is a change to Jeff's plugin folder and is his call. **Previous text follows.** **NOT TAKEABLE ON THE LINUX BOX — it needs REAPER, which is not installed there. The rename itself: bucket A, one commit.** Split out of **N1** 2026-08-21 (linux) after costing it. **Scope, all of it and nothing else:** `SynthEditSem/CMakeLists.txt` (7), `tools/sandbox_audit.py` (4), `tests/s21_bundle_resources_probe.py` (2), `modules/common/CMakeLists.txt`, `scripts/measure-chunk-robustness.py`, `TideModules/build-prefabs.py` (1 each), and the **five `tests/hosts/*.rpp` fixtures** (1 each). **21 references, 11 files.** **Forms are already settled and must not be mixed:** display `TIDE Rack`, shipped files `TIDE-Rack`, CMake targets `TIDE_Rack`. So `project(TIDE)` at `SynthEditSem/CMakeLists.txt:53` becomes `TIDE_Rack`, targets derive as `TIDE_Rack` / `TIDE_Rack_VST3` / `TIDE_Rack_STANDALONE` via `${PROJECT_NAME}_${kind}` (`:533`), and the shipped names become dashed via `OUTPUT_NAME` — **plus `MACOSX_BUNDLE_BUNDLE_NAME`, which `OUTPUT_NAME` does not cover.** **WHY IT IS PLATFORM-BOUND, and it is not a preference:** the five fixtures name the artifact by filename (`<VST "VST3i: TIDE Rack (TIDE Synth)" TIDE_VST3.vst3 0 "" 1386065673{...}`), so the rename **invalidates all five at once** — and those fixtures are v0.1's acceptance evidence, the thing PLAN.md points at to say the product works. A box without REAPER can make the change and cannot tell whether it broke the proof. **Accept** `python3 scripts/render-and-measure.py --control` still detects audio, and `render-and-measure.py` passes on all five fixtures against the renamed artifact. **Do NOT touch `JOURNAL*`, `BACKLOG-DONE.md` or `docs/lessons.md`** — see N1's bucket C. **Size** small, but only on the right box. |
| N1b | DONE | any | **DONE — [#273](https://github.com/JeffMcClintock/TideSynth/pull/273) merged 2026-08-21; flipped 2026-08-22 (windows) per STEP 4, PR state queried rather than assumed.** **Previous text follows.** **IN-REVIEW 2026-08-22 (linux, scheduled run), branch `tide/linux/N1b-live-docs` ([#273](https://github.com/JeffMcClintock/TideSynth/pull/273)). Unblocked because N1a is now DONE.** **The triage is the deliverable, and it corrected N1's own bucketing:** `docs/state-of-the-prototype.md` was listed as a live reference doc on the strength of its filename, and reading it shows it is a **dated observation report** — *"Observation only… Everything below was seen"*, 2026-08-06 — whose Lua transcript, crash-report text and P5 finding would all be falsified by a rename. Same for `docs/p4-resize-crash.md`, whose PDB names sit in a measured before/after byte table. **So they are ANNOTATED, not rewritten**, against the row's original expectation that N1b would edit them — flagged here rather than done silently, because it is a judgement call Jeff may want to overrule. **Genuinely live, and updated:** `docs/building.md` (build commands, the two-target trap, the macOS copy line), `docs/ci/linux-build-deps.md` (the build-and-run instructions), `docs/ci/headless-gui-verification.md` (**wrong within a day of being written** — it told a run to launch `./TIDE_STANDALONE`, which no longer exists; the corrected recipe was re-verified end to end, and `pgrep -x TIDE-Rack` matches), and `docs/n1-tide-rack-rename.md`'s status line. **One authoritative place instead of eleven banners:** `docs/building.md` gains a *Current target and artifact names* table, so the dated records need no edit and a reader has somewhere to check. **Left untouched as records, each self-dated:** `c7-clean-clone`, `carve-out` (a WinDbg transcript), `e2a-prefabs`, `e9-sample-rate`, `module-enumeration`, `p7-resize-audit-mac-x11`, `s11-restore-trace`, `sandbox-audit`, `u1-rack-mode-audit`, `u2e-pin-delivery-trace`, `u2-triage-2026-08-16`, and `decisions.md` (dated ruling rows). **Previous text follows.** **TWO MORE REFERENCES, both PDB names, found 2026-08-22 (macos) while fixing N1a's Windows link:** `docs/state-of-the-prototype.md:106` and `docs/p4-resize-crash.md:461-462` name `TIDE_VST3.pdb` / `TIDE.pdb`. They are live reference docs, so they belong here rather than to N1a, and they are **still true today** — N1a's fix deliberately keeps PDBs target-named, so after it lands the correct names are `TIDE_Rack.pdb`, `TIDE_Rack_VST3.pdb` and `TIDE_Rack_STANDALONE.pdb`. **Previous text follows.** **The live reference docs, after N1a lands.** Split out of **N1** 2026-08-21 (linux). `docs/building.md` (12), `docs/state-of-the-prototype.md` (8), `docs/ci/linux-build-deps.md` (7), `docs/n1-tide-rack-rename.md` (1) — instructions somebody follows today, which are **true until N1a lands and false afterwards**, hence the block rather than a bundle: doing them first makes the docs lie. **Explicitly out of scope, permanently:** the 220 references in `JOURNAL*`, `BACKLOG-DONE.md` and `docs/lessons.md`. Those are the record. **Accept** no live doc names a target or artifact that the build no longer emits, checked with one grep. **Size** small. |
| P7d | DONE | any | **ALREADY DELIVERED — closed 2026-08-20 (macos, scheduled run) by running this row's own Accept, which PASSES on today's tree.** The fix landed in GMPI_Wrappers as `3838493` *"fix(build): every wrapper links UniformTypeIdentifiers, not just standalone"* — `wrapper/cmake/GmpiFrameworks.cmake` provides `gmpi_weak_frameworks()` and **all five wrappers** (VST3, Standalone, CLAP, AU2, AU3) include it, with the header's own `-weak_framework` form (not plain — macOS 10.x has no such framework and these deploy to 10.15). **Measured, not inferred from the comment:** fresh `GMPI-plugins` configure rc=0, `ninja GainGui_VST3` **rc=0 with no extra `-D` flags** (the exact Accept), bundle produced, and `otool -l` shows the UniformTypeIdentifiers load command in the linked binary with the UTType reference resolved through it. **The scope question this row parked for Jeff is MOOT:** the fix went to a third place neither option named — GMPI_Wrappers (ALLOWED), where the wrappers that actually compile `DrawingFrameMac.mm` live — which fixes every consumer at once without touching `GMPI-plugins` or adding per-consumer link lines. **Previous text follows.**  **`GMPI-plugins` cannot link a GUI plugin on macOS.** `Undefined symbols … "_OBJC_CLASS_$_UTType", referenced from: … DrawingFrameMac.mm.o`. `gmpi_ui/backends/MacFileDialog.h:8` imports `<UniformTypeIdentifiers/UniformTypeIdentifiers.h>` and uses `UTType`; `GMPI-plugins`' link line never adds the framework. `SynthEdit/EditorLib/CMakeLists.txt:166` does add it, which is why SynthEdit's own macOS build is fine and this stayed invisible. Pre-existing — nothing in P7 caused it. **Not fixed, on purpose:** `GMPI-plugins` is on neither the ALLOWED nor the GATED list, so it is GATED by default and this is the scope question, named per that rule. The P7 audit worked around it at configure time with `-DCMAKE_MODULE_LINKER_FLAGS="-framework UniformTypeIdentifiers"`, touching no file. **The scope question for Jeff:** the requirement arguably belongs in `gmpi_ui`, declaring the framework it actually needs, rather than in every consumer's link line — that would fix `GMPI-plugins` and any other consumer at once, and it is an ALLOWED path. Accept: `ninja GainGui_VST3` links on macOS with no extra `-D` flags. Size: minutes once the scope is ruled. |
| P8 | DONE-PENDING-CI | win | **Fixed 2026-08-09 on the Windows box, at Jeff's direction, straight to `SE16` master (`4baddfbb4`) — see [docs/decisions.md](docs/decisions.md).** One line at `SynthEdit2/EditorWindowHelper.cpp:294`: `currentContainerView.universalFactory` → `.get()`. `UniversalFactory` derives from `gmpi::api::IUnknown` (`SynthEditLib/Shared/DrawingFrame2_cpu.h:19`) and `renderContainerThumbnail` **borrows** rather than takes the factory, so `.get()` matches what the other three callers already pass and what `DrawingFrame2_win.h:270` does when it hands the same factory out. **Verified:** `MSBuild SynthEditStore.sln -t:SynthEdit2 -p:Configuration=Release -p:Platform=x64` now links, producing `x64\Release\SynthEdit2\SynthEdit2.exe`. **This was bigger than "does not compile from clean" — it was failing the SynthEdit Store release pipeline.** Actions run [31297103293](https://github.com/JeffMcClintock/SynthEdit/actions/runs/31297103293) died on `master` at "Build SynthEditStore (Release x64)" on exactly this error, skipping changelog, MSIX signing, bootstrapper and FTP upload — so nothing shipped. **Left as DONE-PENDING-CI rather than DONE:** the local Release link is proof the error is gone, but the CI job goes on to sign and upload, and nobody has watched a green run yet. Whoever sees one first should flip this to DONE. **Also unblocks the link-stage verification C1b and C2 each had to leave open** — both could compile their new TUs but neither could reach SynthEdit2's link. A Debug build of the same project failed on an unrelated `x64\Debug\pch.pch: Invalid argument` intermediate-file error; not investigated, and not this bug. Original finding below. |
| ~~P8~~ | *(was)* | win | **`SynthEdit2` (the WinUI3 app) does not compile from clean.** Not TIDE, and pre-existing — found during C1b and **A/B-confirmed unrelated** (fails identically with the C1b vcxproj change reverted): `SynthEdit2/EditorWindowHelper.cpp(294)` passes `std::unique_ptr<UniversalFactory>` where `se_cl::renderContainerThumbnail` (`EditorScreenshot/ContainerThumbnail.h:35`) now takes `gmpi::api::IUnknown*` — the thumbnail API moved and the WinUI3 caller never caught up. Matters to TIDE because "leave SynthEdit building" is the standing rule and **C1b's link-stage verification of SynthEdit2 is blocked behind it** (the new `ExportAsPlugin.cpp` ClCompile entry compiles clean; the app cannot reach its link step). Same shape as P6 on macOS: SynthEdit's own bug, in SynthEdit's own files — check with Jeff before touching. |
| S1b | TODO | any | **Compile the module scan out — stages (b) and (c); (a) is done.** ~~Blocked on C0~~ — unblocked 2026-08-08. **(a) DONE:** TIDE no longer compiles `SynthEdit2/SynthEditApp.cpp`, which had been putting a second `LoadOrScanModuleData()` call site, a filesystem-watcher thread and the whole licensing surface into the shipping plugin (`SE16` `40b6008ee`). **(b) and (c):** `ScanFolder`, `LoadModuleData`, `Module_Info3` and an imported `dlopen`/`dlsym`/`dlclose` are still in the Release binary. Needs a new `TIDE_NO_EXTERNAL_MODULES` through `EditorLib/CMakeLists.txt` and a real split of `ModuleFactory_Editor.cpp`. **Do not attempt this with an existing flag** — `SE_EXTERNAL_SEM_SUPPORT` guards two lines and removes none of it (measured), and `GMPI_IS_PLATFORM_JUCE` is a trap. **Recommend riding along with C4:** these are the same files C3 and C4 move, so standalone means doing it twice. Accept: constraint 7 verifiable from the Release binary — none of those symbols present. Measurements: addendum B1–B5 of [docs/module-enumeration.md](docs/module-enumeration.md); row history in [docs/s1b-compile-the-scan-out.md](docs/s1b-compile-the-scan-out.md). |
| X4 | WONTFIX | any | **Closed 2026-08-08 by Jeff: leave the `GIT_TAG origin/main` pins alone. Do not re-file this.** `FetchContent` is not how the SynthEdit-family dependencies are meant to be developed against — they change daily and the intended dev path is the `*_FOLDER_OVERRIDE` variables, which are the normal path on a dev box, not an optimisation. CI clones fresh, so `origin/main` there is real. **The thing to watch for instead is a *missing* override** — that asymmetry, not the pin, is what caused X3. **CMake already announces it:** every dependency prints `Using local <X> folder` or `Fetching <X> from github` at configure time. An unexpected "Fetching" on a SynthEdit-family repo means you are building against whatever `main` looked like when the tree was first configured; confirm with `git log -1` in `build/_deps/<dep>-src` before believing your tree. Full reasoning and the original finding: [docs/x4-fetchcontent-pins.md](docs/x4-fetchcontent-pins.md). |
| S5 | TODO | any | **`getFolderInfo` is undefined behaviour in TIDE.** `TideApp::InitInstance` never calls `CSynthEditAppBase::InitInstance`, so `refreshFolderLocations()` never runs and `m_folder_settings` stays empty; `getFolderInfo` (`SE16/SynthEdit2/Application.cpp:167`) then evaluates `m_folder_settings[0]->current_folder` on an empty vector. Reachable via `CSynthEditAppBase::ShortenFilename` (`SE16/SynthEdit2/SynthEditAppBase.cpp:238`). `ResolveFilename` is unaffected. Fix in `Application.cpp` (guard the empty case) — that is EditorLib, so check carve-out ordering before moving it. |
| S9 | WONTFIX | mac | **Moot 2026-08-15 — S10 ruled retire, not revive.** This row's own scope was surgical cleanup of `TIDE/Plugins` wiring *inside* `SE_IOS_APP.xcodeproj`; deleting the whole project ([SynthEdit#22](https://github.com/JeffMcClintock/SynthEdit/pull/22)) makes that cleanup unnecessary rather than incomplete, exactly as this row itself predicted ("S9 is moot if the answer is retire"). Original row follows. **GATED remainder of S6 — remove the now-dangling `TIDE/Plugins` wiring from `SE_IOS_APP.xcodeproj`.** Filed 2026-08-13 when S6 deleted the files. Three things to remove, all in the **shared, GATED** `SE16/SE_IOS_APP/SE_IOS_APP.xcodeproj/project.pbxproj`, all belonging to the `SeAudioUnitMacOS` target: (a) the Run Script phase at `:2064` (`cp -r "$SRCROOT/TIDE/Plugins" …`); (b) the six `.sem` group/file references (`Oscillator`, `EnveloperAdsr`, `PatchMemory`, `Converters`, `SubControlsXp`, `ControlsXp`, ids `D5F5219525E5F21600E75519` and siblings) — navigator-only, cosmetic, they just show red; (c) **the real ones — the `CpResource` build-file entries for files *inside* those bundles, which are genuine Resources-phase inputs** and are what now errors. **Do not do this without reading S10 first** — if that project is retired rather than revived, this row is moot and the right move is deleting the whole `.xcodeproj`. **Accept:** `SeAudioUnit macOS` builds with no `TIDE/Plugins`-related error (it will still fail on `SE_DSP_CORE` — that is S10, not this). **Size:** minutes, once scope is ruled. **Needs Jeff** — GATED path, and a scheduled run may not edit it. |
| S7 | TODO | any | **Enforce the no-user-skins ruling — PLAN constraint 8, decided 2026-08-07.** The static chain says a TIDE build writes skins to the user's disk today: `CContainer.cpp:97` calls `SkinMgr::Instance()->getSkin(L"default3")` (also `SynthEditDocBase.cpp:386`), and `SkinMgr`'s **constructor** (`SE16/SynthEdit2/SkinMgr.cpp:27-30`) points its folder at `<CommonDocuments>\SynthEdit Projects\skins\`, where `setSkinFolder` (`:47+`) **recursively copies the built-in skins on first use**. So the first container a TIDE instance constructs probably spews the shared skin set over the user's drive — verify at runtime, then make TIDE load its default look from plugin resources only, with no skin folder, scan, or write reachable. `SkinMgr` is EditorLib (C4, GATED) — if the fix cannot be confined to TIDE-side code, do what can be and file the gated remainder naming the exact lines. Overlaps S2's audit; whoever takes either first should check the other's scope. |
| S8 | TODO | any | **MOOT FOR THE MVP as of 2026-08-21 — Jeff ruled that TIDE ships "Oscillator HD" (`SE Oscillator4`) as its oscillator, and that one is ALREADY in the plugin** (compiled by E2c, `SynthEditSem/CMakeLists.txt`). This row's finding about `OscillatorNaive` having zero symbols is unchanged and still true, but it is about a DIFFERENT module and **no longer blocks Tier 0, E2 or the module set**. Left open rather than closed because the underlying packaging fault — a module in a source list belonging to a separate target — is real and will bite the next module that needs linking. **Previous text follows.** **CAUSE CONFIRMED 2026-08-18 (macos, interactive) while V3 hunted for a monophonic MIDI-CV — and it is the explanation this row already suspected.** `modules/OscillatorNaive/OscillatorNaive.cpp` is in a `SynthEditLib/CMakeLists.txt` source list belonging to a **separate target**, so it builds as a LOADABLE module and is **not compiled into the SynthEditLib static library**. TIDE links statically and has no scan (S1a), so it can never appear. Confirmed by build layout, not `nm`: a sibling in the same list, `MidiToGate.cpp`, puts its object under `MidiPlayer2.build/` while `MidiToCv2.o` — genuinely in the library — lands under `SynthEditLib.build/`, and adding an `INIT_STATIC_FILE` for that sibling **fails to link**, which is the linker stating the object is not there. **It is NOT the `#if GMPI_IS_PLATFORM_JUCE==1` block**, a tempting wrong answer: `INIT_STATIC_FILE(ADSR)` sits inside it while ADSR works fine in TIDE. **The fix needs NO GATED change:** add the `.cpp` to `SynthEditSem/CMakeLists.txt`'s source list and stage its XML, exactly as **E7** did for `MidiToGate.cpp` and E2a did for `RectangleGui.cpp`. **Original row follows.** **Corrected 2026-08-17 (windows, interactive): relabel these modules, do NOT delete them — the original premise below would have silenced the plugin.** Measured, not inferred: all three are `RegisterIoModule` seams (`ug_soundcard_out.cpp:69`, `ug_soundcard_in.cpp:63`, `ug_midi_out.cpp:50`), and Sound Out implements `ISpecialIoModuleAudioOut` — `SeAudioMaster` hands it the **host's** output buffers (`SeAudioMaster.cpp:560-562`, `:640-642`), the identical mechanism S12 used for MIDI input. In a plugin these *are* the DAW-IO modules constraint 2 wants; only their standalone-era copy says otherwise. `TIDE.se1` terminates in Sound Out and E2a's Output prefab will too ([docs/e2a-prefabs.md](docs/e2a-prefabs.md) §8). The real S8 work: (a) TIDE-facing name/group/help text — "Sends audio to your speakers… Non-registered SynthEdit limited to 2 output channels" is wrong twice over for a free plugin whose host owns IO; (b) the TIDE module list ([docs/module-enumeration.md](docs/module-enumeration.md) §6 trap: neither existing arm is right) **keeps all three registered**; (c) the oscillator gap is the live E2a blocker: `OscillatorNaive` zero symbols. Original row follows. **TIDE ships modules constraint 2 forbids, and shows SynthEdit licensing copy.** Measured from the macOS Release binary 2026-08-08 (addendum B3 of [docs/module-enumeration.md](docs/module-enumeration.md)): `ug_soundcard_in`, `ug_soundcard_out` and `ug_midi_out` are compiled in *and registered* — each has its `se_static_library_init_*` and `__GLOBAL__sub_I_*` static initialiser — so the module browser offers a **Sound Out** module that opens the soundcard directly, in a plugin whose whole premise is that the DAW owns I/O. `ug_soundcard_out.cpp:10` registers it under Input-Output with the help text *"Sends audio to your speakers… Non-registered SynthEdit limited to 2 output channels"*, which is also wrong for a free unlicensed plugin. Confirms A1's prediction; also confirmed the modern SEM modules are absent (`OscillatorNaive`: zero symbols). Both arms of `initialise_synthedit_modules` are in `SynthEditLib/UgDatabase.cpp:1063-1155`, so there is **no TIDE-side fix** — ~~GATED on C0~~ **unblocked 2026-08-08, C0 approved.** The `SE_EXTRA_STATIC_FILE_CPP` hook (A2) can only *add* modules, not suppress these. Pairs with S1b; whoever takes either should take both. |

| S3g | TODO | any | **GATED remainder of S3 — TIDE still offers menu items for dialogs it does not have.** Filed 2026-08-17 (linux) after S3 closed the TIDE-side half. A loud stub is a backstop; the menu entry is the actual defect, because the user is being offered a command that cannot work. **Three entries, all in GATED paths, all reached from TIDE's module context menu:** (a) `SynthEditLib/MfcDocPresenter.cpp:1274` adds **"Connect..."** when `viewType == CF_STRUCTURE_VIEW` → `POPUP_MENU_CONNECT` → `CUG.cpp:2057` → `CUG::OnConnectDialog()` (`:2633`) → `doDialogConnectUg`; (b) `SynthEditLib/MfcDocPresenter.cpp:1276` adds **"Build Code Skeleton..."** → `POPUP_MENU_DEBUG_CODE` → `CUG.cpp:2034`'s `VO_Notify(OM_SHOW_CODE_SKELETON_DIALOG)`, which TIDE does not handle, so it is a silent no-op **that no stub guards** — S3's loud report does not cover this one; (c) the patch manager, `SynthEditLib/CUG_with_patches.cpp:33` (`HC_CopyPatch`) and `:142` (`ID_PATCH_MGR`) → `DoPatchCopy()` (`:162`) → `doDialogPatchManager`. **The shape is already settled by precedent, which is why this is small once ruled:** `MfcDocPresenter.cpp:1264` already consults `GetLicenseState()` — a per-app runtime query, resolved at link time, added by **C11** for exactly this "the shared file must not know the app's type" problem — to grey out "Selection to Prefab". An equivalent per-app capability query is the fix; an `#ifdef` is not, since EditorLib compiles once for every app. **Accept** with TIDE built, the module context menu offers none of the three, and S3's stderr report never fires because nothing can reach it. **Size** minutes of code; the cost is the scope ruling, not the work. **Also tidy while there:** `SE16/SynthEditCL/CLApp.cpp:26` still defines the dead `doDialogBuildCodeSkeleton` that S3 deleted from `TideApp.cpp`; `SynthEditCL/` is GATED by default too. **Needs Jeff** — every path named here is GATED or GATED-by-default, and the `SynthEditLib` ALLOWED/GATED contradiction the 2026-08-17 macOS run raised as a process note is unresolved and lands squarely on this row. |
| B1 | TODO | any | **HALF DONE 2026-08-20 (interactive, Jeff directing): the `continue-on-error` is gone.** `build.yml`'s matrix job no longer carries it, so when the matrix runs its result means something. **Removed BEFORE C7d deliberately, and it opens no red-checks window:** the job's `if: needs.guard.outputs.buildable == 'true'` skips it entirely until a root `CMakeLists.txt` exists, so the change is inert until the day it is wanted and nobody has to remember it then. `fail-fast: false` was left alone, so one platform going red still lets the other two report and still lets each file its own platform issue. The three comment blocks that described the flag were rewritten in the same edit rather than left to rot — A27's lesson, that a comment contradicting the code is a bug with no test that can fail. **Checked in passing, and it is fine:** `lint.yml`'s six per-step `continue-on-error`s are a deliberate run-all-then-aggregate pattern whose `Summary` step exits 1 on any non-success, so `lint` — the check A4's auto-merge tier actually gates on — is a real gate. **What is left of this row is now exactly one thing: TIDE's root `CMakeLists.txt`, which is C7d's job**, and C7d was unblocked earlier the same day when C14 merged. **Original item follows.** **Partly mitigated 2026-08-16 (interactive, Jeff's credentials) — the symptom is gone, the row is not done.** `build.yml` now has a `guard` job that checks for a root `CMakeLists.txt` and gates the matrix on it, so pre-C7 the three platforms are **skipped** rather than each dying at Configure. That removes the six FAILURE rows every PR was carrying (3 platforms × 2 events — the push and PR runs are both deliberate, see the workflow's `concurrency` comment). Worth being precise about what those rows were costing: **nothing gated on them** — this repo has no branch protection and no required status checks, and A4's auto-merge tier reads `lint`, not this — so their only effect was to make `gh pr checks` permanently red and train every reader to skim past it. A check list that is always red measures as little as a run that always reports success, which is this row's other half. The gate skips rather than passing, because a green matrix would claim a clean-clone build succeeded; and it is keyed on the file itself, like the issue-filing suppression, so it lifts by itself when C7 lands. **What remains for this row, unchanged:** TIDE's top-level CMake still has to be authored (wants C7's shape settled), and `continue-on-error` at job level still has to go, or green here will still mean nothing. Note the diagnosis below was also briefly mis-stated as covering `verify.yml` — it does not: `verify.yml` runs no CMake at all, downloads a published engine, and passes. Only `build.yml` was ever failing. Original row follows. **Bigger than this row implies — measured 2026-08-14 (windows), in passing during the C12 scoping run.** `build.yml` does **not** currently fail for the reason it and this row both claim. All three platforms die at the **Configure** step with `CMake Error: The source directory "…/TideSynth" does not appear to contain CMakeLists.txt` — because **`TideSynth` has no top-level `CMakeLists.txt` at all**; the repo root holds only markdown, `docs/`, `scripts/`, `tests/`, `tools/` and `website/`. So the run never gets as far as the missing private dependency, and the honest-failure state this row asks for cannot be reached by removing toolchain noise — it needs **TIDE's top-level CMake to be authored**, which probably wants C7's shape settled first. Note also the workflow's own header: `continue-on-error` at job level means every run reports success, so nothing should treat it as a gate until this lands. **Still needs Jeff or an interactive session either way** — the file is `.github/workflows/**`, which the bot token structurally cannot push. Original row follows. **CI that builds nothing yet but is correct.** `.github/workflows/build.yml` exists as a skeleton and is expected to fail until C7. Get it to the point where it fails for exactly one honest reason (missing private dependency) rather than for syntax or toolchain errors. |
| D5 | DONE | — | **DONE 2026-08-16 — Jeff did it during this session, and it is verified rather than taken on trust.** The page now renders as **"Jef [TIDE Rack]"** with the browser title **"Buy Jef [TIDE Rack] a Coffee"**; before, it was plain *"Jef"* with nothing naming the product. Checked by loading <https://ko-fi.com/tiderack> in a real browser — **the only way to check it**, per this run's own finding that Ko-fi 403s unfamiliar user-agents even for handles that do not exist. So the Accept (*"the page names TIDE Rack"*) is met, and the website's *"Donate to TIDE Rack on Ko-fi"* link now lands somewhere that agrees with it. **One optional nicety deliberately not re-filed:** the bio still reads *"I'm a dude in New Zealand"* and does not name SynthEdit Ltd, which this row listed as *ideally*, not as its Accept. Not worth a row; mention it only if the about pane ever quotes the bio. Original row follows. **The Ko-fi page does not identify itself as TIDE Rack's, which defeats the point of D2 one hop later.** Found 2026-08-16 by the D2 run, by opening it rather than assuming: <https://ko-fi.com/tiderack> renders as *"Buy Jef a Coffee"*, display name **"Jef"**, bio *"I'm a dude in New Zealand"*. Nothing on it says TIDE Rack, TIDE Synth or SynthEdit. **Why this is not cosmetic:** D2's whole justification is making the identity trail legible on TIDE's own terms, and the website's link text is *"Donate to TIDE Rack on Ko-fi"* — so a user who clicks it lands on a stranger's tip jar and meets exactly the surprise the credit exists to prevent. It is also the page that will be linked from the in-plugin about pane ([docs/about-pane.md](docs/about-pane.md)), so it gets more exposed, not less. **Scope** Ko-fi account settings only — page title, display name, a line of bio, ideally naming SynthEdit Ltd to match the credit. **No repo change, and not an agent's to make**: it needs the account password. **Accept** the page names TIDE Rack. **Size** minutes, in a browser. **Default in effect:** the page stays as it is, and the donation link keeps landing somewhere that looks unrelated. **Decide-by:** before v0.1 ships anything that links it. |
| D4 | WONTFIX | any | **WONTFIX 2026-08-17 — this row's central measurement is FALSE today, and acting on it would have broken the build.** It claims *zero* call sites for `gmpi::browse_to`; re-measured this session there are **two live ones** — `SynthEditLib/SkinMgr.cpp:111` (`SkinMgr::EditSkin`, the "open this skin's folder" command) and `SynthEditLib/MfcDocPresenter.cpp:1106` (the skin-folder context command). Deleting `browseto.mm` would produce an undefined symbol, not a saving "at no functional cost". The positive control was run too (`gmpi::open_url`, 5+ call sites), so a zero would have been distinguishable from a broken grep. The row was filed 2026-08-16 and the C-series carve-out has been moving files into `SynthEditLib` throughout, so the callers most likely arrived with a move after the original grep. **The row's real INTENT — getting an AppKit dependency and the sandbox-hostile `activateFileViewerSelectingURLs:` out of builds that should not have them — is delivered by D3 instead**: `browse_to` compiles to a no-op off macOS, while macOS keeps the behaviour it actually uses. **Do not re-file.** **The generalisable lesson: re-run a row's own measurement before acting on its conclusion, especially a "delete this, nothing uses it" row in a tree being actively carved up.** Original row follows. **`browseto.mm` is compiled into every Apple build and never called.** Found 2026-08-16 by the D1 run: grepping `SynthEdit`, `SynthEditLib`, `gmpi_ui` and `GMPI_Wrappers` finds **zero** call sites for `gmpi::browse_to` (`gmpi::open_url` has real ones — `SynthEdit2/Application.cpp:389` and the Wayland Help menu — so the grep is not simply missing them). Dropping it from `SE16/EditorLib/CMakeLists.txt:162` removes an AppKit dependency and one sandbox-hostile API (`activateFileViewerSelectingURLs:`, i.e. reveal-in-Finder) from every Apple build including TIDE's, at **no functional cost**. Reduces D3's surface to one file rather than two. **Scope** one line of `SE16/EditorLib/CMakeLists.txt`; optionally the now-unreferenced `helpers/browseto.{h,mm}` in `gmpi_ui`, which is ALLOWED and shared, so leave it unless asked. **Accept** SynthEdit, SynthEditCL and TIDE all still build on macOS with the entry removed. **Size** minutes. **GATED path — same file and gate as D3; do them together.** |
| P3 | TODO | win | **Remove the MFC dependency from the two shared-core files that have one.** **Paths updated 2026-08-13: one of the two has moved.** `CContainer.cpp:8` is now `SynthEditLib/CContainer.cpp:8` — C3 moved it to the **public** repo unaltered, so once [SynthEditLib#5](https://github.com/JeffMcClintock/SynthEditLib/pull/5) merges the hard MFC requirement is public, which is the thing this row was filed to prevent. `MfcDocPresenter.cpp:4` is still `SE16/SynthEdit2/` and moves under **C4**, so the second half is best done before or with C4. Both include `afxres.h` under `#ifdef _WIN32`. That header ships only with Visual Studio's MFC component, so a contributor with VS Build Tools (no MFC) cannot build TIDE at all — see the "MFC trap" section of [docs/building.md](docs/building.md). Both files are on the carve-out list (C3 and C4), so this lands in the public repo as a hard MFC requirement unless it is removed first. Find out what they actually need from `afxres.h` — it is likely only `ID_*`/`IDR_*` resource constants, which could move to the local `resource.h`. Verify SynthEdit and SynthEditCL still build. |
| P11 | TODO | any | **MAC HALF RE-MEASURED AND CORRECTED 2026-08-22 (macos, scheduled run), branch `tide/mac/P11-mac-module-visibility` — this row's own mac note had the right symptom and the WRONG mechanism, which is the expensive kind of stale.** It said *"the mac build has NO counterpart to the win post-build module-DB copy at all"*. **There is one.** `copy_plugin()` installs to `$ENV{HOME}/Library/Audio/Plug-Ins/GMPI` (`GMPI/gmpi_plugin.cmake:1225`) under `SE_LOCAL_BUILD`. **The scanner reads a different domain:** `getPlatformPluginsFolder()` returns the string literal `"/Library/Audio/Plug-Ins/"` (`SynthEditLib/modules/se_sdk3_hosting/BundleInfo.cpp:152-164`), `"GMPI"` is appended at `SynthEdit/SynthEdit2/SynthEditApp.cpp:155-164`, and that single path is all `RefreshModuleData` scans — **S35 cites the exact scan line**, since that is where a fix would go, and this row is the measurement rather than the fix. There is **no `NSSearchPathForDirectoriesInDomains` anywhere in the scan path** — unlike VST3 and AU, which search user *and* system by convention. **So the module is installed and invisible.** **Measured on the artifact, not argued:** across every `Plugin-Cache-16-override-*.xml` in `~/Library/Application Support/SynthEdit/`, all **602** recorded module paths are system-domain and **zero** user-domain paths appear in any cache file at any date — so it is not a stale-cache artifact. **The correction matters because the old wording sends the next person to add an install step that already exists**, instead of to the domain mismatch. **TIDE IS NOT AFFECTED — a suspect eliminated.** TIDE does no scan: S1a removed it and the browser reads a force-linked in-memory list (`SynthEditSem/TideApp.cpp:434-442`). This bites **SynthEdit the editor** consuming TIDE as a third-party module, which is the configuration the Windows symptom was found in. **N1a made it permanent:** any pre-rename `/Library/Audio/Plug-Ins/GMPI/TIDE.gmpi` is now an orphan nothing will ever update, since the build emits `TIDE-Rack.gmpi`; two such copies were found on the mac box (system 2026-08-16, user 2026-05-07). **The fix is not in an allowed path** — searching the user domain is a `SynthEditLib` change (GATED) and installing to the system domain is a GMPI one (PR-GATED), so the takeable half was the measurement and [docs/building.md](docs/building.md), both done here. **Filed as S35.** **Previous text follows, mac note superseded.** **Building `TIDE_VST3` alone ships a plug-in that cannot build its DSP graph, and the error blames the user.** Found 2026-08-16 (windows) by the host-verification session, which hit it before it hit anything in U2. **The mechanism:** on Windows the VST3 resolves its built-in `SE *` GUI modules through the *installed module database*, whose TIDE entry is `C:\Program Files\Common Files\SynthEdit\modules\TIDE.gmpi`. That file is written by the **`TIDE` target's** post-build copy, not by **`TIDE_VST3`'s** — so `cmake --build . --target TIDE_VST3` alone leaves whatever `TIDE.gmpi` was there before, and the two halves disagree. **The symptom:** a modal *"Export failed: required module is missing from the module database: &lt;name&gt;. The module scan is incomplete - this installation is broken. Re-scan modules (or repair the modules folder), then export again."* — `SE Background Image` at instantiate, then `SE List Entry` / `SE Text Entry` on each GUI-control insert. **DSP-only modules (`Moog Filter`) export clean**, which is the tell that isolates it to the GUI half rather than to a broken modules folder. Building the `TIDE` target as well cleared every instance. **Why it is worth a row:** the message names the *user's installation* as broken and tells them to re-scan modules, for what is actually a half-built tree — so the first person to hit this on a fresh box will chase the wrong thing, and a scheduled run would report it as an environment failure. **Options:** (a) make the two targets' module-database outputs consistent, or have `TIDE_VST3` refresh `TIDE.gmpi` too; (b) leave the build alone and fix the diagnostic to name the mismatch it can actually detect; (c) document it in [docs/building.md](docs/building.md) as a build-both-targets rule. **(b) plus (c) is probably the cheap correct pair** — (a) is a build-graph change with wider blast radius, and the diagnostic is wrong regardless of whether the build changes. **Scope** `SE16/SynthEditSem/CMakeLists.txt` and whichever file raises the export diagnostic — check the GATED line before reaching. **Accept** a tree built with `--target TIDE_VST3` alone either loads without the dialog, or produces a message that names the stale `TIDE.gmpi` rather than the user's install. **Size** minutes for (c), one session for (b). **Mac finding, 2026-08-16 (interactive U2d session): the mac build has NO counterpart to the win post-build module-DB copy at all** — `/Library/Audio/Plug-Ins/GMPI/TIDE.gmpi` sat 3.5 months stale (May 7 build, pre-P5 identity, zero `ContainerViewPanel` symbols) until refreshed by hand; the fresh `TIDE.gmpi` lands only in `build/SynthEditSem/Release/`. Note the stale DB was RULED OUT as U2d's cause (see that row), so this stays exactly what P11 says it is: a build/install trap with a misleading symptom. Documented meanwhile in [docs/building.md](docs/building.md). |

| S16 | TODO | any | **The mac test suite is 44/57 red for a hardcoded path, so it is useless as a regression signal — found 2026-08-18 (macos) while A/B-ing S13.** `tests/projecttests.cpp:103` and `tests/layouttests.cpp:56` both hardcode the build folder as `/Users/jeffmcclintock/SynthEdit/build/`. This box's checkout is `~/Documents/GitHub/SynthEdit`, so every test that shells out to `SynthEditCL` or the `cancellation` util gets `sh: ...: No such file or directory` and fails. **Failures by suite:** `TestVoiceAllocation` 42, `Basics` 14, `TestWavePlayer` 8, `TestAftertouch` 8, `TestBasicOversampling` 4, `TestAdvancedOversampling` 4, `TestUI` 2, `TestSoundfont` 2, `TestPatchAutomatorSorting` 2, `Filter` 2. **None is a real DSP failure** — they are all the same missing binary. **Why it matters:** the C-stage rows quote "92 tests across three suites all RC=0" as evidence, and that is a *Windows ninja* number. On mac the suite has been almost entirely red, and a run that builds here cannot tell a regression from the path bug. **Cheap fix:** derive the build folder rather than hardcoding it — the tests already compose paths from `getBuildFolderPath()`, so it is one function, and CMake can inject `CMAKE_BINARY_DIR` as a compile definition the way `EditorLib` already injects `SE_APP_BUILD_NUMBER`. **Scope** `SynthEdit/tests/projecttests.cpp`, `SynthEdit/tests/layouttests.cpp` and the tests' `CMakeLists.txt` — **GATED by default** (on neither STEP 5 list), so it needs Jeff or an interactive session. **Accept** `dsp_tests` on mac reports the same pass count as CI, from a checkout at any path. **Size** small. |
| S14 | WONTFIX | any | **NOT A DEFECT — closed 2026-08-18 by Jeff, the same day it was measured and before anything was built on it.** **A bare DSP module is not a rack citizen and was never meant to be one.** Rack modules are **Containers designed in advance and shipped as prefabs**, added to the rack at runtime: the container carries the panel, with patch-points and knobs/sliders on it wired internally to non-GUI modules like an oscillator, and the container is what the rack draws. A module that has its own GUI — a scope — can also sit on the rack directly, and **that already works today**. **So the three bare `1 KHz Tone` modules this row was measured from are not a product composition.** They are what audio testing looks like: drop plain modules in and switch to the **structure view** to check basic audio functionality. That is a developer workflow, not something an end user does on the main screen, which is exactly why nothing gives them rack geometry. **The measurement itself stands and is still worth reading** ([docs/s14-rect-measurement.md](docs/s14-rect-measurement.md), corrected): rack mode routes placement through the panel view and a plain `CUG`'s panel setter is `CDocOb`'s empty body. **One of its findings now reads as confirmation of the design rather than a complaint** — in full SynthEdit's own prefabs the modules carrying a `panelRect` are exactly the GUI-bearing ones, and the plain DSP modules carry none. **And the container half was already provided for, under a name the measurement did not look for:** `CContainer : CUG_with_patches : CUG` overrides the rect accessors itself (`CContainer.h:60-62`) and serialises its panel geometry as **`PanelWndPosition`** (`:214`) — an element already present in the measured chunk. **Superseded by:** the prefab rack modules are **E2a**/**E2**; rack-shaped styling for GUI-bearing modules is **E5**. **Do not re-open this as a geometry bug.** **Original row follows.** **MEASURED 2026-08-18 (macos, scheduled) — the question this row asked is ANSWERED, and the fix is entirely in GATED `SynthEditLib`, filed as S15. Do not re-measure.** Full working: [docs/s14-rect-measurement.md](docs/s14-rect-measurement.md). **The answer is that both halves of the row's either/or are the same event:** rack mode addresses the *panel* rect, a plain DSP module has no panel rect, so the assignment lands on an empty base-class no-op and is discarded — and `structRect`, the only rect such a module persists, is never written and keeps its constructed zero. **The `panelRect` hypothesis is REFUTED as a symptom:** full SynthEdit's own prefabs show `FloatToVolts` and `IO Mod` with **no `panelRect` at all** and a healthy non-zero `structRect`, so an absent `panelRect` is normal for a DSP module and TIDE differs only in the zero `structRect`. **Mechanism, four independently checkable facts:** `CDocOb::setViewObRect` has an empty body and `getViewObRect` returns `{}` (`DocOb.h:89-93`); `CUG` implements both **only** for `CF_STRUCTURE_VIEW` and serialises only `structRect` (`CUG.cpp:2557,2566`, `CUG.h:32`); `panelRect` belongs to `CControl`, not `CUG` (`Control.h:23`); and rack mode places through the top-level **panel** view (`MfcDocPresenter.cpp:811`, `:1421`, and the comment at `SynthEditSem/TideApp.cpp:497-500`). Composed: every TIDE placement is `CUG::setViewObRect(CF_PANEL_VIEW, …)` -> `CDocOb`'s no-op. The `master_container` is zero for the same reason — it is a `CUG` placed the same way. **Not persistence:** S11 landed and the chunk round-trips byte-identically; state right, geometry wrong, exactly as this row predicted. **TideApp's only involvement is `Document()->rackMode = true`, which is correct and is not the bug.** A17's 2026-08-18 ruling covers a **build break** in a GATED path; this is a functional defect, so it does not reach it. **Blocked on S15.** Measured on branch `tide/mac/S14-zero-structrect`. **Original row follows.** **Modules placed from the browser get a zero `structRect`, so they persist correctly but never render — found 2026-08-18 (macos, interactive) as a by-product of S11's verification.** With S11 landed, the document round-trips byte-for-byte and the modules really are in the saved chunk — `<module type="1 KHz Tone" handle="..."><structRect l="0" r="0" t="0" b="0" /></module>`, three of them — yet the rack draws empty. **So this is NOT a persistence bug and S11 does not fix it: the state is right and the geometry is wrong.** Worth stating plainly because the symptom ('I place a module and nothing appears') is identical to the bug S11 just fixed, and a future run will otherwise re-open S11. **Unknown and worth measuring first:** whether the zero rect comes from the placement path not assigning one, or from rack mode expecting a *panel* rect rather than `structRect` — `MfcDocPresenter::getRackLayout` and `ViewBase::snapToGrid` are the rack-mode geometry, and the exported document also carries `PanelLocationCenter`. **Cheap first measurement:** place a module in full SynthEdit, export, and compare the two documents' rects — that separates 'TIDE-specific' from 'rack-mode-specific' in one step. **Scope** likely `SE16/SynthEditSem/` or the shared view code. **Accept** a module placed in the rack is visible, and still visible after reload. **Size** unknown until the measurement above. |
| S15 | WONTFIX | any | **WITHDRAWN 2026-08-18 — the premise is void, ruled by Jeff the same day it was filed.** Both options below are wrong, and so was the request for a decision between them. **(b) give `CUG` a panel rect** would add geometry to the object that is specifically *not* supposed to appear on the rack, and duplicate what `CContainer` already carries as `PanelWndPosition` (`CContainer.h:214`). **(a) route rack placement to `structRect`** would push rack geometry into the schematic view's rect and fight that same existing mechanism. **No shared-format change is needed, so the GATED framing that made this NEEDS-JEFF also goes away** — that framing was the whole reason this row claimed a ruling. See **S14** for what is actually true. **The process lesson, recorded because it cost a row and a request for Jeff's attention:** the measurement behind this was sound and the *architecture* behind it was assumed — three bare DSP modules in a test patch were read as the product's intended composition, and a two-option design fork in shared code was built on that reading. One question first, *what is a rack module supposed to be?*, would have replaced the whole row. **Original row follows.** **Rack-mode placement discards module geometry — the GATED fix behind S14, measured 2026-08-18 (macos).** In rack mode every module placement calls `CUG::setViewObRect(CF_PANEL_VIEW, …)`, which falls through to `CDocOb::setViewObRect` — **an empty body** (`SynthEditLib/DocOb.h:93`). The rect is discarded, nothing is marked modified, and `structRect` stays `0,0,0,0`, so the rack draws nothing. Evidence and the reproduction: [docs/s14-rect-measurement.md](docs/s14-rect-measurement.md). **Why this needs Jeff and not a run:** every site is in **`SynthEditLib`**, which is GATED, and S14 is not a carve-out stage — A17's ruling permits repairing a GATED **build break**, which this is not. It is also a genuine design fork in shared code that full SynthEdit also runs, so guessing is the wrong move. **Option (a) — route rack placement to the structure rect.** In `MfcDocPresenter::SetModuleRect`/`GetModuleRect` (`MfcDocPresenter.cpp:811,~825`) use `CF_STRUCTURE_VIEW` when `getRackLayout().enabled`. Smallest change, persists for free because `CUG` already serialises `structRect`, and the rack's coordinates then share a space with the structure view — which may be exactly right (the rack *is* a layout of modules) or may collide with a user's structure-view arrangement of the same patch. **Option (b) — give `CUG` a real panel rect.** Add the member plus `s("panelRect", …)` to `CUG` alongside `structRect` (`CUG.h:32`, `CUG.cpp:2557,2566`), so a plain module gains panel geometry the way `CControl` already has it (`Control.h:23`). Cleaner conceptually and keeps the two views independent, but it changes the on-disk schema for **every** SynthEdit module, not just TIDE's, and needs a compatibility answer for existing projects. **Recommendation if a tiebreak helps:** (a) is reversible in one line and testable this week; (b) is the better model but is a shared-format change. **Scope** `SynthEditLib/MfcDocPresenter.cpp` for (a), or `SynthEditLib/CUG.{h,cpp}` for (b). **Accept** after the change, a re-exported chunk carries non-zero, distinct rects per module (headless, checkable) **and** the modules are visible in the rack and still visible after reload (needs a GUI session). **Size** an hour for (a) including the headless re-export check; unknown for (b) until the compatibility question is answered. **Unblocks S14.** |
| E5 | DONE | any | **DONE — [#244](https://github.com/JeffMcClintock/TideSynth/pull/244) + [SynthEditLib#30](https://github.com/JeffMcClintock/SynthEditLib/pull/30) merged; flipped 2026-08-21 (macos) per STEP 4, PR states verified rather than assumed.** **THE GRID IS RULED AND BUILT 2026-08-21 (macos, Jeff directing), branch `tide/mac/E5-rack-grid`, [#244](https://github.com/JeffMcClintock/TideSynth/pull/244) + [SynthEditLib#30](https://github.com/JeffMcClintock/SynthEditLib/pull/30) (GATED half).** Jeff's ruling took a **fourth direction** none of this row's three options named: `hpWidth` **stays 15** as the visual HP, a new **`snapWidth = 3`** becomes the placement pitch, the row becomes **384**, and TIDE's standard module width is **48**. **The 3 is `gcd(12, 15)`** — SynthEdit prefers 12s, every VCV module is a multiple of 15, and 3 is the coarsest snap on which both land exactly. Measured before recommending: snap-12 is exact for only **5 of 13** common VCV widths (3–9 DIP gaps, up to 3 mm); snap-3 is exact for **13 of 13**. **`hpWidth` was doing two jobs** — `renderRack` derives rail hole pitch AND radius from it — so setting it to 3 would have given rails a hole every 3 DIPs at 0.39 DIP radius. The split is the fix. **And the rail allowance never existed:** Jeff — *"useable interior is the entire rack module surface (we don't draw mounting hardware)"* — confirmed by the render order, `renderRack` fills the background and modules draw over it. So `build-prefabs.py`'s `380-2*15 = 350` was subtracting nothing real, which is why TIDE's own prefab measured as overlapping the rails. **Shipped:** `snapWidth`/384 in `Presenter.h` + `snapToGrid` (GATED PR), `kRackHeightDips` 384 and `FACE_H` 384 (ALLOWED), MidiCv regenerated, [tests/e5_rack_footprint_probe.py](tests/e5_rack_footprint_probe.py) rewritten to the ruled model (**12 selftest cases, 0 failed**, including a 380 VCV panel and a 384 TIDE panel both passing and a 400 failing), and [docs/panel-design-language.md](docs/panel-design-language.md)'s units table. **MEASURED ON A RUNNING RACK, not just built:** the probe against a live `session.xml` now reports `TIDE MIDI-CV  w=96 h=384  6.400 HP  on-grid  fits row` — **both clauses this row could never satisfy, satisfied** — with no overlaps, and the rails visibly passing BEHIND the panel ([docs/images/e5-rack-ruled-grid-macos.png](docs/images/e5-rack-ruled-grid-macos.png)). The single remaining violation is **`MIDI In` 8x14**, which is TIDE's seeded root plumbing (V3's polyphony workaround, seeded from `TideApp.cpp` — S28 owns the line reference) and not a rack module — filed as **S28** rather than excluded by name, because teaching the probe a name would make it lie. **This closes that doc's own open item 2** (*"the rack unit is 3.2 HP"*) as deliberate rather than open: 48 is 4x12 and sits exactly on the 3-snap. **Previous text follows.** **ANSWERED BY JEFF 2026-08-20, and it is smaller than this row assumed:** *“current GUI modules can draw on the rack already. They might look out-of-place because they don't default to a standard rack-module size. But that's no blocker on the project.”* **So the premise was wrong in a useful direction** — nothing is missing from rendering; what is missing is a **default size**. NEEDS-JEFF → TODO, and explicitly **not a blocker**. **Re-scoped:** give a GUI-bearing module a sensible default rack footprint so it lands on the grid instead of at whatever size its own panel happened to be. The grid is already written down in `build-prefabs.py`'s header — `hpWidth 15`, `rowHeight 380`, a slot being a multiple of 15 wide and at most 350 tall — so this is a default, not a new convention. **Accept** a stock GUI module (a scope is the example Jeff gave) dropped on the rack occupies a whole number of HP and does not overlap its neighbours. **Size** small. **Previous text follows.** **Rack-shaped GUIs for modules that already have their own UI, so they fit the rack stylistically.** Stated as design intent by Jeff 2026-08-18 while closing S14. A GUI-bearing module — a scope is the example — can already be placed on the rack and works today; what it does not do is *look* like a rack module, because its panel was drawn for the ordinary SynthEdit panel view rather than for a rack slot. **Distinct from E2a/E2**, which are the prefab rack modules composed out of plain modules (container + patch-points + knobs wired to an oscillator and the like). This row is the other half: restyling the modules that bring their own UI. **NEEDS-JEFF because the visual language is his to set, not a run's** — a scheduled run must not invent TIDE's rack styling unilaterally, and PLAN constraint 8's no-user-skins ruling means whatever is chosen ships as *the* look. **Scope** the GUI-bearing modules TIDE offers on the rack, restyled to rack proportions; likely `SE16/TideModules/` and the modules' own panels. **Accept** a scope placed on the rack is visually of a piece with a prefab rack module beside it. **Size** unknown — a design task before it is a code task. |
---

## After the carve-out

| ID | Status | Plat | Item |
|---|---|---|---|
| M1 | BLOCKED | mac | AU + AUv3 targets building on macOS. |
| M2 | BLOCKED | mac | **Rescoped 2026-08-15, following S10's retire ruling — same status, different shape.** There is no existing iOS project to fix; S10 confirmed all four of `SE_IOS_APP.xcodeproj`'s targets were already broken and deleted it. Jeff's own direction for S10 — *"lean on a generic AUv3 iOS backend for gmpi_ui as much as possible"* — sets M2's shape too: author a **new, generic AUv3 wrapper** alongside `GMPI_Wrappers/wrapper/`'s existing `VST3`/`AU2`/`CLAP`/`Standalone` siblings (checked — no `AUv3` one exists there yet), usable by any GMPI plugin, not a TIDE-specific rebuild of the retired scaffolding. Still `BLOCKED` — nothing here unblocks it, this only corrects what "done" now means. Original row follows. iOS AUv3 build; validate under real sandbox rules. This is the constraint that validates the whole design. |
| M3 | BLOCKED | mac | `auval` clean. |
| X1 | BLOCKED | linux | VST3 + CLAP on Linux, GCC 13+. See the Linux toolchain memory for WSL specifics. |
| X2 | BLOCKED | linux | Zero-warning build at `-Wall -Wextra`. |
| E9 | TODO | any | **THE CLAP HALF IS NOW MEASURED, and this row's "AU and CLAP unmeasured" clause was stale within hours of being reachable. 2026-08-22 (macos), branch `tide/mac/E9-clap-host-verify`.** The row says AU and CLAP are *"unmeasured, deliberately not claimed, because TIDE builds neither (`SynthEditSem/CMakeLists.txt:59` is `GMPI VST3 STANDALONE`)"*. **R4a changed that** — the list is `GMPI VST3 CLAP STANDALONE`, and it is no longer at that line. **Measured with a real CLAP host handshake** ([tests/e9_clap_rate_probe.c](tests/e9_clap_rate_probe.c), 21 checks, all passing): `dlopen` → `clap_entry` → factory → descriptor → `create_plugin` → `init`, then **activate(48000) → deactivate → activate(44100) → deactivate → activate(48000)**, which is precisely the bracket a DAW uses for a rate change and a stricter exercise of it than REAPER offered. Every transition succeeds. **The mechanism is confirmed to be the same one this row already established for VST3:** `Processor_CLAP::activate()` calls `plugin.start_processor(...)` with the new rate, and `processor_holder.cpp:55` releases the old processor, `:69` creates a fresh one, `:82` calls `open()`, and `:215` re-seeds the blob from retained bytes. So CLAP absorbs a rate change by instance replacement exactly as VST3 does. **AU remains genuinely unmeasured** and belongs to **R3a**, which is `BLOCKED(M1)` — TIDE builds no AU. **A far more serious finding came out of the same handshake and is filed as R9:** the descriptor reports `id="SE SynthEdit"`, and the VST3 GUID is a djb2 hash of that string — so TIDE's shipped identity is SynthEdit's generic one. That is a release-track decision, not an E9 one. **What is left of THIS row:** arguably nothing. Its Accept — *"changing the host's sample rate on a loaded project re-tunes correctly"* — was measured in REAPER for VST3 (eight `rack built for N Hz` lines following the device, playback at −6.2 dBFS / −13.4 RMS, matching 48 kHz), the one real defect underneath it shipped, and CLAP is now measured too. **Left TODO rather than closed on purpose:** closing it is a judgement about whether an unmeasured AU path matters, and R3a already owns AU. **Previous text follows.** **SPEC ANSWERED 2026-08-19 (macos, interactive, Jeff directing). TIDE prepares with NOTHING before a document exists — not an early `prepareToPlay`, not a stand-up document — and `open()` installs the silence writer instead.** The re-spec asked what TIDE should do given that copying `SeGmpiProcessor.cpp:151` would null-deref. The answer is that the early-prepare idea has no content once the crash is removed: at `open()` the processor **structurally cannot have a document** (`processor_holder.cpp:82` calls `open()`, and the blob is a queued `PinSet` seeded at `:215` and drained in the first `process()`), so a document-guarded prepare would no-op 100% of the time; and latching the rate there buys nothing, because `onSetPins` already reads the live `host->getSampleRate()` at push time. **But underneath it sat a real defect, measured:** `subProcess` was installed only at the tail of `onSetPins`, and for TIDE's pin layout a no-chunk instance gets NO pin event at all — `PinStreamingStart` is emitted only for audio INPUTS (TIDE declares none), output pins are skipped, MIDI has no default, and the empty blob `continue`s (`processor_holder.cpp:226`) — so **the plugin never wrote its output buffers**. **A/B measured in REAPER** on a project whose saved chunk is empty: without the `open()` install the log shows `host MIDI arrived BEFORE the rack was prepared` and NO silence line, i.e. `subProcess` never ran; with it, `TIDE: unprepared - writing silence to the host's output buffers`. No wrapper clears output buffers (`Processor_VST3.cpp:906-941`, its silenceFlags block is `#if 0`; AU2 and CLAP likewise), so what a host hears was whatever it left there. **REAPER is NOT exposed** — its render came out identical either way, so this is a contract fix, not an audible bug on this host; VST3 does not guarantee zeroed buffers, so other hosts are unknown and untested. **Shipped** in `SynthEditSem/SynthEdit.cpp` (TIDE's own): an `open()` override that calls `setSleep(false)`/`setSubProcess`, which also makes the existing "silence, exactly as the pre-S12 stub behaved" comment true rather than aspirational. Verified no regression: `tests/hosts/v1-rack.rpp` still renders −6.3 dBFS / −17.0 rms / 2 cables. **Original item, kept for the record:** **RE-SPECCED 2026-08-19 (macos, scheduled run) — the one code change this row had left CANNOT be done as written: it would null-dereference on the audio thread. Measured, not read — [tests/e9_buildgraph_null_probe.cpp](tests/e9_buildgraph_null_probe.cpp), with two positive controls.** The 2026-08-18 research recommended overriding `open()` "like `SeGmpiProcessor.cpp:151` does", calling it a precedent to copy exactly. **It is not a precedent, because of one asymmetry nobody checked: `SeGmpiProcessor` always has a document at `open()` and TIDE never does.** An exported SynthEdit plugin bakes `dsp.se.xml` into its bundle; TIDE's whole design is that the document arrives later as the blob, and `ls ~/Library/Audio/Plug-Ins/VST3/TIDE_VST3.vst3/Contents/Resources` confirms no such file. So preparing from `open()` reaches `BuildDspGraph` with an empty document, where `SeAudioMaster.cpp:409` yields `nullptr` and **`:410` dereferences it** — the existing `if (!pElem) return;` guard sits at `:413`, one line PAST the dereference it looks like it protects. It correctly handles a `<Document>` with no `<DSP>`; it cannot handle a missing document element. **NEEDS-SPEC: what should TIDE prepare with before a document exists?** A guard that no-ops until the blob arrives restores today's behaviour exactly and buys nothing; a minimal stand-up document is a design decision, and `SeAudioMaster.cpp:421-422` asserts the first `<DSP>` child is a `Module`/`Container`, so "empty" cannot mean "no modules" for free. **Also BLOCKED in practice on **E10** — the GATED null guard — which is filed but not fixed, because `SynthEditLib` is GATED and this is not a build break.** Until E10 lands, an `open()` override is a crash waiting for a host that creates a processor before the controller has state to push. **Nothing about the rate conclusion changed: a rate change is still absorbed by the wrappers replacing the plugin, and E9 is still NOT a live detune defect.** New working in [docs/e9-sample-rate.md](docs/e9-sample-rate.md), section "Why the open() precedent does not transfer". **Previous text follows.** **RESEARCHED 2026-08-18 (macos, interactive, Jeff directing) — [#149](https://github.com/JeffMcClintock/TideSynth/pull/149) and SynthEdit [#50](https://github.com/JeffMcClintock/SynthEdit/pull/50) (the comment fix). The row stays TODO because its remaining item — the `open()` latch below — is NOT done; what these PRs land is the research and the corrections. The row's DIAGNOSIS IS WRONG, and the correction is the useful part. Full write-up with every call chain: [docs/e9-sample-rate.md](docs/e9-sample-rate.md).** Jeff asked how SynthEdit's AU and VST3 targets handle a rate change, on the premise that it requires rebuilding the DSP graph. **The premise is right and the rebuild already exists** — `SynthRuntime::prepareToPlay` rebuilds when `generator->SampleRate() != sampleRate` (`SynthEditLib/SynthRuntime.cpp:51`), and every format wrapper drives it. **What is wrong is "the rack would keep the stale rate and everything detuned".** A rate change is absorbed by *destroying the `IProcessor` and constructing a new one*: `gmpi_processor::start_processor` (`GMPI/Hosting/processor_holder.cpp:48`) releases the old plugin at `:55`, creates a fresh instance at `:69`, calls `open()` at `:82`, and **deliberately re-seeds the blob parameter from its retained bytes** (`:215`), so TIDE's chunk arrives again, `onSetPins` fires again, and the rack is built at the NEW rate. Triggers: VST3 `setActive(true)` (`Processor_VST3.cpp:369`, and the spec MANDATES the `setActive(false)` → `setupProcessing(newRate)` → `setActive(true)` bracket, `ivstaudioprocessor.h:307`); AU `Initialize()` (`AU2_Wrapper.cpp:483`, with `StreamFormatWritable` returning false while initialised at `:978` so the host MUST `Uninitialize` first); CLAP `activate()` (`Processor_CLAP.cpp:366`); standalone `onAudioFormatChanged` (`StandaloneHost.cpp:405`). **`gmpi::api::IProcessor` has three methods — `open`, `setBuffer`, `process` (`GMPI/Core/GmpiApiAudio.h:50`) — so there is nowhere to put a rate-change callback; the rate is PULLED via `getSampleRate()`.** **Measured in REAPER on `tests/hosts/v3-midi-pitch.rpp`**, driving Preferences → Audio → Device → Request sample rate 48000 → 44100 → 48000 by hand on the loaded project: eight `TIDE: rack built for N Hz` lines, the rate following the device each time, and playback after the change metering **−6.2 dBFS peak / −13.4 RMS** — the same level the fixture gives at 48 kHz. **Not one line carried the `(rate CHANGED)` suffix, and it never can:** `preparedSampleRate` is a member of the object `start_processor` destroys, so the guard cannot outlive the rebuild that handles the change. The repeated identical `44100` lines prove it — a surviving instance with an unchanged rate prints nothing. **What is LEFT of this row, and it is smaller and different:** (1) **a fresh instance with no chunk stored never prepares at all** — `processor_holder.cpp:225` `continue`s on an empty blob and TIDE's only `prepareToPlay` call site is that blob arriving; the fix is to override `open()` exactly as SynthEdit's own glue does (`se_gmpi/source/SeGmpiProcessor.cpp:151`) and let the blob be a pure document swap; (2) `DoAsyncRestart()` alone CANNOT absorb a rate change — the `resetting` branch rebuilds from the member `sampleRate` (`SynthRuntime.cpp:388`) which only `prepareToPlay` writes (`:33`), and those members are non-atomic, written outside `generatorLock`, read on the audio thread; (3) `prepareToPlay` never joins `dspBuilderThread`, so its precondition is the wrappers' own — no concurrent `process()`. **Two comments in `SynthEditSem/SynthEdit.cpp` were wrong and are fixed with this research:** the stale-rate inference above, and *"the AsyncRestart path is unreachable in the plugin runtime"* — `resetting` is reached via `ug_vst_out.h:65` → `SeAudioMaster::onFadeOutComplete()` → `OnFadeOutComplete()` (`iseshelldsp.h:124`), and `DoAsyncRestart` is reached from `dsp_patch_parameter.cpp:773` for any host control with `requiresAsyncRestart()`, a set that **includes `HC_PATCH_CABLES`** (`dsp_patch_parameter_base.h:187`), i.e. every rack re-cabling. Nothing in TIDE calls it *yet*, which is a property of TIDE's wiring, not of the runtime. **Unmeasured, deliberately not claimed:** AU and CLAP, because TIDE builds neither (`SynthEditSem/CMakeLists.txt:59` is `GMPI VST3 STANDALONE`); and whether parsing a document on the audio thread is safe. **Original item, kept for the record:** **TIDE latches its sample rate at DOCUMENT-PUSH time and has no rate-change path — found 2026-08-18 (macos, interactive) when Jeff asked whether TIDE has the correct sample rate or a hard-coded default.** **It is NOT hard-coded**, which was the first thing checked: `SynthEditSem/SynthEdit.cpp` passes `static_cast<int32_t>(host->getSampleRate())` to `rack.prepareToPlay`, and a temporary log confirmed real values arriving from REAPER — **48000 Hz** and **44100 Hz**, block 512. **The problem is WHERE that call lives:** `prepareToPlay` is reached from exactly one place, the chunk arriving in `onSetPins`, so the rack's rate is fixed when the DOCUMENT is pushed. Nothing in the class handles a rate change. **A host that changes rate without re-pushing the document would leave the rack at the stale rate and everything detuned by the ratio — silently, with no warning and nothing in the saved project to show it.** 48000 → 44100 is 1.47 semitones, so it would sound wrong rather than broken, which is the worst kind. **What is measured and what is not, kept apart deliberately:** an offline render prints both rates, but those are two separate plugin **INSTANCES** (the device/editor one and the render one), each latching once at construction — the guard's "rate CHANGED" branch did **not** fire, which is how that is known rather than assumed. So a rate change *within* an instance has never been observed to be handled, and on this evidence it would not be. **A rate-change test needs the GUI:** REAPER ignores a project's `SAMPLERATE` line when rendering, and hand-writing `RENDER_SRATE` into a `.rpp` makes REAPER stop on a dialog (confirmed by Jeff while it blocked a render), so forcing a different render rate has to go through REAPER's own dialog. **Scope** `SynthEditSem/SynthEdit.cpp` — TIDE's own, ALLOWED. A `preparedSampleRate` guard is already in place and logs `TIDE: rack built for N Hz` on a change; what is missing is acting on it. **Accept** changing the host's sample rate on a loaded project re-tunes correctly rather than staying detuned. **Size** small if a rate check per block is acceptable; larger if it wants a proper host callback. **NOT the cause of E8:** that offset is exactly 3 semitones (ratio 1.18921), a musical interval and not any standard rate ratio — 48000/44100 is 1.47 semitones — and the Oscillator's 5 V → 440.0 Hz holds at both 44100 (via the `.rpp`) and 48000 (via E1's `prefab_oscillator`). |
| E10 | TODO | any | **MEASURED 2026-08-19 (macos, interactive): this is NOT latent — it is a LIVE HOST CRASH, and this row's own Accept clause would not fix it.** Two corrections, both measured rather than argued. **(1) It is reachable today.** A REAPER project whose saved TIDE chunk is the well-formed but foreign `<Patch/>` **segfaulted the render**: `EXC_BAD_ACCESS / KERN_INVALID_ADDRESS at 0x28`, faulting stack `TiXmlNode::FirstChildElement` ← `SeAudioMaster::BuildDspGraph` ← `SynthRuntime::prepareToPlay` ← `SynthEdit::onSetPins` ← `gmpi::Processor::process` ← `Processor_VST3::process`, on a REAPER worker thread. The trigger is **"no `<Document>` root", not "empty"** — `<Patch/>` parses with NO error, so `RootElement()` is non-null and `SynthRuntime.cpp:76`'s bundle fallback is skipped entirely; the missing `dsp.se.xml` is not even required. Nothing validates the bytes: `gmpi::base64Decode` (`GMPI/Core/base64.h:59`) silently skips anything outside its alphabet and returns what falls out, so a truncated or hand-edited project file is enough. **(2) Moving one line is not a fix.** `SynthRuntime.cpp:157` calls `OpenGenerator()` unconditionally after `BuildDspGraph`, and `SeAudioMaster::Open()` dereferences `main_container` (`:2330-2331`), which is null on ANY early return from `BuildDspGraph` — including the `:413` return that exists today. So a document with a `<Document>` root but no `<DSP>` already takes that "graceful" return and then segfaults one frame up, and this row's Accept clause ('the probe's empty-document case reaches the `return`') **can pass while the process still crashes**. Scope is at least three sites: `:409` before the deref, `:420`'s unchecked first child, `:428`'s `<PatchManager>` — plus `SynthRuntime` must learn the build failed instead of opening a graph that was never built. **TIDE's half is shipped** ([#151](https://github.com/JeffMcClintock/TideSynth/pull/152)): `SynthEditSem/SynthEdit.cpp` now refuses a chunk that is not `<Document>`/`<DSP>`/`<Module>`-shaped and logs why, so the four crash shapes measured above are closed from TIDE's side without touching GATED code. **That is a shape check, not a schema check, and the boundary is measured:** a `<Document><DSP><Module/></DSP></Document>` skeleton passes TIDE's guard and **still crashes** (a `<Module>` with no `<PatchManager>` reaches `ug_container.cpp:1469`), which is exactly what this row still has to fix in the engine. Reproduce all of it with [scripts/measure-chunk-robustness.py](scripts/measure-chunk-robustness.py) — six cases, one of them the real chunk from `tests/hosts/v3-midi-pitch.rpp` as a positive control, and the skeleton reported as a KNOWN LIMIT so nobody mistakes it for a pass. **Original item, kept for the record:** **GATED — `SynthEditLib/SeAudioMaster.cpp:409-413`: a null guard that sits one line after the dereference it protects.** `document_xml = hDoc.FirstChildElement("Document").Element();` (`:409`) returns `nullptr` for any document without a `<Document>` root — an empty or unparseable one — and `:410` immediately does `document_xml->FirstChildElement("DSP")`. The guard `if (!pElem) return;` is at `:413`. Its own comment ("should always have a valid root but handle gracefully if it does", itself garbled) shows the intent was to be defensive here; the code is one line short of it. **Measured 2026-08-19 (macos) with [tests/e9_buildgraph_null_probe.cpp](tests/e9_buildgraph_null_probe.cpp)**, which reproduces `SeAudioMaster.cpp:403-413` against the real `SynthEditLib/tinyxml` sources and includes two positive controls, so the NULL is not the probe failing to work. **Not live today** — every current caller has a document, which is why this has never fired — so this is a latent crash, not a break: `SynthRuntime::prepareToPlay` is the only route in, and TIDE's only call site is the blob arriving with real bytes. It becomes live the moment anything prepares before a document exists, which is exactly what **E9** wants to do. **Scope** `SynthEditLib/SeAudioMaster.cpp` — GATED, so filed rather than fixed, per STEP 5 (this is not a build break, so the build-break exception does not apply). **Accept** the probe's empty-document case reaches the `return` instead of dereferencing; one line moved, no behaviour change for any document that parses. **Size** one line. **Consumer note for whoever takes it:** `SeAudioMaster.cpp` ships in SynthEdit as well as TIDE, so it needs a SynthEdit build too, not just TIDE. |
| E11 | WONTFIX | any | **MEASURED 2026-08-21 (macos), [#256](https://github.com/JeffMcClintock/TideSynth/pull/256) — THE HAZARD IS UNREACHABLE — not a bug, and the reason is worth more than the row.** This row asked for the drain ordering to be established before any patch. The ordering turns out not to matter, because **the pointer is never taken.** Static proof, and it is exhaustive rather than a sample: in `GMPI/Hosting/processor_holder.h` the processor-side parameter's `value_` is written in exactly **two** places, both at construction — `:87` (a double default) and `:91` (`std::vector<uint8_t>{}`, an EMPTY blob) — and **`setBlob` at `:135` is a STUB**: its one assignment is commented out at `:137` and it unconditionally `return true`s. `grep` for `value_` across the header and the .cpp finds only those two writes and two reads. **So for a blob parameter the vector is empty for the object's entire lifetime**, and both sites that could publish a raw pointer decline to: the seed at `processor_holder.cpp:224` hits `if (!v || v->empty()) continue;`, and `sendParameterToProcessor` at `:337` takes the inline branch because `v.size() > 8` is false when the size is 0. **`e.oversizeData_` is never assigned on this path**, so nothing can dangle. **The genuine defect is the stub, and it is E6's, not a new row** — see the note added to E6 the same day, per A31's grep-before-filing habit, which is what stopped this becoming a duplicate. **Reopen this row only if `setBlob` is ever implemented**, because the moment the vector holds bytes both pointer sites become live and this row's analysis applies exactly as written. **Previous text follows.** **UNVERIFIED, filed so it is not lost: `processor_holder.cpp:231` publishes a raw pointer into a `std::vector` that another thread may reallocate.** The blob seed does `e.oversizeData_ = v->data()` with no byte copy, and `EventQue` stores the `Event` by value (`processor_holder.h:22-29`). The comment justifying that at `:333-336` argues "queue drain and the next parameter update happen on the same thread, so the vector cannot move underneath the event" — which holds for the `ppc3` path it was written about, but **`start_processor` runs on the host's main thread** while the drain happens in the first `process()` on the audio thread. If a `setBlob` reallocates in between, the event points at freed memory and TIDE parses whatever is there — which is also another route into **E10**'s crash. **Not measured**: found by an agent reading the code during E9's research (2026-08-19) and explicitly flagged "ordering unverified"; nobody chased the drain ordering, so this may be perfectly safe. **Take** = establish the ordering first (does anything write parameter 1 between `start_processor` and the first `process()`? a DAW state restore plus an editor push is the suspicious pair), and only then decide whether the fix is a copy, a shared_ptr, or a comment saying why it cannot happen. **Scope** `GMPI/Hosting/` — Jeff's, not in the GATED list, but a hosting-layer change with every plugin downstream, so it wants a measurement before a patch. **Size** small to fix, unknown to prove. |
| E14 | DONE | any | **CORRECTION 2026-08-20 (linux), and it reverses this row's own explanation.** This row says the placement failure was *"the gesture, not the modules"* and treats the Oscillator control as proof of that. **Jeff then tried with a real mouse and could not insert anything either**, which the same control equally supports — I picked the reading that blamed my technique and did not consider the other. **The mechanism, from `ModuleDragAndDropManager.h`: insertion is ARM-THEN-CLICK, not a drag.** *"1. User selects a module in the browser -> BeginDrag(id). 2. Cross cursor is shown. 3a. User clicks in the editor -> presenter inserts module."* There is a double-click-to-insert-at-view-centre shortcut too (`InsertAtViewCenter`). Nobody was dragging wrong; dragging is simply not the gesture. **Verified by doing it:** clicking `Prefabs > Filter` then clicking the rack inserted **TIDE Filter** (properties: `CONTAINER / TIDE Filter`, X 4304 Y 3968 W 100 H 160). **Clause 2 is now HALF closed for this row's own modules:** clicking `TiDE > Panel` then the rack inserts `SE TiDE:Panel` and the properties pane shows its live pins — `Text`, `Text Color FF101010`, `Rack Units 1`, `Layout`, `Material Brushed Aluminium`, `Panel Color FF2E3238`. **So it DROPS INTO THE RACK and constructs. What is still NOT confirmed is the "and draws" half** — nothing visibly appeared at the click point, which smells like the E2a `PanelWndPosition` trap (a module that drops successfully and draws nothing). **Why it feels broken to a user, and this is the real defect: TIDE gives NO cursor feedback for the armed state.** `ModuleDragAndDropManager::SetCursorHandler` is **defined and never called anywhere in the public tree** — its own comment says MainWindow (WinUI) and SynthEditBridge (Mac) register it, and both live in the private desktop apps. So the cross cursor never appears in TIDE **on any platform**, and whatever cursor was last set persists — which is exactly the stuck left-right cursor Jeff saw. Filed as **S24**. **One more thing that confused this investigation:** `TIDE_STANDALONE` persists its rack to **`~/.config/TIDE Rack/session.xml`** and restores it, so a relaunch is NOT a clean rack — an earlier session's modules reappear. **Previous text follows.** **DONE — [#208](https://github.com/JeffMcClintock/TideSynth/pull/208) merged; flipped 2026-08-20 (linux) per STEP 4. NOTE this closes the row with Accept clause 2 (placement in the rack) still UNVERIFIED — see the row: the control showed it is the gesture, not the modules.** **SHIPPING PATH BUILT AND HALF-VERIFIED 2026-08-20 (linux, scheduled run), branch `tide/linux/E14-modules-in-product`, [#208](https://github.com/JeffMcClintock/TideSynth/pull/208). Taken on linux because the visual check IS possible here after all — see S21's correction; the earlier "not measurable on this box" was wrong.** **Accept clause 1 IS MET, visually:** TIDE's own browser now shows a **TiDE** category containing **Panel** and **knob**. **Clause 2 — "a prefab using them drops into the rack and draws" — is NOT verified, and the reason is mine, not the modules'.** I could not work out the placement gesture: dragging a browser entry into the rack only highlights it. **Controlled, so the next run need not re-derive it:** the **known-good `Oscillator` prefab fails identically** under the same gesture, and E2a placed that one with real mouse drags. So the gap is the interaction, not the two new classes. Double-click does not place either. **Whoever knows the gesture can close clause 2 in a minute.** **What did land:** all three `.cpp` compile into the plugin (`SynthEditSem/CMakeLists.txt`), TIDE links `tide_render`, and `TideModules/build-prefabs.py` allowlists both ids in `TIDE_STATIC_EXTRAS`. Clean clone, configure rc=0, **build rc=0 with zero error lines**, producing `TIDE.gmpi`, `TIDE_VST3.vst3` and `TIDE_STANDALONE`. **Proof at the symbol level rather than by id**, as this row demands: `__GLOBAL__sub_I_TiDEknob.cpp`, `__GLOBAL__sub_I_TiDEknobGui.cpp` and `__GLOBAL__sub_I_TiDEPanelGui.cpp` are **all present in BOTH `TIDE.gmpi` and `TIDE_VST3.so`**, with a negative control (`NotAFile.cpp` -> 0). Those symbols name the FILE, so the legacy rename table cannot fake them. **The row's linker warning did not bite:** no helper TU was dragged in — the two modules are self-contained apart from `tide_render`. **Its naming warning is STALE and should not be acted on:** it says `SE TiDE:knob` declares `name="TiDE:knob"`; on current `main` it declares **`name="knob"`** with `category="TiDE"`, the same shape as Panel (`TiDEknob.cpp:25`, `TiDEPanelGui.cpp:2680`). There is nothing left to settle there. **And the awkward-path half is gone:** this row was written when `SynthEditSem/` lived in the private `SE16` and these sources did not — **C7b moved it into this repo**, so both are now siblings and no new CMake variable was needed. **One judgement call worth review:** `TiDEPanelGui.cpp` marches a distance field and must match the tracer's float model, but TIDE sets `/fp:fast` on MSVC and a reassociating subset on Apple. Applying `tide_render_strict_fp` to the plugin TARGET would change the **DSP engine's** float model to fix a renderer, so the opt-out is applied **per source file** instead, via a new `tide_render_strict_fp_sources()` beside the existing function so the flag list stays in one place. **On Linux neither fast-math block is active, so that is currently a no-op here and was NOT verified on Windows or macOS** — whoever builds those first should confirm the panel still renders. **The two TIDE-specific modules in `modules/` are authored but are not in the product.** Committed 2026-08-19 as `55da97a8e`: **`TiDEknob`** (id `SE TiDE:knob` — a float passthrough DSP plus a vector-knob GUI ported from `SubControlsXp/VectorRingGui.cpp`) and **`TiDEPanel`** (id `SE TiDE:Panel` — a GUI-only faceplate with `Text` / `Text Color` pins). Jeff's direction the same day: build them into TIDE and use them in prefabs. **They do not reach TIDE by being built.** `modules/TiDEknob/CMakeLists.txt:5` calls `gmpi_plugin()`, and the artifact is a **loadable bundle** — `modules/build/TiDEknob/Debug/TiDEknob.gmpi` exists today — which is exactly what TIDE cannot consume: PLAN constraint 7 is a fixed, statically-registered module set and S1a deleted the scan that would find one. **Both builds are wanted, for different jobs, and neither substitutes for the other.** The loadable build is the **authoring** path — SynthEditCL is a scanning editor, so `build-prefabs.py` can only place a module SynthEditCL can see. The **shipping** path is E2a's two-halves rule: the `.cpp` joins `SE16/SynthEditSem/CMakeLists.txt`'s source list. **The second half is already free, which is the good news:** both modules register with `Register<>::withXml` carrying their own pin descriptions (`TiDEknob.cpp:23`, `TiDEPanelGui.cpp:36`), and `TiDEknobGui.cpp:161` attaches to the same id with `withId`. So this is the **`SE Label` case, not the `SE Rectangle XP` case — no `_tide_xmls` entry is needed.** **E2c is the precedent, and it landed 2026-08-19 hours before this row was filed** — it linked `SE SV Filter4` and `SE Oscillator4` by exactly this route, and it confirms both halves of the reading above: Oscillator HD needed no XML entry either, because it too registers with `Register<>::withXml` inline. **Take its two warnings with it.** (1) The `_tide_xmls` list is hardcoded **twice** — staged by `SynthEditSem/CMakeLists.txt` and read by the loop at `TideApp.cpp:488` — so the day a TIDE module *does* need an entry, missing either side fails silently. (2) **Expect a module added to TIDE's source list to drag a helper TU with it:** Converters needed `my_type_convert.cpp`, Oscillator HD needed `shared/real_fft.cpp`, and in both cases the linker was what said so. **The awkward half is the path.** `SynthEditSem/CMakeLists.txt` lives in the private `SE16` and names every borrowed module as `${SYNTHEDITLIB_DIR}/modules/...`; these sources live in the **public** TideSynth repo, for which SE16's CMake has no variable at all (grepped: SE16's only `TideSynth` mention in any `CMakeLists.txt`/`.cmake` is a comment in `SynthEditWayland/CMakeLists.txt:21`). Adding one points the way **C7** wants to go — TIDE built from public code. **Also needed:** `TIDE_STATIC_EXTRAS` (`SE16/TideModules/build-prefabs.py:476`) exists precisely for ids SynthEditCL only ever sees as scanned while TIDE links them statically; both new ids belong there unless the built bundles are staged where SynthEditCL scans. **One naming inconsistency to settle now rather than after prefabs ship it:** `SE TiDE:Panel` declares `name="Panel"` while `SE TiDE:knob` declares `name="TiDE:knob"`, so the browser shows the prefix for one and not the other. **E2** owns the conventions; this is their first real case. **Scope** `SE16/SynthEditSem/CMakeLists.txt`, a CMake variable for the TideSynth checkout, `SE16/TideModules/build-prefabs.py`. **Accept** both ids appear in TIDE's own browser, and a prefab using them drops into the rack and draws. **The authoritative check is placing it in TIDE and looking** — `strings`/`nm` on an id is a documented false positive, and a saved document's `class=` attribute reflects SynthEditCL's registration rather than TIDE's ([docs/e2a-prefabs.md](docs/e2a-prefabs.md) §9.2). **Size** one session. |
| E15 | DONE | any | **DONE — [#241](https://github.com/JeffMcClintock/TideSynth/pull/241) merged; flipped 2026-08-21 (macos) per STEP 4, PR states verified rather than assumed.** **SHIPPED 2026-08-20 (macos, scheduled run), branch `tide/mac/E15-panel-swap`, [#229](https://github.com/JeffMcClintock/TideSynth/pull/229) — every Accept clause measured.** MIDI-CV rebuilt on `SE TiDE:Panel` (`assert_all_modules_linked` clean, its own note naming the id); **drawn in TIDE with legible captions** — [docs/images/e15-midicv-tidepanel.png](docs/images/e15-midicv-tidepanel.png), black heading + PITCH/GATE/VEL/TRIG all on the light plate; `RectangleGui.cpp` and `SubControlsXp/SubControlsXp.xml` deleted from `SynthEditSem/CMakeLists.txt` AND from `TideApp.cpp`'s merge loop (the hardcoded-twice pair, both sides); build green, and the symbol A/B on both shipped binaries: `__GLOBAL__sub_I_RectangleGui.cpp` **0**, `__GLOBAL__sub_I_TiDEPanelGui.cpp` **1** in `TIDE.gmpi` and `TIDE_VST3`. **Two findings the swap forced:** (1) the panel sizes ITSELF at RackUnits × 48 DIPs and SubView unions children, so the authored 105-DIP slot made captions overhang the plate onto the dark rack — fixed with `Rack Units 2` (the one pin the swap sets: geometry, not appearance) and the faceplate grid re-authored to 96; the 3.2-HP-per-U mismatch with RackLayout's 15-DIP HP is E17's own open note. (2) the FIRST mac build of `TiDEPanel.gmpi` (the SynthEditCL authoring bundle) failed to link — `helpers/Timer.cpp`'s mac path is CFRunLoopTimer and the loadable-bundle link line gets no frameworks; one APPLE-guarded CoreFoundation line in `modules/TiDEPanel/CMakeLists.txt`. **Flagged, not acted on:** this row's own note says Jeff ruled the module "ships exactly two pins" — but `main`'s registration XML declares SIX (Text, Text Color, Rack Units, Layout, Material, Panel Color); the reduction never landed, and whether Layout/Material/Panel Color should go compile-time is Jeff's. **Taken with BLOCKED(E14) in the status cell read by its own definition** — E14 is DONE, so the block had expired; noted here because the letter of the never-start-BLOCKED rule and the definition point different ways for an expired blocker. **Previous text follows.**  **THE PANEL ITSELF IS BUILT as of 2026-08-19 (windows, interactive, Jeff directing); what remains is only the swap.** On `tide/win/competitive-review`: `1a49731a9` (face and caption) and `98c4072f9` (the raised edge). `SE TiDE:Panel` now draws a gradient faceplate with material grain, plus a caption rotated a quarter-turn anti-clockwise, bold, 5× body height, starting near the bottom and clipped at the top, with a blurred directional edge highlight. **Two of this row's stated obstacles are VOID, and both were ruled rather than solved.** (1) *“TiDEPanel is not a drop-in yet — the colour pins land first”* is wrong: Jeff ruled the appearance is TIDE's and not the patch author's, so colours, corner radius, grain and glow are **compile-time constants** and the module ships exactly two pins, `Text` and `Text Color` — PLAN constraint 8, one look and no user skins. **So the swap DELETES `build-prefabs.py`'s three `--set-pin` colour lines rather than porting them**; less work than this row assumed, not more. (2) The two `.htm` files are **deleted** — both were stale template copies, the knob's being `Slider`'s help. **What is actually left is the swap and nothing else:** the `build-prefabs.py` edit, and removing `RectangleGui.cpp` plus the `SubControlsXp/SubControlsXp.xml` entry from `SynthEditSem/CMakeLists.txt`. **Still `BLOCKED(E14)` on purpose** — none of it can be verified until the module is linked into TIDE, and every check so far has been in SynthEdit's **scanning** editor, which [docs/e2a-prefabs.md](docs/e2a-prefabs.md) §9.2 says explicitly is not the same thing. **Two findings worth more than the code.** **The offscreen bitmap must be built at DEVICE resolution:** `bounds` is in DIPs and `createCpuRenderTarget` defaults to 96 dpi, so sizing from `bounds` generates one pixel per DIP and lets `drawBitmap` stretch it — soft on HiDPI and softer at panel zoom. The scale is `getRasterizationScale() * transformScale()`, the helper `SynthEditLib/modules/Controls/Scope4Gui.cpp:353` already established. **Not demonstrated by measurement, deliberately not claimed:** the headless screenshot path runs at scale 1.0, where the old and new code are identical by construction. **Use the blur that exists:** the first edge highlight was a hand-rolled 1px alpha erosion, which is a hard rim by construction — it aliases on every diagonal and reads as an outline. `ginSingleChannel` (`gmpi_ui/helpers/GinBlur.h`, the filter `cachedBlur` wraps) is the right tool and `Controls/BumpGui.cpp` is the worked example. It needs a falloff exponent with it: **the blur alone looked worse than the erosion**, because on a thin stroke the blurred occluder never covers the middle and a few percent of lightening lands on every pixel, greying the caption. **Original row follows, and its “not a drop-in yet” paragraph is the part now known to be void.** **`SE TiDE:Panel` replaces `SE Rectangle XP` as the rack prefabs' faceplate background.** Jeff's direction 2026-08-19, alongside **E14**. Today exactly one prefab carries a faceplate — TIDE MIDI-CV, `SE16/TideModules/build-prefabs.py:283` — and it is a rectangle plus five `SE Label`s. **What the swap buys is not cosmetic.** `SE Rectangle XP` is the *only* reason `modules/SubControlsXp/RectangleGui.cpp` sits on `SynthEditSem`'s source list (`:122`), and the *only* reason `SubControlsXp/SubControlsXp.xml` is in `_tide_xmls` (`:280`) — a 28-plugin XML staged into TIDE's bundle so TIDE can describe the one module it borrows from it. Both go, and TIDE owns its faceplate instead of borrowing SynthEdit's. **What it costs, stated so it is not discovered mid-swap: `TiDEPanel` is not a drop-in yet.** `SE Rectangle XP` has seven pins — Corner Radius, four per-corner bools, Top Color, Bottom Color (`SubControlsXp.xml:150-156`) — and the MIDI-CV faceplate sets three of them. `TiDEPanelGui` has `Text` and `Text Color`, paints a hardcoded `Colors::WhiteSmoke` background and hardcoded `Colors::Red` text, and never reads `pinTextColor` at all (`modules/TiDEPanel/TiDEPanelGui.cpp:22,26`). The colour pins land first, and **the faceplate must stay light**: `SE Label` has no colour pin, its skinless fallback is opaque black, and TIDE's bundle ships no skin — E2a's trap, `build-prefabs.py:252`. **It does not retire `SE Label`.** TiDEPanel has a single `Text` pin; MIDI-CV needs a heading plus four jack captions, so `Controls/LabelGui.cpp` stays on the source list. **Small and untracked:** `modules/TiDEPanel/TiDEPanel.htm` and `modules/TiDEknob/TiDEknob.htm` are uncommitted, and the Panel one is a verbatim copy of Rectangle's help — titled “Rectangle”, documenting seven pins TiDEPanel does not have. Fix or drop them with this row. **Scope** `modules/TiDEPanel/TiDEPanelGui.cpp`, `SE16/TideModules/build-prefabs.py`, `SE16/SynthEditSem/CMakeLists.txt`. **Accept** MIDI-CV rebuilt on `SE TiDE:Panel` with `assert_all_modules_linked` clean, the prefab drawn in TIDE with legible captions, **and** `RectangleGui.cpp` plus the `SubControlsXp/SubControlsXp.xml` entry both deleted from `SynthEditSem/CMakeLists.txt` with the build still green. **Not a licence to invent rack styling** — the visual language is **E5**, NEEDS-JEFF. This row swaps the mechanism and keeps the look. **Size** one session after E14. |
| S17 | DONE | any | **DONE — [#200](https://github.com/JeffMcClintock/TideSynth/pull/200) merged; flipped 2026-08-20 (linux) per STEP 4. The GATED SE16 half remains open as S22.** **TIDE-SIDE HALF DONE 2026-08-20 (linux, scheduled run), branch `tide/linux/S17-dependency-provenance`, [#200](https://github.com/JeffMcClintock/TideSynth/pull/200) — fourth item this session, continued at Jeff's direction. The SE16 half is GATED and is filed as S22, per STEP 5's "do the TIDE-side part, then file the gated part".** **FIRST, THE THING THIS ROW ASSUMED AND NOBODY HAD CHECKED: TideSynth's own build does NOT have the defect.** Measured — configure with `-DGMPI_UI_FOLDER_OVERRIDE=~/SE/gmpi_ui` and the compile flags really are **`-I/home/jef/SE/gmpi_ui`**, with **no `_deps/gmpi_ui-src` created at all**. So the shadowing is SE16's, not C7d's root, and a run must not "fix" TideSynth expecting the E12 symptom to move. **Accept clause 2** (*"editing the local clone changes the binary"*) therefore already held here; **clause 1** (*"a build log line naming the resolved path per dependency"*) did not, and is what landed. **All 8 dependencies now name their resolved path**, in a summary block after every `add_subdirectory`: `vst3_sdk`, `SynthEditLib`, `GMPI`, `gmpi_ui`, `CLAP`, `clap_helpers`, `harfbuzz`, `GMPI_Wrappers`. `cmake -B build 2>&1 | grep "dep "` is the whole diagnostic. **Two of the eight were previously never announced at all** — `vst3_sdk` and `harfbuzz` resolve out of **`~/.cache/CPM/`**, a third source neither the old messages nor this row mentioned; a stale entry there is invisible in exactly the way S17 describes. **`tide_check_not_shadowed` makes the SE16 failure loud rather than silent** and is verified by a POSITIVE CONTROL, not by inspection: creating `build/_deps/gmpi_ui-src` while the override is set makes configure exit **1** naming both paths. Without that control the check would be untested code that always passes. **Also verified:** reconfigure keeps the count at 8 rather than doubling (the accumulator is `CACHE INTERNAL` + `FORCE`-cleared, because `PARENT_SCOPE` from `SynthEditSem/` never reaches the root — that bug was live and printed 7 of 8), full Release build **rc=0, zero error lines**, artifacts produced, and S21's probe still passes on the same tree. **One latent bug found in passing, not fixed:** `VST3_SDK_FOLDER_OVERRIDE` is read at `CMakeLists.txt:99` but **never declared as a cache variable** — line 36 declares `VST3_SDK_USE_MIDI_UMP` instead — so unlike the other four it cannot be set from the CMake GUI or a cache, only with `-D`. Filed as part of S22's row text rather than fixed here, being outside this row's scope. **The SynthEdit build compiles `build/_deps/gmpi_ui-src`, NOT the local `gmpi_ui` clone — and they are at DIFFERENT COMMITS. Found 2026-08-19 (macos) while tracing E12.** CMake prints `Using local GMPI-UI folder`, and the include path really is `-I…/SynthEdit/build/_deps/gmpi_ui-src`, so the printed reassurance is about something else. Measured: the fetched copy was at `9094d79` while `~/Documents/GitHub/gmpi_ui` was at `83f3de2`, and a `fprintf` added to the local clone never reached the binary. **This is the same trap as `GMPI_WRAPPER_FOLDER_OVERRIDE`** — everything looks local and one dependency silently is not — but worse, because the two revisions differ, so the class layout you read is not the class layout you compile. That is exactly what cost time in E12: `GmpiPluginEditor2.h` in the local clone has `editorHost2` where the compiled `GmpiPluginEditor.h` has `dialogHost`. **Take** = work out whether the gmpi_ui override is meant to exist (the other three do), make CMake say which folder each dependency actually resolved to, and fail loudly when a `_deps` copy shadows a local override. **Accept** a build log line naming the resolved path per dependency, and editing the local clone changes the binary. **Size** small for the diagnostics, unknown for the override itself. |
| S22 | TODO | any | **The GATED half of S17: SE16's build is where the dependency shadowing was actually observed, and it is still unreported there.** Filed 2026-08-20 (linux) after doing the TIDE-side half, per STEP 5's rule for a fix that spans the gate. **The defect, as S17 measured it on macOS 2026-08-19:** SE16's configure prints `Using local GMPI-UI folder` while the include path is `.../SynthEdit/build/_deps/gmpi_ui-src`, and the two were at **different commits** (`9094d79` fetched vs `83f3de2` local) — so the header being read was not the header being compiled, which is what cost E12 its time. **Confirmed NOT to affect TideSynth:** its C7d root honours the override all the way to the compile flags, measured 2026-08-20 (see S17). This row is SE16's alone. **Scope** `SE16/CMakeLists.txt` — on **neither** STEP 5 list, so GATED by default, and it is **not** a build break, so A17's exception does not reach it. **Take** port `tide_report_dependency` / `tide_check_not_shadowed` from [TideSynth's root CMakeLists.txt](CMakeLists.txt); they are ~30 lines, have no TIDE-specific content, and the shadow check is already proven to fire by a positive control. **Accept** SE16's configure names the resolved path for every dependency, and fails when an override is shadowed by a `_deps` copy. **Carry this with it:** `VST3_SDK_FOLDER_OVERRIDE` is read but never declared as a cache variable in TideSynth's root (`CMakeLists.txt:99` reads it; `:36` declares `VST3_SDK_USE_MIDI_UMP`), so it is settable only via `-D`. Worth checking whether SE16 has the same gap, since that file is what TideSynth's was copied from. |
| S18 | TODO | any | **The public repo contains source that cannot compile without a header from the PRIVATE repo — found 2026-08-19 (linux) while inventorying C7.** Three public files include `soundpipe.h`, which resolves only in `SE16/SDKs/Soundpipe/`: `SynthEditLib/modules/SoundPipe/ReverbChowning.cpp:4`, `ReverbSp.cpp:4` and `ReverbZita.cpp:4` (the third as `"./h/soundpipe.h"`). `modules/SoundPipe/CMakeLists.txt` pulls four `.c` files and two headers out of `${external_sdk_folder}/Soundpipe`, and `external_sdk_folder` is set by `SE16`. **Not a C7 blocker, and that is worth being precise about:** `SynthEditLib/modules` is added by `SE16`'s root (`:416`) and never by `SynthEditLib`'s own, and TIDE links none of it — so a clean-clone TIDE build never reaches it. It is a defect in the *public repo as a public repo*: a stranger who adds `modules/` gets an unbuildable target with no hint why. **Scope:** `SynthEditLib/modules/SoundPipe/`. **Accept:** either Soundpipe is fetched/vendored publicly and the subdirectory configures with no `SE16` on disk, or the target is guarded on `external_sdk_folder` being set and says so. **Size:** small, but it is a licensing/vendoring question first — Soundpipe is third-party, so check its terms before copying it anywhere. **Why no previous stage saw it:** `scripts/dangling_private_includes.py` skips `SDKs` by design (`SKIP_DIRS`, `:57-63`) because vendored SDK headers are not carve-out edges — a correct rule for what that script measures, and the reason its count can read **7 dangling / 0 problems here** while three public files still cannot compile. Cross-checked this run: the script and a hand scan agree exactly on the 7, and disagree only on this. |
| S20 | WONTFIX | any | **ANSWERED BY JEFF 2026-08-20: *“I run CI manually most days. not a biggie.”* So this is a recorded FACT, not an open question** — the manual chain is deliberate and adequate. **Kept open rather than WONTFIX for one reason only:** a run cannot tell from the outside whether CI has exercised its work, so anything claiming “CI is green” must first check when the chain last ran. That is a reading habit, not a change. **Do not file the `on: push` workflow** — the chain signs, notarizes and FTP-uploads to synthedit.com, and Jeff has said the cadence is fine. **Previous text follows.** **SynthEdit's whole CI chain is `workflow_dispatch` only, so nothing is tested on push — found 2026-08-20 (macos) while confirming S19's fix.** `SE16 Kickstart Build` has **`on: workflow_dispatch:` and nothing else**; `SynthEdit 1.6 cmake_win` triggers off *it* via `workflow_run`, and `cmake_mac` triggers off `cmake_win`. So a push to `master` runs **no** build and **no** tests — the last dispatch was 2026-08-19T09:34 and `master` has moved eight times since with zero runs. **This is half the reason S19's five failures survived a week:** `continue-on-error` hid them, and the chain fired rarely enough that few people saw a log at all. **The obvious fix is wrong and that is why this is a row rather than a patch:** the chain is a RELEASE pipeline — it signs, notarizes, staples and **FTP-uploads to synthedit.com** — so `on: push` would publish on every commit. **Scope** a ruling on whether a separate build-and-test-only workflow should run on push, with the release chain left manual. **Accept** a push to `master` produces a pass/fail signal without publishing anything. **Size** the ruling is Jeff's; the workflow is small but is `.github/workflows/**`, which the fleet token structurally cannot push — so this needs his hand either way. **NOT dispatched by the run that found it**, deliberately: triggering that chain is a release, not a test. |
| S21 | DONE | any | **DONE — [#196](https://github.com/JeffMcClintock/TideSynth/pull/196) merged; flipped 2026-08-20 (linux) per STEP 4.** **VERIFIED AT RUNTIME 2026-08-20 (linux), and this row's "NOT covered" note below is WRONG on its facts — read the correction, not it.** It says `TIDE_STANDALONE` cannot build here for want of *"wayland/xkbcommon/libdecor/pipewire"*. **Only `libpipewire-0.3` was missing**; `wayland-client` 1.22.0, `xkbcommon` 1.6.0, `libdecor-0` 0.2.2, `wayland-scanner` and `wayland-protocols` were all installed. That list was the **CI runner's**, read out of the pruned tree built to reproduce [#190](https://github.com/JeffMcClintock/TideSynth/issues/190) and mis-attributed to this box. Jeff caught it. **TIDE_STANDALONE now builds and runs on Linux — the first time it has** — so the reader itself confirms this fix. **Deterministic A/B, 3 runs per layout:** with the fix, `TIDE: 6 rack prefab(s) seeded from the bundle` plus **all five** pin XMLs enriched (`ControlsXp` 2/18, `SubControlsXp` 1/27, `MidiPlayer2` 2/7, `Converters` 26/70, `VaFilters` 2/7); pre-fix, **six** `missing from bundle resources` lines including *"no Prefabs folder in bundle resources - the rack module browser will be empty"*, and zero enriched. **Visually** — [before](docs/images/s21-prefabs-linux-before.png) / [after](docs/images/s21-prefabs-linux-after.png): with the fix the browser's **Prefabs** group lists Envelope, Filter, Midi, MidiCv, Oscillator, Output; without it **the Prefabs group is absent from the tree entirely** and `Sub-Controls` collapses from 27 classes to one (`Label`), which is the XML half of this defect that was previously only inferred. **One thing deliberately NOT attached to this row:** an intermittent `TIDE_STANDALONE` segfault, seen twice but **not** reproduced in 28 controlled runs across both layouts — filed as **S23**, because the run that first reported it (this one) wrongly called it a consequence of this defect. **Previous text follows, including the incorrect note.** **FIXED AND VERIFIED 2026-08-20 (linux, scheduled run), branch `tide/linux/S21-bundle-resources`, [#196](https://github.com/JeffMcClintock/TideSynth/pull/196) — continued past the one-item rule at Jeff's direction, given mid-run.** **Accept met exactly as written**, on a fresh clone + full Release build (configure rc=0, build rc=0, zero error lines): `TIDE_VST3.vst3/Contents/Resources/Prefabs/` holds the **6** `.synthedit` files with the **5** XMLs beside it, and `TIDE.gmpi`'s `Resources/` likewise — **6 and 5**. **Negative control:** the old wrong destination `build/Resources/` **no longer exists**. **The fix gives Linux its own branch and leaves the Windows expression byte-for-byte unchanged**, which is the whole shape of it: S21 was found on Linux, Windows is not verifiable from here, so `$<TARGET_FILE_DIR:tgt>/../Resources` still serves Windows alone rather than being retuned for two platforms at once. Linux needs **two** answers, not one, because `getBundleContentsFolder` (`BundleInfo.cpp:204`) walks for a `Contents` element and falls back to `parent_path()`: the VST3 wants `<name>.vst3/Contents/Resources`, the bare `.gmpi` wants `Resources` beside the binary. **Re-runnable evidence:** [tests/s21_bundle_resources_probe.py](tests/s21_bundle_resources_probe.py) replicates the reader's own path algorithm and answers from its point of view — **pre-fix tree FAILS both formats, post-fix tree PASSES both**, so it is a real A/B and not a listing anyone eyeballed. **What is NOT covered, stated rather than implied:** the plugin was never *loaded*, because `TIDE_STANDALONE` cannot build on this box (missing wayland/xkbcommon/libdecor/pipewire) and no DAW is installed — the claim is that the reader's computed path now contains the files, not that a running TIDE listed them. **One adjacent gap left alone:** with `SE_LOCAL_BUILD=TRUE` (OFF by default, `CMakeLists.txt:37`) `copy_plugin` copies the bundle to `~/.vst3` **before** these POST_BUILD steps run, so the *installed* copy would still miss its resources — the same ordering trap the APPLE block already works around with `_tide_installed`. Not reproduced here, because verifying it means writing a plugin into Jeff's `~/.vst3`. Worth its own row if anyone builds Linux with that flag. **On Linux, TIDE's bundle resources are staged OUTSIDE the bundle, so a Linux TIDE has an empty Prefabs group and un-enriched module pins. Found 2026-08-20 (linux) by the first TIDE build ever done on Linux; not a build failure — the build is rc=0 — so it is filed rather than fixed, per STEP 3.** **Writer and reader disagree, and both sides were measured in the same tree.** `SynthEditSem/CMakeLists.txt:339` sets `_tide_resources` to `$<TARGET_FILE_DIR:${_tide_target}>/../Resources` for the non-APPLE case, with the comment *"the binary sits in Contents/<arch>/, so Resources is its sibling"*. **That premise does not hold on Linux.** `gmpi_plugin.cmake:849-861` links a bare `.so` into the target dir and only *afterwards* copies it into the bundle, so `TARGET_FILE_DIR` is **not** inside the bundle. Measured: the XMLs and `Prefabs/` land in `build/Resources/`, while `TIDE_VST3.vst3/Contents/` contains **only** `x86_64-linux/`. The reader is explicit about where it looks — `BundleInfo.cpp:296-299`, *"Linux VST3 bundles are plain folders … with resources at `<name>.vst3/Contents/Resources/`"*. **Both Linux formats are wrong, by different amounts:** `TIDE_VST3` wants `TIDE_VST3.vst3/Contents/Resources/` (`getBundleContentsFolder` finds the `Contents` component), and `TIDE.gmpi`, a bare `.so` with no `Contents` in its path, falls to `path.parent_path()` and wants `Resources/` **beside the binary** — while staging puts both one level above. **Symptom** is already named in the code: `TideApp::seedPrefabsFromBundle` prints *"TIDE: no Prefabs folder in bundle resources - the rack module browser will be empty"* and returns, and the five staged pin-description XMLs (`ControlsXp`, `SubControlsXp`, `MidiPlayer2`, `Converters`, `VaFilters`) never load — the same silent failure the CMake comment records for V3, where a class is linked but has no pins. **Accept:** on Linux, `TIDE_VST3.vst3/Contents/Resources/Prefabs/` contains the 6 `.synthedit` files and the 5 XMLs sit beside it; `TIDE.gmpi`'s `Resources/` likewise. **Not verified on Windows, which shares the same `else()` branch** — whoever takes this must check `TARGET_FILE_DIR` there before changing the expression, because a fix that is right on Linux could break the platform that branch was written for. `SynthEditSem/` is ALLOWED, so the TIDE-side fix needs no ruling. |
| S23 | TODO | any | **THE TARGETED REPRO RAN, IN AN ISOLATED COMPOSITOR, AND THE CRASH DOES NOT REPRODUCE ON CURRENT `main`. Measured 2026-08-21 (linux), branch `tide/linux/S32-headless-compositor`.** Now possible because S32 is solved — headless `weston` means a run can launch the standalone without risking the developer's desktop. **Three conditions, seven launches, all clean:** **(1) control, resources present** — 60 s, no fault. **(2) resources ABSENT** — the layout both original crashes were in — 60 s plus **four 10 s runs each ended with SIGTERM, every one exit 0** (and a clean SIGTERM exit really is 0 here, so a 139 would have been unambiguous). **(3) resources absent PLUS the quarantined `session.previous.xml`** — the mismatch case nobody had tried, a patch saved when the modules existed replayed when they do not — 45 s, no fault. **Zero `TIDE_STANDALONE` segfaults in `journalctl` throughout.** **The startup state was confirmed identical to the crash runs**, not merely similar: all four `… missing from bundle resources - those controls will have no pins`, `no Prefabs folder in bundle resources`, and `MidiCv.synthedit did not insert a container - the rack will have no MIDI jacks`. **So the reproduction attempt is exhausted on this platform and should not be repeated.** Most likely it was fixed in passing by the churn since 2026-08-20 (E14, E15, S26 and Jeff's TiDEPanel/tide_render work all landed after it); the alternative is an ingredient nobody has named. **Either way, more stress runs are not the way to find out.** **What survives, and it is the useful part: the signature decode stands, and it points at two REAL latent defects regardless of whether they caused this crash — now filed as S34.** **Suggested disposition:** close S23 as not-reproducible once S34 is on the board, rather than leaving an open crash row that no longer describes anything observable. That is Jeff's call, not a run's. **Previous text follows.** **THE FAULT SIGNATURE IS NOW DECODED, AND IT NAMES A CLASS OF SITE THIS PROJECT HAS ALREADY BEEN BITTEN BY ONCE. Measured 2026-08-21 (linux, scheduled run, third item at Jeff's direction), branch `tide/linux/S23-addr2line` ([#263](https://github.com/JeffMcClintock/TideSynth/pull/263)).** **`-8` MEANS `back()` ON AN EMPTY CONTAINER OF 8-BYTE ELEMENTS — measured, not reasoned.** A three-case control program, each case in its own child, read back from the kernel log: `dynamic_cast` on a zeroed vptr faults at **-16**; `back()` on an empty `vector<int>` faults at **-4**; a plain `null->member` faults at **+12** (and `error 4`, not 5). Our crash is **-8 with error 5**, so it is `back()` / `rbegin()` / `end()[-1]` on an **empty vector of 8-byte elements — a vector of raw pointers.** **This kills two earlier guesses, including one made an hour before:** it is not `dynamic_cast` on a dangling handle (that is -16), and it is not a null `->field` (that is positive). **THE PRECEDENT IS ALREADY IN THE TREE, from this fleet's own BACKLOG U2d.** `SynthEditLib/modules/ControlsXp/ClassicControlGuiBase.cpp:22` carries the comment *“`widgets.back()` on an empty vector is UB (crashed TIDE at address -16 = empty `back()` with 16-byte elements; TideSynth BACKLOG U2d)… a host where those don’t fire must not bring the whole process down”* — **same class, same arithmetic, and the same trigger shape: a resource-dependent collection that stayed empty.** U2d's crash was *“check font/skin resources”*; S23's two crashes were both in the layout with the **bundle's `Resources` missing**. **TWO UNGUARDED CANDIDATE SITES, both `std::vector<UPlug*>` (a vector of raw pointers — **S34**) so both fault at exactly -8:** the adder site (**S34** owns the citation) — `auto p = plugs.back();` at the top of `ug_adder2::NewConnection()`, no `empty()` guard; and the feedback-delays site (**S34**) — `auto dummyPin = u->plugs.back();`, no guard. A third sibling, its guarded sibling (**S34**), **is** guarded (`while(!…empty())`), which is what the other two should look like. **The adder is the interesting one:** it implements TIDE's automatic summing when patch cables fan into one input, and `NewConnection` runs while the DSP graph is built from a restored patch — i.e. at startup, which is when both crashes happened. **NOT PROVEN, and do not let the tidiness of the story stand in for a repro:** nothing here shows the fault was at either line. The chain ‘resources missing → pin list empty → `back()`’ is a hypothesis that fits every measured fact, not an observation. **THE OFFSET ROUTE IS PERMANENTLY CLOSED, so do not spend on it — this run closed it properly.** `/var/log/apport.log.1` names the crashing binary as `<scratchpad>/22760dc3-…/s17/TideSynth/build-sa/SynthEditSem/TIDE_STANDALONE` — a clone in the **S21 run's own scratchpad**, wiped by the reboots since, and **a different build tree from the one the last run resolved `0x3b4627` against**. Confirmed by disassembly: `0x3b4627` lands **mid-instruction** in today's binary, so the `CContainer::OnEditContain` hit was never a valid resolution. **No core exists either** — apport logged `core limit 0` and *“executable does not belong to a package, ignoring”* for both crashes. And an exact rebuild is not reproducible: at `21f9c80` there was **no `cpm-package-lock.cmake`**, so every dependency resolved to whatever its branch head was that day. **Take** = a targeted repro, not archaeology: launch `TIDE_STANDALONE` with the bundle's `Resources` absent (the layout both crashes were in) under `gdb`, and see whether it stops in either candidate. That is minutes and it either names the frame or clears both sites. **Read S32 first** — launching the standalone on this box has taken the developer's desktop down, and a nested compositor is the safe way to do it. **Both files are GATED (`SynthEditLib`) and this is not a build break, so A17's exception does not reach it — filed, not fixed.** **One incidental gap, noted rather than filed to avoid a second id for one job:** `ClassicControlGuiBase.cpp:9-11` (`onSetTitle`) `dynamic_cast`s and then calls `header->SetText` with **no null check**, while its sibling `initialize()` at `:31` checks. Positive-offset fault, so not S23, but the same file and the same class of omission. **Size** small. **Previous text follows.** **THE SESSION-FILE HYPOTHESIS IS DEAD, AND THE KERNEL HAD THE ANSWER ALL ALONG. Measured 2026-08-21 (linux, scheduled run), branch `tide/linux/S23-session-replay` ([#261](https://github.com/JeffMcClintock/TideSynth/pull/261)).** **The experiment ran and came back clean:** `session.previous.xml` (17,866 bytes, md5 `010ed62b3baa7bbdda95ee935108d6e7`) copied to `session.xml` under `XDG_CONFIG_HOME=<scratch>` with no `session.loading` beside it, launched under `gdb` — **restored fine, ran ~3 minutes, no signal.** Three independent confirmations that the restore SUCCEEDED rather than merely not crashing: the scratch dir has **no `session.previous.xml`** afterwards (so `parametersAreReadable` passed and `host_.restoreState()` returned true — no quarantine), the app **rewrote** the scratch `session.xml` (17,866 -> 17,346 bytes, new mtime; the debounced save only runs in a healthy session), and a screenshot shows the restored rack drawing. **Jeff’s own config was byte-identical before and after** (both md5s unchanged) — `XDG_CONFIG_HOME` isolation works as advertised. **BUT THE REAL FINDING IS IN `journalctl`, WHICH NOBODY HAD READ.** The kernel logged both original crashes: `Aug 20 17:32:58 TIDE_STANDALONE[23881]: segfault at fffffffffffffff8 ip 000062312e4f7627 ... in TIDE_STANDALONE[3b4627,62312e1cb000+404000]` and `Aug 20 17:33:27 TIDE_STANDALONE[23924]: segfault at fffffffffffffff8 ip 000055bae7342627 ... in TIDE_STANDALONE[3b4627,55bae7016000+404000]`. **Same module offset `3b4627` both times, under different ASLR bases — so this is ONE deterministic code site, not a random memory smear.** The fault address `0xfffffffffffffff8` is **-8**, and `error 5` decodes as a **user-mode READ of a mapped page** — the signature of reading 8 bytes *before* a null-ish base (`back()` on empty, `*(--it)` at `begin()`, a length stored ahead of a null data pointer), not of a plain null `->field`. **29 seconds apart, and that interval is the proof the session file is innocent:** crash 1 at 17:32:58 leaves the breadcrumb, so the NEXT launch quarantines (`session.previous.xml` is stamped **17:33**, exactly between the two) and comes up **at defaults** — and that defaults run **crashed anyway at 17:33:27, at the same offset.** **So the second crash was not loading the quarantined file, and the replay above agrees from the other direction.** **Also settled: `session.previous.xml` IS a quarantine artifact** (its 17:33 stamp sits between the two crashes), not the `File > Revert to Plugin Defaults` false positive the row hedged about — `keepCurrentAside` is reachable from exactly one place, `StandaloneApp.cpp:353`, armed at `:180`. **Not reproduced since:** `journalctl` shows **zero** `TIDE_STANDALONE` segfaults after 2026-08-20 17:33 — across the previous run’s 28 controlled runs and ~1000 driven insertions, and this run’s 3-minute replay. **A LEAD, EXPLICITLY LABELLED UNRELIABLE — do not spend on it without doing the one step that makes it reliable.** `addr2line` on **today’s** binary at `0x3b4627` gives `CContainer::OnEditContain()` at `SynthEditLib/EditorLib/CContainer.cpp:2338`, and `:2330` there does use a `dynamic_cast` result with **no null check** (`placeHolderIoMod = dynamic_cast<CContainer*>(...HandleToObject(placeHolderHandle)); ... placeHolderIoMod->Plugs.size()`). **Tempting, and probably a coincidence:** it is a DIFFERENT binary from the one that faulted, and a null `->Plugs.size()` would fault at a small POSITIVE offset, **not at -8** — so the signature does not match. Recorded only so the next run need not re-derive it. **Take** = rebuild the tree as it stood on 2026-08-20 ~17:30 (same flags; `-g` does not change codegen at `-O3`) and resolve `0x3b4627` in THAT binary. That names the frame and closes this row. Everything else about the crash — deterministic site, -8 read, twice in 30 s — is already measured above. **Do NOT do more stress runs; they have never reproduced it and this box cannot afford them (see S32).** **Size** small, now that the target is one `addr2line` against one rebuild. **Previous text follows.** **TAKEN AND HANDED BACK 2026-08-21 (linux, scheduled run), branch `tide/linux/S23-standalone-segfault` ([#259](https://github.com/JeffMcClintock/TideSynth/pull/259)) — the blocker is cleared, one hypothesis explains the whole observed pattern, and the experiment that decides it is written but NOT RUN.** **`gdb` IS INSTALLED** (15.1, `Ubuntu 15.1-1ubuntu1~24.04.1`), so this row's *"nothing here names a frame"* is stale. **One hypothesis is dead:** `libpipewire-0.3-dev` is `1.0.5-1ubuntu3.3` and the linked runtime is `libpipewire-0.3.so.0.1005.0` — the same version — so S21's `apt-get download` workaround did NOT compile against mismatched headers. **It did not reproduce, with a rate:** 4 native runs at a 25s window (rc=0 each; a clean SIGTERM shutdown exits 0, so a 139 is unambiguous) plus one `gdb` run driven ~13 minutes over the command channel — roughly 1000 arm-then-click prefab insertions each followed by a full-window screenshot — **no signal**; the only stop in the log is the run's own `kill -9`. **THE FINDING WORTH MORE THAN THE REPRO: the app quarantines the input after the first crash, so a crash here is self-limiting to ONE occurrence per bad file.** `SessionState` writes a breadcrumb `session.loading` before reading the patch and removes it after (`GMPI_Wrappers/wrapper/Standalone/SessionState.cpp:395-419`); a launch that finds one concludes the previous run died inside the read, moves `session.xml` aside to `session.previous.xml`, and comes up at defaults (`:325-328`). Its own header says why: an assert inside the parse is *"the failure a return code cannot catch"*, and without this *"the app would die on every launch from now on"*. **That is exactly the shape this row recorded** — two crashes, both on a *first* run, then 28 controlled runs finding nothing. **The 28 runs were not evidence of rarity;** they were runs of an app that had already thrown the offending file away. **And the suspect file is still on disk:** `~/.config/TIDE Rack/session.previous.xml`, **17,866 bytes, dated 2026-08-20 17:33** — same box, same day as the two crashes. **Not proven:** `keepCurrentAside` (`:255`) rotates to that same filename for ordinary reasons, and the header names a false positive of its own (a second instance launched while the first is still in `restore()` quarantines a good file). **Take** = run the one experiment, which needs nothing from Jeff and cannot touch his config: `StandaloneSettings.cpp:49` honours **`XDG_CONFIG_HOME`**, so copy `session.previous.xml` to `<scratch>/TIDE Rack/session.xml`, ensure no `session.loading` is beside it, and launch under `XDG_CONFIG_HOME=<scratch> gdb -batch -ex run -ex bt --args ./TIDE_STANDALONE`. **A segfault names the frame and closes this row; a clean run eliminates the only hypothesis that explains the pattern.** Seconds, against hours of stress. **Do that BEFORE any further stress runs.** **Why this run stopped:** the desktop session on the linux box crashed twice during the work, and it is Jeff's machine. **Stated with its control, because the control is what matters:** the first came during the heavy insertion stress, the second came **while `TIDE_STANDALONE` was not running at all** — so **the correlation with TIDE is unsupported**, and that second event refutes it. The box is a **VirtualBox** VM whose log carries `libEGL warning: Ensure your X server supports DRI3` and repeated `Invalid sequence for VSYNC frame info`, and `gnome-shell` restarted three times in four minutes; neither event was a reboot. Recorded as an observation about the box, **not** a finding about the product — S21 made precisely that mistake with these same crashes and had to retract it. **Previous text follows.** **SUPERSEDED 2026-08-20: Jeff installed `libpipewire-0.3-dev` system-wide on the linux box (1.0.5-1ubuntu3.3), so the `apt-get download` + `dpkg-deb -x` scratch-prefix recipe below is NO LONGER NEEDED and should not be reconstructed.** Verified with the workaround deliberately removed — scratch prefix deleted, `PKG_CONFIG_PATH` unset, build tree wiped: configure **rc=0** with **zero** `STANDALONE skipped` lines, build **rc=0, zero errors**, `TIDE_STANDALONE` produced. **So visual verification on the linux box now costs nothing** — build the target, run it, and it prints a command-channel socket an MCP server can screenshot, click and drag. Rows that say otherwise are stale. **An intermittent `TIDE_STANDALONE` segfault on Linux, seen twice and NOT isolated. Filed 2026-08-20 (linux) so it is not lost, and deliberately NOT attached to S21.** **Observed:** exit 139 with a core dump, both times on the **first run after the bundle's `Resources` directory was moved**, with a 10s run window. **Did not reproduce:** 28 controlled runs — 8 per layout at a 6s window, 6 per layout at 12s, across both the pre-S21 and post-S21 resource layouts — **zero crashes**. So the correlation with the missing-resources layout is **unsupported**, and the run that first reported it had it backwards and says so. **Plausible but unverified:** it may depend on run duration rather than layout, since both crashes had a longer window than any of the 28 clean runs — try 20-30s first. **What is missing to make this actionable: a backtrace.** `gdb` is **not installed** on the linux box, so nothing here names a frame. **Take** = install `gdb`, reproduce with a longer window, get a stack. The app is easy to drive: build the `TIDE_STANDALONE` target, run it, and it prints a command-channel socket an MCP server can screenshot and click. **Note the standalone needs `libpipewire-0.3-dev`**, not installed system-wide on the linux box — this run worked around it with `apt-get download` + `dpkg-deb -x` into a scratch prefix with the `.pc` `prefix=` repointed, because `sudo` needs a password no unattended run has. **Size** unknown — a diagnosis, not a fix. |
| S24 | DONE | any | **DONE — [#231](https://github.com/JeffMcClintock/TideSynth/pull/231) merged; flipped 2026-08-21 (macos) per STEP 4, PR states verified rather than assumed.** **SUPERSEDED 2026-08-20: Jeff installed `libpipewire-0.3-dev` system-wide on the linux box (1.0.5-1ubuntu3.3), so the `apt-get download` + `dpkg-deb -x` scratch-prefix recipe below is NO LONGER NEEDED and should not be reconstructed.** Verified with the workaround deliberately removed — scratch prefix deleted, `PKG_CONFIG_PATH` unset, build tree wiped: configure **rc=0** with **zero** `STANDALONE skipped` lines, build **rc=0, zero errors**, `TIDE_STANDALONE` produced. **So visual verification on the linux box now costs nothing** — build the target, run it, and it prints a command-channel socket an MCP server can screenshot, click and drag. Rows that say otherwise are stale. **MAC HALF SHIPPED 2026-08-20 (macos, scheduled run), branch `tide/mac/S24-cross-cursor`, [#231](https://github.com/JeffMcClintock/TideSynth/pull/231).** TIDE already had the affordance on Windows — an inline Win32 `::SetCursor(IDC_CROSS)` in `SynthEditGui.cpp` (`dragInProgress` tracked at OM_DRAG_NEW_MODULE, re-asserted per pointer move) — so this row's registration-based port was superseded by extending TIDE's own existing mechanism: `TideCursorMac.mm` (`[NSCursor crosshairCursor] set`, the exact AppKit analogue), called at arm/disarm AND re-asserted per move (cursor rects reset like WM_SETCURSOR). **Verified:** builds rc=0, symbol linked in the standalone; the full arm-then-click path driven live over the command channel — browser click armed, rack click placed a List Entry with populated properties, no crash — with the cursor calls on that path. **The pointer bitmap itself is the one unverifiable-by-machine piece** (screenshots exclude the cursor): Jeff's mouse is the instrument, per S26's precedent — asked in the PR. **Linux deliberately not attempted:** gmpi_ui's mac/win frames expose no cursor API and the X11/Wayland TIDE path has none to shortcut to; the Wayland APP works because its own toplevel has setCursor. A linux TIDE needs either a gmpi_ui frame API (design, not a port) or its own shortcut — left for the linux box, noted here rather than guessed at. **Previous text follows.**  **CORRECTED 2026-08-20 (linux), and this row now has a WORKING REFERENCE on the same machine.** Jeff: *"SynthEdit Wayland works fine. Must use some workaround. Its built on this machine also."* He is right, and the "workaround" is simply the Linux platform layer TIDE does not have. **`SE16/SynthEditWayland/WaylandMainWindow.cpp:72` registers the handler** — `dnd->SetCursorHandler(...)` mapping to `WaylandToplevel::Cursor::crosshair` / `::normal`, commented *"the same feedback the JUCE and Windows apps give via their hooks"*. So this row's original claim that it is *"defined and never called"* is **wrong as stated**: it is never called **in the public tree**, and SE16's Wayland app calls it. **Everything else in the notify path is substantively IDENTICAL between the two**, checked side by side: `WaylandMainWindow.cpp:818-822` does `currentView_->DragNewModule(hintData)` and `:826-831` does `AddModule(id, currentView_->getCenter())` — exactly what TIDE does at `SynthEditGui.cpp:554` and `:570`. Its own comment confirms the split: *"The view tracks it; insertion happens on the canvas click, in shared code."* **So the fix is a port with a known-good original**, not a design: register a cursor handler in `TideApp` the way `WaylandMainWindow` does. `SynthEditSem/` is ALLOWED; SE16 is read-only here and was only read. **Still NOT explained, and it should not be guessed at:** Jeff saw **nothing** appear on insert, while injected clicks in TIDE insert successfully and draw a (wrongly-rendered) panel. Those are different observations and the difference is unaccounted for — see S25. **Previous text follows.** **TIDE never shows the module-insert cursor, so arming a module gives the user no feedback at all — and a stale cursor looks stuck. Found 2026-08-20 (linux) after Jeff reported he could not insert anything and the cursor jammed on a left-right icon.** **Cause:** `ModuleDragAndDropManager::SetCursorHandler` (`SynthEditLib/ModuleDragAndDropManager.h:27`) is **defined and never called** in the public tree. Its own comment says *"Platform layer (MainWindow on WinUI, SynthEditBridge on Mac) registers a callback that shows the cross cursor"* — both are private desktop-app classes, so **TIDE registers nothing on any platform**. **Consequence:** insertion is arm-then-click (`BeginDrag` -> click in view -> insert), and the only feedback that a module is armed is the browser row highlight. The view's cursor keeps whatever it last had — over a panel edge that is a resize cursor, which reads as a jam. The feature works; it is undiscoverable and looks broken. **Scope** `SynthEditSem/` (ALLOWED) — TideApp can register a handler at startup, the same way it registers other hooks; the manager is reached through `CSynthEditAppBase`. **Accept** arming a module from the browser changes the cursor over the rack, and clearing/inserting restores it. **Worth pairing with a second question for Jeff, not for a run:** whether arm-then-click is the interaction TIDE wants at all. Two people who know the product both tried to DRAG, which is what every comparable rack (VCV, Blocks) uses. That is E2/E17 territory, not a bug. **Size** small for the cursor; the interaction question is a ruling. | 
| S25 | TODO | any | **DOES NOT REPRODUCE ON MAC — measured 2026-08-20 (macos, scheduled run), branch `tide/mac/S25-fresh-insert-tofu` ([#233](https://github.com/JeffMcClintock/TideSynth/pull/233)), and the negative result narrows the suspect list.** Same protocol as the linux measurement (fresh isolated HOME, genuinely empty rack, Oscillator prefab armed from the browser and click-placed): the fresh insert renders as its two bare jacks, correct glyphs, no tofu, no grey panel — and after kill/relaunch **the session-restored copy renders IDENTICALLY** ([docs/images/s25-mac-inserted.png](docs/images/s25-mac-inserted.png) / [docs/images/s25-mac-restored.png](docs/images/s25-mac-restored.png)). So this row's Accept comparison HOLDS on mac, in a build carrying the E15 panel stack. **What that eliminates:** anything platform-independent in the freshly-inserted path (the E2a `PanelWndPosition` suspect behaves the same in both paths here — the 20×66 child-union size is the documented measure mechanic, insert and restore alike). **What survives: the linux font/resource-binding suspect** — tofu is missing GLYPHS, and this box's font stack differs; the divergence lives somewhere the linux fresh-insert path fails to bind fonts/resources that its restore path binds. **A linux box owns the next step**; mac has no way to compile or observe that path. Jeff's sees-nothing-at-all caution stands unresolved and is still not assumed to be this bug. **Previous text follows.**  **A freshly INSERTED prefab draws as a plain grey panel with missing-glyph text; the SAME prefab restored from a saved session draws correctly. Found 2026-08-20 (linux) on an empty rack.** **Measured, with the session file moved aside so the rack was genuinely empty** (`TIDE_STANDALONE` restores `~/.config/TIDE Rack/session.xml`, so a relaunch is NOT a clean rack — that contaminated three earlier observations): one click-place of the `Oscillator` prefab inserts (`CONTAINER / TIDE Oscillator`, X 4104 Y 3816 W 100 H 160) and draws a **plain grey rectangle with tofu glyphs** where the pin captions should be — the text is laid out correctly (`MIDI-CV`, `PITCH`, `GATE` are recognisable by their punctuation) but every glyph is a missing-glyph box, and there is **no path-traced faceplate**. **Unchanged after 12 s**, so it is not a slow cache. **The control that makes this a real defect:** the same prefab **restored from `session.xml` renders perfectly** — brushed aluminium, knobs, jacks — in the same binary, same session, same rack. So the renderer works and the **freshly-inserted path** is what differs. **Take** = diff what a restored container gets that a freshly-inserted one does not (font/resource binding is the obvious suspect given tofu, and E2a's `PanelWndPosition` is the other). **Accept** a prefab inserted from the browser renders identically to the same prefab after save/reload. **One caution for the taker:** Jeff reports seeing **nothing at all** on insert rather than a grey panel, so his case may not be this one. Do not assume the two are the same bug until someone reproduces his. **Size** unknown. |
| S26 | DONE | any | **[#213](https://github.com/JeffMcClintock/TideSynth/pull/213) merged; flipped 2026-08-20 (macos) per STEP 4.** **VERIFIED BY JEFF WITH REAL HARDWARE — "insert works now. module appears immediately."** Fixed 2026-08-20 (linux, interactive, Jeff directing), branch `tide/linux/S26-pump-se-timers`, [#213](https://github.com/JeffMcClintock/TideSynth/pull/213). **se_sdk timers NEVER FIRED in TIDE on Linux — the presenter's 50ms view-refresh timer included — because nothing called `TimerManager::Pump()`.** Jeff's repro was the diagnosis: click-place inserts into the DOCUMENT (the properties pane lights up, which only happens for modules in the document) but the VIEW never rebuilds until switching views forces one. **His distinction is the key:** *invalidating the pixels* redraws what is there; *refreshing the view* reconstructs it from the document. Pixels were fine all along — cables dragged, hover highlighted, the wheel scrolled. The rebuild is a timer, and the timer was dead. **Mechanism:** `MfcDocPresenter` is its own `se_sdk::TimerClient` — `setView()` does `StartTimer(50)`, each tick services `viewDirty` -> `RefreshView()` -> re-export to JSON -> rebuild (`MfcDocPresenter.h:351`, `.cpp:429-455`). A view-switch works because `setView()` calls `OnTimer(); // intial refresh` directly, needing no timer. On Linux the se_sdk `TimerManager` has **no native source**: `TimerManager.h:94` says the host *"must call [Pump(elapsedMs)] periodically"*. **Jeff predicted SynthEdit Wayland "must use some workaround" — it does, literally:** `SE16/SynthEditWayland/Main.cpp:190` pumps it every loop (`:189` pumps gmpi_ui's separately). TIDE is a plugin with no main loop and pumped nothing. Windows survives via `PatchManager.cpp:1850`'s DX-view path. Note `timerhelper = new AppTimerHelper(this)` is the **last line of the base `InitInstance` that TideApp skips** — **S5's gap, second consequence**. **Fix:** `SeSdkTimerPump` in `SynthEditGui` — Linux-only, 30ms, real elapsed time so longer-interval clients keep their own schedule, living and dying with the editor. Rides gmpi_ui timers because those ARE serviced here (`StandaloneApp.cpp:332`). Not folded into the 500ms heartbeat: S12's sync serialises the whole document per tick. `::TimerManager` needs the global qualifier — ambiguous with `gmpi::TimerManager`, the exact collision its header warns about. **Every se_sdk timer client in a Linux TIDE benefits; the scopes had never ticked either.** **This box could NOT verify it:** injected-pointer inserts refreshed even pre-fix, so the local A/B is blind to the failing path; why the injected path differed is unexplained and worth suspicion. **NOT claimed by this fix:** Jeff's inserts landing at wrong coordinates (structure-view bottom-right; off the left of panel view) where injected ones land sensibly, and **S25**'s tofu render, which persists post-fix. |
| S27 | TODO | any | **FOUR SUSPECTS ELIMINATED BY MEASUREMENT, and the row's own hypothesis is half wrong. Measured 2026-08-22 (macos, scheduled run), branch `tide/mac/S27-isa-vs-libm`.** The row offers *"libm transcendental divergence ... or an x86-vs-arm64 divergence — a hypothesis, not a diagnosis"*. **The ISA half is dead and the libm half now has positive evidence.** **(1) ISA is NOT the cause.** `modules/common` is a dependency-free CMake project, so it builds for both architectures on one Mac — same OS, same libm, same compiler, only the ISA differs. Rendering arm64 and comparing it against the x86_64 build **passes all ten checks**: 0.000–0.396%% of pixels moved, worst delta 1–16, every one *inside* the existing 0.400%%/40 limits. Against the committed references both architectures fail at **35–67%%**, worst delta 46–142. Two orders of magnitude apart, so whatever moves the image is not the instruction set. **(2) Renderer drift since the references were baked is NOT the cause** — the obvious reading, since eight commits touched the tracer after `37d65d5` and the current tip `4128291` (*telecentric depth of field*) did not re-bake. Building at **`25e0bf6`**, the last commit that DID update the references, reproduces the failures with **identical figures to three decimals** (35.301 / 35.007 / 66.917 / 54.465 / 61.535 — the same numbers current `main` gives). The references never matched a mac build, at any commit. **(3) The RNG is NOT the cause.** It is a hand-rolled PCG over `uint32`/`uint64` shifts (`TidePathTracer.cpp:84`) with **no `std::uniform_real_distribution` and no `std::mt19937`** — the usual cross-stdlib trap is simply absent, so the sample sequence is bit-identical everywhere. **(4) The renderer is not inherently nondeterministic** — the five `(fast)` variants pass at **0.000%%** on both architectures, and the code says why: fast mode uses *"a fixed sub-pixel GRID, not jittered draws ... bit-deterministic without the RNG being involved at all"* (`TidePathTracer.cpp:2539`). **WHAT SURVIVES, with a positive argument rather than a guess:** the divergence is confined to the transcendental-heavy Monte Carlo path. `shadeFast()` contains **1** transcendental call against **19** in the tracer overall (`sqrt` excluded — IEEE-754 requires it correctly rounded, so it cannot diverge). Fast = few transcendentals = bit-stable; full = many = 35–67%%. **AND THE REFERENCE BOX IS x86_64, BUT NOT A MAC:** the x86_64 build reproduces all five fast references **bit-exactly (worst delta 0)** while arm64 is off by one — yet x86_64-on-macOS still fails the full scenes identically to arm64. So the references came from an x86_64 machine running a different libm — Windows or Linux. **A SEPARATE, LATENT PROBLEM FOUND WHILE HERE:** `shapes` cross-ISA is **0.396%% against a 0.400%% limit** — 1%% of margin. Even with correct references that scene is borderline flaky, so whatever this row decides must widen it or reduce its noise. **Correction to this row's own text:** it says *"5 of 5 scenes fail"*; it is **5 of 10 checks** — every fast variant passes, and that asymmetry is the most useful fact in the row. **NOT ESTABLISHED:** which specific transcendental dominates, and whether pinning `exp`/`pow` would actually close it. **The decision this row wants is unchanged and is Jeff's** — per-platform references, a measured tolerance, or pinning the math — but it can now be made knowing ISA and RNG are irrelevant and that a bit-stable subset already exists. **Previous text follows.** **`tide_render`'s image-regression references do not reproduce on mac arm64 — 5 of 5 scenes fail, and it is NOT contraction.** Found 2026-08-20 (macos) when the #222 fix first made the test reachable from an SE16-hosted build (TideSynth's own root force-disables `TIDE_RENDER_PREVIEW`, so no CI or standalone build has ever run it — it can only have passed on the box that baked the references, `37d65d5`, and the scenes are in sync with them, same commit). **Measured on mac arm64:** knob 34.262%%, materials 34.528%%, shapes 66.410%%, glass 54.674%%, glow 61.312%% of pixels moved (limit 0.4%%), worst deltas 53–107 (limit 40). **The obvious suspect is ELIMINATED:** rebuilding `tide_render` + the test with `-ffp-contract=off` added to `TIDE_RENDER_STRICT_FP_FLAGS` leaves shapes/glass/glow **bit-identical** (same three figures to three decimals) and all five still failing — so unlike S19's DSP drift this is not FMA contraction. Differences are DENSE and large, the shape of libm transcendental divergence (exp/pow/sqrt differ per platform) compounding through a Monte Carlo render, or an x86-vs-arm64 divergence — labelled a hypothesis, not a diagnosis. **What this means for the design language:** E17's ruling caches traced bitmaps at runtime, so ANY cross-platform nondeterminism in `tide_render` means TIDE's panels look measurably different per platform — worth knowing before panel work scales. **Scope** decide what the regression test should assert cross-platform: per-platform references, a tolerance derived from measured cross-platform residual (E1c's shape), or pinning the math (own `exp`/`pow`). Then re-enable the test somewhere it actually runs. **Accept** `tide_render_regression` passes on at least two platforms from one set of committed references (or per-platform references, each passing on its own platform), and some build actually runs it. **Size** the measurement is done; the design call may want Jeff. |
| S28 | WONTFIX | any | **CLOSED 2026-08-21 — NOT A DEFECT, and this row's premise was wrong twice over.** Jeff: *"if they have no GUI class (which MIDI-CV2 does not), they already don't render on the rack canvas (only on the structure-view). That's already implemented and working."* **Verified rather than accepted, and it splits the two modules this row lumped together:** `SE MIDI to CV 2` has **no GUI class** — no `*Gui*` registration anywhere in `SynthEditLib` — and its saved rect is **0,0,0,0**, which is why the probe already reports it as *"zero rect (no panel presence)"* and skips it. The self-exclusion this row asked for **already exists, by mechanism**. **`MIDI In` is the opposite case and that is why it measured 8x14:** it DOES have a GUI class (`modules_internal/MidiInGui.cpp:123`, `Register<MidiInGui>::withId(L"MIDI In")`), so it renders because it is supposed to. **And under the same day's I/O-modules ruling that is now correct rather than stray** — the I/O modules ARE rack modules, and `MIDI In` is one of them. It is simply not rack-SHAPED yet, which is **E2's authoring work on item #4 of the ruled set**, not a defect row. **What this cost and what it teaches:** the row was filed from a probe result without asking why the two root modules differed, when one grep would have shown one of them has no GUI at all. **Previous text follows.** **SHARPENED 2026-08-21 by Jeff's clarification, which settles half of this row.** Placing MIDI-CV2 at the ROOT and routing it into a Container that presents it as patch-points **is the intended architecture** — *"how the end-user thinks of it, not how it is implemented"*, and *"this is already solved"*. **So the root placement is correct and is NOT what this row questions.** What is left is narrower and still worth answering: **the root implementation modules should not RENDER on the rack canvas at all**, because the Container facade is their user-facing representation — a stray 8x14 rect is the implementation showing through the presentation. **Previous text follows.** **TIDE's seeded root plumbing has a rack panel presence, and it is off-grid.** Found 2026-08-21 (macos) by E5's probe on the ruled grid: with the rack itself now clean (`TIDE MIDI-CV` 96x384, on-grid, fits row), the ONE remaining violation is **`MIDI In`, 8x14 DIPs = 2.667 snap units**. It is not a rack module and was never dropped on the rack — `TideApp.cpp:727` seeds it at the document root (`AddModule(L"MIDI In", {3600, 3620})`) as **V3's polyphony workaround**: a root MIDI-CV chain feeding the rack module as a facade of jacks, because polyphony cannot escape a container (**E7**). **Deliberately not excluded from the probe.** Hardcoding a name would make the check lie, and the measurement is correct as it stands: something 8 DIPs wide does sit on the rack canvas off-grid. The question this row asks is the other one — **should document-root plumbing carry a panel rect at all?** **Scope** `SynthEditSem/TideApp.cpp`'s seeding, or the probe's definition of a rack module if the answer is that root plumbing is legitimately invisible-but-present. **Accept** `e5_rack_footprint_probe.py` on a freshly-seeded rack returns rc=0, without the probe being taught any module's name. **Size** small, but it is a question about the seeding's intent first — read E7 and V3 before changing it. |
| S29 |  IN-REVIEW | any | **AND #258 MERGED WITH A HOLE — CORRECTED on branch `s29-close-coverage-hole`, rebuilt clean on today's main.** The merged guard skips every same-repo PR unconditionally, which assumes a push run already covered the branch — true only for `main` and `tide/**`. Any branch outside that (every one Jeff names by hand) got **zero build coverage**: no push run, and now no PR run either, silently, because a skipped job reports green. Fix adds `!startsWith(github.head_ref, 'tide/')` to the guard's `if:`, so it skips only when a push run definitely ran for that commit. YAML verified; one file, 13 lines. **Previous text follows.** **DONE — [#258](https://github.com/JeffMcClintock/TideSynth/pull/258) merged; flipped 2026-08-22 (macos, scheduled run) per STEP 4's "if you see an IN-REVIEW row whose PRs have all merged, flip it". State queried via the API rather than inferred: `MERGED`. **Previous text follows.** **FIXED 2026-08-21 (macos), branch `s29-one-run-per-commit` — AND THIS ROW'S OWN RECOMMENDED FIX WAS WRONG, which is the part worth keeping.** **Measured first:** every `build.yml` run for `tide/mac/macos-arm64` — four shas, **eight runs**, each an exact `push`/`pull_request` pair about **four seconds apart**. Cross-sha cancellation already works (older runs show `cancelled`); it is only the same-sha pair that escapes. **Why this row's option (b) — 'drop `event_name` from the concurrency group' — does NOT work:** the group also keys on `github.ref`, and that is `refs/heads/<branch>` for push but **`refs/pull/<n>/merge`** for pull_request. Different refs, different groups, with or without `event_name`. Removing it changes nothing. **What shipped is option (c):** an `if:` on the `guard` job — run on push, and on `pull_request` only when the PR head is a **fork** (where no push event reaches this repo). Same-repo PRs are already covered by their push run, whose checks attach to the same sha and so appear on the PR. `build` inherits the skip through `needs: guard`. **Chosen over a sha-keyed concurrency group deliberately:** that variant still lets both runs QUEUE before cancelling one, and on this repo the scarce resource is the macOS runner (**S30**, ~5% completion) — a run that queues and dies has already taken the slot. This one never starts it. **Verified before handing over:** YAML parses (jobs `guard`, `build` intact, triggers unchanged), and `main` has **no required status checks**, so a skipped job cannot block a merge. **The cost, stated:** same-repo PRs are no longer tested as a merge result, only as the branch tip — acceptable while this repo squash-merges quickly. **Jeff pushes it**: the fleet token is `repo`-scope only, no `workflow`. **Previous text follows.** **Every `tide/**` branch with a PR runs the whole three-platform matrix TWICE on the same commit, and macOS is the scarce runner.** Measured 2026-08-21 (macos) while waiting on [#250](https://github.com/JeffMcClintock/TideSynth/pull/250)'s macOS job: `build.yml` triggers on **both** `push: branches: ["main", "tide/**"]` **and** `pull_request:`, so a PR from a `tide/**` branch fires the matrix once for the push and once for the PR — same sha, same result. **Of the last 40 `build.yml` runs, 24 were `push` and 16 `pull_request`**, and `tide/mac/macos-arm64` had two queued simultaneously when this was written. **Why it matters more than it sounds:** the queue was **8 runs deep** with macOS jobs waiting, and a universal macOS build took **160 s** locally (79 s arm64) — so duplicate macOS runs are the single largest consumer of CI wall-clock on this repo. Halving them is a bigger queue win than the arm64 change. **Do NOT just delete `tide/**` from the push trigger without checking the issue-filing path**: the *"File a platform issue on failure"* step is what tells the owning box a platform broke, and a branch pushed WITHOUT a PR would stop being checked entirely if push runs go away. That is the trade this row exists to think about, not a detail. **Options:** (a) drop `tide/**` from `push` and accept that branch-without-PR pushes are unchecked; (b) keep both but add a `concurrency:` group keyed on the sha so the second run cancels the first; (c) keep push, restrict `pull_request` to PRs from forks. **(b) is probably right** — it is one block, changes no trigger semantics, and GitHub cancels the redundant run automatically. **And the reason the duplicates survive today is exact, not vague: `build.yml` ALREADY HAS a concurrency block** (`:55-57`), but its group is `${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}` — **`event_name` is in the key**, so the `push` run and the `pull_request` run of the same commit land in DIFFERENT groups and cannot cancel each other. Dropping `event_name` from the group is the whole fix. **Scope** `.github/workflows/build.yml` — **a workflow file, so Jeff pushes it**; the fleet token has no `workflow` scope, verified 2026-08-21 (bot scopes: `repo`). **Accept** one commit produces one matrix run. **Size** minutes. |
| S30 | TODO | any | **The macOS CI job almost never runs to completion — 2 of the last 40 `build.yml` runs, about 5%. It is starvation, not breakage.** Measured 2026-08-21 (macos) while waiting on [#250](https://github.com/JeffMcClintock/TideSynth/pull/250). **The mechanism is two things meeting:** GitHub's hosted macOS runners for this public repo queue for **over an hour** (run 32428686229's macos job was assigned at 23:27 and had still not started 60+ minutes later, while windows and linux finished in minutes), and `build.yml`'s `concurrency: cancel-in-progress: true` (`:55-57`) kills a pending run when a newer one arrives for the same ref. On a busy day — 16 PRs merged on 2026-08-20 — **every macOS job on `main` was cancelled before it ever started.** **macOS is NOT broken, and that is the useful half:** the two runs that did complete both passed (`32411882991`, `32411834158`), and notably both were runs where **linux failed and macOS succeeded**. So the platform builds; it just rarely gets to say so. **Why it matters beyond tidiness:** C7e's Accept is *"build.yml's three platforms run rather than skip, and pass, on a PR"* — a clause that a 5% completion rate can leave unsatisfiable by luck rather than by fault, and this repo has been bitten before by a green CI that was not measuring what anyone thought (**S20**). **Options:** (a) a self-hosted macOS runner — Jeff has the hardware, and it removes the queue entirely; (b) drop macOS from the per-branch matrix and run it on `main` and tags only, accepting later detection; (c) leave it and rely on local verification, which is what actually catches mac problems today. **The arm64 change helps but does not fix this** — it halves the macOS build (160 s → 79 s measured) and so shortens the window in which a cancel can land, but the queue wait dwarfs the build. **Scope** `.github/workflows/build.yml` (**Jeff pushes it**) or runner infrastructure. **Accept** a macOS job completes on a PR without needing luck. **Size** minutes for (b); an afternoon for (a). |
| S31 | DONE | any | **DONE — [#278](https://github.com/JeffMcClintock/TideSynth/pull/278) merged; flipped 2026-08-22 (macos) as STEP 4 bookkeeping.** `scripts/kill-named.sh` and `tests/s31_kill_named_test.sh` are on `main`, 7/7 on macOS, negative-controlled both ways. **ONE PIECE OF EVIDENCE IS STILL MISSING AND IT IS NOT WORTH A ROW:** the suite has never run on Linux, which is the only platform the bug is real on (BSD `pkill` excludes ancestors by default, GNU procps does not). **It is one command — `sh tests/s31_kill_named_test.sh` — and it is in the `linux` NEXT cell.** If it fails there, reopen this row rather than filing a new one. **Previous text follows.** **SHIPPED 2026-08-22 (macos, scheduled run), branch `tide/mac/S31-kill-by-pid` — [scripts/kill-named.sh](scripts/kill-named.sh) plus [tests/s31_kill_named_test.sh](tests/s31_kill_named_test.sh), 7 cases, all passing. AND THE ROW'S PREMISE IS PLATFORM-SPECIFIC, which is why three retellings did not stop it.** **The measurement that reframes it:** BSD `pkill` (macOS) excludes the calling process **and all of its ancestors** by default — `man pkill`, the `-a` flag — so on a Mac `pkill -f <pattern>` from a shell carrying that pattern is **already safe**. GNU procps (Linux) excludes only the calling process. Verified by trapping `TERM` in the calling shell: it traps a directly-sent signal (rc=143) and receives **nothing** from `pkill -f`, so the signal is not delivered rather than ignored. **So this trap can only ever fire on the `linux` box** — every one of the three recorded hits was linux — and a mac or windows run cannot reproduce it however carefully it tries. *"It worked when I tested it"* was true and useless. **Which forced the test design, and is the transferable part:** cases 1 and 2 (kills the target; the calling shell survives) **both still PASS on macOS with the ancestor walk deliberately broken** — measured, not predicted, by setting `p=1` before the walk. Behaviour tests cannot see this bug here. So the suite asserts the ancestor list **directly** via a `--print-ancestors` mode, which fails on both injected breaks. **Negative controls run both ways:** ancestor walk stubbed → cases 3 and 4 fail; `is_ancestor` forced true → case 1 fails (it spares everything and kills nothing). **[docs/ci/headless-gui-verification.md](docs/ci/headless-gui-verification.md) updated** — it already said *"never `pkill -f`"* and was still not enough, so it now points at the script and explains the platform split. **NOT verified: the suite has never run on Linux or Windows**, which is where the bug actually lives; it is POSIX `sh` with a `ps`-based ancestor walk and no `/proc` dependency, but that is an argument, not a measurement. **The linux box running `sh tests/s31_kill_named_test.sh` once is what would close this row.** **Previous text follows.** **`pkill -f <pattern>` matches the shell that is running it, so a run that uses it to tidy up kills itself.** Recorded as a journal lesson twice (2026-08-20 linux, twice in one session) and hit a **third** time on 2026-08-21 (linux) — `pkill -f 'drive.py 3000'` returned **exit 144**, having matched the Bash tool's own command line, which contains the pattern verbatim. **Two bullets in two journals have not stopped it, which is the argument for this row:** the lesson is durable, cheap to state and apparently impossible to remember at the moment it is needed, so it wants a mechanical form rather than a fourth retelling. **Scope** `scripts/` — a small helper (or a documented one-liner in a place a run actually reads) that kills by pid within a known session rather than by pattern; the shape that worked this run was `for p in $(pgrep -x <exe>); do kill "$p"; done` and, for a script, matching `/proc/<pid>/cmdline` explicitly. **Accept** a documented command that a run can copy which terminates a named child process and, run from a shell whose own command line contains that name, leaves that shell alive — demonstrated both ways. **Size** small. |
| S32 | DONE | linux | **DONE — [#267](https://github.com/JeffMcClintock/TideSynth/pull/267) merged 2026-08-21; flipped 2026-08-22 (windows) per STEP 4, PR state queried rather than assumed.** **Previous text follows.** **SOLVED AND DEMONSTRATED 2026-08-21 (linux, scheduled run), branch `tide/linux/S32-headless-compositor` ([#267](https://github.com/JeffMcClintock/TideSynth/pull/267)), after Jeff installed `weston`. Option (b) works and costs nothing.** **Recipe:** [docs/ci/headless-gui-verification.md](docs/ci/headless-gui-verification.md) — `weston --backend=headless --socket=tide-test`, then `WAYLAND_DISPLAY=tide-test XDG_CONFIG_HOME=<scratch> ./TIDE_STANDALONE`. The headless backend needs no display, no seat and no GPU, and **nothing it does reaches `gnome-shell`**, so the mutter teardown bug cannot be triggered by anything a run does. **Measured, not asserted:** the app came up, the rack drew, the module browser listed every group, and `gmpi_screenshot` returned a correct 1100x626 PNG — because it renders from the app's OWN buffer rather than asking the compositor, so **no verification capability is lost**. **Roughly ten launches across this session** (control, resources-absent, mismatch, four shutdown trials, plus a **continuous 635 s run — 10 m 35 s — that was still answering the command channel at the ten-minute mark**, confirmed by a screenshot taken then, and which then shut down cleanly by pid) with **zero `gnome-shell` crashes** and **zero `TIDE_STANDALONE` segfaults** and Jeff's login session (`loginctl` session 10) unchanged throughout — against four crashes in the two days before. **One cosmetic wart, documented so nobody chases it:** `Gdk-CRITICAL … gdk_seat_get_keyboard: assertion 'GDK_IS_SEAT (seat)' failed`, because the headless backend advertises no keyboard. Harmless. **Nested-but-visible (`weston` with no `--backend`) is only PARTIAL isolation** and the doc says so: the nested compositor is itself a mutter client, so its own exit still runs the path that crashes. **The underlying mutter bug is untouched and still Jeff's to decide about** — option (a). It is not TIDE's to fix. **Previous text follows.** **`gnome-shell` on the linux box segfaults in mutter’s Wayland CLIENT-TEARDOWN path, taking Jeff’s whole desktop to the login screen. Four occurrences in two days, and it is the single biggest tax on runtime work here.** Filed 2026-08-21 (linux, scheduled run) after the fourth. **Occurrences, from `journalctl` (`GNOME Shell crashed with signal 11` / `org.gnome.Shell@wayland.service: Main process exited, code=dumped, status=11/SEGV`):** `Aug 20 17:35:18`, `Aug 21 16:44:21`, `Aug 21 16:48:06`, `Aug 21 18:28:59`. **The backtrace, from the apport core** (`/var/crash/_usr_bin_gnome-shell.1000.crash`, unpacked with `apport-unpack`, read with `gdb -batch -ex "thread apply 1 bt"`): `wl_display_flush_clients` -> `wl_client_destroy` -> libmutter-14 -> `g_signal_handler_disconnect` -> `g_type_check_instance` **SIGSEGV**. **That is a use-after-free in mutter while destroying a disconnecting Wayland client** — a GObject already freed when the teardown tries to unhook its signal handler. `gnome-shell 46.0-0ubuntu6~24.04.14`, Ubuntu 24.04, VirtualBox VM, software GL. **This is the compositor’s bug, not TIDE’s**, and the distinction has been got wrong twice in this journal, so state it carefully both ways. **For TIDE:** the 18:28:59 crash landed within seconds of `TIDE_STANDALONE` being SIGTERM’d, and TIDE’s `gmpi-wl` memfd (`gmpi_ui/backends/DrawingFrameWayland.h:96`) was mapped in the compositor at the time. **Against TIDE:** `wl_display_flush_clients` iterates EVERY client and the core does not name which one was being destroyed; `/memfd:… (deleted)` is how every memfd appears, so it proves only that TIDE was connected; the 2026-08-21 16:48 crash happened with TIDE **not running at all**; and TIDE’s own backend handles a lost connection cleanly (`running_ = false`, `DrawingFrameWayland.h:3856-3868`) rather than faulting. **So: any client exiting can plausibly trigger it, TIDE is not special, and "the compositor crash IS S23’s 139" does NOT follow** — that path exits, it does not segfault. **Why this is worth a row rather than a journal bullet:** it has now cost two runs their runtime work, it destroys the developer’s session without warning, and every future linux run that launches `TIDE_STANDALONE` is exposed to it. **Options:** (a) tell Jeff, and let him decide about the VM’s graphics stack or a `gnome-shell` update — this is his desktop and not a run’s to change; (b) run GUI verification against a **nested/headless compositor** (`weston --backend=headless`, or a nested `wayland` session) so a compositor crash costs a scratch process and not the login session — this is the one a run can actually adopt; (c) accept it and keep runtime work short. **Nothing here is a code change to TIDE.** **Scope** the linux box’s environment, plus possibly a documented headless-compositor recipe under `docs/`. **Accept** a linux run can drive `TIDE_STANDALONE` for ten minutes without risking the desktop session — demonstrated once. **MET — see the measurement above.** **Size** small for (b); (a) is Jeff’s. |
| S33 | TODO | any | **`GmpiParameter::setBlob` is a STUB that returns `true`, so every blob a processor is told to store is silently discarded.** Found 2026-08-21 (macos) while answering E11 ([#266](https://github.com/JeffMcClintock/TideSynth/pull/266)); **recorded on E6 at the time, but E6 is DONE — a live defect on a closed row is invisible, which is A32's failure mode, so it gets its own row here.** **The stub:** `GMPI/Hosting/processor_holder.h:135` — the one assignment is commented out at `:137`, then `return true`. Exhaustive check: `value_` is written in exactly two places in that file, `:87` and `:91`, **both at construction**, and `:91` is an EMPTY vector for blobs. Nothing ever writes bytes into a processor-side blob parameter. **Two callers already branch on the `true`**, which is what makes this more than dead code: `processor_holder.cpp:397` (`onQueMessageReady`) reads the payload into a local vector, calls `setBlob`, gets `true`, then calls `sendParameterToProcessor` — **which reads the still-empty vector and sends a zero-length blob event, discarding the bytes it just read**; and `:652` (`setPin`) marks the controller waiting on a parameter whose stored value is empty. **It also defeats a documented guarantee:** the seed comment at `processor_holder.cpp:215-223` says it exists so a processor created at any time (restartComponent, offline render, state restore) starts with the parameter's CURRENT bytes rather than an empty blob — impossible while nothing writes them. That is exactly E6's measured `parametersPrimed: 0`. **NOT verified at runtime, and the distinction is load-bearing:** TIDE's patch persistence goes through the preset chunk (S12), not the blob-parameter round trip, so TIDE working is NOT evidence this path works. Whether any shipping behaviour depends on it is unmeasured. **An honest `return false` would have surfaced this years ago** — both callers test the result. **Scope** `GMPI/Hosting/processor_holder.h` — **PR-GATED**, a hosting-layer change with every GMPI plug-in downstream, so it wants Jeff rather than a run. **Accept** either `setBlob` stores the bytes and E6/E11 are re-evaluated against it, or the stub returns `false` and its callers stop pretending. **Size** small to write, unknown to prove — see E11 for what goes live the moment it stores anything. |
| S34 | TODO | any | **Two unguarded `back()` calls on `std::vector<UPlug*>` that fault at exactly -8 when the vector is empty — the same defect class the fleet already fixed once, in U2d.** Filed 2026-08-21 (linux) out of S23's signature decode. **NOT a duplicate of S23, and the distinction matters:** S23 is a specific crash that **no longer reproduces** on current `main`; this row is the latent UB the investigation surfaced, which is real whether or not it caused that crash. **The sites:** `SynthEditLib/ug_adder2.cpp:81` — `auto p = plugs.back();`, the first line of `ug_adder2::NewConnection()`, no `empty()` guard; and `SynthEditLib/ug_feedback_delays.cpp:72` — `auto dummyPin = u->plugs.back();`, no guard. `plugs` is `std::vector<UPlug*>` (`ug_base.h:245`), so an empty vector makes `back()` read `data[-1]` — **address `0xfffffffffffffff8`, i.e. -8** — which is not a null-pointer fault and will not be caught by any null check. Measured this run with a three-case control program: -16 for `dynamic_cast` on a zeroed vptr, -4 for an empty `vector<int>`, +12 for a plain null `->member`. **A third sibling is already correct and is the model to copy:** `ug_oversampler.cpp:337` wraps the same call in `while(!…empty())`. **The precedent, and the convention it set:** `SynthEditLib/modules/ControlsXp/ClassicControlGuiBase.cpp:22-33` fixed exactly this at -16 for **U2d** — guard on `empty()`, print a loud one-line `fprintf` saying what will not work, and return rather than aborting. Its comment states the rule: *"a host where those don't fire must not bring the whole process down. Loud, not silent."* **Why it is worth doing even though S23 went quiet:** the adder is what implements TIDE's automatic summing when patch cables fan into one input, so `NewConnection` runs whenever a graph is built from a restored patch — and an empty pin list is reachable whenever module descriptions fail to load, which is precisely what a missing bundle resource causes. **GATED — both files are in `SynthEditLib`, and this is NOT a build break, so STEP 5's A17 exception does not reach it.** Filed for Jeff or an interactive session, not fixable by a scheduled run. **Scope** the two lines named above. **Accept** both guarded in the shape `ClassicControlGuiBase.cpp:26-30` uses, and `SynthEditCL` still builds — it is a shared library, so TIDE building is not evidence on its own. **Size** minutes. |
| S35 | TODO | any | **macOS module scanning ignores the user domain entirely, so every locally built `.gmpi` installs somewhere nothing reads.** Filed 2026-08-22 (macos) out of P11's mac half; P11 carries the full measurement and citations. **The mismatch in one line:** `copy_plugin()` writes the **user** domain `~/Library/Audio/Plug-Ins/GMPI`; `RefreshModuleData` reads only the **system** domain `/Library/Audio/Plug-Ins/GMPI`. P11 cites the install side; the line this row would change is `SynthEditLib/EditorLib/Application.cpp:544`. **This is a convention bug, not just a path bug:** VST3 and AU both search user *and* system domains, and a developer build landing in the user domain is the correct thing for it to do — it is the scanner that is missing half the search. **Evidence it is systemic, not local:** zero user-domain paths in any `Plugin-Cache-16-override-*.xml` on this box, against 602 system-domain ones. **Options:** (a) scan both domains — the right fix, one extra `ScanFolder` call at `Application.cpp:544`, and it makes `SE_LOCAL_BUILD` work as intended on mac; (b) point `copy_plugin()` at the system domain — needs elevation on a fresh machine (SynthEdit's own CI `sudo chmod 777`s the folder, `SynthEdit/.github/workflows/Export_Tests_mac.yml:34-35`), so a build step should not do it; (c) leave it and rely on the documented hand-copy, which is what [docs/building.md](docs/building.md) now describes. **(a) is the correct fix and it is GATED** — `SynthEditLib/EditorLib/Application.cpp` is on the GATED list, so it needs Jeff. **NOT verified:** whether adding the second `ScanFolder` introduces duplicate-module-ID collisions when the same bundle exists in both domains — it plausibly does, and both copies existed on this box, so that is the first thing to test rather than assume. **Windows and Linux unexamined** — this was measured on mac only; `getPlatformPluginsFolder()` has a distinct branch per platform. **Accept** a module built with `SE_LOCAL_BUILD` on macOS is found by a subsequent scan without a hand-copy, and a bundle present in both domains resolves to exactly one entry. **Size** minutes for the scan call; the collision question is the real work. |
| S36 | TODO | any | **On Windows the plug-in's resources are staged one directory above the binary, so every Windows build ships an empty rack module browser and pinless classic controls.** Found 2026-08-22 (windows) while packaging **R2**, and it is **S21's defect on the third platform** -- S21 fixed exactly this on Linux, and `SynthEditSem/CMakeLists.txt`'s Windows arm was left alone at the time, deliberately and with a comment saying why: *"S21 was found on Linux, and Windows is not verifiable from there, so it gets its own branch"*. It is verifiable from here, and the branch is wrong. **The mechanism:** that arm sets `_tide_resources` to `$<TARGET_FILE_DIR:...>/../Resources` on the reasoning that *"the binary sits in Contents/<arch>/, so Resources is its sibling"* -- but Windows builds **no bundle at all** (`gmpi_plugin.cmake`'s VST3 `else()` arm is a bare `SUFFIX ".vst3"`), so with a multi-config generator the binary sits in `Release\` and the resources land in `build/SynthEditSem/Resources`, one directory up. A non-bundled Windows plug-in resolves its resources to **the folder the binary is in** (`BundleInfo::getImbeddedFileFolder`, reached from `getResourceFolder()` when `pluginIsBundle` is false), which is `build/SynthEditSem/Release`. Nothing ever looks where CMake put them. **Measured, with a control, on a clean Release build of `main`:** run the freshly built `TIDE-Rack.exe` and it prints `ControlsXp.xml missing from bundle resources`, the same for `MidiPlayer2.xml`, `Converters.xml` and `VaFilters.xml`, then `no Prefabs folder in bundle resources - the rack module browser will be empty`. Copy that staged `Resources` folder's **contents** beside the binary and the **identical binary** prints `ControlsXp.xml enriched 2 of 18`, ... and `6 rack prefab(s) seeded from the bundle`. So the data is correct and only its address is wrong. **Loud, not silent, and that is why nobody noticed:** the plug-in reports the absence to stderr and carries on, per S13's ruling, and a plug-in inside a DAW has no console. **CI cannot catch it either** -- `build.yml` builds and never runs anything. **Two candidate fixes, and R2 has a bearing on which.** (a) Point the Windows arm at `$<TARGET_FILE_DIR>` (drop the `/..`), which fixes the developer build and leaves the shipped layout to the packaging script. (b) Give Windows a real VST3 bundle in `gmpi_plugin.cmake`, mirroring the Linux POST_BUILD arm, after which the existing `../Resources` expression becomes correct by construction and the build tree matches what ships. **(b) is the tidier answer and reaches further** -- it is a `GMPI` change, so **PR-GATED**, and it would also close [#271](https://github.com/JeffMcClintock/TideSynth/issues/271)'s cousin on Linux. **(a) is TIDE's own file and is ALLOWED.** **R2 is unaffected either way**: `scripts/package-windows.ps1` reads the staged folder from where CMake actually puts it and assembles the bundle itself, refusing to package at all if the resources or the prefabs are missing. **Scope** `SynthEditSem/CMakeLists.txt` for (a); `gmpi_plugin.cmake` for (b). **Accept** a freshly built Windows `TIDE-Rack.exe`, run from the build tree with nothing copied by hand, prints `6 rack prefab(s) seeded from the bundle` and no `missing from bundle resources` line. **Size** minutes for (a); one session for (b). |

| E7 | TODO | any | **THE POLYPHONY MODEL IS RULED 2026-08-21, and it reframes this row rather than closing it.** Jeff: modules are **always monophonic** and the SynthEdit runtime **clones** them per voice, automatically, on the DSP graph only (not the GUI); the user sets a voice count on the MIDI-CV rack module, so *"polyphony is essentially 'free' as far as individual modules are concerned."* **So no TIDE module should be designed polyphonic.** **AND THE CONTAINER QUESTION IS ALREADY SOLVED — corrected by Jeff the same day, against this agent's first reading.** Jeff: *"conceptually they are rack modules, but in reality we place the MIDI-CV2 at the root level and route it into the Container (which represents it on the GUI as patch-points). This is already solved. MIDI-CV as rack module [is] how the end-user thinks of it, not how it is implemented."* **So root-placement-plus-Container-facade is the ARCHITECTURE, not a workaround**, and nothing in the MVP needs polyphony to cross a Container boundary. **The agent had flagged this as newly critical and was wrong**: it read the I/O-modules ruling (*they ARE rack modules, movable*) as putting MIDI-CV back inside a Container, when presentation and placement are deliberately separate. Recorded because the wrong inference is the tempting one — the row's own wording (*"polyphony cannot escape a container"*) invites it. **What remains of this row is the engine limitation as a fact, not as a blocker.** **Previous text follows.** **ROOT CAUSE OF THE INTERIM'S SILENCE FOUND, and it was neither of the two suspects this row listed — it was TIDE having no TYPE CONVERTERS.** Jeff: "the library will automatically insert converters when needed, so long as the converter is linked in." `ug_base.cpp:1751` builds an id like `SE <From>To<To>` for any mixed-datatype connection and calls `GetById()`; a miss is `assert(false); return;`, which in **Release** silently abandons the connection. `Converters.cpp` is a separate target's source, so it shipped only as a loadable `Converters.sem` and TIDE — static, no scan (S1a) — had **no `SE BoolToVolts`**, so `SE MIDItoGate2`'s `BoolOutPin` gate could never reach an audio-rate jack. Fixed by linking it into `SynthEditSem` with `my_type_convert.cpp` and staging `Converters.xml`; **the rack now plays the note** (`tests/hosts/v3-midi-gate.rpp`: silent, 440.0 Hz for the note's duration, silent). **This was a whole class of silent failure** — any mixed-datatype cable in TIDE was dead — so it is worth more than the row it unblocked. **What remains for E7 is unchanged and is now purely the POLYPHONY question:** `SE MIDI to CV 2` has the PITCH output the monophonic stand-in lacks, and it is still unusable because its container becomes a voice container. Jeff's rulings below already reduce that to "where do the jacks live". **Original row follows.** **CORRECTION 2026-08-18, and it narrows the remaining bug to one boundary. The row below says `SE MIDItoGate2` "emits no gate at all" — THAT IS WRONG.** It was an inference from a silent rack, never a direct measurement, and Jeff's question ("is MIDItoGate2 MIDI2 aware?") is what exposed it. **It is fully MIDI 2.0 compatible and it does set the gate.** Two proofs. **By code:** `MidiConverter2::processMidi` opens with an explicit pass-through for MIDI 2.0 input (`modules/se_sdk3/mp_midi.h:966`, *"MIDI 2.0 messages need no conversion — pass through and return"*), and `MidiToGate2::onMidi2Message` requires `ChannelVoice64`, which is exactly the message type TIDE forwards. **By a temporary stderr trace added to `modules/MidiPlayer2/MidiToGate.cpp` and then REVERTED unbuilt** (a temporary trace that got committed is issue [#87](https://github.com/JeffMcClintock/TideSynth/issues/87); this one was not) — `MTG2: onMidiMessage pin=0 size=8 bytes=40 90 3c 00 isMidi2=1` · `MTG2: onMidi2Message msgType=4 status=9 (ChannelVoice64=4 NoteOn=9)` · `MTG2: NOTE ON note=60 -> pinGate=true triggerCounter=22` · `MTG2: trigger pulse ended at sample 6 -> setSleep(true)`. So it receives the UMP, decodes it, reads **note 60** — the fixture's middle C — and sets `pinGate = true`. `triggerCounter=22` is the 0.5 ms pulse at 44.1 kHz, correct. **WHERE THE BREAK ACTUALLY IS.** The rack downstream measured **zero for the entire render**, peak −inf, including the samples while the gate was set. So the gate is set on the pin and never arrives at the Envelope's ADSR. Note this does NOT overturn "a monophonic module's cable is live" — the cable demonstrably drove the GATE jack from its default of 10 down to 0, which is why the rack went silent. **The cable crosses; the VALUE does not.** Suspects, both concrete and neither yet tested: (1) `pinGate` is a `BoolOutPin` and the jack is an audio-rate `SE Patch Point in`, so a static/event bool may simply not drive an audio-rate signal — which would also explain why `--connect $mtg:Gate $pp:Input` is *accepted* by SynthEditCL while carrying nothing; (2) `setSleep(true)` fires 22 samples after the note-on while the gate is still logically HIGH, so a sleeping module may stop driving its output entirely. **Test (1) first** — it is a datatype question answerable with SynthEditCL alone, no GUI: put a `Bool to Volts`-style conversion (or a `Multiply`) between the two and see whether the gate arrives. If it does, the shipped interim just needs that module inside the prefab. **Original row follows.** **UPDATED 2026-08-18 (macos, interactive, Jeff directing) — the cause is now confirmed EMPIRICALLY as well as architecturally, and Jeff's rulings settle most of the design.** **(a) Expansion is purely mechanical** — lifted content behaves exactly as if it had been put in the parent in the first place; the exceptions are cosmetic, e.g. a per-container CPU readout must sum its modules and present the total as a pretence. That removes the one real objection to covert expansion, which was whether it would make MIDI-CV 2 a genuine root-level voice source. It would. **(b) The "two on one rack" problem dissolves:** a MIDI-CV is close to mandatory in an instrument, so add ONE automatically to every new project and remove the option to add more — project furniture rather than something the user places, costing neither a serialised flag nor a name-matching convention. **Shape: one implicit root-level MIDI-CV per project, the rack module reduced to the jacks that expose it. STILL TO DECIDE: where those jacks live**, since PITCH and GATE must be cabled from somewhere. **THE CAUSE, MEASURED.** `SE MIDItoGate2` is monophonic, so it does not make its container a voice container. A rack cabled from it renders **digital silence** — and the silence is the proof, because the Envelope's GATE jack defaults to 10 (open), so a rack that goes SILENT when that jack is cabled has a **live cable driving it to 0**. Cables out of a monophonic container cross; out of a polyphonic one they do not. That is the experiment the original row below asked for, answered. **GETTING IT LINKED, AND A WRONG ANSWER WORTH RECORDING.** `SE MIDItoGate2` was absent from TIDE. **It is NOT the `#if GMPI_IS_PLATFORM_JUCE==1` block around `INIT_STATIC_FILE(MIDItoGate)`** — wrong twice over: `INIT_STATIC_FILE(ADSR)` is inside that same block while ADSR works fine in TIDE (TIDE's ADSR is the legacy `ug_adsr.cpp` `REGISTER_MODULE_1` one, reached through the legacy table), and adding an unconditional `INIT_STATIC_FILE(MIDItoGate)` **fails to link** — `se_static_library_init_MIDItoGate()` undefined. **Real reason:** `modules/MidiPlayer2/MidiToGate.cpp` is in a `SynthEditLib/CMakeLists.txt` source list belonging to a **separate `MidiPlayer2` target**, so it builds as a LOADABLE module and is not in the static library — `MidiToGate.o` lands under `MidiPlayer2.build/`, `MidiToCv2.o` under `SynthEditLib.build/`. TIDE links statically and has no scan (S1a). **Fixed with NO GATED change:** the `.cpp` joined `SynthEditSem/CMakeLists.txt`'s source list, `MidiPlayer2.xml` staged for its pins — E2a's `RectangleGui.cpp` pattern — and **`MIDI to Gate2` now appears in TIDE's browser**, the authoritative check. **WHY THE INTERIM STILL DOES NOT SOUND — delivery is ruled out.** It emits no gate at all: 0 for the whole render, note present, `TIDE: host MIDI reaching the rack` printed. **PROBE D proves MIDI crosses the patch cable out of the `MIDI In` module.** Jeff asked whether that had been proved and it had not: PROBE A (module present AND cabled) gates while PROBE B (module absent) does not, but that moved two variables, leaving live the possibility that the module's mere PRESENCE makes the container a MIDI destination and `ug_container`'s redirector feeds MIDI-CV 2 directly (`ug_base.cpp:2859`). **PROBE D holds the module present with the pin UNCONNECTED: gate flat 0.** A and D differ only in the cable, only A gates, so the cable is the carrier. Not a threshold either — measured, the ADSR opens identically at 1 V and 10 V. **Suspects now inside the module only:** the `setSleep(true)` / `subProcessNothing` path in `MidiToGate2::subProcess` and the cross-class `setSubProcess(&MidiToGate::subProcessNothing)` beside it, both at `modules/MidiPlayer2/MidiToGate.cpp:222`. All four probes run from `build-prefabs.py --diagnostics`. **Original row follows.** **A rack module that is a POLYPHONIC SOURCE does not drive other rack modules through a patch cable — found 2026-08-18 (macos, interactive, Jeff directing) while building V3, and it is what blocks V3 rather than anything about MIDI.** **What is proven.** Host MIDI reaches the rack: TIDE's new check prints `TIDE: host MIDI reaching the rack - first message 8 byte(s), status 0x40` (a MIDI 2.0 UMP, GMPI's `DT_MIDI2`) — [SynthEdit#46](https://github.com/JeffMcClintock/SynthEdit/pull/46). `SE MIDI to CV 2` converts it correctly: with a `Sound Out` INSIDE its own container, its **Gate rises at 0.500 s and falls at 1.200 s**, exactly the fixture's note. **But the same signal taken out through a patch cable produces digital silence** — `tests/hosts/v1-rack-midi.rpp`, four cables, renders **identically with and without the note** (peak −6.3 dBFS, 440.0 Hz throughout), and 440.0 Hz is the *Oscillator's own* 5 V default, so both MIDI cables contributed nothing. A two-module probe (MIDI PITCH → Output L, a jack with **no** stored default, so it would have passed anything that arrived) renders **−inf**. **CAUSE CONFIRMED by Jeff, 2026-08-18, from the architecture rather than from another measurement — this is a PRE-EXISTING constraint, not a new defect: polyphony cannot escape a container.** `SE MIDI to CV 2` is `polyphonicSource="true"` / `cloned="true"` (`MidiToCv2.cpp:18`), so its container is a voice container, and a voice container's outputs do not cross out. **The other half of his answer resolves what looked like a contradiction:** a container with NO MIDI-CV in it is **expanded** on the DSP side already — it is effectively cosmetic, like grouping objects in a paint program — so V1's oscillator→envelope→output cables are just wires in one flat graph, which is exactly why they work. **The rule that follows: as long as the MIDI-CV is not inside a sub-container, the rest of the patch works fine, containers or not.** So the earlier "cable a non-polyphonic source out of a container" experiment is unnecessary and has been dropped from this row — the answer was architectural, not empirical. **TWO CANDIDATE SOLUTIONS, Jeff's, and the choice is his to make.** **(1) Covert expansion:** special-case this one rack module so the DSP side lifts its contents into the parent with every wire destination rerouted, which needs an extra flag serialised on that container. **(2) Convention:** the rack module becomes nothing but a container of patch-points (Trigger, Gate, Pitch...), and any container shaped like that gets a secretly-added `MIDI In` + `MIDI-CV 2` pair wired in at the root level. **A first read, not a ruling:** (1) reuses expansion machinery that already runs on every other container and keeps the module honest in the structure view — you can open it and see what it is, which is how this bug got diagnosed at all; (2) needs no new serialised state but identifies the container by the NAMES of its patch points, and names are the least stable thing in a document. Both have to answer "what happens with two MIDI rack modules on one rack". **Scope** likely `SynthEditLib` (GATED) — the expansion path and the voice/clone machinery — plus a generator change in `TideModules/`. **Accept** `tests/hosts/v1-rack-midi.rpp` renders audio that starts at 0.500 s and stops at 1.200 s. The fixture already exists and already fails, so this is a re-measure, not a new test. **Size** unknown; the two designs differ enough that it wants Jeff's pick first. **Do not start by suspecting the prefabs or the MIDI path** — both are measured good, and the probes behind `build-prefabs.py --diagnostics` re-run that proof in one command. |
| V2 | BLOCKED | any | Host-automatable parameters without a panel view (design note 4 in [docs/design-notes.md](docs/design-notes.md)). **This premise is now stale too — panel view (the rack) is TIDE's default view as of the 2026-08-13 pivot, not something being avoided. Design note 4 needs re-reading against that before this row is trusted; not done here, flagged in passing while splitting E2.** **SHAPE ESTABLISHED 2026-08-18 from how every competitor solves this — [docs/competitive-review.md](docs/competitive-review.md) §3. The field splits in two and the answer is not close.** RNBO (`[param name]`), plugdata and Bitwig's Grid expose parameters **declaratively from the patch with no panel required**; Reaktor and Max for Live make you draw a knob first, and **Reaktor's Multi-Picture controls cannot be automated at all** while its auto-generated automation IDs are not auto-maintained, so deleting a control leaves holes. **The two serious DAW-hosted racks both use a pre-declared slot pool** — VCV Rack Pro **1024 slots**, Reason Rack **>2000 advertised** (~256 practically automatable, UNVERIFIED/community-sourced) — bound automatically as modules are added, which is what makes them work with zero user setup. **Both then made the same mistake, and it is the one thing this row must not repeat: the binding is POSITIONAL.** It follows insertion order and a recycled free list, so deleting a module orphans the DAW's automation lane and **the slot may be reused by a newly created module's parameter, silently driving the wrong thing** — documented in VCV *and* Reason. Reason additionally pushes parameter names so lazily that the known Ableton workaround is *dragging the device a few pixels inside the plugin* to make names populate; its sanctioned answer is "wrap it in a Combinator and automate that", i.e. it deferred the problem and shipped a panel builder to cover it. **So the ruling: (1) pre-declare a pool so automation needs no setup; (2) bind slots to a persistent content-addressed identity — module UUID + parameter id — NEVER to insertion order or a free list; (3) push names to the host eagerly.** **TIDE is unusually well placed for (2)** because S11 already puts the whole document in the plugin state with stable module ids. **Cheap now, near-impossible to retrofit once patches exist in the wild.** **One open position worth noting while here:** the declarative camp pays for it with generic auto-generated panels (RNBO's real UI means writing C++/JUCE), and the panel-first camp taxes the user to build one — **nobody ships automatic host parameters AND a good default panel AND an optional custom one.** Since the 2026-08-13 pivot the rack panel *is* TIDE's default UI, so that combination is nearly free here. |
| E6 |  DONE | any | **THE ROOT CAUSE IS SHARPER THAN THIS ROW STATES, measured 2026-08-21 (macos) while answering E11 — a blob setter DOES exist, and it is a STUB.** This row says *"priming a blob needs a non-scalar setter in GMPI's `processor_holder` — only `setParameterNormalizedFromDaw` exists"*. That is not quite right: **`GmpiParameter::setBlob` exists at `processor_holder.h:135`**, and its body is one commented-out line (`:137`) followed by `return true`. So the API is present, callers already use it, and it silently stores nothing. **Two callers already believe it works**, which is what makes this more than cosmetic: `onQueMessageReady` (`processor_holder.cpp:397`) reads the payload into a local vector, calls `setBlob`, gets `true`, and then calls `sendParameterToProcessor` — **which reads the parameter's still-empty vector and sends a zero-length blob event, discarding the bytes it just read**; and `setPin` (`:652`) marks the controller waiting on a parameter whose stored value is empty. **And it defeats the seed's stated purpose**: the comment at `:215-223` says the seed exists so a processor created at any time (restartComponent, offline render, state restore) starts with the parameter's CURRENT bytes rather than an empty blob — which cannot happen while nothing ever writes them. That is precisely this row's `parametersPrimed: 0`. **NOT verified at runtime, and the distinction matters**: TIDE's patch persistence goes through the preset chunk (`setPresetUnsafe`/`getPresetUnsafe`, S12), not the blob-parameter round-trip, so TIDE working is not evidence this path works. Whether any shipping behaviour depends on it is unmeasured. **Still PR-GATED and still not patched here** — implementing `setBlob` is a hosting-layer change with every GMPI plug-in downstream, and it wants Jeff's eye rather than a run's. **Previous text follows.** **DONE — [GMPI_Wrappers#9](https://github.com/JeffMcClintock/GMPI_Wrappers/pull/9) + [#235](https://github.com/JeffMcClintock/TideSynth/pull/235) merged; flipped 2026-08-21 (macos) per STEP 4, PR states verified rather than assumed.** **THE FALLBACK ARM SHIPPED 2026-08-20 (macos, scheduled run): `render-audio` now reports `parametersUnprimed`, branch `tide/mac/E6-unprimed-report`, [GMPI_Wrappers#9](https://github.com/JeffMcClintock/GMPI_Wrappers/pull/9).** Verified live against a TIDE build with the branch as override: `{peak: 0, parametersPrimed: 0, parametersUnprimed: 2, silent: true}` — TIDE's two blob parameters counted, so "ignored your state" and "your state is silent" are finally distinguishable, which is this row's own or-failing-that scope. `CommandDispatcher.cpp` compiles only into the Standalone wrapper's mcp/ path — no other wrapper and no SynthEditCL involvement, by construction. **The full Accept (render == live audio) still needs the blob-capable prime, which is a non-scalar setter in GMPI's `processor_holder` — PR-GATED, and a genuine API design** (blob event payload lifetime through the queued PinSet path), not a port; left filed here rather than proposed half-baked. **Previous text follows.**  **`gmpi_render_audio` cannot measure TIDE, and reports silence rather than an error — found 2026-08-18 (macos, interactive) while trying to close E2a's audio half.** The standalone's offline render primes a fresh processor from current parameter values and **skips every non-scalar parameter**: `if (!gmpi::hosting::is_scalar(param.info->datatype)) continue;` at `GMPI_Wrappers/wrapper/Standalone/mcp/CommandDispatcher.cpp:858`. **TIDE's entire patch is a blob parameter** (S12's chunk), so the render instance is built with an EMPTY graph and returns `peak: 0` no matter what is on the rack — indistinguishable from "the patch makes no sound", which is exactly the wrong answer to hand someone measuring V1. **The tell is `parametersPrimed: 0`** in the result JSON; TIDE has two parameters and both are blobs. **Not a TIDE bug and not fixable in TIDE:** priming a blob needs a non-scalar setter in GMPI's `processor_holder` — only `setParameterNormalizedFromDaw` exists (`GMPI/Hosting/processor_holder.h:244`) — so this reaches a third repo. **Scope** a blob-capable prime in the render path, or, failing that, `render-audio` reporting `"unprimed": N` so a caller can tell "ignored your state" from "your state is silent". **Accept** rendering a wired TIDE rack through the standalone produces the same audio the live app plays. **Size** unknown; the setter is the unknown half. **Does not block E2a's remaining audio measurement** — the REAPER route (`scripts/render-and-measure.py`) exercises the product path and is unaffected. |
| E1c | TODO | any | **THE HYPOTHESIS THIS ROW NAMES IS ALREADY REFUTED BY DATA IN THE REPO — no engine run needed. Narrowed 2026-08-22 (macos, scheduled run), branch `tide/mac/E1c-reference-provenance`.** The row says to test whether *"the core oscillator's phase increment is cross-platform stable where the naive one's is not"*. **E1a's own table already rules that out.** Both of its cases use the SAME module, `SE Oscillator (naive)`, both measured macOS-vs-Linux-goldens in one run, identical on three independent engines — and they land **50 dB apart**: `osc_naive_sine` −73.5 dBFS (12 LSB, *"not rounding"*) against `voice_midi_note` −123.1 dBFS (1 LSB, *"pure rounding"*). **Same oscillator cannot explain a 50 dB gap, so naive-vs-core is not the variable.** **What does co-vary, across all four cases:** `osc_naive_sine` is the only one whose **pitch input is not connected to anything** — it free-runs on the module default. The other three all drive pitch (`voice_midi_note` from the keyboard; `prefab_oscillator` and `prefab_filter` from a patch point pinned to 5 V) and all three sit in the rounding class (−123.1, −131.1, −121.4). **So the discriminator is the undriven pitch input, not the module** — which also means `prefab_oscillator` and `prefab_filter` are rounding-class cases carrying phase-drift-class gates, and their `tolerance_reason` inherits a mechanism their scripts do not exhibit. **Cheapest experiment to settle it is now one case, not two:** the naive oscillator WITH pitch pinned to 5 V. Rounding class confirms the pitch-input reading; −73 dB class restores the module reading. **DELIVERED HERE, because it is what made the above take an afternoon instead of a minute:** references recorded **nothing** about which platform seeded them, so every number above required journal archaeology, and the four were only comparable because one run happened to produce them all. `--update-refs` now writes `tests/references/<case>.provenance.json` ([tools/render_harness.py](tools/render_harness.py)), the six existing references are backfilled, and `--selftest` covers it in 6 new cases that need no engine — negative-controlled both ways (hardcoded platform → the platform check fails; wrong sidecar name → the path check fails). **Three of the six are `recorded: "unknown"`** (`prefab_envelope`, `prefab_filter`, `prefab_midi`) — nobody wrote it down and it could not be established, so a cross-platform claim about those three is currently unsupported. **NOT DONE, and deliberately: the gates are unchanged.** This row's Accept requires a positive control — tightened gates passing on both platforms while failing a deliberate regression — and the harness needs `SynthEditCL` plus a Linux run ([.github/workflows/verify.yml](.github/workflows/verify.yml) is `ubuntu-24.04` only), neither available on this box. **Changing a gate without that control is exactly what created this row.** **Previous text follows.** **`prefab_oscillator` and `prefab_filter` carry relaxed null gates that are ~55 dB wider than their own measured cross-platform residual, and the measurement those gates were sized from came from a DIFFERENT oscillator.** Found 2026-08-19 (macos) from E2b's CI, by reading the numbers rather than by suspecting anything. **The data, all from one `verify` run on Linux against macOS-seeded references** ([#170](https://github.com/JeffMcClintock/TideSynth/pull/170)): `prefab_oscillator` null **−131.1 dBFS**, peakdiff −90.3; `prefab_filter` null **−121.4 dBFS**, peakdiff −90.3. Both are **rounding class** — inside even the DEFAULT gates (−100/−86) — while both cases declare **−67/−62**. **Where the wide gates came from:** E1a measured a free-running oscillator's cross-platform residual at −73.5 dBFS RMS and sized the gates 6 dB above it. That measurement is sound, but it was taken on **`SE Oscillator (naive)`**, a separately-loaded module — and `prefab_oscillator`/`prefab_filter` use **`Oscillator`**, the core `ug_oscillator2`. The relaxed gates were inherited by analogy ("free-running oscillator, same class of residual"), not measured on the module actually in the path. **Hypothesis to test, NOT a conclusion:** the core oscillator's phase increment is cross-platform stable where the naive one's is not. Note `voice_midi_note` also contains the naive oscillator yet passes at DEFAULT gates (−123.1 dBFS on macOS), so "naive drifts" is not the whole story either — its pitch is MIDI-derived rather than the free-running default, which is the obvious variable to isolate first. **Why it matters:** a −67 dB gate on a case whose true residual is −121 dB leaves ~55 dB of margin for a real regression to hide in, and E1a's own reasoning is that a gate wide enough to be safe is a gate narrow enough to be useless. **Scope** `tests/cases/prefab_oscillator.json` and `tests/cases/prefab_filter.json` — the tolerance triple in each. **Accept** the two cases run at gates justified by a measurement of *their own* residual across mac and Linux (both directions, since a reference seeded on one platform is the asymmetric case), with the numbers and the platform pair recorded in `tolerance_reason` as E1a did; and a positive control showing the tightened gates still pass on both platforms while failing a deliberately introduced regression. **Do not simply drop to the defaults without measuring** — that is the same reasoning-instead-of-measuring this row is about. **Size** small; the measurement is one CI run plus one local run per case. |
| E2 | TODO | any | **SCOPED AND SPLIT 2026-08-19 (macos) — E2 is an umbrella, and as one item it is not takeable: it never says which modules a "first curated set" contains, which is a product decision, so its acceptance check cannot be stated before starting.** Its conventions half is also largely **already written down**, in `build-prefabs.py`'s header (panel geometry, `PanelWndPosition` vs `panel_rect`, faceplate sizing, and the rack grid: `hpWidth 15`, `rowHeight 380`, so a slot is a multiple of 15 wide and at most 350 tall). **What is left of E2 is MODULES, one stage each** — **E2b** (Filter) is the first and is IN-REVIEW, and **E2c** (SV Filter4 / Oscillator HD registered in TIDE) landed as [#171](https://github.com/JeffMcClintock/TideSynth/pull/171) alongside it; the measured candidate list for later stages is in that row, and [tests/README.md](tests/README.md) says how to decide whether a new prefab can have an E1 case at all. **Previous text follows.** **UNBLOCKED 2026-08-18 — V1 is DONE, so this row's stated premise no longer holds.** V1's acceptance test was run and passed ([#140](https://github.com/JeffMcClintock/TideSynth/pull/140)): a saved REAPER project's TIDE rack sounds after reload, measured at **peak −6.3 dBFS, rms −17.0 dBFS, 440.0 Hz**. So "no point authoring a fuller module library for a plugin that cannot yet keep its patch across a host save" is retired by measurement. **Read E2a's archived row before starting** — it built three of these and its findings are what a fuller set needs (a Container's rack rect is `PanelWndPosition`; every module must be a class TIDE links; a module needs both its `.cpp` and its XML). **Original row follows.** **The rest of the first Eurorack module set, beyond E2a's three V1-critical prefabs.** Split 2026-08-13 — see E2a for the part that's unblocked. Everything else here still waits on V1: "no point authoring a fuller module library for a plugin that cannot yet keep its patch across a host save" holds for the broader set even though the minimal V1-critical prefabs couldn't wait for V1 themselves. PLAN "The Eurorack rack", 2026-08-09. Define the naming and I/O conventions for a module Container, then build the rest of a first curated set, each with its own E1 test case. Two dependencies that are not obvious: constraint 7 means the **primitives** a Container is built from must be statically registered — see **S8**, which is about the wrong ones being compiled in today — and a Container is only openable-and-editable if U1's UX can actually navigate into it, which is what the breadcrumb bar is for. Carried over from the superseded `tide-rack` repo, where this was EP-002. |
| E3 | BLOCKED | any | **Curated starter racks** — ready-to-play preset patches shipped with the plugin, treated as a deliverable rather than a demo. PLAN calls this the friendly surface, and it is the Blocks trait the product is explicitly copying. Blocked on **E2**. Two things it needs that nothing else forces: a rack has to survive load without a filesystem path (constraint 3), and it should be the thing a user sees *first*, which is a product decision about default state, not a file format. Carried over from the superseded `tide-rack` repo, where this was EP-003. |
| E4 | BLOCKED(E2) | any | **Ruled 2026-08-17, same day it was filed — constraint 9 answers the question that made this NEEDS-JEFF.** Rather than bless per-platform folders, Jeff ruled the general principle: TIDE Rack only implements features implementable on the lowest-common-denominator target (today AUv3) — see PLAN.md constraint 9 and [docs/decisions.md](docs/decisions.md). For E4 that means: the per-device library is allowed in principle (AUv3 *can* write inside its own container), and every platform implements the same container model — desktop gets no blessed platform-specific folder, it gets the AUv3 semantics or nothing. Now BLOCKED(E2), which was always its real prerequisite: a user-authored Container is the same object as a shipped one, so E2's naming/IO conventions must exist first. One sub-question stays flagged for implementation time, not for now: whether the system document-picker sheet (tier 3, sharing) sits within constraint 5 — it is OS-supplied and user-initiated, but it is modal. Original row follows. **User-authored prefabs — can a user save their own rack modules?** Asked by Jeff 2026-08-17; analysis in [docs/e2a-prefabs.md](docs/e2a-prefabs.md) §7. Short answer: yes — prefabs are XML data, and the sandbox rules that kill module scanning do not apply; an AUv3 extension may write inside its own container, which constraint 4's own wording permits. Three independent tiers: in-project reuse (free once V1 lands, writes nothing new), a per-device library in the plugin's container, and export/import via the system document picker. **The decisions that make this NEEDS-JEFF:** (1) desktop has no OS-enforced container and the natural folder is exactly what constraint 4 bans — bless one decided location as the desktop "container", or make the filesystem library iOS-only; (2) whether the document-picker sheet is compatible with constraint 5. Default in effect: not in v0.1; nothing is written anywhere; users reuse Containers within a project only. Decide-by: with E2's naming/IO conventions — a user-authored Container is the same object as a shipped one, so the conventions must precede the feature. |
| E16 | RESOLVED | any | **RESOLVED 2026-08-21 by Jeff, in session ([#246](https://github.com/JeffMcClintock/TideSynth/pull/246)): TIDE ships TIER 1 as a preliminary set** — and the reason is sequencing, not size: *"we need a testable, installable MVP first"*, so installers and the website can move while the full set is developed in parallel by Jeff, and so a design-language change (E17) cannot invalidate a lot of authored panels. **The doc's own argument for Tier 2 was rejected as false:** *"Tier 1 absolutely can make music. Plenty of real hardware products ship with only this type of functionality."* **THE RULED SET IS NINE MODULES**: Oscillator HD, Envelope, Output, I/O modules, Filter, VCA, LFO, Noise, Attenuverter+Offset. **Three of the surveyed twelve are cut**, each for a mechanism: **Mixer** (*"leave mixer out of MVP to reduce the critical path"*) and **Slew/Glide** because TIDE already does the job — cables fan in with automatic summing, and glide is inside MIDI-CV2 (verified: `CVoiceList.cpp` drives it through `MidiToCv2`'s pitch interpolator) — and **Sample & Hold** as not critical. All three go to the second wave. **No open questions remain in the set.** **Four conventions ruled alongside it** — pitch, gate, polyphony, and the I/O modules — all recorded in [docs/decisions.md](docs/decisions.md), with [docs/module-set.md](docs/module-set.md) corrected. **This unblocks E2**, and with it the release track. **Previous text follows.** **The TIDE module list — answers [docs/module-enumeration.md](docs/module-enumeration.md) §7.2 ("Which modules belong in TIDE's list? Neither existing arm is right"), open since 2026-08-06.** Proposal written 2026-08-18: **[docs/module-set.md](docs/module-set.md)** — a three-tier MVP (**3** modules = the V1 acceptance test, already E2a; **12** = minimum genuinely playable; **20** = credible first release) plus a 20-row ranked nice-to-have list, each annotated with the SynthEditLib primitive that already implements it and the constraint that blocks it. **The measured finding that changes the estimate: the DSP already exists.** `C:\SE\SynthEditLib` carries **75 `ug_*` DSP modules** plus a modern SEM set (`EnvelopeAdsr`, `SVFilter2`, `VaFilters`, `Delay3`, `Reverb`, `StepSequencer`, `Waveshapers`, `BPMClock3`, `Arpeggiator`, `Unison`), so the module set is a **Container-authoring and panel-art job, not a DSP-writing job** — which moves the schedule risk to per-module panel cost, and **nobody has measured that**. **Two MVP rows are not free:** the oscillator is S8's live blocker (`OscillatorNaive`, **zero symbols** in the shipped Release binary) and Output must be authored from scratch (the existing `Output.seprefab` contains **no Sound Out** — measured, [docs/e2a-prefabs.md](docs/e2a-prefabs.md) §8). **Two hardware conventions the doc argues TIDE should deliberately NOT copy:** multiples (software fan-out makes them redundant — *verify SynthEdit permits one-output-to-many-inputs first*) and ubiquitous standalone attenuverters (unnecessary if `Mod A`/`Mod B` lands). **Do not treat the list as agreed** — it is input to **E2**, which owns the curated set. **Scope** doc only today. **Accept** E2 starts from a list somebody has ruled on, rather than from the two wrong arms of `initialise_synthedit_modules`. **Size** the doc exists; the ruling is minutes, the building is E2. |
| E17 | RESOLVED | any | **RESOLVED 2026-08-20, in a THIRD direction neither option below named: TIDE Rack's panels and fittings are PATH TRACED (`tide_render`) into a cached bitmap, with moving parts drawn over in vector.** The (a)-vs-(b) crux — Jeff wanted realism, (a) banned shadows — dissolves rather than being adjudicated: the realism is *simulated*, so shadows and highlights are outputs of a light transport rather than decisions. **(a) is rescoped, not discarded** — it governs the vector overlay, where its bans still stand. Ruled in session; artefact ([modules/TiDEPanel](modules/TiDEPanel)) came before the ruling. See [docs/panel-design-language.md](docs/panel-design-language.md) and [docs/decisions.md](docs/decisions.md). **Open under the new language:** knob sizes are below VCV's library and real hardware, and one rack unit is 3.2 HP rather than a whole HP. *The original undecided text follows, kept for the reasoning it records.* **TIDE's visual design language is undecided, TWO incompatible proposals now exist, and every module panel authored before it is settled is rework.** **Found while committing this row, 2026-08-18: [docs/ui-design-language.md](docs/ui-design-language.md) already exists as an uncommitted working-tree proposal from another session** (plus three generated `docs/images/ui-language-*.svg`). It was NOT written by the run that filed this row, is on no branch and has no commit history, and **it was deliberately left untouched**. **Jeff's status for it, given 2026-08-18: *"we're spit-balling with the marathon stuff"* — it is exploratory, and this row treats the two proposals as PEERS rather than as a baseline and a comment.** The Marathon document is worked out in more detail; **detail is not validation**, and the Blocks-derived proposal is the one anchored to the brief Jeff actually gave and to five shipping UIs in this category. **The two conflict on exactly the point Jeff raised.** **(a) The Marathon/Designers-Republic language** ([docs/ui-design-language.md](docs/ui-design-language.md)): true-black ground, flat `#1C1C1C` panels where the tone step *is* the depth cue, 1px butt-cap seams, acid `#C0FE04` used **only for live state**, per-family livery hues, knobs as arcs, right-angled polyline cables, stepped edges as the signature device — and **"banned outright: gradients as decoration, drop shadows, blur, glow, bevels"**. Its strongest argument is practical: it is a pure 2D language drawable in GMPI code from plain rects, lines and arcs with **no bitmap assets**, which suits constraint 8 and the sandbox, and its own example SVGs double as a feasibility proof. **Its standing risk, which the document itself raises and then guardrails against: the source language's own shipped UI was widely panned for legibility (players coined "fontslop"), and the failure mode named was density on a surface people read continuously — which is exactly what a plugin is.** That caution is not disqualifying, but it is the thing to test before adopting, and it does not apply to (b). **(b) The Blocks-derived language** ([docs/competitive-review.md](docs/competitive-review.md) §5), from five competitor UIs **viewed directly as screenshots, not recalled**: flat matte panels carrying **dimensional controls — top-lit gradient, faint sheen, and a soft low-opacity contact shadow reading as ambient occlusion** — with hue encoding signal type, cables thin/behind/hideable, and inline dataviz. **The crux, stated plainly: Jeff's brief said he likes Reaktor Blocks because it is "abstract but with some tasteful realism like shadows". Proposal (a) bans shadows. Both cannot be satisfied and this is not an agent's call.** **It wants a `PROPOSED:` entry in [docs/decisions.md](docs/decisions.md).** `PROPOSED: which visual design language does TIDE Rack adopt? Options: (a) the Marathon/TDR language in docs/ui-design-language.md, flat with no shadows; (b) the Blocks-derived language in competitive-review §5, flat panels with dimensional controls and soft contact shadows; (c) a synthesis led by (b) — (b)'s material direction and signal-type colour, harvesting from (a) everything that does not conflict: the no-bitmap drawability discipline, 1px butt-cap geometry, a deliberately restricted palette, and accent-as-state rather than accent-as-decoration. Recommended default: (c). (b) supplies the direction because it is what Jeff named liking and it is validated by five shipping UIs in this category rather than imported from a game whose own UI was panned for legibility; (a) supplies craft discipline (b) does not attempt, and a contact shadow is a few alpha steps rather than a bitmap, so nothing in (a)'s drawability argument is given up by admitting it. Default in effect meanwhile: none — TIDE renders SynthEdit's developer UI. May proceed meanwhile: E2a's three prefabs, whose panels are throwaway under any option. Decide-by: before E2 authors the curated set.` **Whoever takes this reads both in full first.** [docs/ui-design-language.md](docs/ui-design-language.md) is the more specified on palette, geometry, type and density — real gaps in §5 that any adopted language has to close, whichever direction wins. [docs/competitive-review.md](docs/competitive-review.md) §5 is the one carrying the evidence: five shipping competitor UIs viewed directly, the density failure that sinks dense cabled racks, and the authoring-cost argument for a small team across 30–60 modules. **The characterisation Jeff's preference needs to be implementable:** in Blocks the realism is **not in the panel** (flat, matte, ~`#f0f0ee`, no screws, no texture, no bevel) but **entirely in the controls** — grey cylinder, subtle top-lit vertical gradient, faint specular sheen, and a **soft low-opacity contact shadow** that reads as ambient occlusion rather than a cast shadow. **The most useful negative result in the survey:** Voltage Modular proves **thin cables do not save a dense rack** — its panels are unreadable under the patch — which argues for hideable cables plus the Grid's show-modulation-at-the-destination model. **An anchor already exists:** the website's accent `#1c6e8c` light / `#6ec2dc` dark on `#101216` is both tide-coloured and close to Blocks' cyan. **Scope** a decisions.md entry, then a spec doc. **Accept** a ruling exists that E2 can author panels against. **Size** the survey is done; the ruling is Jeff's. **STATUS CORRECTION, 2026-08-18 (later session, Jeff directing):** the "uncommitted, on no branch, no commit history" description above is no longer accurate — [docs/ui-design-language.md](docs/ui-design-language.md) and its now-**four** generated SVGs are committed on `tide/win/competitive-review`, together with [scripts/gen-ui-language-svgs.py](scripts/gen-ui-language-svgs.py) which regenerates them. **This does not decide E17** — it commits proposal (a) so it can be read and diffed, and E17 remains NEEDS-JEFF. **One detail above is now stale in a way that matters to the crux:** (a) is described as having "right-angled polyline cables", but Jeff directed the opposite that session — cables are now **fat, curved, physical objects with round plug heads**, an explicit move toward the grounded realism the row attributes to (b). Shadows are still banned in (a), so the conflict stands, but it is narrower than "(a) refuses all realism". Also added under Jeff's direction: plate text, quarter-turn labels, corner-crosshair grouping in place of section borders, a 70%-size unit tier, and a lifted label ink for long-session legibility. **Read (a) as it now stands, not as this row describes it.** |

---

## Release & distribution — UNBLOCKED 2026-08-21 (V1 closed 08-18, C7 closed 08-21)

The plan is [docs/distribution.md](docs/distribution.md). R1 is the only item
with lead time and the only one Jeff can do now; the rest wait for a v0.1 that
passes V1. CI automation additionally waits on C7 — the interim path (build on
each box, `gh release upload`) is in the doc.

| ID | Status | Plat | Item |
|---|---|---|---|
| R1 | RESOLVED | — | **Signing identities and accounts.** **(a) Decided 2026-08-13 by Jeff: TIDE ships under the existing `SynthEdit Limited` identity, deliberately — a second Azure certificate profile is not affordable right now.** So both platforms sign under `SynthEdit Limited`/`SynthEditCertificateProfile` as already configured; no new credential, no R7-style hardening needed for a profile that doesn't exist. Compensating branding decided the same day: **"TIDE Synth — by SynthEdit Ltd"**, placed subtly (about pane, footer, or installer credit — not the plugin name itself, constraints 1 and 5 still rule out anything dialog- or splash-shaped). Placement is unscoped — filed as **D2**. (b) **Confirmed** — Apple Developer ID is `Developer ID Application: SynthEdit Limited (36SNPLRFK3)`, and notarization runs on the app-specific-password route (`APPLE_ID` / `APPLE_ID_PASSWORD` / `APPLE_TEAM_ID`), matching `SynthEdit_cmake_mac.yml:223-244` so the recipe ports without rewriting `notarytool`. An App Store Connect API key would be scoped and revocable where the app-specific password is tied to Jeff's Apple ID and grants far more than notarization — worth switching before anything consumes it, not urgent while nothing does. (c) App Store listing for the iOS AUv3 (needed by M2, not v0.1) — **not started.** (d) **Done early, ahead of C7** — 8 Actions secrets and 4 variables now exist on the public repo: secrets `AZURE_TENANT_ID`, `AZURE_CLIENT_ID`, `AZURE_CLIENT_SECRET`, `APPLE_CERT_P12_BASE64`, `APPLE_CERT_PASSWORD`, `APPLE_ID`, `APPLE_ID_PASSWORD`, `APPLE_TEAM_ID`; variables `AZURE_CODESIGN_ENDPOINT`, `AZURE_CODESIGN_ACCOUNT`, `AZURE_CODESIGN_PROFILE`, `APPLE_SIGNING_IDENTITY`. The non-secret half is deliberately in *variables* — those values are on every binary TIDE ships, and hiding them only makes failures harder to read. Nothing consumes any of them yet. **Two follow-ups, filed as R7.** Detail on the identities, and why the non-secret half is deliberately in variables: [docs/r1-signing-identities.md](docs/r1-signing-identities.md). |
| R7 | TODO | any | **Harden the signing credentials now that they exist (follow-up to R1(d)).** Two things, neither urgent while no workflow consumes them. **(1) The exposure is same-repo branches, not fork PRs.** GitHub withholds secrets from fork-triggered `pull_request` runs, so `build.yml`'s bare `pull_request:` trigger is safe. But it also runs on `push: branches: ["main", "tide/**"]`, and same-repo runs get full secret access — and pushing `tide/**` branches is exactly what the weekly agents do. So today a workflow edit on any agent branch would execute with read access to all 8 credentials, ungated. Fix: create a `release` environment with Jeff as a required reviewer, move the secrets into it (`gh secret set <NAME> --env release`), delete the repo-level copies. Only a job declaring `environment: release` can then reach them, and it pauses for approval. **(2) Drop the two long-lived credentials.** `AZURE_CLIENT_SECRET` → OIDC federated credentials scoped to `repo:JeffMcClintock/TideSynth:ref:refs/tags/*`, which removes the stored secret entirely *and* means even an approved non-tag run cannot sign; `azure/trusted-signing-action@v0` takes the same inputs as the `ArtifactSigning@1` task either way. `APPLE_ID_PASSWORD` → App Store Connect API key (`.p8` base64 + key id + issuer id). Do this before **R5** wires anything up, or R5 will have to be rewritten. |
| R8 | TODO | mac | **No plugin bundle TIDE ships has a `CFBundleIdentifier`, and `codesign` papers over it with a synthesised one.** Found 2026-08-22 (macos) while checking whether CLAP (R4a) had a plist problem — it does not, it has the *same* problem as everything else, which is what makes this pre-existing rather than a regression. **Measured on all four bundles from a clean Release build, and the accurate wording matters:** `TIDE-Rack.clap`, `TIDE-Rack.vst3` and `TIDE-Rack.gmpi` each have a `CFBundleIdentifier` key that is **present and EMPTY** (`<string></string>`) — not absent. Only `TIDE-Rack.app` has a real one (`com.gmpi.standalone.TIDE_Rack`). **One cause explains both symptoms:** the plugin formats fall through to CMake's stock `MacOSXBundleInfo.plist.in`, and `MACOSX_BUNDLE_GUI_IDENTIFIER` is only ever set on the STANDALONE branch (`GMPI/gmpi_plugin.cmake:775`), so the template's substitution yields an empty string — and the same stock template is why all four also declare `CFBundlePackageType=APPL`, the *application* type, rather than `BNDL`. **So the fix is one property per format, not a new plist.** **What actually happens, tested rather than predicted:** `codesign --force --sign -` SUCCEEDS and invents an identifier from the executable name plus a hash — observed as `TIDE-Rack-555549444341029c5cd537c59001e63d20c200d3`. So this does not block signing outright. **Why it still matters, and to whom:** that identifier is the unit Gatekeeper, notarization tickets and any future update or uninstall logic key on, and a name-plus-hash string matches no convention TIDE controls. **This is R5's to hit** — R5 wires notarization, and `notarytool` is stricter than `codesign`. **Do not fix blind.** The Accept is: decide the identifier scheme (`com.synthedit.tiderack.vst3` / `.clap` / `.gmpi` is the obvious shape, but the publisher is *SynthEdit Limited* per R1, so it is a naming decision Jeff owns), then set `MACOSX_BUNDLE_GUI_IDENTIFIER` per format. **The plumbing is GMPI's, and PR-GATED** — `:774` sets it for STANDALONE only, so extending it to the plugin formats is a GMPI change. **NOT verified:** whether `notarytool` actually rejects a synthesised identifier. That is one submission away and R5 will find out; this row exists so R5 is not surprised by it. |
| R9 | TODO | any | **TIDE Rack's VST3 class GUID, its CLAP id and its GMPI id are all derived from `id="SE SynthEdit"` — a generic SynthEdit identifier that TIDE does not own and cannot keep unique. Found 2026-08-22 (macos) by loading the shipped `.clap` in a real CLAP host for the first time (E9/R4a), branch `tide/mac/E9-clap-host-verify`.** **The rename stopped one field short.** [SynthEditSem/SynthEdit.cpp](SynthEditSem/SynthEdit.cpp) declares `<Plugin id="SE SynthEdit" name="TIDE Rack" vendor="TIDE Synth" ...>` — `name` and `vendor` were rebranded, and `id`, the only field that actually functions as identity, was not. The commented-out original two lines further down still reads `<Plugin id="SE SynthEdit" name="SynthEdit" ...>`, which is where the value came from. **This is not cosmetic, and it is proven end to end rather than argued.** `GMPI_Wrappers/wrapper/VST3/MyVstPluginFactory.cpp:200` (`textIdtoUuid`) builds the VST3 class GUID as the ASCII `"PluginGMPI "`, a `P`/`C` role byte, and **`hashString(id)`** — a djb2 hash of the id string, nothing else. `hashString("SE SynthEdit")` = `0x43ED5119`, whose little-endian bytes are **`1951ED43`** — **exactly the trailing four bytes of the GUID in the committed fixture** `1386065673{506C7567696E474D504920501951ED43}` ([tests/hosts/v1-rack.rpp](tests/hosts/v1-rack.rpp)). So the shipped artifact's identity is a pure function of that string. `Factory_CLAP.cpp:25` assigns the same string straight to `clap_descriptor.id`, and GMPI uses it as the id directly. **Measured, not inferred:** a CLAP host handshake reports `id="SE SynthEdit" name="TIDE Rack" vendor="TIDE Synth"` — see [tests/e9_clap_rate_probe.c](tests/e9_clap_rate_probe.c). **What breaks:** any other GMPI plugin declaring that id gets the SAME VST3 GUID and the SAME CLAP id, so a host with both installed cannot tell them apart and a saved project can reload the wrong one. Uniqueness currently depends on nobody else using SynthEdit's own shell id — which is not a property TIDE controls. **WHY THE TIMING MATTERS AND WHY THIS IS NOT A QUIET FIX:** the GUID is a pure function of the id, so **changing the id changes the VST3 GUID**, and every project already saved with TIDE Rack stops finding the plugin. That cost is near zero today and permanent after 1.0 — R2, R3 and R4 are building installers for these exact artifacts right now. **This is Jeff's decision and it should be made before the first release, not after.** **Related but distinct: R8** is the missing/empty `CFBundleIdentifier`, the macOS-bundle half of the same "identity was never set for the shipped product" problem; the two want deciding together since both need a naming scheme. **Scope** the `<Plugin id=...>` string in `SynthEditSem/SynthEdit.cpp`, TIDE's own and ALLOWED — the change is one string; the decision and the migration story are the work. **Accept** the VST3 GUID, CLAP id and GMPI id all derive from an identifier TIDE owns, and the choice is recorded in `docs/decisions.md` with what happens to projects saved under the old GUID. **NOT verified:** whether any SynthEdit build currently ships a GMPI or VST3 plugin declaring `SE SynthEdit` — the collision mechanism is proven, the live collision is not; check before deciding how urgent the migration is. **Size** minutes to change, one decision to make. |
| R2 | DONE | win | **DONE — [#276](https://github.com/JeffMcClintock/TideSynth/pull/276) merged; flipped 2026-08-22 (macos) as STEP 4 bookkeeping, PR state verified rather than assumed.** **Previous text follows.** **BUILT AND MEASURED 2026-08-22 (windows, scheduled run), branch `tide/win/R2-windows-installer`, [#276](https://github.com/JeffMcClintock/TideSynth/pull/276). Both assets exist: `TIDE-Rack-Windows.exe` (3.0 MB) and `TIDE-Rack-Windows.zip` (1.4 MB), from `installer/windows/TIDE-Rack.iss` + `scripts/package-windows.ps1`.** **THE SUBSTANTIVE DECISION IS THAT THE PAYLOAD IS A VST3 BUNDLE, NOT THE BARE DLL THE BUILD EMITS.** `gmpi_plugin.cmake`'s Windows arm gives the VST3 target a plain `SUFFIX ".vst3"` -- macOS gets a real bundle, Linux gets one assembled by POST_BUILD, Windows gets neither. A bare DLL is a legal VST3, but it has nowhere to keep the four pin XMLs and the six rack prefabs TIDE cannot work without, and shipping those loose into the SHARED `Common Files\VST3` folder would put them beside every other vendor's plug-ins. The bundle solves it exactly and the runtime already reads one: `BundleInfo.cpp:670-684` sets `pluginIsBundle` from "a path element with an extension followed by `Contents`", and `getResourceFolder()` (`:266-271`) then returns `<bundle>\Contents\Resources\`. **Verified in three conditions on this box, not argued:** (a) the flat DLL as the build leaves it -- *"no Prefabs folder in bundle resources"* and all four XMLs *"missing"*; (b) the same binary with those resources beside it -- 4 XMLs enriched, **6 rack prefab(s) seeded**; (c) **the shipped bundle, unzipped from the asset itself** -- 4 XMLs enriched, **6 rack prefab(s) seeded**, `rack built for 48000 Hz`. (b) proves the runtime rule and (c) proves the asset obeys it; (a) is the control that makes both mean something. **The installer is proven, not merely compiled:** `-SelfTest` recompiles the SAME `.iss` with two `#define`s overridden (`Vst3Dir` -> a scratch folder, `PrivilegesLevel` -> `lowest`, so no elevation is needed) and runs it silently -- **11 files installed, every one SHA-256 identical to the payload, and uninstall removes the bundle whole.** Windows has no `installer -target <sandbox>`, which is why the destination is a compile-time define rather than a runtime hook. **Signing is deliberately NOT done and NOT claimed.** The Azure Trusted Signing block runs only when `AZURE_TENANT_ID`/`AZURE_CLIENT_ID`/`AZURE_CLIENT_SECRET` are set, and it **refuses to proceed** rather than silently skip if credentials are present without `TRUSTED_SIGNING_DLIB`. It is **unverified**: `ArtifactSigning@1` is an Azure Pipelines task with no local or GitHub Actions equivalent, so this drives `signtool.exe` with the Azure Code Signing dlib instead -- the documented stand-in, which nobody has run. **R5 owns the secret store and should treat that block as a starting point to test.** **Found while packaging and filed as [S36](#), not fixed here:** on Windows the build stages those same resources one directory ABOVE the binary, so a developer's own build has an empty module browser. That is condition (a) above. **Also needed and recorded:** `cmake -S . -B build` picks the VS **BuildTools** instance on this box, which has no MFC, and the build dies on `afxres.h` in two `SynthEditLib/EditorLib` files. `-DCMAKE_GENERATOR_INSTANCE="C:/Program Files/Microsoft Visual Studio/18/Community"` fixes it; `docs/building.md` records that flag only for the old SE16 path. **Previous text follows.** **UNBLOCKED 2026-08-21 (macos, [#270](https://github.com/JeffMcClintock/TideSynth/pull/270)) — BOTH stated blockers cleared and nobody had noticed.** This section's header read *"blocked on V1 (nothing to ship yet)"*; **V1 closed 2026-08-18** with every clause measured. **C7 closed 2026-08-21** — the carve-out is complete and all three platforms build from a clean public clone. **And there is now something to ship**: N1a renamed the artifacts to their shipped forms, so `TIDE-Rack.vst3` / `.gmpi` / `.app` exist under the names [docs/distribution.md](docs/distribution.md) specifies, verified in REAPER. R1 is RESOLVED, so signing identities are settled. **Nothing blocks this row**: the Windows artefact builds, `TIDE-Rack.vst3` is the name Inno Setup installs, and signing is R1's already-configured `SynthEdit Limited` profile. **Platform `win`** — the windows box takes it. **Previous text follows.** **Windows installer.** Inno Setup, modelled on `SE16/SynthEdit2/installer/SynthEdit2.iss`. Installs `TIDE-Rack.vst3` to `Common Files\VST3`; sign the .vst3 and the installer via Azure Trusted Signing. Also produce `TIDE-Rack-Windows.zip` for users who refuse installers. Installer title and shortcuts use the display form **TIDE Rack**; every filename uses **TIDE-Rack** — see the naming table in [docs/distribution.md](docs/distribution.md). Constant asset names — the version lives in the tag and the version resource, per the doc. |
| R3 | DONE | mac | **DONE — [#272](https://github.com/JeffMcClintock/TideSynth/pull/272) merged; flipped 2026-08-22 (macos) as STEP 4 bookkeeping.** `scripts/package-macos.sh` builds `TIDE-Rack-macOS.pkg`, and `hostArchitectures` is now derived from `lipo` rather than taken from `productbuild`, which would have offered an arm64-only payload to Intel machines. **Still open around it:** **R3a** (the AU half) is `BLOCKED(M1)`; **R5** wires signing and notarization and is a `.github/workflows/**` file, so Jeff pushes it; **R8** is the missing bundle identifier that notarization will meet. **Previous text follows.** **THE VST3 HALF IS BUILT AND VERIFIED 2026-08-21 (macos), branch `tide/mac/R3-macos-pkg` ([#272](https://github.com/JeffMcClintock/TideSynth/pull/272)): [scripts/package-macos.sh](scripts/package-macos.sh) produces `TIDE-Rack-macOS.pkg`.** **This row's AU half CANNOT be done and is split out as R3a** — the row says *"AU → `Components`"*, but `SynthEditSem/CMakeLists.txt` sets `FORMATS_LIST GMPI VST3 STANDALONE`: **there is no AU target**, and **M1**, the row that would add one, is BLOCKED. Rather than ship a pkg that silently omits half its stated payload, the script fails if asked for an AU that is not there. **Verified by building and inspecting the artefact, not by reading pkgbuild's output:** payload installs to `./Library/Audio/Plug-Ins/VST3/TIDE-Rack.vst3` exactly as [docs/distribution.md](docs/distribution.md) specifies, and a real `installer` run into a sandbox target reported *"The install was successful"* placing a binary byte-identical to the build. **One live defect found and fixed in the process, and it is this row's own stated worry made real:** `productbuild` writes `hostArchitectures="x86_64,arm64"` **regardless of the payload**, so the arm64-only pkg would have installed happily on an Intel Mac and then failed to load with nothing explaining why. The script now **derives `hostArchitectures` from `lipo` on the built binary** (verified `arm64` in the shipped pkg), so macOS itself refuses the wrong hardware — and it stays correct if the ARM-only ruling is revisited. **NOT signed and NOT notarized, deliberately rather than unfinished:** signing runs only when `APPLE_CERTIFICATE_SIGNING_IDENTITY` / `APPLE_INSTALLER_SIGNING_IDENTITY` are in the environment, and the script says loudly which of the two it produced. Notarization (`notarytool` + `stapler`, modelled on `SynthEdit_cmake_mac.yml:223-244`) belongs to **R5**, which owns the secret store. **An unsigned pkg is not shippable** and the script prints exactly that. **Previous text follows.** **UNBLOCKED 2026-08-21 (macos, [#270](https://github.com/JeffMcClintock/TideSynth/pull/270)) — BOTH stated blockers cleared and nobody had noticed.** This section's header read *"blocked on V1 (nothing to ship yet)"*; **V1 closed 2026-08-18** with every clause measured. **C7 closed 2026-08-21** — the carve-out is complete and all three platforms build from a clean public clone. **And there is now something to ship**: N1a renamed the artifacts to their shipped forms, so `TIDE-Rack.vst3` / `.gmpi` / `.app` exist under the names [docs/distribution.md](docs/distribution.md) specifies, verified in REAPER. R1 is RESOLVED, so signing identities are settled. **Nothing blocks this row.** Note its own ARM-only correction above still applies: the artefact is `arm64` single-slice, so the pkg wants a minimum-hardware note. **Platform `mac`.** **Previous text follows.** **ARM-ONLY AS OF 2026-08-21 (Jeff's ruling, see [docs/decisions.md](docs/decisions.md)): the macOS artefact this row packages is `arm64` single-slice, not universal.** Measured: universal 160 s → arm64 79 s, and `lipo -archs` on all three artefacts reports `arm64` alone. **Two consequences for the packaging this row owns:** the pkg will not run on an Intel Mac **and nothing tells the user why** — the plugin simply fails to load — so if a minimum-hardware note belongs anywhere it is here; and any release-asset naming or notarization step that assumed a universal binary should be re-read before it ships. **Previous text follows.** **macOS pkg.** AU → `Components`, VST3 → `VST3`; Developer ID sign, **notarize and staple** — model on `SE16/SynthEdit_cmake_mac.yml`. AUv3 is deliberately absent: it ships in the container app with M2. |
| R3a | BLOCKED(M1) | mac | **The AU half of the macOS pkg — split out of R3 2026-08-21 (macos) because it cannot be built.** R3 specifies *"AU → `/Library/Audio/Plug-Ins/Components/`"* and [docs/distribution.md](docs/distribution.md)'s macOS row says the same, but **TIDE builds no AU**: `SynthEditSem/CMakeLists.txt` sets `FORMATS_LIST GMPI VST3 STANDALONE`, and **M1** (*"AU + AUv3 targets building on macOS"*) is BLOCKED. **So this is blocked on a row, not on effort** — and naming the blocker is the point, because R3 as written read like one job and is two. **Scope** add `AU` to `FORMATS_LIST` once M1 lands, then one `pkgbuild` component installing to `Components/`; [scripts/package-macos.sh](scripts/package-macos.sh) already has the shape and deliberately fails rather than silently omitting it. **Accept** `TIDE-Rack-macOS.pkg` carries both plug-ins, each landing in its documented directory, verified with `pkgutil --payload-files`. **Size** small once M1 is done. |
| R4 | TODO | linux | **SPLIT 2026-08-22 (macos). The CLAP half is done and lives in R4a; this row is now the Linux tarball ALONE, and it stays on the `linux` box because its deliverable cannot be built or verified anywhere else.** **What changed:** the Accept says the tarball carries *"the VST3 and CLAP bundles"*, and **no platform was building a CLAP at all** — `CLAP` was simply absent from `SynthEditSem/CMakeLists.txt`'s `FORMATS_LIST`. That is shared work serving R2, R3 and R4 equally, so it is not this row's to carry; R4a does it and is verified on macOS. **What is left here:** `TIDE-Rack-Linux.tar.gz` containing `TIDE-Rack.vst3/` and `TIDE-Rack.clap/`, plus an `install.sh` copying them to `~/.vst3` / `~/.clap`. No signing. **Take R4a first — without it there is no `.clap` to put in the tarball.** **Also read [#271](https://github.com/JeffMcClintock/TideSynth/issues/271) before packaging:** until [GMPI#6](https://github.com/JeffMcClintock/GMPI/pull/6) and [#274](https://github.com/JeffMcClintock/TideSynth/pull/274) both land, the Linux VST3 bundle directory and the `.so` inside it disagree, so a tarball built today would ship something no host loads. **Previous text follows.** **UNBLOCKED 2026-08-21 (macos, [#270](https://github.com/JeffMcClintock/TideSynth/pull/270)) — BOTH stated blockers cleared and nobody had noticed.** This section's header read *"blocked on V1 (nothing to ship yet)"*; **V1 closed 2026-08-18** with every clause measured. **C7 closed 2026-08-21** — the carve-out is complete and all three platforms build from a clean public clone. **And there is now something to ship**: N1a renamed the artifacts to their shipped forms, so `TIDE-Rack.vst3` / `.gmpi` / `.app` exist under the names [docs/distribution.md](docs/distribution.md) specifies, verified in REAPER. R1 is RESOLVED, so signing identities are settled. **Nothing blocks this row** — Linux builds green in CI as of #250, and needs no signing. **Platform `linux`.** **Previous text follows.** **Linux tarball.** `TIDE-Rack-Linux.tar.gz` with the VST3 and CLAP bundles and a short `install.sh` targeting `~/.vst3` / `~/.clap`. No signing. |
| R4a | DONE | any | **DONE — [#275](https://github.com/JeffMcClintock/TideSynth/pull/275) merged; flipped 2026-08-22 (macos) as STEP 4 bookkeeping.** CLAP is in `FORMATS_LIST` on every platform, so **R2, R3 and R4 all have the `.clap` their Accept clauses name.** **Previous text follows.** **CLAP was in no platform's build. Enabled and measured 2026-08-22 (macos, scheduled run), branch `tide/mac/R4-clap-and-tarball`.** Split out of R4 because it serves R2, R3 and R4 equally — every installer row's Accept names the CLAP bundle, and [docs/distribution.md](docs/distribution.md) lists CLAP as a shipped format on all three platforms, but `FORMATS_LIST` in [SynthEditSem/CMakeLists.txt](SynthEditSem/CMakeLists.txt) read `GMPI VST3 STANDALONE`. **It cost one word.** GMPI already had the CLAP wrapper wired and the format inherits N1a's `OUTPUT_NAME` for free. **Measured on the artefact, not the build log:** `TIDE-Rack.clap` builds rc=0 with 0 error lines, correct bundle layout, `Resources/Prefabs` staged, arm64, and `nm -gU` finds exactly one exported entry point (`clap_entry`). **A suspect eliminated while here:** the CLAP bundle has no `CFBundleIdentifier` — but neither do `TIDE-Rack.vst3` or `TIDE-Rack.gmpi` on `main`, so this is pre-existing and NOT a CLAP regression. Filed separately as R8, because it is R5's problem. **NOT verified:** no CLAP host on this box, so the plugin has never been loaded — only its layout and entry point checked. Windows and Linux are unbuilt here; the change is one word in a shared list, so CI is the check. |
| R5 | TODO | any | **UNBLOCKED 2026-08-21 (macos, [#270](https://github.com/JeffMcClintock/TideSynth/pull/270)) — BOTH stated blockers cleared and nobody had noticed.** This section's header read *"blocked on V1 (nothing to ship yet)"*; **V1 closed 2026-08-18** with every clause measured. **C7 closed 2026-08-21** — the carve-out is complete and all three platforms build from a clean public clone. **And there is now something to ship**: N1a renamed the artifacts to their shipped forms, so `TIDE-Rack.vst3` / `.gmpi` / `.app` exist under the names [docs/distribution.md](docs/distribution.md) specifies, verified in REAPER. R1 is RESOLVED, so signing identities are settled. **Its own named blocker is gone**: this row said *"Needs C7 for public CI builds"*, and **C7 is DONE**. **One thing a scheduled run still cannot do**: R5 is a `.github/workflows/**` file and the fleet token carries no `workflow` scope by design, so the workflow itself must be pushed by Jeff — the same constraint that held S29 and #250. A run can author and verify it; it cannot push it. **Previous text follows.** **Release workflow.** Tag `v*` → build + sign every platform → one GitHub Release with constant-name assets + `SHA256SUMS.txt`. Runs on tag push only, never on PRs, so fork PRs never see signing secrets. Needs C7 for public CI builds; until then the same release page is fed by `gh release upload` from the platform boxes. |
| R6 | BLOCKED(R5) | any | **RE-BLOCKED ON A NAMED ROW 2026-08-21 (macos), which is strictly better information than the blanket `BLOCKED` it carried.** The section's stale *"blocked on V1"* is void — V1 closed 2026-08-18 — but this row is **not** free, and unblocking it with its siblings would have been wrong: it replaces the honest *"nothing to download yet"* card with `releases/latest/download/<asset>` permalinks, and **those 404 until a release actually exists**. So it waits on **R5** (or on a first manual `gh release upload`), not on V1. Shipping dead links on a public page is worse than the card it replaces. **Previous text follows.** **Website Downloads section.** Replace the "nothing to download yet" card with static `releases/latest/download/<asset>` permalinks — no JS, no version maintenance, keeps the "no cookies, no scripts, no tracking" footer honest. iOS is a plain text App Store link (Apple's badge image would be the page's first external resource — do not use it). |
**The publisher name TIDE ships under is SynthEdit Limited — decided, not
inherited, as of R1(a) 2026-08-13.** `APPLE_SIGNING_IDENTITY` is
`Developer ID Application: SynthEdit Limited (36SNPLRFK3)`, and
`SynthEditCertificateProfile` almost certainly carries the same subject on
Windows. So a user installing TIDE sees *SynthEdit Limited* in the macOS
Gatekeeper prompt and the Windows UAC dialog — deliberately: a second Azure
certificate profile is not affordable right now, and PR #14's website
stripped every SynthEdit mention so TIDE stands on its own there, which is
why a compensating credit was filed as **D2** — "TIDE Synth — by SynthEdit
Ltd", placed on TIDE's own terms rather than only surfacing in the OS
install dialog.

---

## Done

Landed rows live in **[BACKLOG-DONE.md](BACKLOG-DONE.md)** — moved there by A8,
2026-08-12. Look there for precedent: how a past stage was verified, what a
previous run measured.

**As part of STEP 4:** an `IN-REVIEW` row whose PRs have all merged becomes
`DONE` and moves to that file, with the merge date in the `Done` column. Move
the row verbatim; do not rewrite it on the way out.
