# TIDE Synth — Backlog

Ordered. A weekly agent obeys the **NEXT block below first**; only when its
platform's NEXT item is ineligible does it fall back to the topmost item that
is not blocked and matches its machine's platform role. One item, then stop.

## NEXT — per-platform priority, obeyed before file order

| Platform | Take | Why |
|---|---|---|
| win | **RE-POINTED 2026-08-28 (windows, Jeff directing). E48 IS DONE — the three prefab modules ship (its ruling is in docs/decisions.md), quiet mode and `--dialogs` work on Windows (E51's argv fixes, GMPI_Wrappers#29), and a five-prefab rack save/reloads with zero losses. **TAKE **E19**’s windows VST3 cell** — it has been the standing goal since 2026-08-26 and every blocker it ever named is now closed: E27/E29 closed earlier, and “no prepared rack reloads reliably” ended with E48. A prepared rack now restores headlessly, `-quiet` keeps dialogs off screen, and `--dialogs` reports what a host session would have swallowed. **E53 is TAKEN** (open PR #516 from this box's concurrent run, plus the macOS diagnosis in #523 — its fix is GATED and filed). **E56** is filed, GATED, waiting on a ruling. **The one clause below still worth keeping: STEP 1’s feed is STRUCTURALLY EMPTY on this platform** — `build.yml:409` excludes `matrix.platform == 'win'` from filing platform issues, so an empty `gh issue list --label platform:win` has verified nothing; read the branch’s latest run instead. **Older generations of this cell (the E50/E48 investigation, the 2026-08-25 build-break saga) are pruned rather than carried: each is told in full in JOURNAL.md / JOURNAL-2026-08.md under its date, and `check-next-block.py` rightly refuses take-phrases naming rows that have since archived.** |
| mac | **RE-POINTED 2026-09-01 (macos, scheduled run). THE RUN WAS ALL STEP 1.5, AND THE NEXT MAC RUN SHOULD DO STEP 1.5 FIRST TOO.** [#565](https://github.com/JeffMcClintock/TideSynth/pull/565) (E73) had gone **CONFLICTING** overnight while showing 15/15 green checks and zero reviews -- so it read as *"waiting on Jeff"* under STEP 1.5's literal list of three, and was not. **Third occurrence in five days across two boxes; `mergeStateStatus` is one extra field on the `gh pr view` STEP 1.5 already makes you run.** **MERGED 2026-09-01** as [`b824422`](https://github.com/JeffMcClintock/TideSynth/commit/b824422) (Jeff, interactive: *"merge 565"*), 6 checks green, branch deleted; **E73 is DONE and archived.** `tide/mac/E65-panel-draft-render` was deleted the same session at Jeff's instruction -- its probe was already salvaged onto `main` and its fix superseded by `b4bd4f49f`, and the commits survive at `refs/pull/559/head` if anyone needs them. **There are now NO `tide/*` branches and NO open PRs in any of the six repos.** **THE THING TO CHECK BEFORE YOU FILE ANY ROW: `BACKLOG.md` MERGED CLEANLY INTO TWO DIFFERENT `E74`s.** The mac finding (E59's refusal not firing in a hosted AUv3) and the linux one ([#566](https://github.com/JeffMcClintock/TideSynth/pull/566), a hosted VST3 recreating the DSP instance) were filed about four hours apart from branches cut off the same `main`, landed in different hunks, and git reported no conflict at all. **The mac row is renumbered **E77**** -- it moved because the linux E74 had landed and is cited by the `linux` cell, E19 and two journal entries, while E77 existed only on this branch. **Second id collision in two days** (E72 was E70 on 2026-08-31), and **STEP 3's pre-filing grep cannot prevent this shape**: at filing time neither row existed for the other to find. Grep the ids after any bookkeeping merge -- `check-id-refs.py` passes on the merged file. **THE MAC LANE IS BLOCKED FOR AN UNATTENDED RUN, AND IT IS ONE CONSTRAINT RATHER THAN NINE.** The screen was **LOCKED** this run (`ioreg -n Root -d1 -a` -> `CGSSessionScreenIsLocked true`; note its ABSENCE means unlocked, which is the cheaper reading). **E71, E77, E19's mac AU3 cell and E75 all want the SAME single unlocked interactive session with a GUI host** -- that is now four rows on one blocker and the strongest argument yet for scheduling one. The rest of the walk, in file order so nobody repeats it: **S8** GATED, **E7** turns on an unruled question, **E2** not takeable by its own row, **E72** GATED and wants a ruling, **E74** is linux's and needs a hosted VST3 on screen, **E76** is linux-specific despite its `any` platform and its Accept turns on a ruling. **A DATUM NOBODY HAD: `SynthEditLib` `dcdfa6b` + `GMPI_Wrappers` `017bb22` (both current `origin/main`) compile into TIDE on macOS** -- **599/599, 0 errors**, fresh Release/arm64, `SE_LOCAL_BUILD=OFF`, `TIDE_VCV_FUNDAMENTAL=OFF`. **Previous cell follows.** **RE-POINTED 2026-08-31 (macos, scheduled run, later). THE QUEUE WAS NOT BLOCKED — **E69** was filed this morning, was still unclaimed, and its part (1) turned out to be a REGRESSION rather than a gap.** Read E69's row for the numbers; the short version is that [GMPI_Wrappers#36](https://github.com/JeffMcClintock/GMPI_Wrappers/pull/36) makes a CLAP save write an EMPTY document after a load (85 bytes, 0 cables, where the previous commit wrote 18,933 bytes and 4 cables), and [#37](https://github.com/JeffMcClintock/GMPI_Wrappers/pull/37) fixes it. **WHAT THE NEXT MAC RUN SHOULD KNOW BEFORE ANYTHING ELSE:** a row filed today by another box may have had its code landed by an interactive session within the hour, and the row will still say TODO. #36 merged **20 minutes after** E69 was filed. So read the sibling repos' merged PRs before assuming a row's work is untouched — `gh pr list --repo JeffMcClintock/GMPI_Wrappers --state merged --limit 5` costs one command and changes what the item IS. **AND: a PR that says "CI will say" about your platform is an assignment with no addressee.** #36 shipped *"CLAP compiled and installed on Windows; AU3 is mac-only, CI will say"*; CI says a thing compiles, and this defect compiles perfectly. **THE INSTRUMENT IS THE REUSABLE PART AND IT NEEDS NO DAW AT ALL:** `tests/e69_clap_state_probe.c` drives the CLAP C ABI directly — dlopen, `clap_entry`, `create_plugin`, `clap.state` — and `Processor_CLAP`'s constructor builds the plug-in's own controller unconditionally (`Processor_CLAP.cpp:88`), so a bare host exercises the real save path in about a second. Recipe, and the two-clone `-DFETCHCONTENT_SOURCE_DIR_GMPI_WRAPPERS=` A/B that avoids touching the developer's trees, in [docs/ci/headless-gui-verification.md](docs/ci/headless-gui-verification.md); the census that survives re-serialisation is `scripts/dump_preset.py`. **The screen was UNLOCKED this run and the developer was working at the machine** — `CGSSessionScreenIsLocked` is absent from `IOConsoleUsers` when it is unlocked, which is a cheaper check than `screencapture`; no GUI was launched, and none was needed. **TAKEABLE NEXT, cheap:** **E72** (filed as E70; renumbered on a collision) is E69's part (2) and is GATED, so it wants a ruling, not a session; **E71** is a one-line AU3 restore gap that wants the same unlocked interactive session E19's mac cell has wanted since 2026-08-29. **STILL TRUE AND UNCHANGED:** E19's mac AU3 cell needs a human at an unlocked screen (build `TIDE_Rack_AU3_assemble` with `-DTIDE_VCV_FUNDAMENTAL=ON -DCMAKE_CXX_FLAGS=-DRACK_ADAPTOR_TRACE=1`, copy over `~/Applications`, launch once), **E65 closed while this run was in flight:** Jeff salvaged [#559](https://github.com/JeffMcClintock/TideSynth/pull/559)'s probe onto `main` as [`4e72af4`](https://github.com/JeffMcClintock/TideSynth/commit/4e72af4) and closed the PR, so that question is settled — see the cell below. Previous cell follows.** **CORRECTED 2026-08-31 (windows, interactive, Jeff directing): E65 IS FIXED ON `main` AND ARCHIVED -- [`b4bd4f49f`](https://github.com/JeffMcClintock/TideSynth/commit/b4bd4f49f) landed the same one-want-per-panel design directly (Jeff specifying in session; measured: previews 43/53/67 ms all drawn by 113 ms, fulls 1.9/4.2/10.4 s all drawn), so [#559](https://github.com/JeffMcClintock/TideSynth/pull/559) is SUPERSEDED as a fix and must NOT be merged as-is over it. What #559 has that `main` lacks is its test probe (`tests/e65_panel_preview_probe.py` + fixture); salvage-the-probe versus close is Jeff's call. RESOLVED later the same day, Jeff: *"salvage the probe and close 559"* -- the probe (`tests/e65_panel_preview_probe.py` + its pre-fix fixture), the `cfg=` log tokens, the `TIDE_PANEL_LOG_PATH`/TMPDIR log-path fix and the `-DTIDE_PANEL_TRACE_LOG` CMake switch are landed on `main`, verified live (3/3 previews by 67 ms, probe PASS) and by --selftest (the recorded pre-fix trace FAILS the checks, as it must); #559 CLOSED, its scheduler fix left behind as superseded. Previous cell follows.** **RE-POINTED 2026-08-31 (macos, scheduled run). THE mac/any QUEUE IS BLOCKED FOR A SCHEDULED RUN, re-walked in FILE ORDER this run rather than inherited -- the cell below is 2026-08-28 and predates E64, E65, E66 and E67 entirely.** The walk, so nobody repeats it: **S8** GATED (`SynthEditLib/UgDatabase.cpp` and `CMakeLists.txt`, and its own row says the gating change needs a ruling it does not ask for); **E19**'s mac AU3 cell needs a HUMAN and both reasons were measured on 2026-08-29 -- the screen is locked on a scheduled run and a second AUv3 cannot be registered beside the developer's; **E7** turns on Jeff's unruled *"where do the jacks live"*, which STEP 2 forbids working under; **E2** is not takeable by its own row; **E60** is linux's, on [#550](https://github.com/JeffMcClintock/TideSynth/pull/550) + GMPI_Wrappers#32; **E63** is `win`; **X1**/**X2** are `linux`. **E65 IS THIS PLATFORM'S AND IS ALREADY DONE AND OPEN** -- [#559](https://github.com/JeffMcClintock/TideSynth/pull/559) is green with no reviews and nothing unresolved, so per STEP 1.5 it is waiting on Jeff and **is not to be touched**; its branch already flips the row to IN-REVIEW, which is why this run deliberately did NOT flip it on `main`. **WHAT THIS RUN DID INSTEAD, and it was real work rather than a blocked-queue notice:** **E64, E66 and E67 are flipped DONE and archived**, and each shipped from the windows box carrying *"not verified: mac builds"* -- a line that is an assignment nothing points at. Measured here: a FRESH Release/arm64 tree over `main` builds **598/598, 0 errors**, **`SynthEditCL` builds 779/779** against the same merged `SynthEditLib` (the commercial consumer of E67's shared `ViewBase.cpp`), E64's drain diagnostic is **present in every fresh artifact and absent from both installed pre-merge bundles**, and `tests/hosts/v1-rack.rpp` still renders **-6.3 / -17.0 dBFS** through an isolated portable REAPER, its 2026-08-18 reference to the decimal. **THE PLUG-IN-PROVENANCE CONTROL IS THE PART TO REUSE:** narrow `vstpath_arm64` to a staging folder holding only your build, delete `reaper-vstplugins*.ini` to force a rescan, and then REMOVE the bundle and re-render -- REAPER hangs on an unresolvable-plug-in modal and writes no TIDE cache entry, which is what makes "the number came from MY build" a fact instead of a hope. Recipe in [docs/ci/headless-gui-verification.md](docs/ci/headless-gui-verification.md). **Also confirmed unchanged afterwards:** the developer's `~/Library/Application Support/REAPER` compares identical across **2052 files** including mtimes, and his installed VST3 is sha256-unchanged. **Previous cell follows, with its instruction retired -- read it as history.** **CORRECTION 2026-08-28 (macos, scheduled run, later): THE "TWO MAC BRANCHES WITH NO PR" CLAIM BELOW IS FALSE, AND IT HAD ALREADY TRAVELLED.** Both branches DO have PRs -- checked with `gh pr list --head <branch> --state all`, which the two runs that repeated the claim did not do: `tide/mac/icon-tide-app` is [#435](https://github.com/JeffMcClintock/TideSynth/pull/435) **MERGED**, and `tide/mac/E36-renumber-duplicate-e34` is [#445](https://github.com/JeffMcClintock/TideSynth/pull/445) **CLOSED, not merged**. So this is not the end state STEP 5 forbids; it is two leftover branches. **They need different treatment and that is why the distinction matters:** the merged one is redundant and safe to delete, while deleting the closed-not-merged one DISCARDS whatever was on it, so it wants a human who knows why #445 was closed. **The claim originated on the windows box 2026-08-27, was copied into this cell on 08-28, and was reported to Jeff twice before anyone ran the one command that checks it** -- a cell is read far more often than it is verified, so an unchecked assertion here propagates further than the same sentence in a journal entry would. **STEP 4 BOOKKEEPING THIS RUN:** E25, E45 and E54 flipped DONE and archived, every named PR confirmed MERGED by `gh pr view --json state`. **E52 was NOT flipped and is annotated on its row:** its TideSynth PR #515 is docs-only and its actual fix is [GMPI_Wrappers#28](https://github.com/JeffMcClintock/GMPI_Wrappers/pull/28), still OPEN. **The mac/any queue is otherwise blocked for a scheduled run**, re-walked and verified this run rather than inherited: S1b/S8 GATED, E38 and E51 `NEEDS-SPEC`, E19 wants a human at a DAW, E7 a ruling not a task, E2 not takeable by its own row, **E39 GATED -- confirmed by locating `TopView::renderRack` in `SynthEditLib/modules/se_sdk3_hosting/ViewBase.cpp:1518`**, E48 `NEEDS-JEFF` on a product decision, and E53 held by windows on [#516](https://github.com/JeffMcClintock/TideSynth/pull/516). Previous cell follows.** **RE-POINTED 2026-08-28 (macos, scheduled run). THE CELL BELOW IS 2026-08-25 AND PREDATES EIGHTEEN FILED ROWS -- read it as history. `main` is GREEN, there is no open `platform:mac` issue, and the mac queue is NOT empty the way that cell says.** This run took **E52** (see its row); before it, the walk in file order was: S1b/S8 wholly GATED, E38 `NEEDS-SPEC`, E19's mac cell wants AU3 in a real host (a human at a DAW), E7 a ruled non-blocker, E2 not takeable by its own row, E25 done and awaiting merge, E39 GATED with an Accept its row says to re-write, E48 a PLAN constraint 7 product decision and the `win` pick, **E51 now `NEEDS-SPEC`** (annotated by this run). **What the next mac run should do FIRST is STEP 1.5, and it is real work here:** [#513](https://github.com/JeffMcClintock/TideSynth/pull/513) (E25) and [#514](https://github.com/JeffMcClintock/TideSynth/pull/514) (E54, stacked on it) are both green with nothing unresolved, so they are waiting on Jeff rather than on a run -- **leave them alone**, per STEP 1.5's own words. **Two mac branches still sit on the remote with NO PR** -- `tide/mac/E36-renumber-duplicate-e34` and `tide/mac/icon-tide-app` -- which is the one end state STEP 5 forbids; flagged by the windows box on 2026-08-27 and still true, and whoever owns them should either raise a PR or delete them. **Takeable and cheap if you want a second item:** **E52's Windows and Linux OFF builds are unverified** and are one command each on the boxes that have those compilers. **Still true and still needs a HUMAN:** M2's iOS app has never run on a device, E9's AU result is our own probe rather than a DAW, and E19's mac cell needs both. **Previous cell follows.** **RESOLVED THE SAME SESSION — the break below is FIXED and `main` is GREEN on all three platforms. Jeff read the run's report and said *"push to main branches"*, so the missing half went in as [SynthEditLib `af42bd6`](https://github.com/JeffMcClintock/SynthEditLib/commit/af42bd6). Verified twice: the failed build run was re-run against the same TideSynth tree with nothing changed but SynthEditLib's `origin/main` — **`macos=success`, `linux=success`, `windows=success`** — and the local `cl /Zs` A/B went from `C3668` to 0 errors on the same command. [#392](https://github.com/JeffMcClintock/TideSynth/issues/392) and [#393](https://github.com/JeffMcClintock/TideSynth/issues/393) **closed themselves** on that green run, which is `build.yml`'s close-on-success step working in place; [#394](https://github.com/JeffMcClintock/TideSynth/issues/394) was closed by hand because the windows leg has no such automation. **Keep reading the account below — the diagnosis and the reasoning about why a scheduled run must NOT commit the developer's working tree are unchanged and are the durable part.** The only thing that changed is that a human was present to authorise the push.** **Re-pointed 2026-08-25 (windows, scheduled run): this cell named **S47** as its target and S47 is **DONE** — [#387](https://github.com/JeffMcClintock/TideSynth/pull/387) merged 20:52Z the same evening the cell was written. Nothing is takeable.** **There are still NO `platform: mac` TODO rows**, so this box's queue is the `any` queue, and every row left on it is blocked: **S1b** (`EditorLib/CMakeLists.txt` plus a split of `ModuleFactory_Editor.cpp`, GATED); **S8** (`SynthEditLib/CMakeLists.txt:582`, GATED, and MOOT since the Oscillator HD ruling); **E7** (an engine fact Jeff has ruled is *not* a blocker); **E2** (an umbrella whose own row says it is not takeable, because its Accept cannot be stated); **R5** (`.github/workflows/**` plus a certificate only Jeff can export); **V6/V7** (NEEDS-JEFF, which STEP 2 forbids starting); and **X1/X2/V2/E3/E4/R6** (BLOCKED). **Do STEP 1 first, and expect it to be short:** `main` is red on all three platforms from a single cause, diagnosed and measured on the windows box as [#394](https://github.com/JeffMcClintock/TideSynth/issues/394), with the root cause commented onto [#392](https://github.com/JeffMcClintock/TideSynth/issues/392). **Re-verify the error line on your own box per STEP 1 and then leave it** — it is not a mac defect and there is nothing on this platform to fix; it closes when Jeff pushes two uncommitted `SynthEditLib` files. **Do not spend the session on it.** **What this box CAN still usefully do when the queue is blocked, and the previous cell was right about it:** prove the platform is healthy with a cold clean build, and bookkeeping on verified PR state. **The mac-only work that has no row needs a HUMAN** — M2 records the iOS app installed but never launched and its Audio Unit never opened in an iOS host, and E9's AU result is from our own probe, never a DAW. |**Five consecutive days of this cell aiming at consumed work, and this time the row it named was filed and merged inside the same 24 hours.** The Take column is doing its job; the queue behind it is empty. Stated plainly so the next run learns it here rather than from an hour of reading rows.|
| linux |**RE-POINTED 2026-09-01 (linux, interactive continuation). E74 AND E78 ARE BOTH FIXED AND IN-REVIEW. TAKE **E79** -- it is a shipping defect and the biggest thing on this lane: a hosted Linux CLAP with no editor open never receives its document and plays SILENCE.** Then **E80** (E78's unmet half, and it wants a second opinion before a fix), then **E75**/**E76**. **BEFORE ANY CLAP GUI WORK, KNOW THIS: REAPER 7.43 CANNOT HOST TIDE's CLAP GUI ON THIS BOX** -- `TrackFX_Show`, float or chain, kills REAPER inside its own GTK (`gdk_screen_get_root_window: assertion 'GDK_IS_SCREEN (screen)' failed`) before `guiSetParent`. That is the host, not the plug-in, and it is why [tests/e78_clap_gui_probe.c](tests/e78_clap_gui_probe.c) exists: a minimal CLAP host with `clap_host_timer_support`, `clap_host_posix_fd_support`, a real X11 window and `process()` on a second thread. **Do not rebuild it and do not fight REAPER.** **E74 IS FIXED AND IN-REVIEW; also takeable: **E75** (cheap, unblocks two more E19 clauses) or **E76** (one wrapper, and its Accept turns on a ruling).** **THE ONE THING TO CARRY FORWARD, because it is bigger than E74:** **nothing pumped `gmpi::TimerManager` in a hosted Linux plug-in, so no `gmpi::TimerClient` in the process ever ran** -- `gmpi_ui/helpers/Timer.cpp` has `SetTimer` on Windows, `CFRunLoopTimer` on macOS and **nothing on Linux**, where the manager must be pumped, and only the standalone pumped it. That killed `Controller_VST3::onTimer`, the **only** caller of `message_que_dsp_to_ui.pollMessage()`, so the whole DSP->GUI channel was dead in every hosted Linux VST3 -- measured 2,700 updates shipped, 0 received, against one-for-one in the standalone on the same build. **SO: BEFORE BLAMING A LINUX PLUG-IN SYMPTOM ON THE PLUG-IN, ASK WHETHER THE THING THAT SHOULD HAVE TICKED IS A `gmpi::TimerClient`.** **E78 IS THE SAME HOLE IN CLAP** and is filed unmeasured, one line, and it is what stands between this box and E19's linux CLAP cell -- so E78 before that cell, not after. **THE INSTRUMENT IS THE REUSABLE PART AND IT IS IN THE TREE:** the `TIDE: instance #N feedback send #M` / `TIDE: editor received feedback #M` pair (`SynthEditSem/SynthEdit.cpp`, `TideApp.cpp`, both behind `RACK_ADAPTOR_TRACE`) splits the blob parameter at the process boundary and turned a day of reading GATED code into one measurement. **AND THE ROW'S OWN DIAGNOSIS WAS WRONG:** E74 said the editor stayed bound to a RETIRED DSP instance; the editor was never bound to ANY instance, the recreation still happens after the fix, and the fix is elsewhere entirely. A filed row is one run's reading, and STEP 1's "re-verify before acting" is worth applying to BACKLOG rows too. **`scripts/kill-named.sh` EXISTS -- USE IT.** `pkill -f 'REAPER/reaper'` killed this run's own shell with exit 144, the fourth time the fleet has paid for that and the script was written after the third. **DO NOT REBUILD THE HARNESS:** [tests/e19-host-feedback/](tests/e19-host-feedback/) mints its own project and its README is accurate; REAPER is one download. **STILL TRUE FROM THE PREVIOUS CELL:** **E59 and E60 are DONE and archived**, so the 32 KB `stateLoad` cliff is off `main`; the hosted VST3 builds the PREPARED 43 KB rack, and `tests/hosts/v1-rack.rpp` renders **-6.3 / -17.0 dBFS** here. **X1 IS RESOLVED** and archived. **X2**'s remaining half is Windows and macOS. **STEP 1's feed is empty on this platform**, which is not the same as no linux work. **Previous cell follows.** -- it is the whole of what is left of E19's linux VST3 cell.** **E59 AND E60 ARE BOTH DONE AND ARCHIVED** -- E59 as [#547](https://github.com/JeffMcClintock/TideSynth/pull/547), E60 as [#550](https://github.com/JeffMcClintock/TideSynth/pull/550) + [GMPI_Wrappers#32](https://github.com/JeffMcClintock/GMPI_Wrappers/pull/32), both merged 2026-08-31. **So the 32 KB `stateLoad` cliff is gone from `main` and the linux CLAP cell of E19 is MEASURABLE for the first time** -- nobody has run it since, and it is the obvious second item after E74. **TWO CLAIMS THIS CELL HAS CARRIED ARE RETIRED, measured 2026-08-31:** the hosted VST3's DSP builds the PREPARED 43,191-byte rack rather than the 17,957-byte default, and `tests/hosts/v1-rack.rpp` renders **-6.3 / -17.0 dBFS** here where it was digital silence on 08-28 -- the macOS reference to the decimal, `--control` at -6.0/-9.0 and the uncabled fixture at -inf. **WHAT IS LEFT IS E74:** the host recreates the DSP instance (`instance #3` then `#4`) and the editor's rack feedback pins stay bound to the retired one -- DSP at `display-state capture #2100` and climbing, editor frozen at `update #1, 0 bytes`, while the STANDALONE control builds twice as `instance #1` both times and reaches `light update #18100`. **E75** (a fixture whose Scope is actually on the rack page) unblocks two more of E19's clauses and is cheap; **E76** is one wrapper. **DO NOT REBUILD THE HARNESS -- it is in the tree now and mints its own project:** [tests/e19-host-feedback/](tests/e19-host-feedback/), with the `vst_chunk` framing and the REAPER token both measured and written down. REAPER itself is not kept; it is one download and the recipe names it. **Why this cell lives on [#566](https://github.com/JeffMcClintock/TideSynth/pull/566) and not on #550, kept because the rule generalises:** a NEXT cell names rows, so it can only live on a branch where those rows exist. Putting it on #550 made `check-next-block.py` and `check-id-refs.py` both refuse -- correctly -- because E74/E75/E76 and `tests/e19-host-feedback/` are #566's. The two did conflict on this line when #550 merged first, and the resolution was the one written here in advance. **X1 IS RESOLVED** -- Jeff ruled it DONE on 2026-08-31 (*"mark it DONE"*) and it is archived; the three-run deadlock it caused is over, and the lesson it left is in its archive row: a bare `BLOCKED` with no `(id)` is unfalsifiable by construction. **X2**'s remaining half is Windows and macOS. **STEP 1's feed is empty on this platform**, which is not the same as no linux work -- the lesson this cell keeps paying for.|**The lesson this cell keeps paying for: "no `platform: linux` rows" is not "no linux work". Three of the last four linux entries closed a row by MEASURING it rather than by writing code, and the measurement was only possible on this box.**|
| any | **RESOLVED THE SAME SESSION — the break below is FIXED and `main` is GREEN on all three platforms. Jeff read the run's report and said *"push to main branches"*, so the missing half went in as [SynthEditLib `af42bd6`](https://github.com/JeffMcClintock/SynthEditLib/commit/af42bd6). Verified twice: the failed build run was re-run against the same TideSynth tree with nothing changed but SynthEditLib's `origin/main` — **`macos=success`, `linux=success`, `windows=success`** — and the local `cl /Zs` A/B went from `C3668` to 0 errors on the same command. [#392](https://github.com/JeffMcClintock/TideSynth/issues/392) and [#393](https://github.com/JeffMcClintock/TideSynth/issues/393) **closed themselves** on that green run, which is `build.yml`'s close-on-success step working in place; [#394](https://github.com/JeffMcClintock/TideSynth/issues/394) was closed by hand because the windows leg has no such automation. **Keep reading the account below — the diagnosis and the reasoning about why a scheduled run must NOT commit the developer's working tree are unchanged and are the durable part.** The only thing that changed is that a human was present to authorise the push.** **THE `any` QUEUE HAS NOTHING TAKEABLE. Re-pointed 2026-08-25 (windows, scheduled run); this cell named **S47** as its target and S47 merged as [#387](https://github.com/JeffMcClintock/TideSynth/pull/387) hours later the same evening — the third consecutive day this cell has named a row that was consumed within a day of being named.** **The wall, row by row, so nobody re-derives it:** **S1b** (`EditorLib/CMakeLists.txt` plus a split of `ModuleFactory_Editor.cpp`, GATED); **S8** (`SynthEditLib/CMakeLists.txt:582`, GATED, and MOOT since the Oscillator HD ruling); **E7** (an engine fact Jeff has ruled is *not* a blocker); **E2** (an umbrella whose own row says it is not takeable, because its Accept cannot be stated); **R5** (`.github/workflows/**` plus a certificate only Jeff can export); **V6/V7** (NEEDS-JEFF, which STEP 2 forbids starting); and **X1/X2/V2/E3/E4/R6** (BLOCKED). **So the honest state is unchanged and there is no exception left to name: the fleet's scheduled lane is blocked on Jeff.** A run that finds this cell unchanged should say so and stop rather than invent work. **Two things a run may still usefully do, and both paid this run:** **STEP 1** — `main` is red on all three platforms right now ([#394](https://github.com/JeffMcClintock/TideSynth/issues/394), [#392](https://github.com/JeffMcClintock/TideSynth/issues/392), [#393](https://github.com/JeffMcClintock/TideSynth/issues/393)), from one unpushed half of a two-repo change, and **a later run should verify `main` green and close all three** — and **bookkeeping on verified PR state**, which flipped **V4** and **S47** to DONE here. |**A NEW FAILURE SHAPE WORTH THE ROW IT DOES NOT HAVE: a two-repo change where the second repo was never committed at all.** S41 measured the *race* version — halves merged 26 seconds apart, CI straddling the gap — and its lesson is that it **self-heals** and is not worth a session. This is the opposite: `main` stays red until somebody pushes. **They are indistinguishable from a CI log and need opposite responses**, and one command separates them — `git show origin/main:<file> **EXPECT FOUR OPEN `platform:*` ISSUES THAT ARE NOT WORK: [#398](https://github.com/JeffMcClintock/TideSynth/issues/398), [#399](https://github.com/JeffMcClintock/TideSynth/issues/399), [#402](https://github.com/JeffMcClintock/TideSynth/issues/402) and [#403](https://github.com/JeffMcClintock/TideSynth/issues/403)** were filed by CI against two branches that have since merged and been deleted (the count grew from two to four while the session was still running — that is the amplification the journal describes, and it is why a red `main` is not a static cost), and they were **left open on purpose** as the first live test of A33's watchdog sweep — see A33's row. **Do not close them by hand and do not work them**; both also carry the root cause, which is #394 inherited from `main`. **[#396](https://github.com/JeffMcClintock/TideSynth/issues/396)/[#397](https://github.com/JeffMcClintock/TideSynth/issues/397) were the same break on a live branch and CLOSED THEMSELVES** when it went green. **And one thing to know before pushing docs straight to `main`:** `lint.yml` runs on `pull_request` and `workflow_dispatch` only, so a direct push is **not** lint-gated — run the six `check-*.py` scripts by hand, as the 2026-08-25 direct pushes did. | grep <symbol>` in the sibling repo. Recorded here because both cells above now point at it.|

Status: `TODO` · `DOING` · `IN-REVIEW` · `DONE` · `BLOCKED` / `BLOCKED(<id>)` ·
`NEEDS-JEFF` · `WONTFIX`

- `IN-REVIEW` = work complete, PRs open (linked in the row). A run never sets
  `DONE` on its own fresh work; `DONE` is set when every linked PR has merged,
  by Jeff or by a later run that observes it.
- `BLOCKED(<id>)` = blocked until `<id>` is DONE. **Eligibility lives in this
  column alone** — section prose never overrides it.
- `NEEDS-JEFF` rows should carry a `Default in effect:` and a `Decide-by:` line
  (template in [docs/decisions.md](docs/decisions.md)), so an unanswered
  question cannot silently become the answer.
- `WONTFIX` = considered and declined, with the reasoning in the row. Do not
  re-file it.

Platform: `any` · `win` · `mac` · `linux`

New items state three things, tersely: **Scope** (files/dirs), **Accept** (a
command or observable that proves it done), **Size** (fits one session, or
names its stages). Anything longer than a few lines lives in `docs/<id>.md`
with the row pointing at it. An agent that cannot state an item's acceptance
check before starting must skip it and mark it `NEEDS-SPEC` rather than guess.

Update this file at the end of every run. An item left in `DOING` with no
matching JOURNAL entry means a previous run died — reset it to `TODO` and note
that in the journal (claims younger than 24h are presumed live; see the run
prompt).

---

## Carve-out — APPROVED 2026-08-08, C0 cleared

Stages are sequential. Each must leave SynthEdit, SynthEditCL and TIDE all
building before the next starts. See [docs/carve-out.md](docs/carve-out.md).

| ID | Status | Plat | Item |
|---|---|---|---|

| C7c | RESOLVED | any | **ANSWERED BY JEFF 2026-08-20, and the answer is neither option this row offered: TIDE drops the breadcrumb bar, so `EditorScreenshot` stays private and nothing moves.** *“let's remove the breadcrumb bar from TIDE, it's a bit redundant in a product where you seldom dig deeper than 1 level in.”* **C7e is unblocked from this direction** — its clean-clone test no longer needs an `EditorScreenshot` a stranger cannot clone. **The removal itself is [U3](#), not this row**, because the code forces two questions the ruling does not settle: the crumbs are the only way back *up* a level, and the About pane's only entry point is anchored to the crumb strip. See [decisions.md](docs/decisions.md) 2026-08-20. **Previous text follows.** **`EditorScreenshot` — TIDE links it, and it is on neither STEP 5 list.** TIDE's targets `target_link_libraries(... EditorScreenshot)` (`SynthEditSem/CMakeLists.txt`) and `SynthEditGui.cpp` includes `ContainerThumbnail.h`; the directory is `SE16/EditorScreenshot/`, 13 files, and it is GATED by default. It is what draws the breadcrumb thumbnails (BACKLOG U1b). **The question:** does `EditorScreenshot` become public — the whole folder, or just `ContainerThumbnail` + `ScreenshotRenderer` + the platform `ScreenshotFactory` — or does TIDE get a thumbnail-free path instead? `Default in effect:` nothing moves and C7e cannot pass, because a stranger's clone has no `EditorScreenshot` to link. `May proceed meanwhile:` C7b and C7d, both of which are identical under every answer. `Decide-by:` before C7e. G3 is the precedent — the P4 crash fix lived entirely in two repos on neither list, and filing the ruling question took one day to answer. |

---

## Process hardening — from the process review, 2026-08-09

The findings, red-team verdicts, and the reasoning behind the ordering are in
[docs/process-review-2026-08-09.md](docs/process-review-2026-08-09.md). The
sequence is the decision: A1 gates everything, and cadence (A7) comes last.
Rejected recommendations are listed in that doc — do not re-file them.

| ID | Status | Plat | Item |
|---|---|---|---|
| A7 | WONTFIX | — | **ANSWERED BY JEFF IN SESSION 2026-08-17: the 7x/week cadence is DELIBERATE, and the extra same-day firings are Jeff starting runs manually. "not a scheduling problem."** So this row closes: it asked to raise cadence from 1x to 2x, and the fleet has been running 7x on purpose the whole time. **Nothing to schedule, nothing to decide, and no run should re-file it.** What the measurement was still good for, and what remains true: the task description reads *"Weekly ... (Sat 02:00)"* while `cronExpression` is `0 6 * * *`, so **the description is simply stale** — cosmetic, Jeff's to correct if he cares, not a defect. The **documents** that assume 1x are the part worth knowing about: `docs/weekly-run-prompt.md` is named for a weekly cadence, and **A8 sized journal rotation against "192 KB across 37 entries in six days" on a 1x assumption** — that sizing is the one thing here that could actually bite, and it is A8's to revisit, not this row's. **Also settled by the same answer: the concurrent-session collisions are expected**, being Jeff's manual runs overlapping, not a fleet defect — though the observation stands on its own that **the claim protocol protects a pushed DOING mark and cannot protect an uncommitted working tree** (A14/A16 are that shape). **Original text follows.** **Measured 2026-08-17 (macos): this box runs 7x/week, not 1x.** The `tidesynth-weekly-macos` task reports `cronExpression: 0 6 * * *` ("At 06:03 AM, every day") while its own description still reads *"Weekly ... (Sat 02:00)"* — the cron is what runs, and the two have disagreed silently. **So this row is not "should we go 1x -> 2x"; it is "we are at 7x, what did we intend?"** A7's red-team reasoning was that cadence multiplies journal growth, PR volume and the credential-exposure window linearly — that has been happening unmeasured, and A8 sized journal rotation against a 1x assumption. It is also the same silent-staleness class the bootstrap redesign was built to kill, except it lives in the **cron**, which the bootstrap deliberately holds nothing about — no run can observe its own cadence, so no run had ever reported it. **It plausibly bears on the collisions too:** three sessions shared these working trees on 2026-08-17, and A14/A16 both traced to concurrent sessions in one checkout; the claim protocol protects a *pushed DOING mark* and cannot protect an uncommitted working tree. **Deliberately not changed by any run** — cron is standing configuration and this row is Jeff's. **Still NEEDS-JEFF, but the question has changed.** (Also observed: a firing at 20:11 local that a 06:00 daily cron does not explain; cause unknown — manual trigger, catch-up, or otherwise — recorded as an observation, not a diagnosis.) **Previous text follows.** **Re-pointed from `BLOCKED(A4)` to `NEEDS-JEFF` on 2026-08-16 (macos), and that is a correction, not bookkeeping.** A4 went DONE this run, so by the status column alone — which is where eligibility lives — this row would have become claimable, and the next run to read it would have taken an item whose own text says the work is *"per-box manual steps (Jeff or an interactive session on each box)"*: editing three machines' scheduled-task cron. A scheduled run cannot do that for the two boxes it is not running on, and doing it for its own box only would leave the fleet staggered wrong. The blocker chain (A1, A6, A4) is now **fully cleared** — nothing technical is outstanding, so this is purely Jeff's to schedule. **Default in effect:** weekly, as today. **Decide-by:** no deadline; the A6 digests are the evidence to review it against. **A6 and A1 both landed 2026-08-13 — the remaining blocker is A4 alone, unblocked but not yet built.** **Raise cadence to 2 staggered runs per box per week.** Also requires A1 and A4 (the red team's ordering: cadence multiplies journal growth, PR volume, and the secret-exposure window linearly, so the drains come first — this row's blocker chain encodes A6, and A1/A4 are NEEDS-JEFF/BLOCKED upstream of it). The stagger is no longer load-bearing — the pushed-DOING claim protocol is what prevents collisions — so weekly is a historical artifact. The task-cron edits are per-box manual steps (Jeff or an interactive session on each box). Start at 2×, review after a month against the A6 digests. |

---

## Ready now — no dependency on the above

| ID | Status | Plat | Item |
|---|---|---|---|
| P8 | DONE-PENDING-CI | win | **Fixed 2026-08-09 on the Windows box, at Jeff's direction, straight to `SE16` master (`4baddfbb4`) — see [docs/decisions.md](docs/decisions.md).** One line at `SynthEdit2/EditorWindowHelper.cpp:294`: `currentContainerView.universalFactory` → `.get()`. `UniversalFactory` derives from `gmpi::api::IUnknown` (`SynthEditLib/Shared/DrawingFrame2_cpu.h:19`) and `renderContainerThumbnail` **borrows** rather than takes the factory, so `.get()` matches what the other three callers already pass and what `DrawingFrame2_win.h:270` does when it hands the same factory out. **Verified:** `MSBuild SynthEditStore.sln -t:SynthEdit2 -p:Configuration=Release -p:Platform=x64` now links, producing `x64\Release\SynthEdit2\SynthEdit2.exe`. **This was bigger than "does not compile from clean" — it was failing the SynthEdit Store release pipeline.** Actions run [31297103293](https://github.com/JeffMcClintock/SynthEdit/actions/runs/31297103293) died on `master` at "Build SynthEditStore (Release x64)" on exactly this error, skipping changelog, MSIX signing, bootstrapper and FTP upload — so nothing shipped. **Left as DONE-PENDING-CI rather than DONE:** the local Release link is proof the error is gone, but the CI job goes on to sign and upload, and nobody has watched a green run yet. Whoever sees one first should flip this to DONE. **Also unblocks the link-stage verification C1b and C2 each had to leave open** — both could compile their new TUs but neither could reach SynthEdit2's link. A Debug build of the same project failed on an unrelated `x64\Debug\pch.pch: Invalid argument` intermediate-file error; not investigated, and not this bug. Original finding below. |
| ~~P8~~ | *(was)* | win | **`SynthEdit2` (the WinUI3 app) does not compile from clean.** Not TIDE, and pre-existing — found during C1b and **A/B-confirmed unrelated** (fails identically with the C1b vcxproj change reverted): `SynthEdit2/EditorWindowHelper.cpp(294)` passes `std::unique_ptr<UniversalFactory>` where `se_cl::renderContainerThumbnail` (`EditorScreenshot/ContainerThumbnail.h:35`) now takes `gmpi::api::IUnknown*` — the thumbnail API moved and the WinUI3 caller never caught up. Matters to TIDE because "leave SynthEdit building" is the standing rule and **C1b's link-stage verification of SynthEdit2 is blocked behind it** (the new `ExportAsPlugin.cpp` ClCompile entry compiles clean; the app cannot reach its link step). Same shape as P6 on macOS: SynthEdit's own bug, in SynthEdit's own files — check with Jeff before touching. |
| X4 | WONTFIX | any | **Closed 2026-08-08 by Jeff: leave the `GIT_TAG origin/main` pins alone. Do not re-file this.** `FetchContent` is not how the SynthEdit-family dependencies are meant to be developed against — they change daily and the intended dev path is the `*_FOLDER_OVERRIDE` variables, which are the normal path on a dev box, not an optimisation. CI clones fresh, so `origin/main` there is real. **The thing to watch for instead is a *missing* override** — that asymmetry, not the pin, is what caused X3. **CMake already announces it:** every dependency prints `Using local <X> folder` or `Fetching <X> from github` at configure time. An unexpected "Fetching" on a SynthEdit-family repo means you are building against whatever `main` looked like when the tree was first configured; confirm with `git log -1` in `build/_deps/<dep>-src` before believing your tree. Full reasoning and the original finding: [docs/x4-fetchcontent-pins.md](docs/x4-fetchcontent-pins.md). |
| S9 | WONTFIX | mac | **Moot 2026-08-15 — S10 ruled retire, not revive.** This row's own scope was surgical cleanup of `TIDE/Plugins` wiring *inside* `SE_IOS_APP.xcodeproj`; deleting the whole project ([SynthEdit#22](https://github.com/JeffMcClintock/SynthEdit/pull/22)) makes that cleanup unnecessary rather than incomplete, exactly as this row itself predicted ("S9 is moot if the answer is retire"). Original row follows. **GATED remainder of S6 — remove the now-dangling `TIDE/Plugins` wiring from `SE_IOS_APP.xcodeproj`.** Filed 2026-08-13 when S6 deleted the files. Three things to remove, all in the **shared, GATED** `SE16/SE_IOS_APP/SE_IOS_APP.xcodeproj/project.pbxproj`, all belonging to the `SeAudioUnitMacOS` target: (a) the Run Script phase at `:2064` (`cp -r "$SRCROOT/TIDE/Plugins" …`); (b) the six `.sem` group/file references (`Oscillator`, `EnveloperAdsr`, `PatchMemory`, `Converters`, `SubControlsXp`, `ControlsXp`, ids `D5F5219525E5F21600E75519` and siblings) — navigator-only, cosmetic, they just show red; (c) **the real ones — the `CpResource` build-file entries for files *inside* those bundles, which are genuine Resources-phase inputs** and are what now errors. **Do not do this without reading S10 first** — if that project is retired rather than revived, this row is moot and the right move is deleting the whole `.xcodeproj`. **Accept:** `SeAudioUnit macOS` builds with no `TIDE/Plugins`-related error (it will still fail on `SE_DSP_CORE` — that is S10, not this). **Size:** minutes, once scope is ruled. **Needs Jeff** — GATED path, and a scheduled run may not edit it. |
| S8 | TODO | any | **THE CAUSE THIS ROW NAMES IS WRONG, AND THE BLAST RADIUS IS 79 FILES NOT ONE. Measured 2026-08-24 (macos, interactive), documented at the gate in [SynthEditLib#49](https://github.com/JeffMcClintock/SynthEditLib/pull/49) (merged, comment only).** This row says `OscillatorNaive.cpp` is *"in a source list belonging to a separate target"*. It is not: the list at `SynthEditLib/CMakeLists.txt:677` IS `set(srcs ...)`, and `:799` puts `${srcs}` straight into the STATIC library. **The list is simply never evaluated — it sits inside `IF(SE2JUCE)` at `:582`, which is off for every build but a JUCE one.** The row also rules out *"the `#if GMPI_IS_PLATFORM_JUCE==1` block, a tempting wrong answer"* — correct about the C++ preprocessor, and then it landed on the wrong explanation, because the real gate is a JUCE conditional one level up, in CMake. Two JUCE suspects; one ruled out properly, the other never considered. **79 `.cpp` files are inside that block**, not one module. **Measured with a control:** `SE2JUCE` off, `libSynthEditLib.a` has 182 members; `OscillatorNaive`, `Waveshapers`, `SoftDistortion`, `NoteExpression` and `Inverter` all absent by `ar -t`, against `MidiToCv2.cpp.o` (outside the block) present. **Left as a comment rather than a code change deliberately:** the gating is CORRECT for SynthEdit — a JUCE host cannot load external modules so they are linked in, and everyone else gets them as loadable `.sem`/`.gmpi`, which SynthEdit finds by scanning. It is wrong only for an app that links statically and does not scan (TIDE, S1a), whose answer is to compile what it ships into its own target as E2c does. Changing the gating needs a ruling this row does not ask for. **Not verified:** Windows and Linux, though `SE2JUCE` is off on all three. **MOOT FOR THE MVP as of 2026-08-21 — Jeff ruled that TIDE ships "Oscillator HD" (`SE Oscillator4`) as its oscillator, and that one is ALREADY in the plugin** (compiled by E2c, `SynthEditSem/CMakeLists.txt`). This row's finding about `OscillatorNaive` having zero symbols is unchanged and still true, but it is about a DIFFERENT module and **no longer blocks Tier 0, E2 or the module set**. Left open rather than closed because the underlying packaging fault — a module in a source list belonging to a separate target — is real and will bite the next module that needs linking. **Previous text follows.** **CAUSE CONFIRMED 2026-08-18 (macos, interactive) while V3 hunted for a monophonic MIDI-CV — and it is the explanation this row already suspected.** `modules/OscillatorNaive/OscillatorNaive.cpp` is in a `SynthEditLib/CMakeLists.txt` source list belonging to a **separate target**, so it builds as a LOADABLE module and is **not compiled into the SynthEditLib static library**. TIDE links statically and has no scan (S1a), so it can never appear. Confirmed by build layout, not `nm`: a sibling in the same list, `MidiToGate.cpp`, puts its object under `MidiPlayer2.build/` while `MidiToCv2.o` — genuinely in the library — lands under `SynthEditLib.build/`, and adding an `INIT_STATIC_FILE` for that sibling **fails to link**, which is the linker stating the object is not there. **It is NOT the `#if GMPI_IS_PLATFORM_JUCE==1` block**, a tempting wrong answer: `INIT_STATIC_FILE(ADSR)` sits inside it while ADSR works fine in TIDE. **The fix needs NO GATED change:** add the `.cpp` to `SynthEditSem/CMakeLists.txt`'s source list and stage its XML, exactly as **E7** did for `MidiToGate.cpp` and E2a did for `RectangleGui.cpp`. **Original row follows.** **Corrected 2026-08-17 (windows, interactive): relabel these modules, do NOT delete them — the original premise below would have silenced the plugin.** Measured, not inferred: all three are `RegisterIoModule` seams (`ug_soundcard_out.cpp:69`, `ug_soundcard_in.cpp:63`, `ug_midi_out.cpp:50`), and Sound Out implements `ISpecialIoModuleAudioOut` — `SeAudioMaster` hands it the **host's** output buffers (`SeAudioMaster.cpp:560-562`, `:640-642`), the identical mechanism S12 used for MIDI input. In a plugin these *are* the DAW-IO modules constraint 2 wants; only their standalone-era copy says otherwise. `TIDE.se1` terminates in Sound Out and E2a's Output prefab will too ([docs/e2a-prefabs.md](docs/e2a-prefabs.md) §8). The real S8 work: (a) TIDE-facing name/group/help text — "Sends audio to your speakers… Non-registered SynthEdit limited to 2 output channels" is wrong twice over for a free plugin whose host owns IO; (b) the TIDE module list ([docs/module-enumeration.md](docs/module-enumeration.md) §6 trap: neither existing arm is right) **keeps all three registered**; (c) the oscillator gap is the live E2a blocker: `OscillatorNaive` zero symbols. Original row follows. **TIDE ships modules constraint 2 forbids, and shows SynthEdit licensing copy.** Measured from the macOS Release binary 2026-08-08 (addendum B3 of [docs/module-enumeration.md](docs/module-enumeration.md)): `ug_soundcard_in`, `ug_soundcard_out` and `ug_midi_out` are compiled in *and registered* — each has its `se_static_library_init_*` and `__GLOBAL__sub_I_*` static initialiser — so the module browser offers a **Sound Out** module that opens the soundcard directly, in a plugin whose whole premise is that the DAW owns I/O. `ug_soundcard_out.cpp:10` registers it under Input-Output with the help text *"Sends audio to your speakers… Non-registered SynthEdit limited to 2 output channels"*, which is also wrong for a free unlicensed plugin. Confirms A1's prediction; also confirmed the modern SEM modules are absent (`OscillatorNaive`: zero symbols). Both arms of `initialise_synthedit_modules` are in `SynthEditLib/UgDatabase.cpp:1063-1155`, so there is **no TIDE-side fix** — ~~GATED on C0~~ **unblocked 2026-08-08, C0 approved.** The `SE_EXTRA_STATIC_FILE_CPP` hook (A2) can only *add* modules, not suppress these. Pairs with S1b; whoever takes either should take both. |

| D4 | WONTFIX | any | **WONTFIX 2026-08-17 — this row's central measurement is FALSE today, and acting on it would have broken the build.** It claims *zero* call sites for `gmpi::browse_to`; re-measured this session there are **two live ones** — `SynthEditLib/SkinMgr.cpp:111` (`SkinMgr::EditSkin`, the "open this skin's folder" command) and `SynthEditLib/MfcDocPresenter.cpp:1106` (the skin-folder context command). Deleting `browseto.mm` would produce an undefined symbol, not a saving "at no functional cost". The positive control was run too (`gmpi::open_url`, 5+ call sites), so a zero would have been distinguishable from a broken grep. The row was filed 2026-08-16 and the C-series carve-out has been moving files into `SynthEditLib` throughout, so the callers most likely arrived with a move after the original grep. **The row's real INTENT — getting an AppKit dependency and the sandbox-hostile `activateFileViewerSelectingURLs:` out of builds that should not have them — is delivered by D3 instead**: `browse_to` compiles to a no-op off macOS, while macOS keeps the behaviour it actually uses. **Do not re-file.** **The generalisable lesson: re-run a row's own measurement before acting on its conclusion, especially a "delete this, nothing uses it" row in a tree being actively carved up.** Original row follows. **`browseto.mm` is compiled into every Apple build and never called.** Found 2026-08-16 by the D1 run: grepping `SynthEdit`, `SynthEditLib`, `gmpi_ui` and `GMPI_Wrappers` finds **zero** call sites for `gmpi::browse_to` (`gmpi::open_url` has real ones — `SynthEdit2/Application.cpp:389` and the Wayland Help menu — so the grep is not simply missing them). Dropping it from `SE16/EditorLib/CMakeLists.txt:162` removes an AppKit dependency and one sandbox-hostile API (`activateFileViewerSelectingURLs:`, i.e. reveal-in-Finder) from every Apple build including TIDE's, at **no functional cost**. Reduces D3's surface to one file rather than two. **Scope** one line of `SE16/EditorLib/CMakeLists.txt`; optionally the now-unreferenced `helpers/browseto.{h,mm}` in `gmpi_ui`, which is ALLOWED and shared, so leave it unless asked. **Accept** SynthEdit, SynthEditCL and TIDE all still build on macOS with the entry removed. **Size** minutes. **GATED path — same file and gate as D3; do them together.** |

| S14 | WONTFIX | any | **NOT A DEFECT — closed 2026-08-18 by Jeff, the same day it was measured and before anything was built on it.** **A bare DSP module is not a rack citizen and was never meant to be one.** Rack modules are **Containers designed in advance and shipped as prefabs**, added to the rack at runtime: the container carries the panel, with patch-points and knobs/sliders on it wired internally to non-GUI modules like an oscillator, and the container is what the rack draws. A module that has its own GUI — a scope — can also sit on the rack directly, and **that already works today**. **So the three bare `1 KHz Tone` modules this row was measured from are not a product composition.** They are what audio testing looks like: drop plain modules in and switch to the **structure view** to check basic audio functionality. That is a developer workflow, not something an end user does on the main screen, which is exactly why nothing gives them rack geometry. **The measurement itself stands and is still worth reading** ([docs/s14-rect-measurement.md](docs/s14-rect-measurement.md), corrected): rack mode routes placement through the panel view and a plain `CUG`'s panel setter is `CDocOb`'s empty body. **One of its findings now reads as confirmation of the design rather than a complaint** — in full SynthEdit's own prefabs the modules carrying a `panelRect` are exactly the GUI-bearing ones, and the plain DSP modules carry none. **And the container half was already provided for, under a name the measurement did not look for:** `CContainer : CUG_with_patches : CUG` overrides the rect accessors itself (`CContainer.h:60-62`) and serialises its panel geometry as **`PanelWndPosition`** (`:214`) — an element already present in the measured chunk. **Superseded by:** the prefab rack modules are **E2a**/**E2**; rack-shaped styling for GUI-bearing modules is **E5**. **Do not re-open this as a geometry bug.** **Original row follows.** **MEASURED 2026-08-18 (macos, scheduled) — the question this row asked is ANSWERED, and the fix is entirely in GATED `SynthEditLib`, filed as S15. Do not re-measure.** Full working: [docs/s14-rect-measurement.md](docs/s14-rect-measurement.md). **The answer is that both halves of the row's either/or are the same event:** rack mode addresses the *panel* rect, a plain DSP module has no panel rect, so the assignment lands on an empty base-class no-op and is discarded — and `structRect`, the only rect such a module persists, is never written and keeps its constructed zero. **The `panelRect` hypothesis is REFUTED as a symptom:** full SynthEdit's own prefabs show `FloatToVolts` and `IO Mod` with **no `panelRect` at all** and a healthy non-zero `structRect`, so an absent `panelRect` is normal for a DSP module and TIDE differs only in the zero `structRect`. **Mechanism, four independently checkable facts:** `CDocOb::setViewObRect` has an empty body and `getViewObRect` returns `{}` (`DocOb.h:89-93`); `CUG` implements both **only** for `CF_STRUCTURE_VIEW` and serialises only `structRect` (`CUG.cpp:2557,2566`, `CUG.h:32`); `panelRect` belongs to `CControl`, not `CUG` (`Control.h:23`); and rack mode places through the top-level **panel** view (`MfcDocPresenter.cpp:811`, `:1421`, and the comment at `SynthEditSem/TideApp.cpp:497-500`). Composed: every TIDE placement is `CUG::setViewObRect(CF_PANEL_VIEW, …)` -> `CDocOb`'s no-op. The `master_container` is zero for the same reason — it is a `CUG` placed the same way. **Not persistence:** S11 landed and the chunk round-trips byte-identically; state right, geometry wrong, exactly as this row predicted. **TideApp's only involvement is `Document()->rackMode = true`, which is correct and is not the bug.** A17's 2026-08-18 ruling covers a **build break** in a GATED path; this is a functional defect, so it does not reach it. **Blocked on S15.** Measured on branch `tide/mac/S14-zero-structrect`. **Original row follows.** **Modules placed from the browser get a zero `structRect`, so they persist correctly but never render — found 2026-08-18 (macos, interactive) as a by-product of S11's verification.** With S11 landed, the document round-trips byte-for-byte and the modules really are in the saved chunk — `<module type="1 KHz Tone" handle="..."><structRect l="0" r="0" t="0" b="0" /></module>`, three of them — yet the rack draws empty. **So this is NOT a persistence bug and S11 does not fix it: the state is right and the geometry is wrong.** Worth stating plainly because the symptom ('I place a module and nothing appears') is identical to the bug S11 just fixed, and a future run will otherwise re-open S11. **Unknown and worth measuring first:** whether the zero rect comes from the placement path not assigning one, or from rack mode expecting a *panel* rect rather than `structRect` — `MfcDocPresenter::getRackLayout` and `ViewBase::snapToGrid` are the rack-mode geometry, and the exported document also carries `PanelLocationCenter`. **Cheap first measurement:** place a module in full SynthEdit, export, and compare the two documents' rects — that separates 'TIDE-specific' from 'rack-mode-specific' in one step. **Scope** likely `SE16/SynthEditSem/` or the shared view code. **Accept** a module placed in the rack is visible, and still visible after reload. **Size** unknown until the measurement above. |
| S15 | WONTFIX | any | **WITHDRAWN 2026-08-18 — the premise is void, ruled by Jeff the same day it was filed.** Both options below are wrong, and so was the request for a decision between them. **(b) give `CUG` a panel rect** would add geometry to the object that is specifically *not* supposed to appear on the rack, and duplicate what `CContainer` already carries as `PanelWndPosition` (`CContainer.h:214`). **(a) route rack placement to `structRect`** would push rack geometry into the schematic view's rect and fight that same existing mechanism. **No shared-format change is needed, so the GATED framing that made this NEEDS-JEFF also goes away** — that framing was the whole reason this row claimed a ruling. See **S14** for what is actually true. **The process lesson, recorded because it cost a row and a request for Jeff's attention:** the measurement behind this was sound and the *architecture* behind it was assumed — three bare DSP modules in a test patch were read as the product's intended composition, and a two-option design fork in shared code was built on that reading. One question first, *what is a rack module supposed to be?*, would have replaced the whole row. **Original row follows.** **Rack-mode placement discards module geometry — the GATED fix behind S14, measured 2026-08-18 (macos).** In rack mode every module placement calls `CUG::setViewObRect(CF_PANEL_VIEW, …)`, which falls through to `CDocOb::setViewObRect` — **an empty body** (`SynthEditLib/DocOb.h:93`). The rect is discarded, nothing is marked modified, and `structRect` stays `0,0,0,0`, so the rack draws nothing. Evidence and the reproduction: [docs/s14-rect-measurement.md](docs/s14-rect-measurement.md). **Why this needs Jeff and not a run:** every site is in **`SynthEditLib`**, which is GATED, and S14 is not a carve-out stage — A17's ruling permits repairing a GATED **build break**, which this is not. It is also a genuine design fork in shared code that full SynthEdit also runs, so guessing is the wrong move. **Option (a) — route rack placement to the structure rect.** In `MfcDocPresenter::SetModuleRect`/`GetModuleRect` (`MfcDocPresenter.cpp:811,~825`) use `CF_STRUCTURE_VIEW` when `getRackLayout().enabled`. Smallest change, persists for free because `CUG` already serialises `structRect`, and the rack's coordinates then share a space with the structure view — which may be exactly right (the rack *is* a layout of modules) or may collide with a user's structure-view arrangement of the same patch. **Option (b) — give `CUG` a real panel rect.** Add the member plus `s("panelRect", …)` to `CUG` alongside `structRect` (`CUG.h:32`, `CUG.cpp:2557,2566`), so a plain module gains panel geometry the way `CControl` already has it (`Control.h:23`). Cleaner conceptually and keeps the two views independent, but it changes the on-disk schema for **every** SynthEdit module, not just TIDE's, and needs a compatibility answer for existing projects. **Recommendation if a tiebreak helps:** (a) is reversible in one line and testable this week; (b) is the better model but is a shared-format change. **Scope** `SynthEditLib/MfcDocPresenter.cpp` for (a), or `SynthEditLib/CUG.{h,cpp}` for (b). **Accept** after the change, a re-exported chunk carries non-zero, distinct rects per module (headless, checkable) **and** the modules are visible in the rack and still visible after reload (needs a GUI session). **Size** an hour for (a) including the headless re-export check; unknown for (b) until the compatibility question is answered. **Unblocks S14.** |
---

## After the carve-out

| ID | Status | Plat | Item |
|---|---|---|---|
| E37 | BLOCKED(E42) | any | **HALF ANSWERED BY JEFF AND HALF REDIRECTED TO E42, 2026-08-26.** Jeff re-saved `DefaultRack.synthedit` (`f046787` *"Default Racks opens at a good zoom/pan"*): the stored view moved to `PanelLocationCenter (940.4, 584.7)` at **zoom 0.745** — so the 38% zoom this row complained about is gone and the rack is legible. **He left the module POSITIONS alone** (`TiDE MIDI-CV` at `36..150`, `TiDE Output` at `1329..1377`, still 1341 DIP apart), which is a fair call: at a legible zoom you cannot see both anyway. **BUT IT STILL OPENS WRONG, AND THAT IS NOT THE RE-SAVE.** Measured on a fresh standalone: only `TiDE Output` is visible; `TiDE MIDI-CV` is off-screen left. The cause is **E42** — the restored centre is applied **240 DIP off**, exactly the width of the module-browser strips, because `calcViewTransform` uses half the pane's SIZE where it means its MIDPOINT. Jeff framed the rack correctly and TIDE reopened it shifted by the strips. **So do not re-save this file again until E42 lands** — the framing that looks right while saving will keep reopening wrong. **What remains of E37 after that:** genuinely nothing but the layout question — whether the two default modules should sit adjacent so both fit at zoom 1, which is the product call this row already flagged and which Jeff has now implicitly answered *"not by moving them"*. **Recommend closing E37 once E42 is in and the framing is confirmed**, rather than carrying it further. Previous text follows. **RE-SCOPED 2026-08-26 (macos, scheduled run): V6 VOIDED HALF OF THIS ROW AND E33 HAS NOW DONE THE OTHER HALF, so what is left is a DATA question about the shipped default rack.** **(a) is void:** this row's scope was `seedRootMidiCv`, and V6 deleted it — the root assembly is a stored document now, `DefaultRack.synthedit`. **(b) was E33's and is done** (branch `tide/mac/E33-restore-pan-zoom`): the view now opens where the document says, which is what made the rack visible again. **WHAT REMAINS, measured rather than inferred.** The shipped `DefaultRack.synthedit` places `TiDE MIDI-CV` at `l=36` and `TiDE Output` at `l=1329`, both at `t=276` — **1341 DIPs apart, on rack row −9**, while the rack's own panel rect is `(3732, 3732, 4212, 4212)`: **480 wide, at row 0**. So the default content sits outside the rack the rails are drawn from and outside the window a DAW would export, and it is 2.8x wider than that window. The file compensates with `PanelLocationZoom="0.38146973"` — which is why the out-of-box view is now legible but **tiny**, and that zoom is almost certainly an artifact of wheel-zooming while authoring rather than a choice. **The fix is data, not code:** place the two modules adjacent at the rack origin and save at zoom 1. **NOT DONE HERE ON PURPOSE** — `DefaultRack.synthedit` is V6's, landed hours earlier from another box, and re-authoring someone's shipped default is a product change rather than a bug fix. **Accept** a fresh standalone opens with both default modules visible, adjacent, at zoom 1, and `e5_rack_footprint_probe.py` reports them on-grid with no overlaps. **Size** small. Original item follows. **A CORRECTLY-PLACED INSERT IS INVISIBLE: the rack ORIGIN, the only module on a fresh document, and the live viewport are in three different places.** Measured 2026-08-26 (macos, scheduled run) while closing E36, with numbers from a temporary probe in `SynthEditGui.cpp` rather than from reading code. **The three facts, and any two of them are fine; it is the third that hurts:** (1) the rack's row 0 / column 0 is **(3732, 3732)** -- `getRackLayout()` anchors it to the master container's panel rect, and V5 records the same number off `TideApp.cpp:793`; (2) the V3-seeded root MIDI-CV, the only thing on a fresh document, sits at **(480, 464)** -- **not on the rack at all**, 3200 DIPs up and left of row 0; (3) the live pane is looking at the MIDI-CV, so an insert that correctly takes row 0 column 0 happens **off-screen** and a user sees their double-click do nothing. **E36 SHIPPED WITHOUT A SCROLL-TO-REVEAL, AND THE REASON IS THE INTERESTING PART.** One was written and then measured, and it can never fire: `E36PROBE placed=3732,3732,3780,4116 visible=3734,3734,4234,4234 onScreen=1`. `CContainer::getVisibleRect(CF_PANEL_VIEW)` is computed from the container's STORED view centre -- the canvas centre (3984, 3984) with the no-view-open half-size of 250 -- so by the document's own reckoning the module is on screen. **The document is not wrong; the live pane is the thing out of step with it**, which is precisely what [E33] states. **So this is not one bug, and splitting it is the first job:** (a) should `seedRootMidiCv` place the MIDI-CV at the rack origin -- almost certainly yes, and it is the cheap half; (b) should the live view follow the document's stored centre -- that is E33's, and E37 should not duplicate it. **Do (a) first and re-measure**: with the MIDI-CV on the rack, the view centred on it is also the view centred on the rack, and (c) may evaporate. **Scope** `SynthEditSem/TideApp.cpp` `seedRootMidiCv` (ALLOWED). **Accept** on a fresh standalone the root MIDI-CV's saved rect starts at the rack origin reported by `getRackLayout()`, and a module inserted by double-click is inside the window the user is already looking at -- checked on a screenshot, since the geometry alone is what misled this row's predecessor. **Size** small for (a); (b) is E33's. **BLOCKED on nothing** -- (a) stands alone. |
| E19 | TODO | any | **THE linux VST3 CELL IS RE-MEASURED AGAINST E59's MERGED FIX AND STILL FAILS -- BUT THE CAUSE HAS MOVED, AND HALF THIS ROW'S HISTORY IS NOW RETIRED. 2026-08-31 (linux, scheduled run), branch `tide/linux/E19-vst3-linux-cell`, [#566](https://github.com/JeffMcClintock/TideSynth/pull/566), REAPER 7.43 on headless weston, `TIDE_VCV_FUNDAMENTAL=ON -DRACK_ADAPTOR_TRACE=1`, 553/553 0 errors.** Cell back to TODO; no product code changed. **RETIRED -- the 2026-08-28 headline is gone.** That run measured `29101` then `17957` and concluded the DSP ends up running the DEFAULT rack. It now builds the PREPARED rack and only that: `instance #3 building rack from 43191 byte document`, twice, and the 17,957-byte default never appears after the restore. **RETIRED -- and this is the number to quote.** `tests/hosts/v1-rack.rpp`, the fixture PLAN cites for *the patch plays after reload*, rendered **peak -inf, digital silence** on this box on 2026-08-28. It now renders **peak -6.3 / rms -17.0 dBFS**, the macOS 2026-08-18 reference to the decimal, with `--control` at **-6.0 / -9.0** and `v1-rack-uncabled.rpp` at **-inf** as the negative control. `v3-midi-pitch` -6.2/-21.1 and `v3-midi-gate` -6.3/-21.2 as well. Table in [tests/hosts/README.md](tests/hosts/README.md). **FAIL -- the animation clause, and the mechanism is NAMED rather than guessed.** 75 s with the transport rolling throughout (`playstate=1`, position 0 -> 74.919). The DSP side is healthy and busy: `RackProcessor: 'Scope' display-state capture #2100 (65548 bytes)`, still climbing at the end. The editor side never moves: `display-state update #1 arrived (0 bytes)` and `light ... update #2 value 0.000`. **The mechanism is the log ORDER, not any one line:** `instance #3` builds the rack, the five RackEditors are constructed and each pin takes its initial default -- and THEN `instance #4` builds. From that point every capture belongs to #4 and **not one further `RackEditor:` line is ever printed.** The editor's feedback pins are bound to a processor generation the host has retired. **THE CONTROL IS WHAT MAKES THAT A FINDING.** Same build, same document, same box, same compositor, only the host differs: the STANDALONE also builds twice -- but as **`instance #1` both times** (`Legacy chunk`, then `Build chunk, rack already prepared`) -- and its editor runs to **`light ... update #18100`** with varying values and **`display-state update #1820 arrived (65548 bytes)`**. Filed as **E74**. **NOT MEASURABLE FROM THIS FIXTURE, AND IT IS THE FIXTURE RATHER THAN THE HOST -- do not read the next number as a result.** The hosted pixel diff is **0 of 690,800**, and the standalone control over the same interval gives **byte-identical screenshots**, so the zero says nothing. All five VCV editors construct with panel art in BOTH arms (`RackEditor: 'Scope' model=yes art=yes(res/Scope.svg) art-size=195x380`) and **none is on the visible rack page**; vertical and horizontal scrolling did not reach them. The DEFAULT rack in the same build draws its `Out` panel on the rails, which is the negative control that makes this the fixture's layout. So the **pixel-diff** and **int/bool/enum** clauses (the latter needs a right-click on a panel that is not on screen) are unmeasured, and want a fixture with a visible Scope -- **E75**. **string** still has no producer, as this row has always said. **THE HARNESS IS IN THE TREE NOW** -- [tests/e19-host-feedback/](tests/e19-host-feedback/), with the `vst_chunk` framing measured off a default instance and a mint route that makes REAPER write its own token, so **E29** cannot be got wrong by construction. **AND E29 IS SOFTER THAN THIS ROW ASSUMED:** the committed fixtures carry the macOS 7.45 token and REAPER 7.43/Linux **loads them anyway**. **WHY NOBODY HAD THE AUDIO NUMBERS:** `scripts/render-and-measure.py` SEGFAULTS REAPER (rc -11) run from a scheduled run's own shell, because it inherits `WAYLAND_DISPLAY`; the downstream symptom is an `EOFError` in Python's `wave` module, which reads as a corrupt fixture. One `env -u WAYLAND_DISPLAY ... GDK_BACKEND=x11` wrapper fixes every one of them -- **E76**. **Previous text follows.** **THE mac AU3 CELL IS NOW HALF MEASURED, AND THE HALF THAT IS NOT HAS A NAMED CAUSE. 2026-08-31 (macos, interactive, Jeff present), branch `tide/mac/E19-au3-registered`.** Cell back to TODO; no product code changed. **A DAW HAS NOW HOSTED TIDE'S AUv3 — the first time on any box.** **PASS — registration:** the 2026-08-29 run's five failed ways to register a current build beside the developer's were right, and the remedy is the displacement it correctly refused to do unattended. With Jeff present and **a `ditto` backup taken first** (which is what makes it recoverable, and answers that run's stated objection), installing over `~/Applications/TIDE-Rack-AUv3.app` and launching once moved `pluginkit -mv` from UUID `DBE224FD…`/2026-08-25 to `793D00A0…`/2026-08-31. **PASS — `auval -v aumu Drck Dsyh`: AU VALIDATION SUCCEEDED, rc=0**, Apple's own validator, stricter than this project's probes and the first such result it has. **PASS — hosting:** REAPER 7.45 scanned it into its AU cache as `TiDE Synth:TiDE Rack` (his cache had never held a TIDE entry), instantiated it as **`AUi: TiDE Rack (TiDE Synth)`**, floated its editor, which **drew**, and rolled the transport 43 s at `playstate=1` with the position advancing throughout. **AND THE SCREENSHOT SETTLES THE 2026-08-29 `strings` CONFUSION WITH A PICTURE:** the hosted editor's module browser lists `LFO`, `LFO2`, `Scope`, `SEQ3`, `SHASR`, `Quantizer` and the rest under a **`Rack-VCV Fundamental`** heading — VCV Fundamental linked and enumerated *inside the extension*, which no symbol check could show because ids are composed at runtime. **NOT MEASURED, and the cause is structural rather than effort:** the 60-second animation window, the int/bool/enum toggle and the pixel diff all read `RACK_ADAPTOR_TRACE`'s counters, which go to **stderr** — and an audio-unit extension runs out-of-process, so **none of them reach the host's stderr**. Verified rather than assumed: the strings are in the appex, the plug-in loads and runs, and grepping REAPER's stderr for `TIDE:`/`RackProcessor` returns nothing. **Filed as E73**, whose fix is the file-log route E65 already built one layer up. Those clauses also want a PREPARED rack in a hosted AUv3, which is the same shape as E60's CLAP blocker. **Also measured, and it belongs to V2:** REAPER sees **3** parameters on the instance — `Bypass`, `Wet`, `Delta`, all REAPER's own AU wrapper params — so none of TIDE's parameters are visible to the host for automation. **Recipe, both traps and the appex-stderr finding:** [docs/ci/headless-gui-verification.md](docs/ci/headless-gui-verification.md). **Branch** `tide/mac/E19-au3-registered` — [#564](https://github.com/JeffMcClintock/TideSynth/pull/564). **Previous text follows.** **THE mac AU3 CELL IS NOT MEASURABLE BY A SCHEDULED RUN, AND BOTH HALVES OF WHY ARE NOW MEASURED RATHER THAN ASSUMED. 2026-08-29 (macos, scheduled run), branch `tide/mac/E19-au3-mac-leg`, [#552](https://github.com/JeffMcClintock/TideSynth/pull/552).** Cell back to TODO; no product code changed. **(1) THE HOST CAN BE ISOLATED ON macOS, WHICH WINDOWS COULD NOT BE** -- a portable REAPER (copy `REAPER.app` to a folder, `touch reaper.ini` beside it) keeps its whole resource tree there. After four launches and four renders the developer's `~/Library/Application Support/REAPER` compared IDENTICAL including mtimes, and his plug-in folders plus `~/Applications` compared identical across **1,019 files**. So a mac run has no excuse for touching his REAPER; the un-isolatable result is a WINDOWS fact, not a fleet fact. **Trap: a FRESH portable config hangs on a first-run modal and every symptom blames the plug-in** -- `--control`, which loads no plug-in at all, also times out. Seed the directory with the developer's `reaper.ini` and the control returns rc=0 in 14 s at the expected -6.0/-9.0 dBFS. **(2) THE BLOCKER IS THE LOCKED SCREEN, and it is narrower than 'no GUI'** -- a scheduled run finds the login window up. Offline `REAPER -renderproject` **works** while locked (3-4 s a fixture); a full GUI launch driven by `Scripts/__startup.lua` **hangs** with no script output, both measured minutes apart in one session. So E19's audio clauses are reachable unattended and its animation, pixel-diff and int/bool/enum clauses are not, because all three need the editor. Note `screencapture` on a locked session returns an **all-black frame of full screen size**, which reads as 'the window drew nothing' rather than 'the display is off' -- E58's lesson in a new place. **(3) A SECOND AUv3 CANNOT BE REGISTERED ALONGSIDE THE INSTALLED ONE**, which is what makes this cell need a human rather than a better harness. The registered extension is Jeff's `~/Applications/TIDE-Rack-AUv3.app` dated 2026-08-25, i.e. PRE-E59 and without `TIDE_VCV_FUNDAMENTAL` or `RACK_ADAPTOR_TRACE`, so it cannot answer this cell. Five ways to register a current build beside it all failed: launching the built app; `pluginkit -a` on its appex; a clone given a distinct CFBundleIdentifier AND a distinct AU subtype (`Dr19`); valid inside-out ad-hoc signatures (`codesign -vvv --strict` passes on both bundle and appex); and `lsregister -f`. `pluginkit -m -i <id> -v` answers `(no matches)` throughout while Jeff's stays registered. **Measuring a CURRENT build's AU3 therefore requires displacing the developer's registration, which an unattended run must not do** -- if the run died mid-way his AUv3 would point at a deleted build tree. **WHAT A HUMAN NEEDS TO DO, and it is small:** on an unlocked session, build `TIDE_Rack_AU3_assemble` with `-DTIDE_VCV_FUNDAMENTAL=ON -DCMAKE_CXX_FLAGS=-DRACK_ADAPTOR_TRACE=1`, copy the resulting `TIDE-Rack-AUv3.app` over the one in `~/Applications`, launch it once (launch is what registers it -- a copy alone does not), and the cell's instruments are then the same ones the linux box used. **The build side is already proven this run:** `TIDE_Rack_AU3_assemble` is GREEN on macOS main with 0 errors, the appex links VCV Fundamental (76 references in its link edge, 38 module objects built), and carries both `RACK_ADAPTOR_TRACE` and E59's `declined to publish the startup default`. **Do not re-read `strings` for `VCV: Scope` as a presence check** -- ids are composed at runtime as `"VCV: " + slug` (`RackAutoRegister.h:40`), so the literal is absent from a correctly-linked binary. **AND E59'S FIX IS CONFIRMED ON macOS main through a real host:** `tests/hosts/v1-rack.rpp` renders **peak -6.3 dBFS, rms -17.0 dBFS**, the 2026-08-18 reference to the decimal, with `v1-rack-uncabled.rpp` at **-inf** as the negative control. Recipe in [docs/ci/headless-gui-verification.md](docs/ci/headless-gui-verification.md). **Previous text follows.** **THE CAUSE OF THE WINDOWS/LINUX VST3 FAILURE IS FIXED AND IN REVIEW -- E59, 2026-08-28 (windows, scheduled run), branch `tide/win/E59-controller-chunk-seeding`.** The sender was this plug-in's own `syncState()`, publishing its startup default because the host asks for state before it restores any; the bytes are retained and re-seeded into the next processor. With the refusal in place, `tests/hosts/v1-rack.rpp` renders **-6.3 dBFS / -17.0 dBFS / 440.0 Hz on Windows**, the macOS 2026-08-18 reference to the decimal, instead of digital silence. **THIS CELL IS STILL NOT RE-TAKEABLE YET, and the reason has changed:** E59 is `IN-REVIEW`, not merged, so a run on `origin/main` still measures the old behaviour -- and the clauses this cell never got to (the 60-second animation window, the int/bool/enum toggle, the pixel diff) were blocked by the DSP running a different document, which is only true again once E59 lands. **After it merges these clauses should be cheap**, and the harness for them already exists: `scripts/render-and-measure.py` runs on all three platforms, `scripts/make-host-fixture.py` builds a .rpp carrying any prepared rack, and `tests/hosts/README.md` now carries the Windows plug-in-isolation recipe that makes a measurement attributable to the build you just made. **Previous text follows.** **THE WINDOWS VST3 CELL IS MEASURED AND IT FAILS, FOR THE SAME CAUSE AS LINUX -- E59, WHICH IS NOT PLATFORM-SPECIFIC. 2026-08-28 (windows, scheduled run), branch `tide/win/E19-vst3-windows-leg`, [#536](https://github.com/JeffMcClintock/TideSynth/pull/536), REAPER 7.78.** **FAIL -- win/VST3.** A project carrying a prepared rack restores it into the processor and then throws it away: `TIDE: building rack from 38654 byte document (Sync chunk, rack not yet prepared)` followed seconds later by `TIDE: building rack from 17961 byte document (Sync chunk, rack not yet prepared)` -- 17,955 to 17,961 bytes across four runs, squarely inside the 17,957-17,963 the linux run measured for the DEFAULT rack. **So the DSP ends up running the default rack on Windows exactly as it does on Linux**, and E19's own definition of a frozen feedback path is met for a reason that has nothing to do with forwarding. **THE AUDIO ARTIFACT IS THE STRONG ONE, AND IT IS THIS REPO'S OWN v0.1 FIXTURE.** `tests/hosts/v1-rack.rpp` -- three prefabs cabled to the output, the fixture PLAN cites for *the patch plays after reload* -- renders **peak -inf, rms -inf, digital silence** through REAPER 7.78 on this box, with its 2 patch cables intact in the saved document. On macOS 2026-08-18 the same fixture rendered **-6.3 dBFS at 440.0 Hz**. Its log shows the same two builds: `building rack from 14136 byte document (Legacy chunk...)` then `17957 byte document (Sync chunk...)`. **The control that makes that silence mean something:** `python3 scripts/render-and-measure.py --control` on this box reports **peak -6.0 / rms -9.0 dBFS**, its expected values, so the chain detects audio here. **NOT MEASURED, and it is the rest of this cell's Accept:** the 60-second animation window, the int/bool/enum toggle and the pixel diff were **not** run, because they cannot pass while the DSP is running a different document -- there is nothing to animate from, which is E59's finding and not a new one. **Do not re-take this cell until E59 closes.** **WHAT THIS RUN LEFT BEHIND SO THE RE-TAKE IS CHEAP:** `scripts/make-host-fixture.py` builds a .rpp carrying any prepared rack (tests/hosts/README.md said no script wrote these), and `scripts/render-and-measure.py` now runs on all three platforms rather than macOS only. **Two traps, each of which cost a measurement.** (1) **A local build does not shadow the installed plug-in and REAPER will silently load either one** -- it alternated between them across runs here. Put a distinguishing string in the build and read it back off the log; the `(%s chunk, rack %s)` suffix above is that string. (2) **A bare .vst3 DLL copied out of the build tree has no `Contents/Resources`**, so it starts with an empty rack and every measurement of rack CONTENT taken against it is void -- assemble the bundle folder. **Also confirmed on Windows:** REAPER 7.78 needs E29's token swap, as tests/hosts/README.md already says. **Previous text follows.** **THE LINUX VST3 CELL IS MEASURED AND IT FAILS, AND THE CAUSE IS NOT THE FEEDBACK PATH -- THE DSP IS RUNNING A DIFFERENT RACK FROM THE ONE THE EDITOR IS SHOWING. 2026-08-28 (linux, scheduled run), branch `tide/linux/E19-linux-feedback-leg`.** **PASS -- linux/STANDALONE** (not in this row's linux matrix, recorded because it is the control that makes the FAIL mean something): the same build, the same document, the same box, headless weston -- `light` reaches update **#276800** and `display-state` **#65150** at 65,548 bytes/frame over **~40 minutes** unbroken, and two screenshots 16 min apart differ by **1,214 px**, **498** of them in the 100x100 tile over the Scope display. **FAIL -- linux/VST3, REAPER 7.43:** the prepared rack **restores in the EDITOR** (screenshot: VCV LFO and cabled VCV Scope drawn in the rack, in the positions the document names), but the processor logs `building rack from 29101 byte document` and then, immediately, `building rack from 17957 byte document` -- the size of the DEFAULT rack. So the DSP runs the default rack while the editor shows the restored one, and there is nothing for the LFO light or the Scope display to animate FROM. **The numbers, transport rolling, `playstate=1`, position 0 -> 65 s:** `light` frozen at update **#2 value 0.000**, `display-state` frozen at **#1 arrived (0 bytes)**, `apply` reads `expect=65548 sum=0`, and **0 of 690,800 pixels changed between screenshots 60 s apart** -- this row's own definition of frozen. **THE AUDIO CONTROL IS WHAT MAKES THAT A FACT RATHER THAN A GUESS:** with the LFO's TRI output ALSO cabled to the rack output, `reaper -renderproject` renders **-inf, digital silence**, which is what the DEFAULT rack sounds like and not what the prepared one does. Filed as **E59**. **NOT MEASURED -- linux/CLAP, and the blocker is measured rather than assumed:** REAPER 7.43 lists and loads it (`CLAPi: TiDE Rack (TiDE Synth)`, editor opens, default rack draws) but **no prepared rack could be got into a hosted CLAP instance** -- a 39 KB `<STATE>` block read back as the default 116 chars, and `SetTrackStateChunk` with 53,605 chars returned `true` and read back **698**. The identical construction works for VST3. Filed as **E60**. **DO NOT RE-DERIVE THE HARNESS:** REAPER 7.43 Linux is portable, runs under `weston --backend=headless --xwayland`, and needs `GDK_BACKEND=x11` **with `WAYLAND_DISPLAY` unset** or it SEGFAULTS in libX11 through a chain of `gdk_x11_*` assertion failures; audio needs `linux_audio_mode=2` (Dummy Audio) in `reaper.ini` -- `prefs_audiodev` is NOT the key. Written up in [docs/ci/headless-gui-verification.md](docs/ci/headless-gui-verification.md). **Two side facts recorded so nobody spends a session on them:** REAPER 7.43/Linux writes the VST3 token `1013510754{506C7567696E474D50492050A2A07287}` -- the SAME hex as the committed 7.45/macOS token with a different leading id, which is an **E29** datum; and a SECOND TIDE instance in one REAPER process logs `VCV Fundamental - 0 module(s) registered`, so the deferred-registration queue is one-shot per process (only reachable with `TIDE_VCV_FUNDAMENTAL=ON`, which is not a shipped build). **Previous text follows.** **THE WINDOWS VST3 CELL IS STILL NOT MEASURED, AND THE BLOCKER IS NO LONGER THE ONE THIS ROW NAMES. 2026-08-27 (windows, scheduled run), branch `tide/win/E19-vst3-feedback-leg`.** **The two blockers the row named have both closed** -- **E27** is DONE (the ~14.1 KB "threshold" was the fixture generator, not the plugin) and **E29** is WONTFIX with a per-box `sed` workaround -- so I took the cell expecting the VST3 leg to be reachable. It is not, and the new wall is upstream of REAPER entirely: **I could not get a prepared rack to reload in the STANDALONE, let alone in a host.** Three separate failures, all measured, all filed: **E48** (reloading a saved rack raises a modal `Connectors lost while loading` and drops 3,577 bytes of the document), **E49** (yesterday's 38,658-byte fixture now segfaults on load, 4/4), and **E50** (a `VCV: Compare` constructed at startup from a default rack that contains none -- the probable common cause of the other two). **What this means for the row, stated so the next run does not re-take it blind:** the VST3 leg needs a prepared rack restored inside a hosted instance, and until **E48/E49/E50** are understood there is no prepared rack this build will reliably reload ANYWHERE. **Fix those first; this cell is cheap afterwards** -- the plugin already loads in REAPER 7.78, its editor draws, and the adaptor trace already reaches a shell-redirected stderr, all established 2026-08-26. **Do not re-derive:** the standalone command channel builds a rack unattended (double-click a browser entry inserts; `--drag` on a rack module did NOT move it, twice, so layout must come from the document); a screenshot path must be a Windows path, not an MSYS one; and the config folder is resolved with `SHGetKnownFolderPath`, so `%APPDATA%` **cannot** be redirected by environment -- back the folder up and restore it instead. **Original text follows.** **WINDOWS STANDALONE CELL IS DONE — taking the WINDOWS VST3 cell, whose two named blockers have both closed since this row was written: **E27** is DONE (the ~14.1 KB "threshold" was the fixture generator, not the plugin) and **E29** is WONTFIX with a documented per-box `sed` workaround in [tests/hosts/README.md](tests/hosts/README.md). The GMPI cell is NOT in scope — it needs SynthEdit hosting a rack built by hand, which this row already records as un-automatable.**  **WINDOWS STANDALONE CELL IS DONE; THE WINDOWS VST3 AND GMPI CELLS ARE BLOCKED, AND THE BLOCKER IS WORTH MORE THAN THE CELLS. 2026-08-26 (windows, scheduled run), branch `tide/win/E19-feedback-format-matrix`, PR [#425](https://github.com/JeffMcClintock/TideSynth/pull/425).** **PASS — win/STANDALONE, float + blob:** VCV LFO cabled SIN->IN 1 into a VCV Scope, pins verified in the trace (`LFO ... outs=1000`, `Scope ... ins=100`), sustained **63 s** with display-state applies #22,100 -> #38,160, light updates #20,300 -> #27,800, 65,548-byte frames, 52 distinct checksums across the last 200 applies, and **5,157 pixels changed** between screenshots 63 s apart with the busiest 100x100 tile on the Scope display. No freeze signature. **int/bool/enum and string: NOT measured.** **THE INSTRUMENT THIS ROW SHOULD HAVE NAMED is the standalone command channel** (`\.\pipe\gmpi-standalone.<pid>`, printed at startup, `GMPI_Wrappers/wrapper/Standalone/mcp/`): `--drag`, `--pointer-*`, `--screenshot`, `--set-param`. It calls the editor's own input client rather than injecting OS input, so it **works where the 2026-08-25 `SetCursorPos`/`PostMessage` dead end did not** — the whole rack above was built unattended. Coordinates are DIPs from the window top-left; screenshot pixel / `scale` = DIP. **WHY VST3 AND GMPI COULD NOT BE MEASURED: that channel is standalone-only**, so a rack cannot be built inside a hosted instance, and restoring a prepared one hits **E27** (any document over ~14.1 KB is silently dropped on VST3 state restore) while the fixtures themselves hit **E29** (REAPER 7.78 does not match the plugin id the committed `tests/hosts/*.rpp` record). **Both must close before these cells are automatable**, or one human-built fixture per format has to be saved and committed. **Do not re-derive:** framing, preset size, preset prolog/attributes, registration order and document content were each controlled out — see the journal entry. Original text follows. **Test the DSP→GUI feedback path on EVERY format target, for every datatype.** The return path landed 2026-08-25 ([#410](https://github.com/JeffMcClintock/TideSynth/pull/410) plus its fix commit): the processor drains its rack's `queDspToUi` and forwards WHOLE `ppc` messages to the editor as blob parameter 2 (`SynthEdit::drainRackFeedback` → `TideApp::receiveRackFeedback` → `SynthRuntime_editor::receiveDspMessages`). Verified so far on ONE target and ONE platform only — the Windows STANDALONE — for two of the five datatype shapes: **float** (VCV LFO's light animates live) and **blob including 64 KB display-state frames at the full ~30 Hz rate, sustained 60 s past the freeze horizon** (VCV Scope's trace sweeps; the first cut wedged after seconds because a GMPI parameter is last-writer-wins and a dropped ARBITRARY-BYTE chunk tears the stream framing — whole-message forwarding is the load-bearing fix, do not "simplify" it away). **Accept, per format target — VST3, AU3, CLAP, GMPI, STANDALONE — on its natural platform matrix (win: VST3+GMPI+STANDALONE in REAPER or Ableton; mac: AU3 in a real host, not just auval; linux: VST3+CLAP in REAPER or Bitwig):** load a rack with a VCV LFO (light) and a cabled VCV Scope (display state); BOTH animate continuously for ≥60 s — the measured freeze horizon, so a shorter look is not a pass; **int/bool/enum** verified by toggling a VCV context-menu option and seeing the DSP obey (menu options are the int/bool parameters this path carries); **string** has no current producer — note that in the row rather than inventing one. The adaptor's `RACK_ADAPTOR_TRACE` build switch is the instrument: `display-state update #N` arrivals plus `apply #N … sum=` checksums MUST still be advancing at the end of the window, and `light … update #N` likewise. A pixel-diff of two screenshots ≥5 s apart at the scope display is the screen-level check (`~20k sampled pixels changed` on the working Windows standalone; **zero means frozen**, which is a FAIL even if audio runs). Watch specifically for the freeze-after-a-handful signature — it means chunked/partial forwarding regressed. |
| X2 | TODO | linux | **EVERY LINKED PR HAS MERGED AND NOTHING IS OPEN, SO IN-REVIEW IS FALSE -- AND THE ROW'S OWN WORDS SAY DONE WOULD BE FALSE TOO. 2026-08-27 (windows, scheduled run), STEP 4 bookkeeping; the E32 precedent.** [#484](https://github.com/JeffMcClintock/TideSynth/pull/484) is MERGED, verified with `gh pr view`. The row states *"ACCEPT MET FOR TIDE'S OWN CODE; NOT MET TREE-WIDE"*, and its Accept is a tree-wide zero -- so it goes back to TODO rather than to DONE. **WHAT IS LEFT, and it splits cleanly in two:** (1) tree-wide zero is **a decision about `SynthEditLib`, not a task** -- 1,547 of the remaining 1,967 unique warnings are GATED, which is Jeff's call and not a row a run can take; (2) **the row's own "NOT VERIFIED" half is Windows and macOS**, which warn about different things and were never built. **(2) is takeable and (1) is not**, so whoever re-scopes this should probably split them. The Windows half belongs to this box and I did not do it -- this run was on E19 and STEP 3 allows one item. **Previous text follows.** **TIDE'S OWN SHARE IS ZERO: 15 -> 0. 2026-08-27 (linux, interactive, Jeff directing), branch `tide/linux/X2-wall-wextra`.** **THE CENSUS FIRST, because *"zero"* is not a plan until the shape is known — and the shape is the finding.** Full tree at `-Wall -Wextra`, GCC 13.3.0: **12,255 warning LINES but only 1,982 UNIQUE warnings** — a 6x inflation from headers being re-included, so the raw count this row implies is not the number to work from. **By origin, which is what makes this row tractable:** **SynthEditLib 1,547 (78%, GATED)**, fetched `_deps` 134, gmpi_ui 132, GMPI_Wrappers 112, GMPI 26 (PR-GATED), third-party 16, and **TideSynth's own code: 15.** So the row reads like a mountain and TIDE's share was fifteen lines. **ALL 15 FIXED, and one of them was a trap:** five variables in `TiDEPanelGui.cpp` exist only to feed `TIDE_LOG`, which compiles to `((void)0)` when `TIDE_PANEL_TRACE_LOG=0` — **deleting them would have broken the diagnostic build**, so they are `[[maybe_unused]]`. Nine unused parameters on overrides are likewise `[[maybe_unused]]` rather than unnamed, because the names document the interface. **`monotonicMs()` is DELETED, not silenced:** its own comment claimed it was *"for the settle timer below"*, but that timer waits on a chrono duration and never polls a clock, so it had **no callers at all** — a comment naming a consumer that no longer consumes is what makes dead code look live. **VERIFIED BOTH WAYS, which is the control:** `-Wall -Wextra` with logging OFF **and** with `-DTIDE_PANEL_TRACE_LOG=1` — both rc=0, both **0 TIDE-own warnings**. The logging build is what a deletion would have broken. **ACCEPT MET FOR TIDE'S OWN CODE; NOT MET TREE-WIDE** — the other 1,967 belong to other repos and 78% of them are behind the GATED line, so a tree-wide zero is a decision about `SynthEditLib`, not a task. **NOT VERIFIED:** Windows and macOS were not built, and their compilers warn about different things. **CLAIMED 2026-08-27 (linux, interactive — Jeff asked for a linux-only task, which is what lifts the BLOCKED mark; the staleness is recorded on X1 below).** Branch `tide/linux/X2-wall-wextra`. Measuring first: nobody has ever published a warning count for this tree, and *"zero"* is not a plan until the current number and its shape are known. Zero-warning build at `-Wall -Wextra`. |
| E11 | WONTFIX | any | **MEASURED 2026-08-21 (macos), [#256](https://github.com/JeffMcClintock/TideSynth/pull/256) — THE HAZARD IS UNREACHABLE — not a bug, and the reason is worth more than the row.** This row asked for the drain ordering to be established before any patch. The ordering turns out not to matter, because **the pointer is never taken.** Static proof, and it is exhaustive rather than a sample: in `GMPI/Hosting/processor_holder.h` the processor-side parameter's `value_` is written in exactly **two** places, both at construction — `:87` (a double default) and `:91` (`std::vector<uint8_t>{}`, an EMPTY blob) — and **`setBlob` at `:135` is a STUB**: its one assignment is commented out at `:137` and it unconditionally `return true`s. `grep` for `value_` across the header and the .cpp finds only those two writes and two reads. **So for a blob parameter the vector is empty for the object's entire lifetime**, and both sites that could publish a raw pointer decline to: the seed at `processor_holder.cpp:224` hits `if (!v || v->empty()) continue;`, and `sendParameterToProcessor` at `:337` takes the inline branch because `v.size() > 8` is false when the size is 0. **`e.oversizeData_` is never assigned on this path**, so nothing can dangle. **The genuine defect is the stub, and it is E6's, not a new row** — see the note added to E6 the same day, per A31's grep-before-filing habit, which is what stopped this becoming a duplicate. **Reopen this row only if `setBlob` is ever implemented**, because the moment the vector holds bytes both pointer sites become live and this row's analysis applies exactly as written. **Previous text follows.** **UNVERIFIED, filed so it is not lost: `processor_holder.cpp:231` publishes a raw pointer into a `std::vector` that another thread may reallocate.** The blob seed does `e.oversizeData_ = v->data()` with no byte copy, and `EventQue` stores the `Event` by value (`processor_holder.h:22-29`). The comment justifying that at `:333-336` argues "queue drain and the next parameter update happen on the same thread, so the vector cannot move underneath the event" — which holds for the `ppc3` path it was written about, but **`start_processor` runs on the host's main thread** while the drain happens in the first `process()` on the audio thread. If a `setBlob` reallocates in between, the event points at freed memory and TIDE parses whatever is there — which is also another route into **E10**'s crash. **Not measured**: found by an agent reading the code during E9's research (2026-08-19) and explicitly flagged "ordering unverified"; nobody chased the drain ordering, so this may be perfectly safe. **Take** = establish the ordering first (does anything write parameter 1 between `start_processor` and the first `process()`? a DAW state restore plus an editor push is the suspicious pair), and only then decide whether the fix is a copy, a shared_ptr, or a comment saying why it cannot happen. **Scope** `GMPI/Hosting/` — Jeff's, not in the GATED list, but a hosting-layer change with every plugin downstream, so it wants a measurement before a patch. **Size** small to fix, unknown to prove. |
| S20 | WONTFIX | any | **ANSWERED BY JEFF 2026-08-20: *“I run CI manually most days. not a biggie.”* So this is a recorded FACT, not an open question** — the manual chain is deliberate and adequate. **Kept open rather than WONTFIX for one reason only:** a run cannot tell from the outside whether CI has exercised its work, so anything claiming “CI is green” must first check when the chain last ran. That is a reading habit, not a change. **Do not file the `on: push` workflow** — the chain signs, notarizes and FTP-uploads to synthedit.com, and Jeff has said the cadence is fine. **Previous text follows.** **SynthEdit's whole CI chain is `workflow_dispatch` only, so nothing is tested on push — found 2026-08-20 (macos) while confirming S19's fix.** `SE16 Kickstart Build` has **`on: workflow_dispatch:` and nothing else**; `SynthEdit 1.6 cmake_win` triggers off *it* via `workflow_run`, and `cmake_mac` triggers off `cmake_win`. So a push to `master` runs **no** build and **no** tests — the last dispatch was 2026-08-19T09:34 and `master` has moved eight times since with zero runs. **This is half the reason S19's five failures survived a week:** `continue-on-error` hid them, and the chain fired rarely enough that few people saw a log at all. **The obvious fix is wrong and that is why this is a row rather than a patch:** the chain is a RELEASE pipeline — it signs, notarizes, staples and **FTP-uploads to synthedit.com** — so `on: push` would publish on every commit. **Scope** a ruling on whether a separate build-and-test-only workflow should run on push, with the release chain left manual. **Accept** a push to `master` produces a pass/fail signal without publishing anything. **Size** the ruling is Jeff's; the workflow is small but is `.github/workflows/**`, which the fleet token structurally cannot push — so this needs his hand either way. **NOT dispatched by the run that found it**, deliberately: triggering that chain is a release, not a test. |
| S23 | WONTFIX | any | **CLOSED 2026-08-24 BY JEFF, in session: *"close S23"*.** This row's own suggested disposition — *"close S23 as not-reproducible once S34 is on the board, rather than leaving an open crash row that no longer describes anything observable"* — is now taken, and its stated precondition is met: **S34 is DONE** (both guards merged in [SynthEditLib#34](https://github.com/JeffMcClintock/SynthEditLib/pull/34)), so the two real latent defects the signature decode found are fixed regardless of whether they ever caused this crash. **Closed as not-reproducible, NOT as fixed** — nobody identified the cause, and the row is explicit that the likeliest explanation is it was fixed in passing by the churn after 2026-08-20 (E14, E15, S26, TiDEPanel, tide_render). **Do not re-file:** the reproduction attempt is exhausted on this platform — three conditions, seven launches, zero faults, zero `TIDE_STANDALONE` segfaults in `journalctl` — and the row says more stress runs are not the way to find out. If it reappears, it is new evidence and deserves a new row citing this one. **THE TARGETED REPRO RAN, IN AN ISOLATED COMPOSITOR, AND THE CRASH DOES NOT REPRODUCE ON CURRENT `main`. Measured 2026-08-21 (linux), branch `tide/linux/S32-headless-compositor`.** Now possible because S32 is solved — headless `weston` means a run can launch the standalone without risking the developer's desktop. **Three conditions, seven launches, all clean:** **(1) control, resources present** — 60 s, no fault. **(2) resources ABSENT** — the layout both original crashes were in — 60 s plus **four 10 s runs each ended with SIGTERM, every one exit 0** (and a clean SIGTERM exit really is 0 here, so a 139 would have been unambiguous). **(3) resources absent PLUS the quarantined `session.previous.xml`** — the mismatch case nobody had tried, a patch saved when the modules existed replayed when they do not — 45 s, no fault. **Zero `TIDE_STANDALONE` segfaults in `journalctl` throughout.** **The startup state was confirmed identical to the crash runs**, not merely similar: all four `… missing from bundle resources - those controls will have no pins`, `no Prefabs folder in bundle resources`, and `MidiCv.synthedit did not insert a container - the rack will have no MIDI jacks`. **So the reproduction attempt is exhausted on this platform and should not be repeated.** Most likely it was fixed in passing by the churn since 2026-08-20 (E14, E15, S26 and Jeff's TiDEPanel/tide_render work all landed after it); the alternative is an ingredient nobody has named. **Either way, more stress runs are not the way to find out.** **What survives, and it is the useful part: the signature decode stands, and it points at two REAL latent defects regardless of whether they caused this crash — now filed as S34.** **Suggested disposition:** close S23 as not-reproducible once S34 is on the board, rather than leaving an open crash row that no longer describes anything observable. That is Jeff's call, not a run's. **Previous text follows.** **THE FAULT SIGNATURE IS NOW DECODED, AND IT NAMES A CLASS OF SITE THIS PROJECT HAS ALREADY BEEN BITTEN BY ONCE. Measured 2026-08-21 (linux, scheduled run, third item at Jeff's direction), branch `tide/linux/S23-addr2line` ([#263](https://github.com/JeffMcClintock/TideSynth/pull/263)).** **`-8` MEANS `back()` ON AN EMPTY CONTAINER OF 8-BYTE ELEMENTS — measured, not reasoned.** A three-case control program, each case in its own child, read back from the kernel log: `dynamic_cast` on a zeroed vptr faults at **-16**; `back()` on an empty `vector<int>` faults at **-4**; a plain `null->member` faults at **+12** (and `error 4`, not 5). Our crash is **-8 with error 5**, so it is `back()` / `rbegin()` / `end()[-1]` on an **empty vector of 8-byte elements — a vector of raw pointers.** **This kills two earlier guesses, including one made an hour before:** it is not `dynamic_cast` on a dangling handle (that is -16), and it is not a null `->field` (that is positive). **THE PRECEDENT IS ALREADY IN THE TREE, from this fleet's own BACKLOG U2d.** `SynthEditLib/modules/ControlsXp/ClassicControlGuiBase.cpp:22` carries the comment *“`widgets.back()` on an empty vector is UB (crashed TIDE at address -16 = empty `back()` with 16-byte elements; TideSynth BACKLOG U2d)… a host where those don’t fire must not bring the whole process down”* — **same class, same arithmetic, and the same trigger shape: a resource-dependent collection that stayed empty.** U2d's crash was *“check font/skin resources”*; S23's two crashes were both in the layout with the **bundle's `Resources` missing**. **TWO UNGUARDED CANDIDATE SITES, both `std::vector<UPlug*>` (a vector of raw pointers — **S34**) so both fault at exactly -8:** the adder site (**S34** owns the citation) — `auto p = plugs.back();` at the top of `ug_adder2::NewConnection()`, no `empty()` guard; and the feedback-delays site (**S34**) — `auto dummyPin = u->plugs.back();`, no guard. A third sibling, its guarded sibling (**S34**), **is** guarded (`while(!…empty())`), which is what the other two should look like. **The adder is the interesting one:** it implements TIDE's automatic summing when patch cables fan into one input, and `NewConnection` runs while the DSP graph is built from a restored patch — i.e. at startup, which is when both crashes happened. **NOT PROVEN, and do not let the tidiness of the story stand in for a repro:** nothing here shows the fault was at either line. The chain ‘resources missing → pin list empty → `back()`’ is a hypothesis that fits every measured fact, not an observation. **THE OFFSET ROUTE IS PERMANENTLY CLOSED, so do not spend on it — this run closed it properly.** `/var/log/apport.log.1` names the crashing binary as `<scratchpad>/22760dc3-…/s17/TideSynth/build-sa/SynthEditSem/TIDE_STANDALONE` — a clone in the **S21 run's own scratchpad**, wiped by the reboots since, and **a different build tree from the one the last run resolved `0x3b4627` against**. Confirmed by disassembly: `0x3b4627` lands **mid-instruction** in today's binary, so the `CContainer::OnEditContain` hit was never a valid resolution. **No core exists either** — apport logged `core limit 0` and *“executable does not belong to a package, ignoring”* for both crashes. And an exact rebuild is not reproducible: at `21f9c80` there was **no `cpm-package-lock.cmake`**, so every dependency resolved to whatever its branch head was that day. **Take** = a targeted repro, not archaeology: launch `TIDE_STANDALONE` with the bundle's `Resources` absent (the layout both crashes were in) under `gdb`, and see whether it stops in either candidate. That is minutes and it either names the frame or clears both sites. **Read S32 first** — launching the standalone on this box has taken the developer's desktop down, and a nested compositor is the safe way to do it. **Both files are GATED (`SynthEditLib`) and this is not a build break, so A17's exception does not reach it — filed, not fixed.** **One incidental gap, noted rather than filed to avoid a second id for one job:** `ClassicControlGuiBase.cpp:9-11` (`onSetTitle`) `dynamic_cast`s and then calls `header->SetText` with **no null check**, while its sibling `initialize()` at `:31` checks. Positive-offset fault, so not S23, but the same file and the same class of omission. **Size** small. **Previous text follows.** **THE SESSION-FILE HYPOTHESIS IS DEAD, AND THE KERNEL HAD THE ANSWER ALL ALONG. Measured 2026-08-21 (linux, scheduled run), branch `tide/linux/S23-session-replay` ([#261](https://github.com/JeffMcClintock/TideSynth/pull/261)).** **The experiment ran and came back clean:** `session.previous.xml` (17,866 bytes, md5 `010ed62b3baa7bbdda95ee935108d6e7`) copied to `session.xml` under `XDG_CONFIG_HOME=<scratch>` with no `session.loading` beside it, launched under `gdb` — **restored fine, ran ~3 minutes, no signal.** Three independent confirmations that the restore SUCCEEDED rather than merely not crashing: the scratch dir has **no `session.previous.xml`** afterwards (so `parametersAreReadable` passed and `host_.restoreState()` returned true — no quarantine), the app **rewrote** the scratch `session.xml` (17,866 -> 17,346 bytes, new mtime; the debounced save only runs in a healthy session), and a screenshot shows the restored rack drawing. **Jeff’s own config was byte-identical before and after** (both md5s unchanged) — `XDG_CONFIG_HOME` isolation works as advertised. **BUT THE REAL FINDING IS IN `journalctl`, WHICH NOBODY HAD READ.** The kernel logged both original crashes: `Aug 20 17:32:58 TIDE_STANDALONE[23881]: segfault at fffffffffffffff8 ip 000062312e4f7627 ... in TIDE_STANDALONE[3b4627,62312e1cb000+404000]` and `Aug 20 17:33:27 TIDE_STANDALONE[23924]: segfault at fffffffffffffff8 ip 000055bae7342627 ... in TIDE_STANDALONE[3b4627,55bae7016000+404000]`. **Same module offset `3b4627` both times, under different ASLR bases — so this is ONE deterministic code site, not a random memory smear.** The fault address `0xfffffffffffffff8` is **-8**, and `error 5` decodes as a **user-mode READ of a mapped page** — the signature of reading 8 bytes *before* a null-ish base (`back()` on empty, `*(--it)` at `begin()`, a length stored ahead of a null data pointer), not of a plain null `->field`. **29 seconds apart, and that interval is the proof the session file is innocent:** crash 1 at 17:32:58 leaves the breadcrumb, so the NEXT launch quarantines (`session.previous.xml` is stamped **17:33**, exactly between the two) and comes up **at defaults** — and that defaults run **crashed anyway at 17:33:27, at the same offset.** **So the second crash was not loading the quarantined file, and the replay above agrees from the other direction.** **Also settled: `session.previous.xml` IS a quarantine artifact** (its 17:33 stamp sits between the two crashes), not the `File > Revert to Plugin Defaults` false positive the row hedged about — `keepCurrentAside` is reachable from exactly one place, `StandaloneApp.cpp:353`, armed at `:180`. **Not reproduced since:** `journalctl` shows **zero** `TIDE_STANDALONE` segfaults after 2026-08-20 17:33 — across the previous run’s 28 controlled runs and ~1000 driven insertions, and this run’s 3-minute replay. **A LEAD, EXPLICITLY LABELLED UNRELIABLE — do not spend on it without doing the one step that makes it reliable.** `addr2line` on **today’s** binary at `0x3b4627` gives `CContainer::OnEditContain()` at `SynthEditLib/EditorLib/CContainer.cpp:2338`, and `:2330` there does use a `dynamic_cast` result with **no null check** (`placeHolderIoMod = dynamic_cast<CContainer*>(...HandleToObject(placeHolderHandle)); ... placeHolderIoMod->Plugs.size()`). **Tempting, and probably a coincidence:** it is a DIFFERENT binary from the one that faulted, and a null `->Plugs.size()` would fault at a small POSITIVE offset, **not at -8** — so the signature does not match. Recorded only so the next run need not re-derive it. **Take** = rebuild the tree as it stood on 2026-08-20 ~17:30 (same flags; `-g` does not change codegen at `-O3`) and resolve `0x3b4627` in THAT binary. That names the frame and closes this row. Everything else about the crash — deterministic site, -8 read, twice in 30 s — is already measured above. **Do NOT do more stress runs; they have never reproduced it and this box cannot afford them (see S32).** **Size** small, now that the target is one `addr2line` against one rebuild. **Previous text follows.** **TAKEN AND HANDED BACK 2026-08-21 (linux, scheduled run), branch `tide/linux/S23-standalone-segfault` ([#259](https://github.com/JeffMcClintock/TideSynth/pull/259)) — the blocker is cleared, one hypothesis explains the whole observed pattern, and the experiment that decides it is written but NOT RUN.** **`gdb` IS INSTALLED** (15.1, `Ubuntu 15.1-1ubuntu1~24.04.1`), so this row's *"nothing here names a frame"* is stale. **One hypothesis is dead:** `libpipewire-0.3-dev` is `1.0.5-1ubuntu3.3` and the linked runtime is `libpipewire-0.3.so.0.1005.0` — the same version — so S21's `apt-get download` workaround did NOT compile against mismatched headers. **It did not reproduce, with a rate:** 4 native runs at a 25s window (rc=0 each; a clean SIGTERM shutdown exits 0, so a 139 is unambiguous) plus one `gdb` run driven ~13 minutes over the command channel — roughly 1000 arm-then-click prefab insertions each followed by a full-window screenshot — **no signal**; the only stop in the log is the run's own `kill -9`. **THE FINDING WORTH MORE THAN THE REPRO: the app quarantines the input after the first crash, so a crash here is self-limiting to ONE occurrence per bad file.** `SessionState` writes a breadcrumb `session.loading` before reading the patch and removes it after (`GMPI_Wrappers/wrapper/Standalone/SessionState.cpp:395-419`); a launch that finds one concludes the previous run died inside the read, moves `session.xml` aside to `session.previous.xml`, and comes up at defaults (`:325-328`). Its own header says why: an assert inside the parse is *"the failure a return code cannot catch"*, and without this *"the app would die on every launch from now on"*. **That is exactly the shape this row recorded** — two crashes, both on a *first* run, then 28 controlled runs finding nothing. **The 28 runs were not evidence of rarity;** they were runs of an app that had already thrown the offending file away. **And the suspect file is still on disk:** `~/.config/TIDE Rack/session.previous.xml`, **17,866 bytes, dated 2026-08-20 17:33** — same box, same day as the two crashes. **Not proven:** `keepCurrentAside` (`:255`) rotates to that same filename for ordinary reasons, and the header names a false positive of its own (a second instance launched while the first is still in `restore()` quarantines a good file). **Take** = run the one experiment, which needs nothing from Jeff and cannot touch his config: `StandaloneSettings.cpp:49` honours **`XDG_CONFIG_HOME`**, so copy `session.previous.xml` to `<scratch>/TIDE Rack/session.xml`, ensure no `session.loading` is beside it, and launch under `XDG_CONFIG_HOME=<scratch> gdb -batch -ex run -ex bt --args ./TIDE_STANDALONE`. **A segfault names the frame and closes this row; a clean run eliminates the only hypothesis that explains the pattern.** Seconds, against hours of stress. **Do that BEFORE any further stress runs.** **Why this run stopped:** the desktop session on the linux box crashed twice during the work, and it is Jeff's machine. **Stated with its control, because the control is what matters:** the first came during the heavy insertion stress, the second came **while `TIDE_STANDALONE` was not running at all** — so **the correlation with TIDE is unsupported**, and that second event refutes it. The box is a **VirtualBox** VM whose log carries `libEGL warning: Ensure your X server supports DRI3` and repeated `Invalid sequence for VSYNC frame info`, and `gnome-shell` restarted three times in four minutes; neither event was a reboot. Recorded as an observation about the box, **not** a finding about the product — S21 made precisely that mistake with these same crashes and had to retract it. **Previous text follows.** **SUPERSEDED 2026-08-20: Jeff installed `libpipewire-0.3-dev` system-wide on the linux box (1.0.5-1ubuntu3.3), so the `apt-get download` + `dpkg-deb -x` scratch-prefix recipe below is NO LONGER NEEDED and should not be reconstructed.** Verified with the workaround deliberately removed — scratch prefix deleted, `PKG_CONFIG_PATH` unset, build tree wiped: configure **rc=0** with **zero** `STANDALONE skipped` lines, build **rc=0, zero errors**, `TIDE_STANDALONE` produced. **So visual verification on the linux box now costs nothing** — build the target, run it, and it prints a command-channel socket an MCP server can screenshot, click and drag. Rows that say otherwise are stale. **An intermittent `TIDE_STANDALONE` segfault on Linux, seen twice and NOT isolated. Filed 2026-08-20 (linux) so it is not lost, and deliberately NOT attached to S21.** **Observed:** exit 139 with a core dump, both times on the **first run after the bundle's `Resources` directory was moved**, with a 10s run window. **Did not reproduce:** 28 controlled runs — 8 per layout at a 6s window, 6 per layout at 12s, across both the pre-S21 and post-S21 resource layouts — **zero crashes**. So the correlation with the missing-resources layout is **unsupported**, and the run that first reported it had it backwards and says so. **Plausible but unverified:** it may depend on run duration rather than layout, since both crashes had a longer window than any of the 28 clean runs — try 20-30s first. **What is missing to make this actionable: a backtrace.** `gdb` is **not installed** on the linux box, so nothing here names a frame. **Take** = install `gdb`, reproduce with a longer window, get a stack. The app is easy to drive: build the `TIDE_STANDALONE` target, run it, and it prints a command-channel socket an MCP server can screenshot and click. **Note the standalone needs `libpipewire-0.3-dev`**, not installed system-wide on the linux box — this run worked around it with `apt-get download` + `dpkg-deb -x` into a scratch prefix with the `.pc` `prefix=` repointed, because `sudo` needs a password no unattended run has. **Size** unknown — a diagnosis, not a fix. |
| S28 | WONTFIX | any | **CLOSED 2026-08-21 — NOT A DEFECT, and this row's premise was wrong twice over.** Jeff: *"if they have no GUI class (which MIDI-CV2 does not), they already don't render on the rack canvas (only on the structure-view). That's already implemented and working."* **Verified rather than accepted, and it splits the two modules this row lumped together:** `SE MIDI to CV 2` has **no GUI class** — no `*Gui*` registration anywhere in `SynthEditLib` — and its saved rect is **0,0,0,0**, which is why the probe already reports it as *"zero rect (no panel presence)"* and skips it. The self-exclusion this row asked for **already exists, by mechanism**. **`MIDI In` is the opposite case and that is why it measured 8x14:** it DOES have a GUI class (`modules_internal/MidiInGui.cpp:123`, `Register<MidiInGui>::withId(L"MIDI In")`), so it renders because it is supposed to. **And under the same day's I/O-modules ruling that is now correct rather than stray** — the I/O modules ARE rack modules, and `MIDI In` is one of them. It is simply not rack-SHAPED yet, which is **E2's authoring work on item #4 of the ruled set**, not a defect row. **What this cost and what it teaches:** the row was filed from a probe result without asking why the two root modules differed, when one grep would have shown one of them has no GUI at all. **Previous text follows.** **SHARPENED 2026-08-21 by Jeff's clarification, which settles half of this row.** Placing MIDI-CV2 at the ROOT and routing it into a Container that presents it as patch-points **is the intended architecture** — *"how the end-user thinks of it, not how it is implemented"*, and *"this is already solved"*. **So the root placement is correct and is NOT what this row questions.** What is left is narrower and still worth answering: **the root implementation modules should not RENDER on the rack canvas at all**, because the Container facade is their user-facing representation — a stray 8x14 rect is the implementation showing through the presentation. **Previous text follows.** **TIDE's seeded root plumbing has a rack panel presence, and it is off-grid.** Found 2026-08-21 (macos) by E5's probe on the ruled grid: with the rack itself now clean (`TIDE MIDI-CV` 96x384, on-grid, fits row), the ONE remaining violation is **`MIDI In`, 8x14 DIPs = 2.667 snap units**. It is not a rack module and was never dropped on the rack — `TideApp.cpp:727` seeds it at the document root (`AddModule(L"MIDI In", {3600, 3620})`) as **V3's polyphony workaround**: a root MIDI-CV chain feeding the rack module as a facade of jacks, because polyphony cannot escape a container (**E7**). **Deliberately not excluded from the probe.** Hardcoding a name would make the check lie, and the measurement is correct as it stands: something 8 DIPs wide does sit on the rack canvas off-grid. The question this row asks is the other one — **should document-root plumbing carry a panel rect at all?** **Scope** `SynthEditSem/TideApp.cpp`'s seeding, or the probe's definition of a rack module if the answer is that root plumbing is legitimately invisible-but-present. **Accept** `e5_rack_footprint_probe.py` on a freshly-seeded rack returns rc=0, without the probe being taught any module's name. **Size** small, but it is a question about the seeding's intent first — read E7 and V3 before changing it. |
| S33 | WONTFIX | any | **WONTFIX — THE ROW IS BASED ON A MISREADING: the "stub" is inside 87 lines of COMMENTED-OUT DEAD CODE. Measured 2026-08-23 (macos, interactive), branch `tide/mac/S33-claim`, marker PR [GMPI#14](https://github.com/JeffMcClintock/GMPI/pull/14).** `Hosting/processor_holder.h` lines **71-158** are a single comment block, and BOTH `struct GmpiParameter` (line 72) and its `setBlob` (line 135) sit inside it. Line 11 of that same file includes `controller_holder.h`, whose `GmpiParameter::setBlob` is a **correct implementation** — so nothing is discarded. **PROVED, not argued:** a log line placed inside the commented-out `setBlob` **never fires**, while blobs reach the processor normally — **13232 bytes on parameter 1**, TIDE's S12 document-sync XML, in the same run. An A/B of "stub" against "implemented" showed no difference either (13216 vs 13227 bytes) because both builds compiled the identical comment. **THIS BLOCK HAS NOW COST TWO INVESTIGATIONS:** it was filed as this row, and chased again as a candidate cause of the blank AUv3 editor (M4), where the "fix" was written, built and tested before being discarded — the symptom did not move because a comment had been edited. Marked as dead in GMPI#14 rather than deleted; 87 lines of reference code is not mine to remove. **The two live `setBlob` call sites in `processor_holder.cpp` (:397, :652) resolve to controller_holder's working implementation.** **NOT VERIFIED:** whether the block was left deliberately as reference or by accident — GMPI#14 asks that. **Previous text follows.** **`GmpiParameter::setBlob` is a STUB that returns `true`, so every blob a processor is told to store is silently discarded.** Found 2026-08-21 (macos) while answering E11 ([#266](https://github.com/JeffMcClintock/TideSynth/pull/266)); **recorded on E6 at the time, but E6 is DONE — a live defect on a closed row is invisible, which is A32's failure mode, so it gets its own row here.** **The stub:** `GMPI/Hosting/processor_holder.h:135` — the one assignment is commented out at `:137`, then `return true`. Exhaustive check: `value_` is written in exactly two places in that file, `:87` and `:91`, **both at construction**, and `:91` is an EMPTY vector for blobs. Nothing ever writes bytes into a processor-side blob parameter. **Two callers already branch on the `true`**, which is what makes this more than dead code: `processor_holder.cpp:397` (`onQueMessageReady`) reads the payload into a local vector, calls `setBlob`, gets `true`, then calls `sendParameterToProcessor` — **which reads the still-empty vector and sends a zero-length blob event, discarding the bytes it just read**; and `:652` (`setPin`) marks the controller waiting on a parameter whose stored value is empty. **It also defeats a documented guarantee:** the seed comment at `processor_holder.cpp:215-223` says it exists so a processor created at any time (restartComponent, offline render, state restore) starts with the parameter's CURRENT bytes rather than an empty blob — impossible while nothing writes them. That is exactly E6's measured `parametersPrimed: 0`. **NOT verified at runtime, and the distinction is load-bearing:** TIDE's patch persistence goes through the preset chunk (S12), not the blob-parameter round trip, so TIDE working is NOT evidence this path works. Whether any shipping behaviour depends on it is unmeasured. **An honest `return false` would have surfaced this years ago** — both callers test the result. **Scope** `GMPI/Hosting/processor_holder.h` — **PR-GATED**, a hosting-layer change with every GMPI plug-in downstream, so it wants Jeff rather than a run. **Accept** either `setBlob` stores the bytes and E6/E11 are re-evaluated against it, or the stub returns `false` and its callers stop pretending. **Size** small to write, unknown to prove — see E11 for what goes live the moment it stores anything. |

| E7 | TODO | any | **RE-MEASURED 2026-08-28 ON A WORKING SUBSTRATE (macos, interactive, Jeff directing), AFTER E59 WAS FIXED ([#547](https://github.com/JeffMcClintock/TideSynth/pull/547)) -- THE ACCEPT IS MEASURABLE AGAIN AND STILL FAILS, WHICH IS THE EXPECTED STATE AND NOW A CLEAN ONE.** Envelope timing, 10 ms windows, threshold 5% of peak: `v1-rack` (no MIDI) sounds **0.00-2.99 s** (the oscillator drone, correct); **`v1-rack-midi` sounds 0.00-2.99 s -- identical, so the note contributes NOTHING**, the historical E7 signature; and `v3-midi-pitch` sounds **0.50-1.30 s**, i.e. the fixture's note ON at 0.500 s and OFF at 1.200 s plus release tail. **THAT CONTRAST IS THE FINDING: the ROOT MIDI-CV path already works end to end, and only the RACK-MODULE MIDI-CV path is dead.** So the architecture Jeff ruled (root placement + Container facade) is not a plan, it is demonstrably running in `v3-midi-pitch` -- what E7 still needs is only the facade, i.e. this row's own remaining question *"where do the jacks live"*, with a working reference implementation to copy. The E59 blockage noted in the previous annotation is CLEARED: `v1-rack` is back to -6.3/-17.0 dBFS, matching its 2026-08-18 and Aug-25-build numbers. Previous text follows. **RE-MEASURE ATTEMPTED 2026-08-28 (macos, interactive, Jeff: *"run the measurement"*) -- AND THE ACCEPT IS CURRENTLY UNMEASURABLE, blocked by [[E59]].** `v1-rack-midi.rpp` renders **-inf**, but so do `v1-rack.rpp` and `v3-midi-pitch.rpp` -- fixtures that previously rendered correctly -- and stderr shows E59's exact signature on macOS for the first time: two `building rack from` lines, the saved rack (14,136 B, "Legacy chunk") discarded for the default (17,957 B, "Sync chunk"). Silence is therefore a fact about E59, not about MIDI cables. Bracketed with the Aug 25 build: it renders `v1-rack` at **-6.3 dBFS** on the same harness, so the macOS regression landed between 2026-08-25 and -28. Nothing here moves E7 itself; re-run the measurement once E59 closes. Previous text follows. **THE POLYPHONY MODEL IS RULED 2026-08-21, and it reframes this row rather than closing it.** Jeff: modules are **always monophonic** and the SynthEdit runtime **clones** them per voice, automatically, on the DSP graph only (not the GUI); the user sets a voice count on the MIDI-CV rack module, so *"polyphony is essentially 'free' as far as individual modules are concerned."* **So no TIDE module should be designed polyphonic.** **AND THE CONTAINER QUESTION IS ALREADY SOLVED — corrected by Jeff the same day, against this agent's first reading.** Jeff: *"conceptually they are rack modules, but in reality we place the MIDI-CV2 at the root level and route it into the Container (which represents it on the GUI as patch-points). This is already solved. MIDI-CV as rack module [is] how the end-user thinks of it, not how it is implemented."* **So root-placement-plus-Container-facade is the ARCHITECTURE, not a workaround**, and nothing in the MVP needs polyphony to cross a Container boundary. **The agent had flagged this as newly critical and was wrong**: it read the I/O-modules ruling (*they ARE rack modules, movable*) as putting MIDI-CV back inside a Container, when presentation and placement are deliberately separate. Recorded because the wrong inference is the tempting one — the row's own wording (*"polyphony cannot escape a container"*) invites it. **What remains of this row is the engine limitation as a fact, not as a blocker.** **Previous text follows.** **ROOT CAUSE OF THE INTERIM'S SILENCE FOUND, and it was neither of the two suspects this row listed — it was TIDE having no TYPE CONVERTERS.** Jeff: "the library will automatically insert converters when needed, so long as the converter is linked in." `ug_base.cpp:1751` builds an id like `SE <From>To<To>` for any mixed-datatype connection and calls `GetById()`; a miss is `assert(false); return;`, which in **Release** silently abandons the connection. `Converters.cpp` is a separate target's source, so it shipped only as a loadable `Converters.sem` and TIDE — static, no scan (S1a) — had **no `SE BoolToVolts`**, so `SE MIDItoGate2`'s `BoolOutPin` gate could never reach an audio-rate jack. Fixed by linking it into `SynthEditSem` with `my_type_convert.cpp` and staging `Converters.xml`; **the rack now plays the note** (`tests/hosts/v3-midi-gate.rpp`: silent, 440.0 Hz for the note's duration, silent). **This was a whole class of silent failure** — any mixed-datatype cable in TIDE was dead — so it is worth more than the row it unblocked. **What remains for E7 is unchanged and is now purely the POLYPHONY question:** `SE MIDI to CV 2` has the PITCH output the monophonic stand-in lacks, and it is still unusable because its container becomes a voice container. Jeff's rulings below already reduce that to "where do the jacks live". **Original row follows.** **CORRECTION 2026-08-18, and it narrows the remaining bug to one boundary. The row below says `SE MIDItoGate2` "emits no gate at all" — THAT IS WRONG.** It was an inference from a silent rack, never a direct measurement, and Jeff's question ("is MIDItoGate2 MIDI2 aware?") is what exposed it. **It is fully MIDI 2.0 compatible and it does set the gate.** Two proofs. **By code:** `MidiConverter2::processMidi` opens with an explicit pass-through for MIDI 2.0 input (`modules/se_sdk3/mp_midi.h:966`, *"MIDI 2.0 messages need no conversion — pass through and return"*), and `MidiToGate2::onMidi2Message` requires `ChannelVoice64`, which is exactly the message type TIDE forwards. **By a temporary stderr trace added to `modules/MidiPlayer2/MidiToGate.cpp` and then REVERTED unbuilt** (a temporary trace that got committed is issue [#87](https://github.com/JeffMcClintock/TideSynth/issues/87); this one was not) — `MTG2: onMidiMessage pin=0 size=8 bytes=40 90 3c 00 isMidi2=1` · `MTG2: onMidi2Message msgType=4 status=9 (ChannelVoice64=4 NoteOn=9)` · `MTG2: NOTE ON note=60 -> pinGate=true triggerCounter=22` · `MTG2: trigger pulse ended at sample 6 -> setSleep(true)`. So it receives the UMP, decodes it, reads **note 60** — the fixture's middle C — and sets `pinGate = true`. `triggerCounter=22` is the 0.5 ms pulse at 44.1 kHz, correct. **WHERE THE BREAK ACTUALLY IS.** The rack downstream measured **zero for the entire render**, peak −inf, including the samples while the gate was set. So the gate is set on the pin and never arrives at the Envelope's ADSR. Note this does NOT overturn "a monophonic module's cable is live" — the cable demonstrably drove the GATE jack from its default of 10 down to 0, which is why the rack went silent. **The cable crosses; the VALUE does not.** Suspects, both concrete and neither yet tested: (1) `pinGate` is a `BoolOutPin` and the jack is an audio-rate `SE Patch Point in`, so a static/event bool may simply not drive an audio-rate signal — which would also explain why `--connect $mtg:Gate $pp:Input` is *accepted* by SynthEditCL while carrying nothing; (2) `setSleep(true)` fires 22 samples after the note-on while the gate is still logically HIGH, so a sleeping module may stop driving its output entirely. **Test (1) first** — it is a datatype question answerable with SynthEditCL alone, no GUI: put a `Bool to Volts`-style conversion (or a `Multiply`) between the two and see whether the gate arrives. If it does, the shipped interim just needs that module inside the prefab. **Original row follows.** **UPDATED 2026-08-18 (macos, interactive, Jeff directing) — the cause is now confirmed EMPIRICALLY as well as architecturally, and Jeff's rulings settle most of the design.** **(a) Expansion is purely mechanical** — lifted content behaves exactly as if it had been put in the parent in the first place; the exceptions are cosmetic, e.g. a per-container CPU readout must sum its modules and present the total as a pretence. That removes the one real objection to covert expansion, which was whether it would make MIDI-CV 2 a genuine root-level voice source. It would. **(b) The "two on one rack" problem dissolves:** a MIDI-CV is close to mandatory in an instrument, so add ONE automatically to every new project and remove the option to add more — project furniture rather than something the user places, costing neither a serialised flag nor a name-matching convention. **Shape: one implicit root-level MIDI-CV per project, the rack module reduced to the jacks that expose it. STILL TO DECIDE: where those jacks live**, since PITCH and GATE must be cabled from somewhere. **THE CAUSE, MEASURED.** `SE MIDItoGate2` is monophonic, so it does not make its container a voice container. A rack cabled from it renders **digital silence** — and the silence is the proof, because the Envelope's GATE jack defaults to 10 (open), so a rack that goes SILENT when that jack is cabled has a **live cable driving it to 0**. Cables out of a monophonic container cross; out of a polyphonic one they do not. That is the experiment the original row below asked for, answered. **GETTING IT LINKED, AND A WRONG ANSWER WORTH RECORDING.** `SE MIDItoGate2` was absent from TIDE. **It is NOT the `#if GMPI_IS_PLATFORM_JUCE==1` block around `INIT_STATIC_FILE(MIDItoGate)`** — wrong twice over: `INIT_STATIC_FILE(ADSR)` is inside that same block while ADSR works fine in TIDE (TIDE's ADSR is the legacy `ug_adsr.cpp` `REGISTER_MODULE_1` one, reached through the legacy table), and adding an unconditional `INIT_STATIC_FILE(MIDItoGate)` **fails to link** — `se_static_library_init_MIDItoGate()` undefined. **Real reason:** `modules/MidiPlayer2/MidiToGate.cpp` is in a `SynthEditLib/CMakeLists.txt` source list belonging to a **separate `MidiPlayer2` target**, so it builds as a LOADABLE module and is not in the static library — `MidiToGate.o` lands under `MidiPlayer2.build/`, `MidiToCv2.o` under `SynthEditLib.build/`. TIDE links statically and has no scan (S1a). **Fixed with NO GATED change:** the `.cpp` joined `SynthEditSem/CMakeLists.txt`'s source list, `MidiPlayer2.xml` staged for its pins — E2a's `RectangleGui.cpp` pattern — and **`MIDI to Gate2` now appears in TIDE's browser**, the authoritative check. **WHY THE INTERIM STILL DOES NOT SOUND — delivery is ruled out.** It emits no gate at all: 0 for the whole render, note present, `TIDE: host MIDI reaching the rack` printed. **PROBE D proves MIDI crosses the patch cable out of the `MIDI In` module.** Jeff asked whether that had been proved and it had not: PROBE A (module present AND cabled) gates while PROBE B (module absent) does not, but that moved two variables, leaving live the possibility that the module's mere PRESENCE makes the container a MIDI destination and `ug_container`'s redirector feeds MIDI-CV 2 directly (`ug_base.cpp:2859`). **PROBE D holds the module present with the pin UNCONNECTED: gate flat 0.** A and D differ only in the cable, only A gates, so the cable is the carrier. Not a threshold either — measured, the ADSR opens identically at 1 V and 10 V. **Suspects now inside the module only:** the `setSleep(true)` / `subProcessNothing` path in `MidiToGate2::subProcess` and the cross-class `setSubProcess(&MidiToGate::subProcessNothing)` beside it, both at `modules/MidiPlayer2/MidiToGate.cpp:222`. All four probes run from `build-prefabs.py --diagnostics`. **Original row follows.** **A rack module that is a POLYPHONIC SOURCE does not drive other rack modules through a patch cable — found 2026-08-18 (macos, interactive, Jeff directing) while building V3, and it is what blocks V3 rather than anything about MIDI.** **What is proven.** Host MIDI reaches the rack: TIDE's new check prints `TIDE: host MIDI reaching the rack - first message 8 byte(s), status 0x40` (a MIDI 2.0 UMP, GMPI's `DT_MIDI2`) — [SynthEdit#46](https://github.com/JeffMcClintock/SynthEdit/pull/46). `SE MIDI to CV 2` converts it correctly: with a `Sound Out` INSIDE its own container, its **Gate rises at 0.500 s and falls at 1.200 s**, exactly the fixture's note. **But the same signal taken out through a patch cable produces digital silence** — `tests/hosts/v1-rack-midi.rpp`, four cables, renders **identically with and without the note** (peak −6.3 dBFS, 440.0 Hz throughout), and 440.0 Hz is the *Oscillator's own* 5 V default, so both MIDI cables contributed nothing. A two-module probe (MIDI PITCH → Output L, a jack with **no** stored default, so it would have passed anything that arrived) renders **−inf**. **CAUSE CONFIRMED by Jeff, 2026-08-18, from the architecture rather than from another measurement — this is a PRE-EXISTING constraint, not a new defect: polyphony cannot escape a container.** `SE MIDI to CV 2` is `polyphonicSource="true"` / `cloned="true"` (`MidiToCv2.cpp:18`), so its container is a voice container, and a voice container's outputs do not cross out. **The other half of his answer resolves what looked like a contradiction:** a container with NO MIDI-CV in it is **expanded** on the DSP side already — it is effectively cosmetic, like grouping objects in a paint program — so V1's oscillator→envelope→output cables are just wires in one flat graph, which is exactly why they work. **The rule that follows: as long as the MIDI-CV is not inside a sub-container, the rest of the patch works fine, containers or not.** So the earlier "cable a non-polyphonic source out of a container" experiment is unnecessary and has been dropped from this row — the answer was architectural, not empirical. **TWO CANDIDATE SOLUTIONS, Jeff's, and the choice is his to make.** **(1) Covert expansion:** special-case this one rack module so the DSP side lifts its contents into the parent with every wire destination rerouted, which needs an extra flag serialised on that container. **(2) Convention:** the rack module becomes nothing but a container of patch-points (Trigger, Gate, Pitch...), and any container shaped like that gets a secretly-added `MIDI In` + `MIDI-CV 2` pair wired in at the root level. **A first read, not a ruling:** (1) reuses expansion machinery that already runs on every other container and keeps the module honest in the structure view — you can open it and see what it is, which is how this bug got diagnosed at all; (2) needs no new serialised state but identifies the container by the NAMES of its patch points, and names are the least stable thing in a document. Both have to answer "what happens with two MIDI rack modules on one rack". **Scope** likely `SynthEditLib` (GATED) — the expansion path and the voice/clone machinery — plus a generator change in `TideModules/`. **Accept** `tests/hosts/v1-rack-midi.rpp` renders audio that starts at 0.500 s and stops at 1.200 s. The fixture already exists and already fails, so this is a re-measure, not a new test. **Size** unknown; the two designs differ enough that it wants Jeff's pick first. **Do not start by suspecting the prefabs or the MIDI path** — both are measured good, and the probes behind `build-prefabs.py --diagnostics` re-run that proof in one command. |
| V2 | BLOCKED | any | Host-automatable parameters without a panel view (design note 4 in [docs/design-notes.md](docs/design-notes.md)). **This premise is now stale too — panel view (the rack) is TIDE's default view as of the 2026-08-13 pivot, not something being avoided. Design note 4 needs re-reading against that before this row is trusted; not done here, flagged in passing while splitting E2.** **SHAPE ESTABLISHED 2026-08-18 from how every competitor solves this — [docs/competitive-review.md](docs/competitive-review.md) §3. The field splits in two and the answer is not close.** RNBO (`[param name]`), plugdata and Bitwig's Grid expose parameters **declaratively from the patch with no panel required**; Reaktor and Max for Live make you draw a knob first, and **Reaktor's Multi-Picture controls cannot be automated at all** while its auto-generated automation IDs are not auto-maintained, so deleting a control leaves holes. **The two serious DAW-hosted racks both use a pre-declared slot pool** — VCV Rack Pro **1024 slots**, Reason Rack **>2000 advertised** (~256 practically automatable, UNVERIFIED/community-sourced) — bound automatically as modules are added, which is what makes them work with zero user setup. **Both then made the same mistake, and it is the one thing this row must not repeat: the binding is POSITIONAL.** It follows insertion order and a recycled free list, so deleting a module orphans the DAW's automation lane and **the slot may be reused by a newly created module's parameter, silently driving the wrong thing** — documented in VCV *and* Reason. Reason additionally pushes parameter names so lazily that the known Ableton workaround is *dragging the device a few pixels inside the plugin* to make names populate; its sanctioned answer is "wrap it in a Combinator and automate that", i.e. it deferred the problem and shipped a panel builder to cover it. **So the ruling: (1) pre-declare a pool so automation needs no setup; (2) bind slots to a persistent content-addressed identity — module UUID + parameter id — NEVER to insertion order or a free list; (3) push names to the host eagerly.** **TIDE is unusually well placed for (2)** because S11 already puts the whole document in the plugin state with stable module ids. **Cheap now, near-impossible to retrofit once patches exist in the wild.** **One open position worth noting while here:** the declarative camp pays for it with generic auto-generated panels (RNBO's real UI means writing C++/JUCE), and the panel-first camp taxes the user to build one — **nobody ships automatic host parameters AND a good default panel AND an optional custom one.** Since the 2026-08-13 pivot the rack panel *is* TIDE's default UI, so that combination is nearly free here. |
| E2 | TODO | any | **SCOPED AND SPLIT 2026-08-19 (macos) — E2 is an umbrella, and as one item it is not takeable: it never says which modules a "first curated set" contains, which is a product decision, so its acceptance check cannot be stated before starting.** Its conventions half is also largely **already written down**, in `build-prefabs.py`'s header (panel geometry, `PanelWndPosition` vs `panel_rect`, faceplate sizing, and the rack grid: `hpWidth 15`, `rowHeight 380`, so a slot is a multiple of 15 wide and at most 350 tall). **What is left of E2 is MODULES, one stage each** — **E2b** (Filter) is the first and is IN-REVIEW, and **E2c** (SV Filter4 / Oscillator HD registered in TIDE) landed as [#171](https://github.com/JeffMcClintock/TideSynth/pull/171) alongside it; the measured candidate list for later stages is in that row, and [tests/README.md](tests/README.md) says how to decide whether a new prefab can have an E1 case at all. **Previous text follows.** **UNBLOCKED 2026-08-18 — V1 is DONE, so this row's stated premise no longer holds.** V1's acceptance test was run and passed ([#140](https://github.com/JeffMcClintock/TideSynth/pull/140)): a saved REAPER project's TIDE rack sounds after reload, measured at **peak −6.3 dBFS, rms −17.0 dBFS, 440.0 Hz**. So "no point authoring a fuller module library for a plugin that cannot yet keep its patch across a host save" is retired by measurement. **Read E2a's archived row before starting** — it built three of these and its findings are what a fuller set needs (a Container's rack rect is `PanelWndPosition`; every module must be a class TIDE links; a module needs both its `.cpp` and its XML). **Original row follows.** **The rest of the first Eurorack module set, beyond E2a's three V1-critical prefabs.** Split 2026-08-13 — see E2a for the part that's unblocked. Everything else here still waits on V1: "no point authoring a fuller module library for a plugin that cannot yet keep its patch across a host save" holds for the broader set even though the minimal V1-critical prefabs couldn't wait for V1 themselves. PLAN "The Eurorack rack", 2026-08-09. Define the naming and I/O conventions for a module Container, then build the rest of a first curated set, each with its own E1 test case. Two dependencies that are not obvious: constraint 7 means the **primitives** a Container is built from must be statically registered — see **S8**, which is about the wrong ones being compiled in today — and a Container is only openable-and-editable if U1's UX can actually navigate into it, which is what the breadcrumb bar is for. Carried over from the superseded `tide-rack` repo, where this was EP-002. |
| E3 | BLOCKED | any | **Curated starter racks** — ready-to-play preset patches shipped with the plugin, treated as a deliverable rather than a demo. PLAN calls this the friendly surface, and it is the Blocks trait the product is explicitly copying. Blocked on **E2**. Two things it needs that nothing else forces: a rack has to survive load without a filesystem path (constraint 3), and it should be the thing a user sees *first*, which is a product decision about default state, not a file format. Carried over from the superseded `tide-rack` repo, where this was EP-003. |
| E4 | BLOCKED(E2) | any | **Ruled 2026-08-17, same day it was filed — constraint 9 answers the question that made this NEEDS-JEFF.** Rather than bless per-platform folders, Jeff ruled the general principle: TIDE Rack only implements features implementable on the lowest-common-denominator target (today AUv3) — see PLAN.md constraint 9 and [docs/decisions.md](docs/decisions.md). For E4 that means: the per-device library is allowed in principle (AUv3 *can* write inside its own container), and every platform implements the same container model — desktop gets no blessed platform-specific folder, it gets the AUv3 semantics or nothing. Now BLOCKED(E2), which was always its real prerequisite: a user-authored Container is the same object as a shipped one, so E2's naming/IO conventions must exist first. One sub-question stays flagged for implementation time, not for now: whether the system document-picker sheet (tier 3, sharing) sits within constraint 5 — it is OS-supplied and user-initiated, but it is modal. Original row follows. **User-authored prefabs — can a user save their own rack modules?** Asked by Jeff 2026-08-17; analysis in [docs/e2a-prefabs.md](docs/e2a-prefabs.md) §7. Short answer: yes — prefabs are XML data, and the sandbox rules that kill module scanning do not apply; an AUv3 extension may write inside its own container, which constraint 4's own wording permits. Three independent tiers: in-project reuse (free once V1 lands, writes nothing new), a per-device library in the plugin's container, and export/import via the system document picker. **The decisions that make this NEEDS-JEFF:** (1) desktop has no OS-enforced container and the natural folder is exactly what constraint 4 bans — bless one decided location as the desktop "container", or make the filesystem library iOS-only; (2) whether the document-picker sheet is compatible with constraint 5. Default in effect: not in v0.1; nothing is written anywhere; users reuse Containers within a project only. Decide-by: with E2's naming/IO conventions — a user-authored Container is the same object as a shipped one, so the conventions must precede the feature. |
| E16 | RESOLVED | any | **RESOLVED 2026-08-21 by Jeff, in session ([#246](https://github.com/JeffMcClintock/TideSynth/pull/246)): TIDE ships TIER 1 as a preliminary set** — and the reason is sequencing, not size: *"we need a testable, installable MVP first"*, so installers and the website can move while the full set is developed in parallel by Jeff, and so a design-language change (E17) cannot invalidate a lot of authored panels. **The doc's own argument for Tier 2 was rejected as false:** *"Tier 1 absolutely can make music. Plenty of real hardware products ship with only this type of functionality."* **THE RULED SET IS NINE MODULES**: Oscillator HD, Envelope, Output, I/O modules, Filter, VCA, LFO, Noise, Attenuverter+Offset. **Three of the surveyed twelve are cut**, each for a mechanism: **Mixer** (*"leave mixer out of MVP to reduce the critical path"*) and **Slew/Glide** because TIDE already does the job — cables fan in with automatic summing, and glide is inside MIDI-CV2 (verified: `CVoiceList.cpp` drives it through `MidiToCv2`'s pitch interpolator) — and **Sample & Hold** as not critical. All three go to the second wave. **No open questions remain in the set.** **Four conventions ruled alongside it** — pitch, gate, polyphony, and the I/O modules — all recorded in [docs/decisions.md](docs/decisions.md), with [docs/module-set.md](docs/module-set.md) corrected. **This unblocks E2**, and with it the release track. **Previous text follows.** **The TIDE module list — answers [docs/module-enumeration.md](docs/module-enumeration.md) §7.2 ("Which modules belong in TIDE's list? Neither existing arm is right"), open since 2026-08-06.** Proposal written 2026-08-18: **[docs/module-set.md](docs/module-set.md)** — a three-tier MVP (**3** modules = the V1 acceptance test, already E2a; **12** = minimum genuinely playable; **20** = credible first release) plus a 20-row ranked nice-to-have list, each annotated with the SynthEditLib primitive that already implements it and the constraint that blocks it. **The measured finding that changes the estimate: the DSP already exists.** `C:\SE\SynthEditLib` carries **75 `ug_*` DSP modules** plus a modern SEM set (`EnvelopeAdsr`, `SVFilter2`, `VaFilters`, `Delay3`, `Reverb`, `StepSequencer`, `Waveshapers`, `BPMClock3`, `Arpeggiator`, `Unison`), so the module set is a **Container-authoring and panel-art job, not a DSP-writing job** — which moves the schedule risk to per-module panel cost, and **nobody has measured that**. **Two MVP rows are not free:** the oscillator is S8's live blocker (`OscillatorNaive`, **zero symbols** in the shipped Release binary) and Output must be authored from scratch (the existing `Output.seprefab` contains **no Sound Out** — measured, [docs/e2a-prefabs.md](docs/e2a-prefabs.md) §8). **Two hardware conventions the doc argues TIDE should deliberately NOT copy:** multiples (software fan-out makes them redundant — *verify SynthEdit permits one-output-to-many-inputs first*) and ubiquitous standalone attenuverters (unnecessary if `Mod A`/`Mod B` lands). **Do not treat the list as agreed** — it is input to **E2**, which owns the curated set. **Scope** doc only today. **Accept** E2 starts from a list somebody has ruled on, rather than from the two wrong arms of `initialise_synthedit_modules`. **Size** the doc exists; the ruling is minutes, the building is E2. |
| E17 | RESOLVED | any | **RESOLVED 2026-08-20, in a THIRD direction neither option below named: TIDE Rack's panels and fittings are PATH TRACED (`tide_render`) into a cached bitmap, with moving parts drawn over in vector.** The (a)-vs-(b) crux — Jeff wanted realism, (a) banned shadows — dissolves rather than being adjudicated: the realism is *simulated*, so shadows and highlights are outputs of a light transport rather than decisions. **(a) is rescoped, not discarded** — it governs the vector overlay, where its bans still stand. Ruled in session; artefact ([modules/TiDEPanel](modules/TiDEPanel)) came before the ruling. See [docs/panel-design-language.md](docs/panel-design-language.md) and [docs/decisions.md](docs/decisions.md). **Open under the new language:** knob sizes are below VCV's library and real hardware, and one rack unit is 3.2 HP rather than a whole HP. *The original undecided text follows, kept for the reasoning it records.* **TIDE's visual design language is undecided, TWO incompatible proposals now exist, and every module panel authored before it is settled is rework.** **Found while committing this row, 2026-08-18: [docs/ui-design-language.md](docs/ui-design-language.md) already exists as an uncommitted working-tree proposal from another session** (plus three generated `docs/images/ui-language-*.svg`). It was NOT written by the run that filed this row, is on no branch and has no commit history, and **it was deliberately left untouched**. **Jeff's status for it, given 2026-08-18: *"we're spit-balling with the marathon stuff"* — it is exploratory, and this row treats the two proposals as PEERS rather than as a baseline and a comment.** The Marathon document is worked out in more detail; **detail is not validation**, and the Blocks-derived proposal is the one anchored to the brief Jeff actually gave and to five shipping UIs in this category. **The two conflict on exactly the point Jeff raised.** **(a) The Marathon/Designers-Republic language** ([docs/ui-design-language.md](docs/ui-design-language.md)): true-black ground, flat `#1C1C1C` panels where the tone step *is* the depth cue, 1px butt-cap seams, acid `#C0FE04` used **only for live state**, per-family livery hues, knobs as arcs, right-angled polyline cables, stepped edges as the signature device — and **"banned outright: gradients as decoration, drop shadows, blur, glow, bevels"**. Its strongest argument is practical: it is a pure 2D language drawable in GMPI code from plain rects, lines and arcs with **no bitmap assets**, which suits constraint 8 and the sandbox, and its own example SVGs double as a feasibility proof. **Its standing risk, which the document itself raises and then guardrails against: the source language's own shipped UI was widely panned for legibility (players coined "fontslop"), and the failure mode named was density on a surface people read continuously — which is exactly what a plugin is.** That caution is not disqualifying, but it is the thing to test before adopting, and it does not apply to (b). **(b) The Blocks-derived language** ([docs/competitive-review.md](docs/competitive-review.md) §5), from five competitor UIs **viewed directly as screenshots, not recalled**: flat matte panels carrying **dimensional controls — top-lit gradient, faint sheen, and a soft low-opacity contact shadow reading as ambient occlusion** — with hue encoding signal type, cables thin/behind/hideable, and inline dataviz. **The crux, stated plainly: Jeff's brief said he likes Reaktor Blocks because it is "abstract but with some tasteful realism like shadows". Proposal (a) bans shadows. Both cannot be satisfied and this is not an agent's call.** **It wants a `PROPOSED:` entry in [docs/decisions.md](docs/decisions.md).** `PROPOSED: which visual design language does TIDE Rack adopt? Options: (a) the Marathon/TDR language in docs/ui-design-language.md, flat with no shadows; (b) the Blocks-derived language in competitive-review §5, flat panels with dimensional controls and soft contact shadows; (c) a synthesis led by (b) — (b)'s material direction and signal-type colour, harvesting from (a) everything that does not conflict: the no-bitmap drawability discipline, 1px butt-cap geometry, a deliberately restricted palette, and accent-as-state rather than accent-as-decoration. Recommended default: (c). (b) supplies the direction because it is what Jeff named liking and it is validated by five shipping UIs in this category rather than imported from a game whose own UI was panned for legibility; (a) supplies craft discipline (b) does not attempt, and a contact shadow is a few alpha steps rather than a bitmap, so nothing in (a)'s drawability argument is given up by admitting it. Default in effect meanwhile: none — TIDE renders SynthEdit's developer UI. May proceed meanwhile: E2a's three prefabs, whose panels are throwaway under any option. Decide-by: before E2 authors the curated set.` **Whoever takes this reads both in full first.** [docs/ui-design-language.md](docs/ui-design-language.md) is the more specified on palette, geometry, type and density — real gaps in §5 that any adopted language has to close, whichever direction wins. [docs/competitive-review.md](docs/competitive-review.md) §5 is the one carrying the evidence: five shipping competitor UIs viewed directly, the density failure that sinks dense cabled racks, and the authoring-cost argument for a small team across 30–60 modules. **The characterisation Jeff's preference needs to be implementable:** in Blocks the realism is **not in the panel** (flat, matte, ~`#f0f0ee`, no screws, no texture, no bevel) but **entirely in the controls** — grey cylinder, subtle top-lit vertical gradient, faint specular sheen, and a **soft low-opacity contact shadow** that reads as ambient occlusion rather than a cast shadow. **The most useful negative result in the survey:** Voltage Modular proves **thin cables do not save a dense rack** — its panels are unreadable under the patch — which argues for hideable cables plus the Grid's show-modulation-at-the-destination model. **An anchor already exists:** the website's accent `#1c6e8c` light / `#6ec2dc` dark on `#101216` is both tide-coloured and close to Blocks' cyan. **Scope** a decisions.md entry, then a spec doc. **Accept** a ruling exists that E2 can author panels against. **Size** the survey is done; the ruling is Jeff's. **STATUS CORRECTION, 2026-08-18 (later session, Jeff directing):** the "uncommitted, on no branch, no commit history" description above is no longer accurate — [docs/ui-design-language.md](docs/ui-design-language.md) and its now-**four** generated SVGs are committed on `tide/win/competitive-review`, together with [scripts/gen-ui-language-svgs.py](scripts/gen-ui-language-svgs.py) which regenerates them. **This does not decide E17** — it commits proposal (a) so it can be read and diffed, and E17 remains NEEDS-JEFF. **One detail above is now stale in a way that matters to the crux:** (a) is described as having "right-angled polyline cables", but Jeff directed the opposite that session — cables are now **fat, curved, physical objects with round plug heads**, an explicit move toward the grounded realism the row attributes to (b). Shadows are still banned in (a), so the conflict stands, but it is narrower than "(a) refuses all realism". Also added under Jeff's direction: plate text, quarter-turn labels, corner-crosshair grouping in place of section borders, a 70%-size unit tier, and a lifted label ink for long-session legibility. **Read (a) as it now stands, not as this row describes it.** |
| E29 | WONTFIX | any | **CLOSED 2026-08-26 BY JEFF — WONTFIX. *"this product is not released. We can break DAW sessions, they only exist only for our tests anyhow. no harm."* and *"we simply don't care about broken test sessions. don't waste time on it."*** So the question this row escalated is withdrawn, not answered: **there is no decision to make and no work to do.** Do not re-file it. **What stays, because it was already written and costs nothing:** the [tests/hosts/README.md](tests/hosts/README.md) note with both tokens and the one-line `sed`, so a box that wants a fixture to load can make it load in seconds; and the `render-and-measure.py` timeout, which is worth keeping on its own merits — it turns an indefinite hang on ANY modal dialog into a bounded failure with a reason in the log. **The accepted consequence, stated plainly:** a box whose REAPER disagrees with the committed token cannot render these fixtures until someone runs that `sed`. That is a known cost, not an oversight. **Everything below is the measurement, kept for whoever hits the symptom and wants to know what it is rather than what to do about it.** **THE MAC BOX IS NOT AFFECTED, AND THE TWO TOKENS ARE MUTUALLY EXCLUSIVE — so this row's first repair option would fix Windows and BREAK macOS. Measured both directions 2026-08-26 (macos, interactive, Jeff directing), branch `tide/mac/E29-mac-token-verdict`.** **The open question this row names — *"Not yet known: whether the mac box is affected"* — is ANSWERED: it is not.** REAPER **7.45** on macOS loads the COMMITTED raw-TUID token and renders `v1-rack.rpp` at **peak -6.3 dBFS / rms -17.0 dBFS**, which is M7's and E2a's figure exactly. Negative control run first and passes: `v1-rack-uncabled.rpp` -> `-inf`, SILENCE, so the harness discriminates. **AND THE OTHER DIRECTION, which is the finding that changes the plan.** Substituting the 7.78 token into the same fixture makes REAPER 7.45 refuse it — confirmed INTERACTIVELY, not inferred: *"Project Load Warning — The following effects were in the project file and are not available. Track 1: VST3i: TIDE Rack (TIDE Synth)"*, and the FX slot reads *"could not be loaded"*. **So the tokens are mutually exclusive across these two REAPER versions, and this row's option "a re-save from a current REAPER" is RULED OUT** unless the fleet first agrees a single REAPER version. Worth knowing before anyone does: this mac is on **7.45** and REAPER is offering **7.79**, so upgrading it would flip it to the Windows behaviour and break the committed fixtures here too. All of v0.1's `render-and-measure.py` evidence was produced on 7.45. **IT FAILS AS A HANG, NOT AN ERROR, and that is the part that would have bitten CI.** The warning is MODAL, so `REAPER -renderproject` blocks on it forever: the first attempt sat for **over seven minutes** producing no render and no error and had to be killed by hand. `scripts/render-and-measure.py` had no timeout. It does now — `RENDER_TIMEOUT_SECONDS = 300`, kills REAPER, and writes into the log exactly why, naming this row. Verified by firing the path deliberately (25 s cap: `rc=-9999`, REAPER gone, message present) and by re-running the control and `v1-rack.rpp` unchanged. **DELIVERED, and it is this row's SECOND option verbatim:** a note in [tests/hosts/README.md](tests/hosts/README.md) naming both tokens, which REAPER writes which, the one-line `sed` to swap a LOCAL copy, how to discover what your own REAPER writes, and the hang. **THE ACCEPT IS HALF MET AND I WILL NOT CLAIM OTHERWISE:** *"`v1-rack.rpp` loads its plugin on all three boxes"* is **NOT** true — Windows still needs the swap applied locally, and no commit here can fix that without breaking macOS. The *"or a note ... naming the token and how to update it"* branch **is** delivered. **What is left is a RULING, not work:** either (a) standardise the fleet's REAPER version and re-save the fixtures for it — one box breaks until it upgrades — or (b) accept per-box local swaps using the documented `sed`. **Default in effect:** (b). The committed fixtures stay on the raw-TUID token, macOS and the `render-and-measure.py` evidence keep working unchanged, and the Windows box applies the swap locally when it needs to render one. Nothing silently rots under this default — the README names the recipe and the harness now fails loudly instead of hanging. **Decide-by:** before the next release run that depends on a REAPER-rendered measurement from more than one box (R5's territory). Until then (b) costs one `sed` on one box. Original item follows. **EVERY COMMITTED `tests/hosts/*.rpp` FIXTURE FAILS TO LOAD THE PLUGIN ON THIS BOX — REAPER 7.78 records a different byte order for the same UID.** Found 2026-08-26 (windows, scheduled run). The fixtures name the plugin `1386065673{506C7567696E474D50492050A2A07287}`; REAPER 7.78 writes and expects `1558955188{67756C506E694D4750492050A2A07287}`. **Same UID, opposite byte order** — the TUID is literally `"PluginGMPI     "` with byte 11 `'P'` plus a 4-byte id hash (`GMPI_Wrappers/wrapper/VST3/MyVstPluginFactory.cpp:200`), so the fixtures hold the raw TUID and REAPER now writes its COM little-endian rendering. **The plugin's identity has not changed.** Symptom: *"Project Load Warning - the following effects were in the project file and are not available"* and a track with no plugin. Reproduces with the **untouched** `v1-rack.rpp` against the **installed** build, so it is not a build artefact; substituting the token makes every fixture load. **Not yet known: whether the mac box is affected.** `scripts/render-and-measure.py` hardcodes `/Applications/REAPER.app/...` and has never run on Windows, so the v0.1 evidence it produced is untouched by this — **but the fixtures are shared and the UID is platform-independent, so the mac box should check whether its REAPER agrees with the committed token before trusting a re-run.** **Accept:** `v1-rack.rpp` loads its plugin on all three boxes, whatever that takes — a re-save from a current REAPER, or a note in `tests/hosts/README.md` naming the token and how to update it. |
| E41 | BLOCKED | any | **PARKED 2026-08-26 BY JEFF'S RULING: *"3rd-party module compatibility is not important at this stage. We ship with only our own modules."* — recorded in [docs/decisions.md](docs/decisions.md). `BLOCKED`, not `WONTFIX`, deliberately: *"at this stage"* is a sequencing call and the work below keeps its value if it is revisited. Do not take this row until that ruling changes.** **Adaptor-registered modules are exactly what the ruling parks, so this defect can no longer be reached by anything TIDE ships.** The measurement stands and is worth keeping — it is a real gap in E36's placement for a module class that is not a prefab. **An adaptor-registered module inserted by double-click SKIPS E36's next-free-slot placement and lands overlapping whatever is near the view centre.** Measured 2026-08-26 (macos, scheduled run) while closing E24: HetrickCV **Crackle**, inserted by double-click, came to rest at `l=1352 t=280 r=1442 b=664` — **overlapping `TiDE Output` at `1329..1377`**, not at the rack origin, and **not even on the 3-DIP snap grid** (`(1352-3732)/3 = -793.33`). A TIDE prefab inserted the same way lands correctly (E36's Accept passes), so this is specific to rack-adaptor modules. **The likely cause, to CONFIRM before fixing rather than assume:** `SynthEditGui.cpp`'s handler calls `p->AddPrefab(...)`, whose contract is a before/after diff of the container's top-level modules, and E36 places whatever that returns. An adaptor module is not a prefab and its id carries no `*P=`, so if the diff comes back EMPTY the placement silently does nothing. **But that does not explain the OFF-GRID position** — the fallback is `view->snapToGrid(view->getCenter())`, which cannot produce an off-grid result, so something earlier in the path differs too. Instrument it; do not guess. **Scope** `SynthEditSem/SynthEditGui.cpp` (ALLOWED). **Accept** insert a HetrickCV module by double-click into a rack that already holds one; `e5_rack_footprint_probe.py` reports **zero overlaps** and the new module `on-grid`. **Size** small once the cause is confirmed. **Blocks nothing** — E20-E22 can proceed with modules placed by hand. |
| E44 | DONE-PENDING-ACCEPT | any | **BUILT AND MERGED, AND THIS ROW'S ACCEPT IS UNSATISFIABLE AS WRITTEN -- for a reason that is about TIDE'S MENUS, not the verb. DONE-PENDING-ACCEPT 2026-08-27 (macos, interactive, Jeff directing)**, [GMPI_Wrappers#27](https://github.com/JeffMcClintock/GMPI_Wrappers/pull/27), merged. **THE ROW'S OWN FIRST INSTRUCTION PAID OFF:** *"check first whether the action can be invoked without raising the menu at all"*. It can. `MenuBarView` keeps its items as a MODEL -- `struct Item { label; std::function<void()> action; enabled; checked; }` -- so the verb calls the function and the drawn bar is never involved. That is the small case the row hoped for. **`--menu`** lists every menu and item with enabled/checked state; **`--menu <label>`** invokes; **`--menu <menu>/<label>`** disambiguates. Case-insensitive, trailing `...` ignored. **THE ACCEPT SAYS `--menu save`, AND TIDE HAS NO SAVE ITEM.** Measured: `File` holds exactly *Revert to Plugin Defaults*, a separator, and *Quit*; `Options` holds *Audio/MIDI Settings...*, a separator, and *Quit*. **There is no Save and no Open** -- this row was written assuming `File > Save` existed. So the Accept's instrument cannot run, and *"the session file on disk changes"* has nothing to trigger it. **Re-state it against a real item before closing this row.** **THE ACCEPT'S DISCRIMINATING CLAUSE IS MET, which is the part that matters** -- *"`--info` answers immediately afterwards on the SAME connection"*, the clause that deliberately separates this from E43's bounded error. Three commands on ONE connection: `--menu Revert to Plugin Defaults` **0.002 s**, `--info` **0.018 s**, `--ping` **0.014 s**. Ordinary latency, not a deadline. **AND THE ACTIONS REALLY RUN:** invoking `Audio/MIDI Settings...` visibly switched the window to the settings page, and **`--menu File/Quit` exits the process CLEANLY** -- no `kill -9`, which is exactly what a pointer click could never do. **TWO DESIGN CHOICES WORTH KNOWING:** TIDE has **two** items called *Quit*, so a bare ambiguous label is an **error naming the candidates** rather than a guess; and a slash is only a menu/item split when the LEFT side names a menu, or `Audio/MIDI Settings...` would be read as menu *Audio*. **WHAT THIS DOES NOT FIX, and the row's motivation says it does:** every Accept phrased *"save and reload"* is still unreachable, because the wedge is gone but **there is still no Save item to invoke**. Those Accepts need either a Save menu item or E43's `kill -TERM` route, which saves unconditionally on the normal teardown. Previous text follows. **A VERB THAT INVOKES A MENU ACTION DIRECTLY, so a test run can drive `File > Save` / `Open` / `Quit` at all.** Split out of **E43** 2026-08-27 (macos, scheduled run) when E43's half (a) shipped: (a) made the wedge *bounded and self-describing*, and E43's own words are that it *"does not make menus drivable"*. Recorded as its own row rather than left on a row about to close, which is how a live finding gets lost (S33). **Why the pointer can never be the route, measured on E43:** `--pointer-down` over the menu bar opens a native `NSMenu` whose nested modal run loop runs INSIDE the command's job, so the best possible outcome is now a bounded *"this command STARTED and has not finished"* - the menu is up, nothing else can run, and **the app no longer even answers SIGTERM**, so a run that tries it must `kill -9`. There is no sequence of pointer verbs that gets from there to a saved file. **Scope** `GMPI_Wrappers/wrapper/Standalone/mcp/CommandDispatcher.cpp` plus whatever seam reaches `MenuBarView`'s model - ALLOWED, but **PR-GATED like the rest of that directory: propose, never merge**. The verb should name the ACTION (`--menu save`), not a coordinate, precisely so it never goes near the modal loop. **Check first whether the action can be invoked without raising the menu at all** - if `MenuBarView` dispatches through a table of named commands, this is small; if the action only exists as a closure hung off a drawn item, it is medium and wants designing. **Accept** from a test run with nobody at the keyboard: `--menu save` returns ok within the command channel's ordinary latency (not its deadline), the session file on disk changes, and `--info` answers immediately afterwards on the SAME connection. **The last clause is the one that distinguishes this from E43** - a bounded error would satisfy E43 and must NOT satisfy this. **Why it matters:** every Accept in this backlog phrased *"save and reload"* or *"quit and relaunch"* is still unreachable by a scheduled run on macOS. E43 found the `kill -TERM` workaround for SAVE specifically (`StandaloneApp.cpp`'s teardown calls `session.saveNow` unconditionally on the normal teardown), which is genuinely useful and is not a substitute: it cannot open, cannot choose a file, and cannot be undone once the app is gone. **Not the same as E38**, which is the EDITOR's context menu reached through the frame; this is the app's own menu bar. Same file, different surface. **Size** small if the actions are already a named table, medium otherwise. |
| E49 | DONE-PENDING-ACCEPT | any | **LANDED — [SynthEditLib#64](https://github.com/JeffMcClintock/SynthEditLib/pull/64) merged 2026-08-27 at Jeff's instruction in session (*"merge 64"*), `796bbc2` on `SynthEditLib` `main`. DONE-PENDING-ACCEPT rather than DONE, because this row's Accept has two clauses and only one of them is met.** *"A `(moduleHandle, parameterId)` pair the patch manager does not have is refused with a diagnostic rather than dereferenced"* — **met, and measured** (3 of 5 rack modules constructed → 5 of 5, connections made, DSP running, channel opening). *"The 38,658-byte document loads without faulting"* — **NOT met**: it still segfaults 3/3, after the graph is live rather than during the build. **That clause is now E53's**, and E53 is the row to take; nothing further belongs to this one. Flip to DONE when E53 closes, or when somebody decides the clause moved with the work. **Previous text follows.** **THE GUARD IS WRITTEN AND MEASURED, AND THE ACCEPT IS HALF MET — 2026-08-27 (windows, Jeff directing: *"do the gated fixes"*).** [SynthEditLib#64](https://github.com/JeffMcClintock/SynthEditLib/pull/64), branch `tide/win/E49-E46-E47-null-guards`. **A/B on this row's own 38,658-byte document, same build tree, the commit the only difference:** rack modules constructed **3 of 5 -> 5 of 5**; connections made where none were; DSP running (`processing (block 96)`, `first NONZERO OUTPUT`); command channel opening where it never opened. **THE SECOND HALF IS NOT MET AND THAT IS THE HONEST STATE: the document still segfaults, 3/3 — but LATER AND ELSEWHERE.** Before, the process died DURING graph build, after `Scope constructed`. Now the graph completes, all five modules produce audio, and it faults after `LFO2 first nonzero light`. So *"a pair the patch manager does not have is refused with a diagnostic rather than dereferenced"* is met and *"the document loads without faulting"* is not. Filed as **E53** rather than buried here. **AND THE DIAGNOSTIC ANSWERED THIS ROW'S OWN OPEN QUESTION, which is worth more than the guard.** It prints `no patch parameter for module 987654321 parameter id 0..7`. **987654321 is the `VCV: Scope`, and the document DOES define it** — `<Module Id="987654321" Type="VCV: Scope">`. So the pair misses because that module has **no patch-manager parameters at all**, not because the handle dangles: this row's refinement (*"the miss must come from the PARAMETER ID, on a module that does exist"*) is right that the module exists and wrong that a single id is at fault — all eight are absent. **A module whose parameters went missing is a module whose connectors are lost, so this is a direct lead for E48**, and it is not fixed here. **Consumers built:** TIDE standalone (Windows Release, rc=0) **and SynthEdit itself** (full `SE16` Ninja Release, **1088/1088, rc=0**, producing `SynthEditCL.exe`) — `SynthEditLib` ships in both, so TIDE alone would not have been evidence. No new warning class (`C4530` is pre-existing at 153 occurrences in an unpatched cold build). **Not run: SynthEdit's `ctest`**, which launches GUI tests. **Previous text follows.** **E49 REPRODUCES, AND ITS NEVER-REACHED LIST NAMES A MODULE ITS OWN DOCUMENT DOES NOT CONTAIN — 2026-08-27 (windows, scheduled run, taking E50).** Re-run on a `main` `TIDE_VCV_FUNDAMENTAL=ON` build with `_scratch/e19-fixture-doc.xml` as `session.xml`: `building rack from 38658 byte document`, then `Pulses / SHASR / Scope constructed`, then **exit `0xC0000005`**. Same document, same three modules, same fault — so this row is reproducible on demand and its diagnosis stands. **But `Compare` is not in that document.** Its module types are `VCV: LFO`, `VCV: LFO2`, `VCV: Pulses`, `VCV: SHASR`, `VCV: Scope`, three bare `VCA`, plus TIDE's own parts; `grep -ci compare` is **0**. Read the never-reached list as `LFO, LFO2, VCA x3` and treat the fourth name as an error — **E50** carries the measurement and the reasoning. **AND E50 IS NO LONGER A CANDIDATE CAUSE FOR THIS ROW**, so the fix is what this row already says it is: a null check where `ug_patch_param_setter.cpp:172` hands an unchecked `GetParameter` result to `ConnectParameter`. Status untouched — annotated by a run that did not take it. **Original item follows.** **A NULL PARAMETER POINTER IS DEREFERENCED ON THE AUDIO THREAD WHILE BUILDING THE DSP GRAPH. Measured with a debugger 2026-08-27 (windows, scheduled run) -- faulting address, faulting instruction and register state, not inference.** Reproduced 4/4 on a `TIDE_VCV_FUNDAMENTAL=ON` Release build off `origin/main`, loading a 38,658-byte document written by the 2026-08-26 windows run (whose journal records that same document restoring correctly that day). **The exception record:** `ExceptionAddress: TIDE_Rack!ug_patch_param_setter::ConnectParameter+0x5e`, `ExceptionCode: c0000005`, `Parameter[0]=0` (a read), **`Parameter[1]=0x64`** -- so the address read is **0x64, an offset from NULL, not a wild pointer**. **The faulting instruction and the one after it name the source line exactly:** `mov ecx,dword ptr [rbp+64h]` followed by `call TIDE_Rack!HostControlisPolyphonic`, which is `HostControlisPolyphonic(parameter->getHostControlId())` in `SynthEditLib/ug_patch_param_setter.cpp` -- with `parameter` **null**. **Where the null comes from, two lines up:** `ug_patch_param_setter::ConnectParameter(int32_t moduleHandle, int moduleParameterId, UPlug* plug)` (`SynthEditLib/ug_patch_param_setter.cpp:172`) does `auto parameter = parent_container->get_patch_manager()->GetParameter(moduleHandle, moduleParameterId); ConnectParameter(parameter, plug);` -- **the lookup can miss and nothing checks it**. The only guards on the receiving overload are `assert`s, and TIDE ships `-DNDEBUG`. **Caller:** `ug_base::HookUpParameters` (`SynthEditLib/ug_base.cpp:745`), via `ug_base::Setup` -> `AudioMasterBase::BuildModules` -> `SeAudioMaster::BuildDspGraph` -> `SynthRuntime::prepareToPlay` -> `SynthEdit::onSetPins` (`SynthEditSem/SynthEdit.cpp:336`) -> `gmpi::Processor::process` -> `StandaloneHost::processAudio` -> **`AudioDriverWasapi::renderThread`. The graph is built on the audio thread, so this kills the render thread rather than surfacing as a load error.** **RELATION TO E46, stated carefully because I got it half wrong first.** This is **the same SHAPE as E46** -- an assert-only guard on a lookup that can miss, dereferenced under NDEBUG -- at a **different site in a different repo path**, and it is the measured crash E46 says it does not have. **But E46's specific data condition is ABSENT here, and that was measured:** parsing the crashing document for every handle-bearing attribute (`Module`, `module`, `tiedtomod`, `fMod`, `tMod`, case-insensitive) against every `<Module Id=>`/`<module handle=>` it defines gives **25 defined, 13 referenced, zero dangling**. **The refinement that follows, and it is the useful part:** `GetParameter` takes a `(moduleHandle, moduleParameterId)` PAIR, and my test only checked module handles -- so **the miss must come from the PARAMETER ID, on a module that does exist**. That is a narrower and more findable thing than E46's case. **The wrapper is not the variable:** reproduced identically with and without the 4-byte `TDs1` header today's builds write in front of the document, so the reader skips it. **Probably the same root as E48 and E50** -- a parameter that cannot be resolved is also a connector that gets dropped, and E50 would explain why a document written yesterday resolves differently today. **Scope** `SynthEditLib/ug_patch_param_setter.cpp:172` and its overload -- **GATED**, so this is filed, not written. **Accept** the 38,658-byte document loads without faulting, and a `(moduleHandle, parameterId)` pair the patch manager does not have is refused with a diagnostic rather than dereferenced; shown by loading that document in a Release build. **Size** small to guard (`if (!parameter) return;` plus deciding what to report), unknown to fix properly -- the real question is why the pair is missing, which is E50. **Repro, for whoever takes it:** put the document in `%APPDATA%\TIDE Rack\session.xml` inside a `<Preset standalonePlugin="TiDE Rack">` wrapper and launch the standalone under `cdb -y <release dir> -c ".symopt-0x100; sxe av; g; .exr -1; r rip; u @rip L4; kb; q"`. **`cdb.exe` IS on this box** at `C:\Program Files\WindowsApps\Microsoft.WinDbg_1.2603.20001.0_x64__8wekyb3d8bbwe\amd64\cdb.exe`; `WindowsApps` denies directory LISTING, so a recursive search with `-ErrorAction SilentlyContinue` returns empty and reads as "no debugger installed". Probe the path directly. |

| E63 | TODO | win | **THE WINDOWS RELEASE PACKAGE OMITS `DefaultRack.synthedit` AND TWO PIN XMLs, SO A SHIPPED WINDOWS BUILD STARTS WITH AN EMPTY RACK AND E48'S NEW MODULES HAVE NO PINS. Found in passing 2026-08-28 (windows, scheduled run, taking E19).** `scripts/package-windows.ps1:127` enumerates its resources BY NAME -- `$ResourceXmls = 'ControlsXp.xml', 'MidiPlayer2.xml', 'Converters.xml', 'VaFilters.xml'` -- plus `Prefabs\`, and nothing else. **The build stages SIX xmls and a default rack:** `ls build/SynthEditSem/Release` gives `ControlsXp.xml`, `Converters.xml`, `DefaultRack.synthedit`, `EnvelopeAdsr.xml`, `MidiPlayer2.xml`, `Oscillator.xml`, `Prefabs/`. So the packaged bundle is missing `EnvelopeAdsr.xml`, `Oscillator.xml` and `DefaultRack.synthedit`. **Two separate consequences, and the second is the worse one.** (1) `EnvelopeAdsr.xml` and `Oscillator.xml` are E48's -- the pin descriptions for `SynthEdit ADSR` and `SE Oscillator`, which E48 added to the compiled-in set on 2026-08-28 -- so a packaged Windows build ships those modules with no pins, which is **S21's failure**, the one this script's own error message refuses to package for. (2) With no `DefaultRack.synthedit` the plug-in prints `TIDE: no DefaultRack.synthedit in bundle resources - starting with an empty rack` and a first-run user gets nothing. **OBSERVED, not inferred:** a copy of the plug-in without `Contents/Resources` printed exactly those lines this session, which is how the gap was noticed at all. **WHY ONLY WINDOWS.** `package-linux.sh` copies the whole bundle and the whole `Resources` directory (`cp -a "$SRC/TIDE-Rack.vst3"`, `cp -a "$SRC/Resources"`) and macOS likewise copies assembled bundles, so neither can drift. Windows is the only script that maintains a second list -- and `SynthEditSem/CMakeLists.txt`'s own POST_BUILD install loops over `_tide_xmls` and copies `DefaultRack.synthedit` explicitly, so the LOCAL install is correct while the PACKAGE is not. That is why no local test catches it. **The script says so itself** at `:122`: *THIS LIST AND SynthEditSem/CMakeLists.txt's `_tide_xmls` MUST MOVE TOGETHER*. It did not move with E48. **Scope** `scripts/package-windows.ps1` -- drive the xml list from `_tide_xmls` rather than restating it, or at minimum add the three missing files and a check that fails when the two lists diverge. **Accept** package a Windows build and assert `TIDE-Rack.vst3\Contents\Resources\` contains every file the build staged, `DefaultRack.synthedit` included; and a launch of the packaged plug-in prints neither `missing from bundle resources` nor `no DefaultRack.synthedit`. **Size** small. **NOT FIXED BY THIS RUN** -- STEP 3 scope, and a packaging change wants its own review. **Unverified:** whether any RELEASE was cut with this gap; the CI packaging job was not read. |
| E72 | TODO | any | **RENUMBERED FROM E70 2026-08-31 (macos) — a genuine collision, not a typo.** This row and the archived **E70** (the render-pool priority fix) were filed 33 minutes apart from branches cut off the same `main`, landed at different points in the file, and git merged both cleanly; `check-id-refs.py` caught it at merge time, which is what A23 exists for. **This row moved rather than the other one** even though the other is technically the newer filing, because it is already DONE and archived and *"archiving never rewrites a row"* is the stronger rule. **The 2026-08-31 macos JOURNAL entry calls this row E70** and is left as written — the journal is the record — so this note is how you get from that name to this row. **GATED — filed 2026-08-31 (macos) as E69's part (2), which E69 could not do: the code is in `SynthEditLib/EditorLib/`, which STEP 5 gates, and this is not a build break so the STEP 5 exception does not reach it.** **The finding, read rather than measured:** `MfcDocPresenter::AddPatchCable` (`SynthEditLib/EditorLib/MfcDocPresenter.cpp:280`) and `RemovePatchCable` (`:363`) do a bare `patchManager->setParameterValue(...)` and construct no `SuspendDSP` guard, so they never reach `invalidateDsp()` and never set `dspDirty` (`SynthEditAppBase.h:250,262`; the RAII guard that does is `SuspendDSP.cpp:27`). Every other structural edit — module add/delete, container surgery — goes through that guard, which is exactly why `TideApp::serviceDocumentSync` can gate its push on the flag alone (`SynthEditSem/TideApp.cpp:585`). **Harmless for SAVES since E68/E69**, because the save now mints from the controller rather than waiting for a push; the path still owed the flag is the LIVE store between saves, i.e. a processor recreated after a cable edit with no save in between. **Scope** `SynthEditLib/EditorLib/MfcDocPresenter.cpp:280,363` — one `SuspendDSP` guard each, or an explicit `invalidateDsp()`. **Accept** recreate the processor after a cable edit with no save in between; the cables are present. **Size** two lines plus its verification. **Note it is SHARED with SynthEdit proper**, so the ruling wanted is whether a cable edit should mark the DSP dirty in the commercial product too — which is why this is a row and not a patch. |
| E77 | TODO | any | **RENUMBERED FROM E74 2026-08-31 (macos) — the second genuine id collision in one day, and the same shape as E72's.** This row and the linux **E74** below (the hosted VST3's recreated DSP instance) were filed hours apart from branches cut off the same `main`, landed at different points in the file, and **git merged both cleanly** — `BACKLOG.md` reported no conflict at all, which is exactly the failure A23's duplicate-id check exists for and exactly what the STEP 3 grep is meant to catch before filing. **This row moved rather than the linux one** because the linux E74 is already on `main` (merged as [#566](https://github.com/JeffMcClintock/TideSynth/pull/566)) and is cited by the `linux` NEXT cell, the E19 row and two journal entries, while this row existed only on the unmerged `tide/mac/E73-trace-to-file`: renumber the one with the fewest references, and never rewrite what has landed. **The 2026-08-31 macos E73 JOURNAL entry calls this row E74** and is left as written — the journal is the record — so this note is how you get from that name to this row. **Found 2026-08-31 (macos) by E73's fix, in the first log it ever produced — which is the argument for E73 in one row.** **E59's refusal did not fire in a hosted AUv3.** Two adjacent lines from the container log, REAPER hosting a fresh instance: `TIDE: controller #1 startup default is 17959 bytes (syncState will not publish this document)` then, immediately, `TIDE: controller #1 syncState exporting 17959 byte document (host asked for state)`. **Same size, and it published anyway** — so the guard's test (`xml == startupDefaultChunk`, byte equality, `SynthEditSem/SynthEditController.cpp`) found a difference the byte COUNT does not show. E59's own comment predicts this direction explicitly: *"If the two ever differ spuriously this publishes -- today's behaviour"*, and calls a false negative the cheap way to be wrong. **Why it matters anyway:** the published bytes are retained by the processor holder and re-seeded into the next processor it starts, which is the entire E59 defect. The same log shows the consequence in the harmless case — `instance #2 building rack from 17959 byte document (Sync chunk, rack not yet prepared)`. **This run was harmless because nothing was restored:** a fresh instance with no saved state SHOULD build the default rack. **The failure needs a restore**, and that is the case nobody has run on this platform. **Scope** `SynthEditSem/SynthEditController.cpp`'s `syncState` guard — diff the two documents to find what differs at equal length (a timestamp, a handle, a pointer-valued parameter are the candidates), then decide whether the comparison should ignore it. **Accept** with a prepared rack restored into a hosted AUv3, the DSP builds the RESTORED document, not a 17,9xx-byte default. **Size** one session; the diff is the whole job and E73's log is the instrument. **Do not read this as E59 being broken everywhere** — E59's Accept was re-measured green on macOS through REAPER on 2026-08-29 (`-6.3 / -17.0 dBFS`), so whatever differs here is specific to this path or this platform. |
| E71 | TODO | mac | **Found in passing 2026-08-31 (macos) while measuring E69; NOT part of it, and deliberately not fixed by a run that cannot drive an AUv3 host.** `AU3_Wrapper.mm`'s `setFullState` (`:531`) writes the controller's store and then delivers the restored values to the processor queue, the editor and the AUParameterTree in one loop (`AU3_Wrapper.mm:549-555`) — but that loop **never calls `notifyControllerOfPreset`**, which is the one call that hands the restored parameters to the plug-in's own `<Controller/>`. Both other wrappers do: `Controller_VST3.cpp:526` and `StandaloneHost.cpp:318`, and the holder's own comment says a plug-in whose state IS a parameter *"had no way to learn its state had been restored, and started blank however good the preset was"* — which describes TIDE exactly, since it builds its whole application object in its controller. **This is a code reading, not a measurement, and it may already be covered** by the parameter loop below it; the loop calls `sendParameterToProcessorQueue` and `notifyGui`, neither of which reaches an `IController`. **Scope** `GMPI_Wrappers/wrapper/AU3/AU3_Wrapper.mm:549-555`, the delivery loop (ALLOWED). **Accept** restore a saved rack in an AUv3 host and see the modules; today unreachable unattended — E19's mac cell measured five ways to register a second AUv3 beside the developer's, all failing, so this wants the same one unlocked interactive session. **Size** one line, if the reading holds. **NOT a duplicate of E69, which reads the same function for the opposite reason:** E69 cites the `setPresetXmlFromDaw` call as EVIDENCE that AU3's load feeds the controller's store — which is why AU3 does not share CLAP's save defect and needed no fix there. This row is about a DIFFERENT store one layer on: the plug-in's own `<Controller/>`, which the delivery loop after that call does not reach. |
| E74 | IN-REVIEW | any | **FIXED AND MEASURED 2026-09-01 (linux, scheduled run), branches `tide/linux/E74-editor-processor-rebind` ([#569](https://github.com/JeffMcClintock/TideSynth/pull/569)) and `tide/linux/E74-linux-timer-pump` ([gmpi_ui#17](https://github.com/JeffMcClintock/gmpi_ui/pull/17) + [GMPI_Wrappers#38](https://github.com/JeffMcClintock/GMPI_Wrappers/pull/38)). **The fix is the two-repo pair, which must merge TOGETHER -- #38 does not compile without #17, and #17 alone changes nothing; #569 is the instrument and the bookkeeping.** **THIS ROW'S OWN DIAGNOSIS WAS WRONG, AND THE WAY IT WAS WRONG IS THE USEFUL PART: THE EDITOR WAS NEVER BOUND TO ANY PROCESSOR, RETIRED OR OTHERWISE.** Instrumenting both ends of the blob parameter split it in one run -- the processor shipped **2,700 feedback updates and the editor received ZERO**, from instance #3 as well as #4, while the standalone control was one-for-one on the same build. So the instance recreation was a correlation; it still happens after the fix and the editor is fine. **THE CAUSE IS THAT NOTHING PUMPS `gmpi::TimerManager` IN A HOSTED LINUX PLUG-IN.** `gmpi_ui/helpers/Timer.cpp` has `SetTimer` on Windows and `CFRunLoopTimer` on macOS and **no native timer source on Linux** (`Timer::start`, the `#if !defined(_WIN32) && !defined(__APPLE__)` arm), where it must be pumped from a UI-thread loop; the standalone pumps it (`StandaloneApp.cpp:468`) and **a plug-in has no loop of its own**. So no `gmpi::TimerClient` in the process ever ran -- including `Controller_VST3::onTimer`, which is **the one caller** of `gmpiController.message_que_dsp_to_ui.pollMessage()`. **The fix** is `TimerManager::pump()` with no argument, which measures its own elapsed time so several open editors cannot make the process-wide singleton run fast, called from `SEVSTGUIEditorLinux::onTimer` and its Wayland twin -- the host `IRunLoop` tick being the only UI-thread tick a hosted plug-in gets here. **Accept met, same harness, transport rolling 75 s:** after the last `building rack` line the editor goes `display-state update #30 -> #2160 arrived (65548 bytes)` and `light 0 update #200 -> #9100 value 0.516` (varying), against `#1 (0 bytes)` and `#2 value 0.000` frozen before. Standalone control unchanged across the A/B. **The GUI->DSP half was dead too** and is fixed by the same line: `Controller_VST3::onTimer` also flushes `queueToDsp_`. **The CLAP wrapper has the identical hole and is NOT fixed here -- filed as E78.** **Original item, kept for the record:** **Filed 2026-08-31 (linux) from E19's linux VST3 cell, which it blocks; it is the whole of that cell's remaining FAIL.** **A HOSTED VST3 RECREATES THE DSP INSTANCE AND THE EDITOR'S RACK FEEDBACK PINS STAY BOUND TO THE RETIRED ONE.** Measured on REAPER 7.43/Linux over 75 s with the transport rolling: the DSP reaches `RackProcessor: 'Scope' display-state capture #2100 (65548 bytes)` and is still climbing, while the editor is frozen at `display-state update #1 arrived (0 bytes)` and `light ... update #2 value 0.000`. **The evidence is ordering.** `TIDE: instance #3 building rack from 43191 byte document` -> the RackEditors are constructed and each pin takes its initial default -> `TIDE: instance #4 building rack from 43191 byte document` -> every subsequent capture is instance #4's and **no `RackEditor:` line is ever printed again**. **Control:** the standalone builds twice too but as `instance #1` both times, and its editor runs to `light ... update #18100` with varying values. So a double build is not itself the defect -- the changing INSTANCE is. **This is NOT E59** (that was the DSP running the wrong document, and it is fixed -- the document here is the right one, 43,191 bytes, both times), and it is not the ui->dsp direction **E64** fixed. **Where to look first:** whatever pairs a rack module's GUI pins to its RackProcessor across a `setActive` recreation. **Scope:** likely `SynthEditSem/` or `GMPI_Wrappers/wrapper/VST3/`, both ALLOWED; if it turns out to be in `SynthEditLib/` it is GATED and this row becomes a filing. **Accept:** with a prepared rack in a hosted VST3, `RackEditor: display-state update #N arrived (65548 bytes)` and `light ... update #N` are both still advancing 60 s after the last `building rack` line. **Harness:** [tests/e19-host-feedback/](tests/e19-host-feedback/) -- no authoring needed, it mints its own project. |
| E75 | TODO | any | **Filed 2026-08-31 (linux) from E19, whose pixel-diff and int/bool/enum clauses it blocks.** **`tests/fixtures/e53-vcv-rack-segv.xml` IS NAMED BY THREE ROWS AS E19's REPRODUCTION AND CANNOT SERVE TWO OF ITS FOUR CLAUSES**, because none of its five VCV modules is on the visible rack page. Measured both ways so this is not a rendering complaint: all five editors construct with panel art (`RackEditor: 'Scope' model=yes art=yes(res/Scope.svg) art-size=195x380`) in the hosted VST3 **and** in the standalone, and the DEFAULT rack in the same build draws its `Out` panel on the rails. Vertical and horizontal scroll (`--scroll <x,y> --horiz --notches N`) did not reach them. **What this costs:** the hosted pixel diff reads **0 of 690,800** and the standalone control reads **byte-identical screenshots**, so neither number is evidence about anything. **Accept:** a committed fixture whose VCV LFO and cabled VCV Scope are both visible on the rack at the default view, so that a pixel diff over the Scope and a right-click on its panel are both possible. **Note it may be two questions:** whether a rack can hold a module the view cannot reach is worth answering before authoring around it. |
| E76 | TODO | any | **Filed 2026-08-31 (linux); one wrapper, and it is why this box had no audio numbers for three days.** **`scripts/render-and-measure.py` SEGFAULTS REAPER ON LINUX when run from a scheduled run's own environment** -- rc **-11**, preceded by the `gdk_screen_get_root_window` / `gdk_x11_window_get_xid` assertions [docs/ci/headless-gui-verification.md](docs/ci/headless-gui-verification.md) already attributes to an inherited `WAYLAND_DISPLAY`. **The downstream symptom is an `EOFError` out of Python's `wave` module on a zero-length render, which reads as a corrupt fixture and is not one** -- it cost this run a wrong provisional conclusion about E29's token before the log was read. **With `env -u WAYLAND_DISPLAY DISPLAY=:N GDK_BACKEND=x11 XDG_RUNTIME_DIR=... ` every committed fixture renders**, at the macOS reference figures. **The script cannot fix itself from outside:** `subprocess.call([REAPER, "-renderproject", ...])` passes its own environment. So this is either a documented wrapper (done, in the harness doc) or a linux arm inside `render()` that scrubs the two variables -- **the second needs a ruling on whether a measurement script may edit the caller's environment**, which is why this is filed rather than done. **Accept:** `python3 scripts/render-and-measure.py tests/hosts/v1-rack.rpp` returns -6.3/-17.0 on linux with no wrapper, or the docstring says in one line that a wrapper is required and what it is. |
| E78 | IN-REVIEW | any | **FIXED AND MEASURED 2026-09-01 (linux, interactive continuation, Jeff directing *"fix E78 too while you have the harness up"*), on `tide/linux/E74-linux-timer-pump` ([GMPI_Wrappers#38](https://github.com/JeffMcClintock/GMPI_Wrappers/pull/38), the same PR as E74) plus the probe on `tide/linux/E74-editor-processor-rebind` ([#569](https://github.com/JeffMcClintock/TideSynth/pull/569)).** **THE CHANNEL IS ALIVE; ONE CLAUSE OF THIS ROW'S ACCEPT IS NOT MET AND IS FILED AS E80.** Same tree, one change apart, 30 s, the five-module prepared rack: **shipped 1,600 / received 0 -> shipped 1,600 / received 1,600**, and `RackEditor: light` goes from **frozen at `#2 value 0.000`** to **`#3300 value 0.754`, varying**. **The control that makes it a statement about the plug-in: the host timer ticked 1,782 times in the BEFORE arm and 1,604 in the AFTER** -- the tick was always there and the plug-in was not using it. **WHAT IS NOT MET: `display-state update #N arrived (65548 bytes)` never happens on CLAP**, where it does on VST3 (#2160) and in the standalone (#1400). The largest payload TIDE's own `drainRackFeedback` ever packs on CLAP is **200 bytes against VST3's 65,673**, at block sizes 128, 512 and 2048 alike -- so the blob is not reaching the queue at all and it is not a pacing artefact. **Floats traverse and blobs do not**, which is E60's shape. Filed as **E80**. **AND A SEPARATE LIFETIME DEFECT THIS FIX CANNOT REACH, filed as E79:** the CLAP timer is registered in `guiSetParent` and unregistered in `guiDestroy`, so a hosted Linux CLAP with **no editor open** has no UI-thread tick at all and never receives its document. **THE INSTRUMENT IS NEW AND REUSABLE:** [tests/e78_clap_gui_probe.c](tests/e78_clap_gui_probe.c) -- a minimal CLAP host supplying `clap_host_timer_support` and `clap_host_posix_fd_support`, a real X11 window, and `process()` on a second thread. **It exists because REAPER 7.43 CANNOT host this GUI on this box**: `TrackFX_Show`, float or chain, kills REAPER inside its own GTK (`gdk_screen_get_root_window: assertion 'GDK_IS_SCREEN (screen)' failed` and three more) before it ever reaches `guiSetParent`. **Original item, kept for the record:** **Filed 2026-09-01 (linux) from E74, whose cause it shares exactly; it blocks E19's linux CLAP cell.** **THE CLAP WRAPPER HAS E74'S DEFECT AND E74'S FIX DOES NOT REACH IT.** `Controller_CLAP` is a `gmpi::TimerClient` (`wrapper/CLAP/Controller_CLAP.h:9`) that `startTimer(15)` in its constructor (`Controller_CLAP.cpp:20`), and its `onTimer()` (`:28`) is the only caller of `gmpiController.message_que_dsp_to_ui.pollMessage()` **and** of `pendingQueueClients.ServiceWaitersIncremental(&message_que_ui_to_dsp, 100000)` -- so on Linux, where `gmpi_ui/helpers/Timer.cpp` has no native timer source, **both** directions of the parameter channel are dead. The CLAP host tick that exists and does not pump is `Processor_CLAP::onTimer(clap_id)` at `wrapper/CLAP/Editor_CLAP.cpp:1062`, which today calls only `processEvents()` and `drawingframe.onTimer()`. **Scope** `GMPI_Wrappers/wrapper/CLAP/Editor_CLAP.cpp` -- ALLOWED; one call to `gmpi::TimerManager::instance()->pump()`, the no-argument overload E74 added to gmpi_ui. **Accept** a prepared rack in a hosted Linux CLAP with the editor open: `RackEditor: display-state update #N arrived (65548 bytes)` and `light ... update #N` both advancing 60 s after the last `building rack` line, i.e. E74's Accept with CLAP in place of VST3. **NOT DIAGNOSED BY MEASUREMENT -- this is inspection plus E74's A/B**, and it is filed rather than fixed for exactly that reason: no CLAP GUI host was driven this run. **Size** small; the fix is one line and the cost is standing up a CLAP host with a window. `tests/e60_clap_state_probe.cpp` will NOT do it -- it drives the C ABI with no GUI, and no editor means no run-loop tick to pump. |
| E79 | TODO | any | **Filed 2026-09-01 (linux) while fixing E78; it is a LIFETIME question, not a pump question, and E78's fix cannot reach it.** **A HOSTED LINUX CLAP WITH NO EDITOR OPEN NEVER RECEIVES ITS DOCUMENT, SO IT PLAYS SILENCE.** The Linux CLAP registers its host timer in `guiSetParent` (`GMPI_Wrappers/wrapper/CLAP/Editor_CLAP.cpp:269`) and unregisters it in `guiDestroy` (`:213`), and that timer is the only UI-thread tick a hosted plug-in gets on this platform. `Controller_CLAP::onTimer` is the only thing that services the controller->processor queue, so with no window there is nothing to carry the restored document to the DSP. **Measured in REAPER 7.43**, a project carrying a prepared 43,199-byte rack, transport rolling 8 s with the editor deliberately closed: `TIDE: controller #1 restore of a 43199 byte document -> imported` and then `TIDE: unprepared - writing silence to the host's output buffers`, with **no `building rack` line ever**. **This is a shipping defect, not a harness artefact: a user who loads a project and presses play without opening the plug-in window gets silence.** **Scope** `GMPI_Wrappers/wrapper/CLAP/` -- ALLOWED. The shape of the answer is a timer whose lifetime is the PLUG-IN's rather than the editor's; note `clap_host_timer_support` is available from `init()` onward, so registering there and unregistering in `destroy()` is legal, and the editor's own 16 ms timer can stay separate. **Accept** the REAPER measurement above with the editor never shown: a `building rack` line, and `v1-rack.rpp`-style audio rather than `-inf`. **Size** small-to-medium; the risk is timer lifetime across `activate`/`deactivate`, not the registration itself. **Check VST3 for the same shape before assuming it is CLAP-only** -- VST3 restored fine here, but its document reaches the processor by a different route (`setState` + the chunk parameter at `setActive`), so this is about who carries the document, not about the timer alone. |
| E80 | TODO | any | **Filed 2026-09-01 (linux) from E78, whose Accept it is the unmet half of; it blocks E19's linux CLAP cell.** **AFTER E78, FLOATS TRAVERSE THE CLAP PARAMETER CHANNEL AND A 65,548-BYTE BLOB DOES NOT.** With the fix in, `RackEditor: light` runs to `#3300 value 0.754` (varying) while `RackEditor: display-state update` stays frozen at `#1 arrived (0 bytes)`. **The measurement that locates it:** TIDE's own `TIDE: instance #N feedback send #M (B bytes)` counter -- which is INSIDE the plug-in, upstream of any wrapper -- never packs more than **200 bytes** on CLAP, against **65,673** repeatedly on VST3 and in the standalone on the same tree. So the blob is not entering `queDspToUi` at all. **Not a pacing artefact:** identical at block sizes 128, 512 and 2048, and `RackProcessor: 'Scope' display-state capture #700` says the DSP captured it every time. **This is E60's shape** (a large-payload path that works for small values), which is where to look first. **THE HONEST CONFOUND, and it is why this is TODO rather than a diagnosis:** the only host that has driven a TIDE CLAP GUI is [tests/e78_clap_gui_probe.c](tests/e78_clap_gui_probe.c), our own, and no DAW has arbitrated -- **REAPER 7.43 cannot, because `TrackFX_Show` on this CLAP kills it inside its own GTK** (`gdk_screen_get_root_window: assertion 'GDK_IS_SCREEN (screen)' failed`, float and chain modes alike, before `guiSetParent` is reached). **So step one is a second opinion, not a fix:** either another CLAP host with a GUI (Bitwig, or `free-audio/clap-host`), or establishing that the probe CAN carry 65 KB by making a small GMPI plug-in that sends one. **Scope** unknown until then; `GMPI_Wrappers/wrapper/CLAP/` and the blob arm of the inner rack's parameter path are the candidates, and if it lands in `SynthEditLib/` it is GATED and becomes a filing. **Accept** `RackEditor: display-state update #N arrived (65548 bytes)` advancing in a hosted CLAP. **Size** medium. |
| E20 | BLOCKED | any | **PARKED 2026-08-26 BY JEFF'S RULING: *"3rd-party module compatibility is not important at this stage. We ship with only our own modules."* — recorded in [docs/decisions.md](docs/decisions.md). `BLOCKED`, not `WONTFIX`, deliberately: *"at this stage"* is a sequencing call and the work below keeps its value if it is revisited. Do not take this row until that ruling changes.** **PORTABILITY MEASURED AND THE ADAPTOR WORK IS DONE, 2026-08-25 (macos, interactive, Jeff directing), branch `tide/mac/E20-hetrickcv-port`. TWO CORRECTIONS TO THIS ROW BELOW, one of them licensing.** **THE MEASUREMENT: 66 of HetrickCV's 79 source files compile against the adaptor** (syntax-only, C++23), up from **1 of 79** before — the first one died on the first missing Rack-SDK include path. The mock additions that got it there are [SynthEdit_Rack_Adaptor#3](https://github.com/JeffMcClintock/SynthEdit_Rack_Adaptor/pull/3): an `Engine` with a global sample rate the adaptor keeps in step with `ProcessArgs`, six SDK path shims, `Rogan`, `settings::preferDarkPanels`, `SvgPanel::fb`, a `setPanel(shared_ptr<Svg>)` overload, `getPanel()`, `TL1105`/`CKD6`, and the `app::` widgets re-exported into `rack::`. **The 13 failures are named and small, not structural:** 2 are not modules (`HetrickCV.cpp` is the plugin entry, `HetrickUtilities.cpp` a shared impl unit), and the rest want `dsp::approxExp2_taylor5` (3), `LEDBezel` (2), simd `int32_4` and an `abs` overload (3), `getOutput`, `NVG_ONE_MINUS_DST_COLOR` and `SvgPanel::setBackground` (1 each). **REGRESSION CONTROL: TIDE with `TIDE_VCV_FUNDAMENTAL=ON` against the modified adaptor still registers 39 Fundamental modules**, rc=0, clean runtime — the additions are new declarations plus one call. **CORRECTION 1, LICENSING: this row says "no attribution obligation ... no artwork question at all". THAT IS WRONG.** HetrickCV's own tree is CC0, but it carries **Gamma as a git submodule** (`github.com/mhetrick/Gammin`), which is **MIT, © Lance Putnam 2006** — permissive and GPL-free, so the pack is still bundleable, **but MIT requires the notice be retained** and `HetrickUtilities.hpp` pulls Gamma in, so effectively every module depends on it. **So E20 needs E22's attribution mechanism after all**, and the two are no longer independent. **CORRECTION 2:** `HCVThemedRogan` names VCV's own `res/ComponentLibrary/Rogan1P*.svg`. Nothing ships — `asset::system` and `Svg::load` are mock stubs and `RackEditor` draws its own knobs — but the reference exists in source and should not be mistaken for a licence problem later. **THE REMAINING BLOCKER IS NOT TECHNICAL AND IS NOT MINE:** the option needs a port repo shaped like `VCV_Fundamental_gmpi` (per-module dir, two-line wrapper `#include "RackModule.h"` + the upstream `.cpp`, `res/`, and a `static_library/` aggregate of OBJECT libraries). **Creating repositories is Jeff's call — STEP 5 forbids a run creating one.** Once `HetrickCV_gmpi` exists this row is the CMake option plus mechanical per-module files, and the port shape is proven. **Previous text follows.** **UNBLOCKED AND RESTORED AS THE FIRST ATTEMPT, 2026-08-25 (macos, interactive) — the demotion below was based on a stale comment and is withdrawn.** E23's premise turned out to be false: `RackEditor` **already draws jacks, knobs and lights** from the module's own reported geometry, so HetrickCV's labels-only panels are not a defect. They supply background art and labels; the adaptor supplies the controls. **So under Jeff's constraint — *"avoid modules unless we can use the artwork too"* — HetrickCV PASSES on both readings**: CC0-1.0 covers code and panel art (verified from the project's own `LICENSE.txt`), and the panels render usably because the components are drawn for them. **Both prerequisites are now met:** the adaptor is dual-licensed on `main` ([#1](https://github.com/JeffMcClintock/SynthEdit_Rack_Adaptor/pull/1) merged) and `tide-rack-bot` has write access. **This is the row to take first.** **Previous text follows.** **LICENSING PREREQUISITE MET 2026-08-25: the adaptor is dual-licensed on `main` ([#1](https://github.com/JeffMcClintock/SynthEdit_Rack_Adaptor/pull/1) merged).** So the only thing still standing between this row and a shippable permissive pack is the panel-art problem E23 fixes — which is why this stays `BLOCKED(E23)` rather than becoming TODO. **Previous text follows.** **DEMOTED 2026-08-25 (macos, interactive): NOT a first attempt, on Jeff's artwork constraint.** E24 rendered two HetrickCV panels and **neither draws any control art** — labels and leader lines on a blank faceplate. The licensing is still the best available (CC0 for code AND art, verified) and ~70 modules is still the biggest catalogue here, so **this row is not withdrawn** — but combined with `RackEditor`'s no-knob-caps-no-jacks behaviour it would ship a rack of unusable-looking modules. **BLOCKED on E23 rather than E24**, because E23 is what makes this pack viable; after E23 the panels' emptiness stops mattering. **Previous text follows.** **`TIDE_HETRICKCV` — bundle the HetrickCV pack, CC0 code AND artwork, as a CMake option shaped exactly like `TIDE_VCV_FUNDAMENTAL`.** Filed 2026-08-25 (macos, interactive, Jeff directing). **THE PICK, and why this one first:** ~70 modules under **CC0-1.0 for code and panel art alike** — verified by fetching the project's own `LICENSE.txt`, not taken from a summary — so there is no attribution obligation, no share-alike, and no artwork question at all. Its catalogue is **complementary rather than duplicative**: phasors, boolean logic, chaos, analog/digital conversion, Rungler, Waveshaper, Dust, Crackle — utilities TIDE has none of, rather than a second VCO/VCF/ADSR. **PREREQUISITE, and it is a real one: the adaptor must be dual-licensed.** While `SynthEdit_Rack_Adaptor` is GPL-3.0-or-later-only, a CC0 module still yields a GPL binary and this row buys nothing. Ruled by Jeff 2026-08-25 and recorded in [docs/decisions.md](docs/decisions.md); the change is prepared but **the fleet token has no push access to that repo** (`push=false`), so it lands by Jeff applying it or by granting `tide-rack-bot` write. **Scope** root `CMakeLists.txt` — a new `option(TIDE_VCV_HETRICKCV ...)` following the existing block at `:389-455` verbatim in shape: OFF by default, everything inside the `if()` so OFF means COMPLETELY ABSENT, a `HETRICKCV_FOLDER_OVERRIDE`, and the same `SOURCE_SUBDIR static_library` pattern. Plus the registration flush in `TideApp::InitInstance`, which already exists and is per-pack-agnostic. **Accept** `cmake -B build -DTIDE_VCV_HETRICKCV=ON` builds, the standalone prints a `HetrickCV — N module(s) registered` line the way Fundamental prints its 39, the modules appear in the RACK browser (their category must start with `Rack` — see V4 / `ModuleScope::RackOnly`), and **the negative control holds: a default configure greps clean for `hetrick`, exactly as `vcv-fundamental.md` documents for Fundamental.** **Size** medium — the option is a copy of a working one; the unknown is per-module porting, which E24 sizes. |
| E21 | BLOCKED | any | **PARKED 2026-08-26 BY JEFF'S RULING: *"3rd-party module compatibility is not important at this stage. We ship with only our own modules."* — recorded in [docs/decisions.md](docs/decisions.md). `BLOCKED`, not `WONTFIX`, deliberately: *"at this stage"* is a sequencing call and the work below keeps its value if it is revisited. Do not take this row until that ruling changes.** **RE-SCOPE BELOW WITHDRAWN 2026-08-25 (macos, interactive):** it said NLC's per-module mix of panel art made it the safer first attempt. That reasoning depended on E23's false premise — `RackEditor` draws the components regardless — so **NLC has no advantage over E20 on artwork, and E20 has ~70 modules to NLC's 18.** Order restored: E20 first, this row after it. Status stays `BLOCKED(E20)` for the original reason, which was always the right one — this is the pack-wide CMake option and it reuses E20's pattern. **Previous text follows.** **RE-SCOPED 2026-08-25 (macos, interactive): Nonlinear Circuits is MIXED per-module, and that is better news than it sounds.** E19 rendered two: `1050 Mixer Sequencer` draws its knobs and jacks clearly — **the best panel of any candidate measured, control included** — while `8BitCipher` draws none. So this pack contains genuine first-attempt candidates **at the module level**, which is exactly what Jeff's artwork constraint asks for. **This row stays BLOCKED(E20) — it is the pack-wide CMake option and that still depends on E20's pattern.** The per-module path is **E24's** to run, and E24 is TODO: screen these 31 panels by rendering them, take a module that draws its own components, and port that one. Naming it here would put takeable-sounding prose on a blocked row, which is the exact thing the status column is supposed to settle. 18 modules, CC0 code and artwork. **Previous text follows.** **`TIDE_NONLINEARCIRCUITS` — 18 more CC0 modules, and it is nearly free once E20 lands.** Filed 2026-08-25 (macos, interactive, Jeff directing). **Same author as HetrickCV (Michael Hetrick) and the same licence** — CC0-1.0, code and artwork, verified from the project's own `LICENSE.txt`. So one relationship and one licence review covers **~88 modules across the two rows**, which is why they are filed as a pair rather than judged separately. Catalogue is Andrew Fitch's Nonlinear Circuits hardware ported: Sloth (Torpor/Apathy/Inertia), Squid Axon, Double Neuron, GENiE, Divine CMOS, Numberwang, 8-Bit Cipher — chaotic and idiosyncratic, which is exactly what a rack built only from clean utilities lacks. **Scope** the same option block as E20 with the pack swapped; by this point the pattern is established and this row is mostly configuration. **Accept** as E20 — builds ON, registers a counted line, modules appear in the rack browser, and a default configure greps clean for `nonlinear`. **Size** small, ASSUMING E20 landed the pattern; if E20 turned out to need per-pack work then re-cost this before starting. **Attribution:** none required — CC0 waives it. The pack's `Audiowide-Regular.ttf` is OFL-1.1-RFN, which permits redistribution and only reserves the font NAME, so it needs no action unless the font is modified. |
| E22 | BLOCKED | any | **PARKED 2026-08-26 BY JEFF'S RULING: *"3rd-party module compatibility is not important at this stage. We ship with only our own modules."* — recorded in [docs/decisions.md](docs/decisions.md). `BLOCKED`, not `WONTFIX`, deliberately: *"at this stage"* is a sequencing call and the work below keeps its value if it is revisited. Do not take this row until that ruling changes.** **COUPLED TO E20 AS OF 2026-08-25 (macos, interactive): E20 needs this row's mechanism, which it previously did not.** E20's pack was filed as CC0-with-no-attribution-obligation. It carries **Gamma as a submodule — MIT, © Lance Putnam** — and `HetrickUtilities.hpp` pulls it in, so every HetrickCV module depends on MIT-licensed third-party code whose notice must be retained. **So the attribution mechanism is needed by the FIRST pack bundled, not only by the MIT tier**, and this row should be read as a prerequisite of E20 rather than a follow-on. Its status is unchanged because the work is unchanged; what changed is who needs it. **Previous text follows.** **The MIT tier — `TIDE_CVFUNK` (43 modules) and `TIDE_DHE` (28) — plus the ATTRIBUTION mechanism that CC0 let the earlier rows skip.** Filed 2026-08-25 (macos, interactive, Jeff directing). **Both verified MIT at source**, from each project's own `plugin.json` (`"license": "MIT"`), with Cardinal's table independently recording their artwork as *"same license as source code"* — so unlike the Fundamental case the panels come with the code. **THE NEW WORK IS NOT THE OPTION, IT IS THE NOTICE.** CC0 waives attribution; **MIT does not** — it requires the copyright and permission notice to be retained in redistributions, and a plugin binary that ships panel art and DSP from a third party is a redistribution. TIDE has no mechanism for this today. **Scope** two option blocks per E20's pattern, plus a place for third-party notices that actually SHIPS — the about pane (D6) is the obvious host, since constraints 1 and 5 rule out a dialog. Model it on Cardinal's `docs/LICENSES.md`: per-pack, code and artwork listed separately. **Accept** both options build and register; a default configure greps clean for both; **and the MIT notices are reachable from the running plugin**, not merely present in the repo — that last clause is the row's real content and the reason it is not folded into E20. **Size** medium. **Do NOT bundle these before the notice mechanism exists** — shipping MIT code without its notice is the one failure here that is a licence breach rather than a bug. **Explicitly EXCLUDED and recorded so nobody re-derives it:** **AS** (MIT code but **CC-BY-NC-ND-4.0** artwork) and **Mog** (CC0 overall but `components/*` is **CC-BY-NC-4.0**) — both are the exact code-permissive/art-restricted trap [docs/vcv-permissive-modules.md](docs/vcv-permissive-modules.md) documents. |
| E23 | BLOCKED | any | **PARKED 2026-08-26 BY JEFF'S RULING: *"3rd-party module compatibility is not important at this stage. We ship with only our own modules."* — recorded in [docs/decisions.md](docs/decisions.md). `BLOCKED`, not `WONTFIX`, deliberately: *"at this stage"* is a sequencing call and the work below keeps its value if it is revisited. Do not take this row until that ruling changes.** **AND E24 CONTRADICTS THIS ROW'S CENTRAL CLAIM, which matters if anyone revives it.** This row states, from reading the code, that *"`drawKnobs()` draws rim, body AND pointer"* and that *"a panel that is labels-only still gets grabbable controls"*. E24 then MEASURED a HetrickCV Crackle on a running rack and found the knob draws **nothing at all**, and its `CKSS` switch likewise. One of the two is wrong and the measurement is the one with a screenshot behind it. **Whoever revives this starts by reconciling them, not by trusting either.** **THIS ROW'S PREMISE IS FALSE AND IT IS MOSTLY ALREADY DONE. Corrected 2026-08-25 (macos, interactive, Jeff directing) by READING THE CODE instead of its header comment.** `RackEditor`'s render path is `panel art -> drawModuleWidgets(-1) -> drawJacks() -> drawKnobs() -> drawLights()`. **`drawJacks()` draws a five-ring VCV-style jack sized from the module's own widget; `drawKnobs()` draws rim, body AND pointer** — not "an indicator line". Both read `layout.inputs`/`outputs`/`params`, which come from the module's `ModuleWidget` and have **nothing to do with the panel SVG**. So the thing this row proposed to build already exists, and a panel that is labels-only still gets grabbable controls. **Where the false premise came from:** the comment at `RackEditor.h:25-29` says the opposite of the code twelve lines below it. It was accurate at `d4de897` (the adaptor's initial commit) and superseded by `623f1f7` *"Draw the jacks, VCV-style, sized from the module's own widget"* **the same day**; nobody updated it. Fixed in [SynthEdit_Rack_Adaptor#2](https://github.com/JeffMcClintock/SynthEdit_Rack_Adaptor/pull/2), comment-only. **WHAT IS ACTUALLY LEFT, and it is small:** (1) **screws are still not drawn** — cosmetic; (2) **verify component placement on a MILLIMETRE-authored panel.** `RackEditor` carries an explicit 75-dpi-vs-96-dpi correction (`panelMetrics`, `rackDpi`/`cssDpi`) whose own comment warns a mm-authored panel *"draws 96/75 = 28% too large for the coordinates its own module places controls at"*. HetrickCV panels are 380-unit (Rack px, unaffected), but **CVfunk and DHE are viewBox height 128.5 — millimetres, the exact case the correction exists for.** The mechanism is there; nobody has checked it lands right. **That is a DIFFERENT risk from the one this row was filed about, and it belongs to E22's packs, not E20's.** **Accept** screws drawn or explicitly declined, and one mm-authored panel (a CVfunk or DHE module) shown with its knobs and jacks on their printed positions. **Size** small. **This row no longer blocks anything.** **Previous text follows.** **ACCESS GRANTED 2026-08-25, so this row's stated blocker is GONE and the row is takeable by any box.** It said `tide-rack-bot` has `push=false` on `SynthEdit_Rack_Adaptor`, needing Jeff or a grant. Jeff granted it the same day: the bot now has **`push=true`** on `SynthEdit_Rack_Adaptor` **and** on `VCV_Fundamental_gmpi` (which is PRIVATE — that is why it used to 404 for the bot, not because it was renamed). Both invitations accepted from the bot side. **The adaptor is also now DUAL-LICENSED on `main`** — [SynthEdit_Rack_Adaptor#1](https://github.com/JeffMcClintock/SynthEdit_Rack_Adaptor/pull/1) merged, 15 files `ISC OR GPL-3.0-or-later`, zero GPL-only leftovers — so E20-E22's licensing prerequisite is met as well. **Note the repo is still on NEITHER STEP 5 list, so it is GATED by default**; write access is not the same as permission, and this row should be taken interactively or after Jeff rules on where it sits. **Previous text follows.** **Teach `RackEditor` to draw TIDE's own components at the positions a module already reports — this is the row that makes every permissive pack viable at once, and E24 measured why it is needed.** Filed 2026-08-25 (macos, interactive, Jeff directing). **`RackEditor.h:25-29` says it draws only *"the moving part — an indicator line per knob"*, and that Fundamental's panels get away with it because they carry their own knob caps, jacks and screws.** E24 rendered the permissive candidates and found that **most do not**: HetrickCV draws no control art on either sample, DHE draws none, CVfunk's are not evident, and Nonlinear Circuits is mixed per-module. **So panel art being load-bearing is the single thing standing between TIDE and ~160 permissively-licensed modules**, and it is a TIDE-side fix rather than a licensing negotiation. **The pieces already exist:** TIDE ships `TiDEknob` and `TiDE Patch Point In/Out` (compiled in per [#406](https://github.com/JeffMcClintock/TideSynth/pull/406)), and the adaptor already knows every control's TYPE and POSITION — `RackAdaptor.h:131` notes positions are in Rack panel pixels, the same space the panel SVG uses, and `RackEditor` is already placing an indicator line per knob, so it has the coordinates. The work is drawing TIDE's component at those coordinates instead of assuming the panel did it. **Scope** `RackEditor.h` in `SynthEdit_Rack_Adaptor` — **PR-GATED-adjacent: that repo is on no STEP 5 list, and `tide-rack-bot` has `push=false` on it**, so this needs either Jeff or granted access. **Accept** a module whose panel carries NO control art — HetrickCV's `Chaos1Op` is the worked negative case from E24 — renders in TIDE with visible, grabbable knobs and jacks, shown by a before/after screenshot pair. **The before shot is the control and it already exists** in E24's evidence. **Size** medium, and it is the highest-leverage row in E20-E24: it converts four packs from "looks broken" to "usable" without touching a licence. |
---

## Release & distribution — UNBLOCKED 2026-08-21 (V1 closed 08-18, C7 closed 08-21)

The plan is [docs/distribution.md](docs/distribution.md). R1 is the only item
with lead time and the only one Jeff can do now; the rest wait for a v0.1 that
passes V1. CI automation additionally waits on C7 — the interim path (build on
each box, `gh release upload`) is in the doc.

| ID | Status | Plat | Item |
|---|---|---|---|
| R1 | RESOLVED | — | **Signing identities and accounts.** **(a) Decided 2026-08-13 by Jeff: TIDE ships under the existing `SynthEdit Limited` identity, deliberately — a second Azure certificate profile is not affordable right now.** So both platforms sign under `SynthEdit Limited`/`SynthEditCertificateProfile` as already configured; no new credential, no R7-style hardening needed for a profile that doesn't exist. Compensating branding decided the same day: **"TIDE Synth — by SynthEdit Ltd"**, placed subtly (about pane, footer, or installer credit — not the plugin name itself, constraints 1 and 5 still rule out anything dialog- or splash-shaped). Placement is unscoped — filed as **D2**. (b) **Confirmed** — Apple Developer ID is `Developer ID Application: SynthEdit Limited (36SNPLRFK3)`, and notarization runs on the app-specific-password route (`APPLE_ID` / `APPLE_ID_PASSWORD` / `APPLE_TEAM_ID`), matching `SynthEdit_cmake_mac.yml:223-244` so the recipe ports without rewriting `notarytool`. An App Store Connect API key would be scoped and revocable where the app-specific password is tied to Jeff's Apple ID and grants far more than notarization — worth switching before anything consumes it, not urgent while nothing does. (c) App Store listing for the iOS AUv3 (needed by M2, not v0.1) — **not started.** (d) **Done early, ahead of C7** — 8 Actions secrets and 4 variables now exist on the public repo: secrets `AZURE_TENANT_ID`, `AZURE_CLIENT_ID`, `AZURE_CLIENT_SECRET`, `APPLE_CERT_P12_BASE64`, `APPLE_CERT_PASSWORD`, `APPLE_ID`, `APPLE_ID_PASSWORD`, `APPLE_TEAM_ID`; variables `AZURE_CODESIGN_ENDPOINT`, `AZURE_CODESIGN_ACCOUNT`, `AZURE_CODESIGN_PROFILE`, `APPLE_SIGNING_IDENTITY`. The non-secret half is deliberately in *variables* — those values are on every binary TIDE ships, and hiding them only makes failures harder to read. Nothing consumes any of them yet. **Two follow-ups, filed as R7.** Detail on the identities, and why the non-secret half is deliberately in variables: [docs/r1-signing-identities.md](docs/r1-signing-identities.md). |
| R7 | WONTFIX | any | **THE APPLE HALF OF (2) IS DONE AFTER ALL, 2026-08-24 — Jeff created the App Store Connect key while this row was being written up, which changed the economics.** The WONTFIX below was the right call at the time: the cost was the credential work, not the code. With `APPLE_API_KEY_P8_BASE64`, `APPLE_API_KEY_ID` and `APPLE_API_ISSUER_ID` already in the `release` environment, the workflow change is one step. `notarytool submit` and `notarytool log` now take `--key/--key-id/--issuer` instead of `--apple-id/--password/--team-id`, and **`APPLE_ID`, `APPLE_ID_PASSWORD` and `APPLE_TEAM_ID` are now referenced NOWHERE in the workflow** — all three were used only by that one step, so they can be deleted from the environment. **Role: Developer is sufficient for notarisation** — Admin is not needed. **Verified by extracting the step's bash out of the YAML and running the key-decoding prologue for real**, not by reading it: a genuine `openssl`-generated P-256 `.p8`, base64'd **with a trailing newline deliberately included**, decodes to 241 bytes and reports itself; a non-base64 secret fails with `0 bytes`; valid base64 that is not a PEM key fails with `11 bytes`. Both failures name the secret and print the `gh secret set` line to fix it. **The trailing-newline case is the point** — that is exactly what bit R5 on `APPLE_CERT_PASSWORD`, so the key is stripped of whitespace before decoding. **The AZURE half stays deferred** per the ruling below; it is the one with an untestable OIDC migration. **Not verified:** an actual notarisation. That needs a real tag, and the guard exists precisely because it cannot be tested here. **PART (1) IS DONE — VERIFIED, NOT ASSUMED. PART (2) IS DEFERRED BY RULING: Jeff, 2026-08-24, *"forget it till it breaks, for now"*.** **(1) The ungated exposure this row was written about is closed.** Measured on the live repo: a `release` environment exists with **JeffMcClintock as a required reviewer**, all **8** credentials live in it, **repo-level secret count is 0**, `release.yml` declares `environment: release` and holds all 11 gated-secret references, and `build.yml` / `auto-merge.yml` reference only `GITHUB_TOKEN`. So the scenario the row describes — a workflow edit on a `tide/**` agent branch reading all 8 credentials ungated — cannot happen. **(2) Dropping the two long-lived credentials is deliberately NOT being done.** Jeff's challenge was the right one: *"we're already successfully codesigning with azure, why do anything"*. With (1) in place the remaining case is thin — an Entra client secret carries an expiry, and OIDC would issue a short-lived per-run token scoped to tags instead — and the cost is real: it changes a **working, untestable-from-here** release path, verifiable only by cutting an actual tag. **The research is recorded so nobody repeats it:** `azure/trusted-signing-action@v2` DOES support OIDC and `azure-client-secret` IS optional, but the OIDC path also wants `azure-subscription-id`, which is configured nowhere today; the job would need `id-token: write` (it has only `contents: read`); and the Apple half means swapping `notarytool --apple-id/--password/--team-id` for `--key/--key-id/--issuer` behind a new App Store Connect API key. **THE TRIGGER FOR REVISITING is a signing or notarisation failure that looks like an auth error mid-release.** Both secrets were created **2026-08-09**; the Entra expiry is visible only in the portal, and checking it is the one cheap thing that would pre-empt a surprise. **Harden the signing credentials now that they exist (follow-up to R1(d)).** Two things, neither urgent while no workflow consumes them. **(1) The exposure is same-repo branches, not fork PRs.** GitHub withholds secrets from fork-triggered `pull_request` runs, so `build.yml`'s bare `pull_request:` trigger is safe. But it also runs on `push: branches: ["main", "tide/**"]`, and same-repo runs get full secret access — and pushing `tide/**` branches is exactly what the weekly agents do. So today a workflow edit on any agent branch would execute with read access to all 8 credentials, ungated. Fix: create a `release` environment with Jeff as a required reviewer, move the secrets into it (`gh secret set <NAME> --env release`), delete the repo-level copies. Only a job declaring `environment: release` can then reach them, and it pauses for approval. **(2) Drop the two long-lived credentials.** `AZURE_CLIENT_SECRET` → OIDC federated credentials scoped to `repo:JeffMcClintock/TideSynth:ref:refs/tags/*`, which removes the stored secret entirely *and* means even an approved non-tag run cannot sign; `azure/trusted-signing-action@v0` takes the same inputs as the `ArtifactSigning@1` task either way. `APPLE_ID_PASSWORD` → App Store Connect API key (`.p8` base64 + key id + issuer id). Do this before **R5** wires anything up, or R5 will have to be rewritten. |
**The publisher name TIDE ships under is SynthEdit Limited — decided, not
inherited, as of R1(a) 2026-08-13.** `APPLE_SIGNING_IDENTITY` is
`Developer ID Application: SynthEdit Limited (36SNPLRFK3)`, and
`SynthEditCertificateProfile` almost certainly carries the same subject on
Windows. So a user installing TIDE sees *SynthEdit Limited* in the macOS
Gatekeeper prompt and the Windows UAC dialog — deliberately: a second Azure
certificate profile is not affordable right now, and PR #14's website
stripped every SynthEdit mention so TIDE stands on its own there, which is
why a compensating credit was filed as **D2** — "TIDE Synth — by SynthEdit
Ltd", placed on TIDE's own terms rather than only surfacing in the OS
install dialog.

---

## Done

Landed rows live in **[BACKLOG-DONE.md](BACKLOG-DONE.md)** — moved there by A8,
2026-08-12. Look there for precedent: how a past stage was verified, what a
previous run measured.

**As part of STEP 4:** an `IN-REVIEW` row whose PRs have all merged becomes
`DONE` and moves to that file, with the merge date in the `Done` column. Move
the row verbatim; do not rewrite it on the way out.
