# TIDE Synth — Backlog

Ordered. A weekly agent obeys the **NEXT block below first**; only when its
platform's NEXT item is ineligible does it fall back to the topmost item that
is not blocked and matches its machine's platform role. One item, then stop.

## NEXT — per-platform priority, obeyed before file order

| Platform | Take | Why |
|---|---|---|
| win | C5, if C4's three PRs have merged; otherwise **E1a** | C4 went IN-REVIEW 2026-08-13 ([SynthEdit#15](https://github.com/JeffMcClintock/SynthEdit/pull/15), [SynthEditLib#6](https://github.com/JeffMcClintock/SynthEditLib/pull/6), [#49](https://github.com/JeffMcClintock/TideSynth/pull/49)), which unblocks C5 — the carve-out's critical path, and a `win` row — the moment they land. **C5 is cheaper than its row suggests now:** C9's mechanism shipped with C4, so `Application.cpp` needs one `#include` swap and one macro rename, not new machinery. **If they have not merged, C5 is still BLOCKED(C4) and the answer is E1a** — its own row says it belongs in the mac or win NEXT and no linux run can ever satisfy it, and the 2026-08-13 linux run recommended exactly this move without being able to make it. The render half is a download and two numbers; the judgement half is setting the RMS gate from them |
| mac | E1a | P6 closed 2026-08-14 ([#50](https://github.com/JeffMcClintock/TideSynth/pull/50)) — confirmed fixed on the Xcode generator with an A/B positive control, so it is no longer eligible. **E1a is mac-or-win work by construction and this is the box taking it**: the P7c run corrected the linux pointer on 2026-08-13 because E1a's Accept clause is *"one render of both cases on a second platform"* — second to linux, the only lane that has ever rendered — so no linux run can ever satisfy it. The render half is cheap here (the engine is a download, not a build); the judgement half is setting the RMS gate from the two numbers it produces. The only other `mac` rows are S9 and S10, and both still need Jeff (GATED pbxproj / a revive-or-retire decision). If E1a turns out blocked, fall back to **A13** — small, on `scripts/` so a scheduled run may edit it, and it gates A4's usefulness as a merge check |
| linux | P7c | **E1a was this row's pointer and it is not a linux item — corrected 2026-08-13.** E1a's Accept clause is "one render of both cases **on a second platform**", i.e. mac or win; the linux box is the platform the harness has *already* run on, so no linux run can satisfy it. The old wording here ("this is the box that can actually test cross-platform") had it exactly backwards. **E1a now belongs in the mac or win NEXT row, not this one** — see the E1a row. P7c is the topmost genuinely-eligible linux item, on an ALLOWED path (`gmpi_ui`), and it is the X11 half of P7 that the row always said a Linux run could take alone |
| any | **Not A4 — a scheduled run cannot do it.** A10 and A12 are the same wall; A9 has an open NEEDS-JEFF prerequisite. **A13 was the topmost `any` item a scheduled run can actually complete, and the 2026-08-14 win run took it** | A4 is an *auto-merge action*, i.e. a file under `.github/workflows/**`, and the bot token is deliberately `repo` scope with **no `workflow`** — measured 2026-08-14, `X-OAuth-Scopes: repo`. So the push is rejected no matter how correct the work is; this is the same wall A12's row, A3/A5/A6 and C9(a) all name, and A4's row never said so. **A4 is really NEEDS-JEFF-or-interactive, not TODO** — a one-cell change that is Jeff's to make. Whoever re-points this row after A13: the next `any` candidates all need the same judgement applied before they are claimed |

Status: `TODO` · `DOING` · `IN-REVIEW` · `DONE` · `BLOCKED` / `BLOCKED(<id>)` ·
`NEEDS-JEFF` · `WONTFIX`

- `IN-REVIEW` = work complete, PRs open (linked in the row). A run never sets
  `DONE` on its own fresh work; `DONE` is set when every linked PR has merged,
  by Jeff or by a later run that observes it.
- `BLOCKED(<id>)` = blocked until `<id>` is DONE. **Eligibility lives in this
  column alone** — section prose never overrides it.
- `NEEDS-JEFF` rows should carry a `Default in effect:` and a `Decide-by:` line
  (template in [docs/decisions.md](docs/decisions.md)), so an unanswered
  question cannot silently become the answer.
- `WONTFIX` = considered and declined, with the reasoning in the row. Do not
  re-file it.

Platform: `any` · `win` · `mac` · `linux`

New items state three things, tersely: **Scope** (files/dirs), **Accept** (a
command or observable that proves it done), **Size** (fits one session, or
names its stages). Anything longer than a few lines lives in `docs/<id>.md`
with the row pointing at it. An agent that cannot state an item's acceptance
check before starting must skip it and mark it `NEEDS-SPEC` rather than guess.

Update this file at the end of every run. An item left in `DOING` with no
matching JOURNAL entry means a previous run died — reset it to `TODO` and note
that in the journal (claims younger than 24h are presumed live; see the run
prompt).

---

## Carve-out — APPROVED 2026-08-08, C0 cleared

Stages are sequential. Each must leave SynthEdit, SynthEditCL and TIDE all
building before the next starts. See [docs/carve-out.md](docs/carve-out.md).

| ID | Status | Plat | Item |
|---|---|---|---|
| C4 | IN-REVIEW | win | **Done 2026-08-13 (windows) — twelve files moved, C9 implemented, everything rebuilt. PRs: [SynthEdit#15](https://github.com/JeffMcClintock/SynthEdit/pull/15) (deletions + `EditorLib/CMakeLists.txt` + vcxproj), [SynthEditLib#6](https://github.com/JeffMcClintock/SynthEditLib/pull/6) (the twelve files + `se_version.h`), [#49](https://github.com/JeffMcClintock/TideSynth/pull/49) (docs). All three must merge together.** **C3's two checks, run first.** Build files: `EditorLib/CMakeLists.txt` is the only build file that compiles any of the twelve; `SynthEdit2.vcxproj`/`.filters` carry three navigator-only `ClInclude` entries (repointed to the `..\..\SynthEditLib\` form C3 established); `SynthEditCL`/`SynthEditWayland` mention `SkinMgr` in comments only; **all 14 `project.pbxproj` in the repo: zero hits**, so the mac side needed no edit. Relative includes: two of the twelve use `#include "../"`, and only one resolves for real — `SE16/tinyxml2/` **does not exist**, so `ThemeManager.cpp`'s `"../tinyxml2/tinyxml2.h"` already resolves through a search path (`SynthEditLib/modules/se_sdk3_hosting/../tinyXml2/`) and a move cannot affect it; `SE16/se_build_number.h` **does** exist, which is C9. **C9 implemented as ruled (option (c)):** new `SynthEditLib/se_version.h` defaults `SE_APP_BUILD_NUMBER` to 0, `EditorLib/CMakeLists.txt` injects SynthEdit's real value by reading `se_build_number.h` where the three release workflows still grep for it — **no workflow file touched**. Injected rather than owned by the library on purpose: both uses are cache invalidation that tracks the *application* (`SkinMgr` copies skins out of the application's own `Resources` folder), so a library-owned constant would stop SynthEdit invalidating on upgrade — a behaviour regression dressed as a decoupling. **Verification artifact:** fresh scratch Ninja tree **905/905 RC=0**, five products; the six moved TUs verified compiled from `EditorLib.dir\C_\SE\SynthEditLib\`; `dsp_tests` 58/58, `synth_ui_tests` 24/24, `ui_tests` 10/10, all RC=0; **SynthEdit2 (WinUI3) MSBuild Release x64 RC=0** — the first carve-out stage to actually reach that link step, which C1b and C2 both had to leave open behind P8. C9 proven by probe: `se_build_number.h` says 183, `build.ninja` carries `SE_APP_BUILD_NUMBER=183`, a probe TU prints **0** with no injection and **183** with it, and the define appears on **57 build statements, all EditorLib** (PRIVATE holds). No line-ending churn: `core.autocrlf=false` in both repos, all twelve files `cmp`-identical to their originals. **Measured cost, filed as C11:** C4 closes 11 dangling private includes in the public repo and **opens 20** — net 47 → 56, measured from git refs on both sides. 18 of the 20 are headers a later stage already moves; **2 are not**, both in `MfcDocPresenter.cpp`. **Toolchain note:** `MSBuild SynthEditStore.sln` now needs the **VS 2026 (v145)** toolset — under VS 2022 it dies at `build\ZERO_CHECK.vcxproj` with `MSB8020` before reaching `SynthEdit2`, so P8's journal command no longer works as written on this box. Original item follows. Move views and browsers (`ModuleBrowser`, `PropertiesBrowser`, `MfcDocPresenter`, `ModuleFactory_Editor`, `SkinMgr`, `ThemeManager`). **Read C9 first — `ModuleFactory_Editor.cpp` and `SkinMgr.cpp` both use `se_build_number.h` live, and it does not survive the move.** Also run C3's two checks before moving anything: grep every build file for each candidate (C3 found `SynthEditDoc2` compiled by `SynthEdit2.vcxproj` as well as by EditorLib), and grep the candidates for `#include "../`, testing whether each target actually exists at that relative path — most resolve through the search path and are harmless, and the one that resolves for real is the one that breaks. |
| C5 | BLOCKED(C4) | win | Move the app base (`SynthEditAppBase`, `ApplySynthEditConfig`, `SynthRuntime_editor`, `UIoManager`, `IO_base`, `IO_None`). **`Application.cpp` uses `se_build_number.h` live — see C9.** |
| C6 | BLOCKED(C5) | any | Move `EditorLib/CMakeLists.txt` itself into `SynthEditLib`. |
| C7 | BLOCKED(C6) | any | Repoint TIDE at public `SynthEditLib` only. Prove it with a clean-clone CI build that has no access to the private repo. **Before claiming a stranger can build TIDE, grep `SynthEditLib` for `#include`s of headers that live in `SE16/SynthEdit2/`.** C2 found one — `SynthEditLib/modules/se_sdk3_hosting/ModuleViewStruct.cpp:11` included `"cpu_accumulator.h"` while that header was still private, and it compiled only because that `.cpp` is on *EditorLib's* source list and so gets `../SynthEdit2` on its include path. A public file that its own repo cannot compile. C2 closed that one by accident; each remaining one is invisible until a clean-clone build. |
| C9 | TODO | any | **`se_build_number.h` blocks C4 and C5 — a private-repo header that public files include.** Found during C3, 2026-08-13. `SE16/se_build_number.h` holds SynthEdit's product version (`SE_MAJOR_VERSION`/`SE_MINOR_VERSION`/`SE_BUILD_NUMBER`), lives at the **private** repo's root, is bumped by the `[Build-Machine]` on every release, and is greped for the version string by three release workflows (`SynthEdit_cmake_mac.yml:153`, `SynthEdit_cmake_win.yml:206`, `SynthEdit_store_win.yml:266`). Five `SynthEdit2/` files include it as `"../se_build_number.h"` — and unlike this stage's other relative includes it **genuinely resolves relative to the file**, so it does not survive a move to `SynthEditLib`. C3 escaped only by luck: its one includer, `SynthEditDocBase.cpp`, never used the macros, so the fix was deleting a dead line. **The other four are live uses and cannot be deleted:** `ModuleFactory_Editor.cpp` (2) and `SkinMgr.cpp` (2) are **C4**; `Application.cpp` (1) is **C5**; `ExportAsPlugin.cpp` (1) stays private and is fine. **Options, none free:** (a) move the header into `SynthEditLib` — but the three workflows read it at the SE16 path, and `.github/workflows/**` is off-limits to a scheduled run (the bot token deliberately lacks `workflow` scope), so this needs Jeff; (b) add SE16's root to the include path — leaks a private path into the public repo and **fails at C7**, whose whole test is a clean clone with no access to SE16; (c) decouple — give `SynthEditLib` its own version header, or pass the version in as a compile definition, leaving SynthEdit's build-number file where its workflows expect it. ~~**(c) looks right and is the one that needs a decision, not just work.** Settle it before C4 starts, or C4 will settle it by accident.~~ **Resolved 2026-08-13 by Jeff: option (c).** `SynthEditLib` gets its own version header (or a compile definition), decoupled from `se_build_number.h`, which stays exactly where SynthEdit's three release workflows expect it. C4 implements it for `ModuleFactory_Editor.cpp` and `SkinMgr.cpp`, C5 for `Application.cpp`. Ruling and reasoning: [docs/decisions.md](docs/decisions.md). **C4 half implemented 2026-08-13** ([SynthEditLib#6](https://github.com/JeffMcClintock/SynthEditLib/pull/6), [SynthEdit#15](https://github.com/JeffMcClintock/SynthEdit/pull/15)): the mechanism now exists and is proven — `SynthEditLib/se_version.h` defaults `SE_APP_BUILD_NUMBER` to 0, and `EditorLib/CMakeLists.txt` injects SynthEdit's value with a `file(READ)` + `string(REGEX MATCH)` on `se_build_number.h`, `PRIVATE` to EditorLib. **C5 reuses it verbatim for `Application.cpp` — one `#include` swap and one macro rename, no new machinery.** This row stays TODO until that lands. **One thing C6 must not miss:** the injection block reads a private-repo path, so it cannot travel with `EditorLib/CMakeLists.txt` when C6 moves that file — a comment in the block says so, and the injection then belongs in each SynthEdit application's own build. |
| C10 | BLOCKED(C6) | any | **Re-home the moved editor files from `SynthEditLib`'s root into a subfolder.** C2 chose root for 16 files; C3 deliberately kept root for 27 more (reasoning in [SynthEditLib#5](https://github.com/JeffMcClintock/SynthEditLib/pull/5)) — root is already an include dir in all three build systems, so both moves cost **zero** include-path edits, which is what keeps a large move's breakage unambiguous. That trade gets worse as C4/C5 land and ~120 editor files sit beside the DSP engine in one flat root. **Blocked on C6 on purpose:** once `EditorLib/CMakeLists.txt` lives in `SynthEditLib`, one `target_include_directories` line there covers every CMake consumer at once, instead of the same edit being smeared across C3/C4/C5 and several build systems — including the macOS/iOS ones no Windows run can verify. The move itself is a `git mv`. Non-CMake consumers still need checking by hand: `SynthEdit2.vcxproj` and the SynthEditMac Xcode project. |
| C11 | TODO | any | **C4 published two headers no carve-out stage moves, and one of them is a licence gate.** Found 2026-08-13 by the C4 run, which measured it rather than assuming: C4 closed 11 dangling private includes in `SynthEditLib` and opened 20 (net 47 → 56, both sides read from git refs). 18 of the 20 name headers already on `EditorLib/CMakeLists.txt`'s source list, so C5 and later stages close them by construction. **Two are on no stage's list, both reached from the now-public `MfcDocPresenter.cpp`:** (a) **`SynthEditApp.h`** — deliberately excluded from EditorLib so each app chooses its own `SE_MOONBASE_SUPPORT` without ODR conflicts, per that file's own comment. `MfcDocPresenter.cpp:1258-1261` declares `extern SynthEditApp* theApp` and calls `theApp->isMoonbaseEnabled()` / `licenseIsActive()` to gray out "Selection to Prefab", so **a licence-gate call site now sits in the public repo.** No Moonbase implementation is published — two method names and the existence of the gate are. (b) **`ModulePicker.h`** — 19 KB, header-only, no `.cpp`, on no stage's list; `MfcDocPresenter.cpp:536` does `return new ModulePicker(pparent)`. **Why this is C7's problem and not C4's:** C7's whole test is a clean clone with no access to SE16, and `MfcDocPresenter.cpp` cannot compile there while either include stands — the same shape as the `cpu_accumulator.h` case C2 closed by accident, except these two are visible in advance. **Options, and (a) and (b) probably want different ones:** for `ModulePicker.h`, add it to a stage's list (it is UI, not commercial, and has no `.cpp` to complicate the move); for `SynthEditApp.h`, either narrow the dependency to an interface the public side can own — the call is two bools behind a pointer, so a small `ILicenseState`-shaped hook would do it — or accept a `#ifdef`. **This is a boundary decision, not just work, and it is Jeff's**: it is the first time the carve-out has put licensing-adjacent code on the public side, and the standing direction is to keep commercial code private as practical. **Scope** `SynthEditLib/MfcDocPresenter.cpp` plus whichever stage list grows. **Accept** the dangling-private-include count for `MfcDocPresenter.cpp` reaches zero, and the file compiles with only `SynthEditLib` on the include path. **Size** minutes once (a) is ruled; the measurement script is in the C4 journal entry. |

---

## Process hardening — from the process review, 2026-08-09

The findings, red-team verdicts, and the reasoning behind the ordering are in
[docs/process-review-2026-08-09.md](docs/process-review-2026-08-09.md). The
sequence is the decision: A1 gates everything, and cadence (A7) comes last.
Rejected recommendations are listed in that doc — do not re-file them.

| ID | Status | Plat | Item |
|---|---|---|---|
| A4 | TODO | any | **Unblocked 2026-08-13 — A3 landed and is proven live (PR #41).** **Auto-merge tier for coordination PRs.** A path-allowlisted action auto-merges PRs touching only `JOURNAL.md`, `BACKLOG.md`, and `docs/**` — excluding `docs/weekly-run-prompt.md` and `PLAN.md` (they steer the fleet) and never `website/**` (a merge there IS a production deploy, see the pages.yml audit) — once the A3 lints pass. Also requires A1 done (secrets out of casual reach). Fixes the memory-propagation latency (Sat/Sun boxes currently see Friday's learnings only if Jeff merged in time) and stops training the rubber-stamp reflex on trivia. Human merge remains for the prompt, PLAN, workflows, website, and all code repos. |
| A7 | BLOCKED(A4) | — | **A6 and A1 both landed 2026-08-13 — the remaining blocker is A4 alone, unblocked but not yet built.** **Raise cadence to 2 staggered runs per box per week.** Also requires A1 and A4 (the red team's ordering: cadence multiplies journal growth, PR volume, and the secret-exposure window linearly, so the drains come first — this row's blocker chain encodes A6, and A1/A4 are NEEDS-JEFF/BLOCKED upstream of it). The stagger is no longer load-bearing — the pushed-DOING claim protocol is what prevents collisions — so weekly is a historical artifact. The task-cron edits are per-box manual steps (Jeff or an interactive session on each box). Start at 2×, review after a month against the A6 digests. |
| A10 | TODO | any | **The bare-ID-cross-reference half of A3, deliberately deferred — 2026-08-13.** A3 shipped link checking (`scripts/check-links.py`) but not this: verifying a bare prose mention of a backlog/journal ID (`see C9`, `blocked on A6`) actually names a row that exists, rather than a stale reference to something renamed, deleted, or typo'd. **Why it's harder than it looks:** short alphanumeric tokens are everywhere in prose that are not IDs — `V1` (a version), `x64`, `P7a` inside a sentence about `checkSizeConstraint(0,0,2178,32672)`, build numbers, CMake variables — so a naive `\b[A-Z]\d+[a-z]?\b` regex will false-positive constantly, and a noisy lint erodes trust in the other three checks fast. Needs a real design pass: likely restrict matching to a fixed, enumerated set of real IDs pulled from BACKLOG.md/BACKLOG-DONE.md's own ID column (so it can never invent a false ID), then only flag a match that looks like a genuine reference (bold `**C9**`, or specific phrasing like "blocked on", "see", "filed as") rather than every substring hit. Scope: `.github/workflows/lint.yml` (explicitly authorized, same file A3 touched) plus a new `scripts/check-id-refs.py`. Accept: same as A3's own bar — passes on the current clean tree, fails on a deliberately introduced stale reference, with zero false positives against the tree as it stands today (that last part is the real test — run it once, read every flagged line, confirm each is real). Size: one session, but budget it as a design session first, not just an implementation one. |
| A9 | TODO | any | **Unblocked 2026-08-13 — A6 landed.** **Community research routine — PROPOSED-only output.** Design in the review doc. Sources with structured access: VCV Community Discourse JSON, the VCV Library manifest repo, Cardinal + Surge XT issue trackers; Audiobus/Loopy Pro forums are a quarterly human skim (iOS AUv3 audience lives there, no API); Reddit/KVR demoted (ToS-hostile to bulk fetch). Guardrails are hard rules: read-only, courtesy rates, never post/vote/DM, dedup against existing backlog AND a rejection memory, provenance links on every item. Output: PROPOSED rows for Jeff's triage — the routine never decides product direction. Prerequisite worth doing first (NEEDS-JEFF, five minutes): write TIDE's product philosophy in 2–3 sentences as the auto-reject filter, Cardinal-style. Standing hypothesis to track: **no open-source modular exists on iOS AUv3** — Cardinal ships every format but iOS, miRack is closed-source; watch for anyone moving into the gap. |
| A11 | DONE | any | **All three boxes confirmed 2026-08-13 — linux, win, then mac. Ready to rotate into BACKLOG-DONE.md.** **SSH remotes bypass A2 entirely, and every check A2 was closed on is blind to it.** Found 2026-08-13 on the linux box, interactive session. A credential helper keyed to `credential.https://github.com.helper` is **never consulted for a `git@github.com:` URL**, so on any repo with an SSH remote a run authenticates with Jeff's key and pushes through his admin bypass. **Both existing guards pass anyway:** STEP 0.7's `gh api user` answers `tide-rack-bot` because `gh`'s API path reads `GH_TOKEN` and never touches git's transport; and the PR-authorship check A2 was flipped DONE on reads `tide-rack-bot` because author/committer come from the four `GIT_*` exports, which are set unconditionally. **Authorship proves authorship, not authentication** — correct `git log`, correct assertion, ruleset bypassed. On linux **8 of 9 repos were SSH** (`SynthEditLib`, `SE16`, `gmpi_ui`, `GMPI_Wrappers`, `GMPI`, `GMPI_Adaptors`, `GMPI-plugins`, `gimpi_ui_tests`); only `TideSynth` was HTTPS, which is the sole reason linux runs looked clean — the one repo they touched was the one repo covered. **Scope:** one `git config` line per box, plus [docs/a2-actor-separation.md](docs/a2-actor-separation.md) and STEP 0.7 in [docs/weekly-run-prompt.md](docs/weekly-run-prompt.md) (both already corrected — setup step 3 and a second assertion). **linux: DONE 2026-08-13. win: DONE 2026-08-13, interactive session with Jeff.** All 22 local repos on the Windows box (not just the 5 in the fleet's usual scope — the full `find C:\SE -name .git` sweep) already used HTTPS; nothing there was actually exposed. Applied the global rewrite anyway, since the acceptance test wants it as a structural safeguard against a future SSH clone, not just a fix for today's state. Full three-way proof run against the private `SynthEdit` over HTTPS: bogus `GH_TOKEN` → `fatal: Authentication failed` (exit 128, proves `gh` is genuinely consulted); real bot token → succeeds, `gh api user` confirms `tide-rack-bot`; no token → succeeds, confirms `JeffMcClintock`. **mac: DONE 2026-08-13** — swept, then fixed by Jeff mid-session and re-verified. The sweep ran (`find ~ -maxdepth 5 -name .git`, **28 repos**): **zero SSH GitHub remotes** — all nine fleet repos and the eight `VST_SDK` submodules are `https://`, same picture as Windows, not linux. **So the macOS A2 evidence is NOT void after all** — `gmpi_ui#3`/`#4` and `GMPI_Wrappers#1`/`#2` were pushed from HTTPS remotes and did authenticate as the bot. The read-only three-way proof also passes here in full (bogus token → `fatal: Authentication failed`; real token → succeeds as `tide-rack-bot`; no token → succeeds as `JeffMcClintock`), so setup steps 1–2 were already correct and only step 3 was missing. Jeff applied it mid-session; re-verified after: assertion 2 prints `git@github.com:`, all nine fleet repos still `https://`, an explicit `git@github.com:…` URL now **rewrites** to `https://…` (the safeguard proven working, not merely absent), and the three-way proof re-run unchanged. **The run halted first, correctly** — STEP 0.7 fires before STEP 1/2, so a scheduled mac run detects this gap but is forbidden from fixing it; it was cleared only because a human was at the keyboard, which an unattended 03:00 run would not have. That residual risk is **A12**, not this row. Evidence and full sweep: JOURNAL 2026-08-13 macos part 1. **Accept:** on each box, `git config --global --get url."https://github.com/".insteadOf` prints `git@github.com:`, and `git -C <repo> ls-remote --get-url origin` starts with `https://` for all nine repos. Prove the wiring read-only, no push needed: against the private `SynthEdit` over HTTPS a bogus `GH_TOKEN` must **fail** auth (that failure is the proof git consults `gh` at all rather than the SSH key or keyring), the real token must succeed, and no token must succeed as Jeff. **Size:** minutes per box; each is its own short session. **Flip DONE only when all three are confirmed.** **Consequence for Jeff, not for runs:** his interactive pushes now resolve through `gh`'s keyring token, which has no `workflow` scope, so a commit touching `.github/workflows/**` is rejected until he runs `gh auth refresh -h github.com -s workflow` once — `SE16` holds nine workflow files and is where it bites. Same scope wall C9(a) already hit from the other side. **Did not bite on Windows** — checked before applying: `gh auth status` there already shows `workflow` in the token scopes, so nothing changed for Jeff's own `.github/workflows/**` edits on that box. **A global rewrite, not per-repo `remote set-url`:** a fresh clone defaults back to SSH and would re-open the hole with no signal. |
| A12 | TODO | any | **A halted box is invisible to the fleet, and nothing escalates it.** Filed 2026-08-13 from A11's mac half. STEP 0.7's assertions can put a box into a state that **only an interactive session can clear** — the check that detects the gap also forbids fixing it (*"do not 'fix' it by carrying on as whoever you are"*), and STEP 0.7 fires before STEP 1/2 so the run can never claim the item that would repair it. On 2026-08-13 the mac box was in exactly that state; it was cleared within minutes only because Jeff happened to be at the keyboard. **Unattended, it would have burned a run per week indefinitely, each producing an identical journal entry that nobody is prompted to read.** The same shape applies to any STEP 0.7 failure (missing/expired token file, revoked credential) — note the bot token **expires 2026-11-07**, which will halt all three boxes at once and is the obvious first real instance. **Scope:** A6's fleet watchdog digest is the natural place — it already runs and already reports; it should flag a box that halted or has not completed a run in N days, distinct from a box that ran and did nothing. Likely also worth a line in [docs/weekly-run-prompt.md](docs/weekly-run-prompt.md) telling a halted run to label its PR so the digest can find it cheaply. **Accept:** a box halted at STEP 0.7 appears in the next digest as halted, named, with the failing assertion — demonstrated by a deliberately induced halt (e.g. a temporarily bogus token path on one box), not by reasoning. **Size:** one session. **Note the digest workflow lives in `.github/workflows/`, so a scheduled run cannot edit it** — same wall as A3/A5/A6 and C9(a). Needs Jeff or an interactive session. |
| A13 | IN-REVIEW | any | **Done 2026-08-14 (windows) — fixed, and a second defect in the same function found and fixed with it. PR: [#51](https://github.com/JeffMcClintock/TideSynth/pull/51).** **Verification artifact — A/B against GitHub's own renderer, not against a belief about the algorithm:** the contents API with `Accept: application/vnd.github.html` returns each file rendered by GitHub's real markdown pipeline with its anchors intact, and **254 of 254 headings across all 28 markdown files now match, zero mismatches**. Two negative controls prove the A/B discriminates rather than passing vacuously: the old slugger gets **111 headings wrong across 23 files**, and not skipping fenced code gets **34 wrong across 4**. *(Method note for whoever repeats this: the `/markdown` API is **not** an oracle — it renders headings with no `id` at all. Use the contents endpoint.)* **The second defect:** `anchors_of()` never tracked code fences, so nine fenced lines were registered as real anchors — including `JOURNAL.md`'s own template heading `## YYYY-MM-DD — <machine> — <BACKLOG id>` and `#include "it_empty.h"` in `docs/c8-it-empty-header.md`. That is a false *negative*: a link to `#include-it_emptyh` passed. Same both-directions character as the slug bug, same function, so fixed in the same pass. **Also added `--selftest`** — six golden slugs read off GitHub's renderer plus a duplicate/fence case, offline and baked in, so the regression is permanent rather than a thing someone has to remember to re-measure; confirmed discriminating (5 of 6 cases fail when slugify is reverted to the old algorithm, RC=1). Deliberate-break demonstrations all pass: a nonexistent anchor fails (RC=1), a link in the old collapsed form now correctly fails (RC=1) — that is the false negative closing — and the correct GitHub form passes (RC=0). Tree is clean: 186 links, 0 broken, and the single previously-flagged line (`JOURNAL.md:376`) was read and confirmed to be exactly the false positive this row predicted. Original row follows. **`check-links.py`'s slugger disagrees with GitHub's on every em-dash heading — so the link check is red on `main` today, and is wrong in both directions.** Found 2026-08-13 by the P7c run, which hit it as a pre-existing failure and did not cause it. `scripts/check-links.py:44` does `re.sub(r'\s+', '-', s)`, collapsing a **run** of whitespace to ONE hyphen; GitHub emits one hyphen **per space**. Line 43 has already deleted the em-dash, so `## 2026-08-13 — macos — S6 (part 2 of 2)` leaves two spaces where the dash was and the script produces `2026-08-13-macos-s6-part-2-of-2` while GitHub produces `2026-08-13--macos--s6-part-2-of-2`. **Every journal entry heading and most doc headings in this repo use em-dashes**, so this hits all of them. **It is wrong in both directions, which is the part that matters:** a correct link is reported BROKEN (false positive — [JOURNAL.md](JOURNAL.md)'s S6 part-2 cross-reference, the first intra-journal anchor anyone wrote, which is why A3 could honestly claim zero false positives when it landed), and a link written in the script's own single-hyphen form would PASS the check and be **broken on GitHub** (false negative). A3's row is explicit that a noisy lint erodes trust in the other three checks fast, so this is worth fixing before A4 makes any of them a merge gate. **Scope** `scripts/check-links.py` — no workflow edit needed, the script is called by an existing step, so a scheduled run **can** do this one. **Accept** the checker reproduces GitHub's slug for a heading with em-dashes, parentheses and digits; the existing S6 link passes; a deliberately wrong anchor still fails; and the whole tree comes back clean (read every previously-flagged line and confirm each was a false positive, per A10's standard). Worth checking the same line against GitHub's other rules while in there — consecutive spaces, trailing punctuation, and duplicate headings, which get a `-1` suffix. **Size** well under one session. |

---

## Ready now — no dependency on the above

| ID | Status | Plat | Item |
|---|---|---|---|
| N1 | TODO | any | **Carry the TIDE Rack rename through the tree** — PLAN naming ruling, 2026-08-08, reaffirmed 2026-08-09. Product is **TIDE Rack**, **TIDE Synth is the organisation**; domain and repo name stay. **The prose half is DONE** (website 2026-08-08, `README.md` 2026-08-09); what remains is the build-system half, which needs decisions rather than edits. **Do it after C7, not during** — C2–C7 touch the same build files and nothing has shipped under either name, so a rename now only doubles the conflict surface. Forms are settled and must never be mixed in one filename: display `TIDE Rack`, shipped files `TIDE-Rack`, CMake targets `TIDE_Rack`. **Do not start with a global search-and-replace** — "TIDE" is a product name, an org name, a CMake target, a fixed-width 4-char vendor code and a filename prefix, and they do not all move together. Depends on **P5** or supersedes it. Reasoning, the per-surface cost and the open sub-questions: [docs/n1-tide-rack-rename.md](docs/n1-tide-rack-rename.md). |
| P7d | TODO | any | **`GMPI-plugins` cannot link a GUI plugin on macOS.** `Undefined symbols … "_OBJC_CLASS_$_UTType", referenced from: … DrawingFrameMac.mm.o`. `gmpi_ui/backends/MacFileDialog.h:8` imports `<UniformTypeIdentifiers/UniformTypeIdentifiers.h>` and uses `UTType`; `GMPI-plugins`' link line never adds the framework. `SynthEdit/EditorLib/CMakeLists.txt:166` does add it, which is why SynthEdit's own macOS build is fine and this stayed invisible. Pre-existing — nothing in P7 caused it. **Not fixed, on purpose:** `GMPI-plugins` is on neither the ALLOWED nor the GATED list, so it is GATED by default and this is the scope question, named per that rule. The P7 audit worked around it at configure time with `-DCMAKE_MODULE_LINKER_FLAGS="-framework UniformTypeIdentifiers"`, touching no file. **The scope question for Jeff:** the requirement arguably belongs in `gmpi_ui`, declaring the framework it actually needs, rather than in every consumer's link line — that would fix `GMPI-plugins` and any other consumer at once, and it is an ALLOWED path. Accept: `ninja GainGui_VST3` links on macOS with no extra `-D` flags. Size: minutes once the scope is ruled. |
| P8 | DONE-PENDING-CI | win | **Fixed 2026-08-09 on the Windows box, at Jeff's direction, straight to `SE16` master (`4baddfbb4`) — see [docs/decisions.md](docs/decisions.md).** One line at `SynthEdit2/EditorWindowHelper.cpp:294`: `currentContainerView.universalFactory` → `.get()`. `UniversalFactory` derives from `gmpi::api::IUnknown` (`SynthEditLib/Shared/DrawingFrame2_cpu.h:19`) and `renderContainerThumbnail` **borrows** rather than takes the factory, so `.get()` matches what the other three callers already pass and what `DrawingFrame2_win.h:270` does when it hands the same factory out. **Verified:** `MSBuild SynthEditStore.sln -t:SynthEdit2 -p:Configuration=Release -p:Platform=x64` now links, producing `x64\Release\SynthEdit2\SynthEdit2.exe`. **This was bigger than "does not compile from clean" — it was failing the SynthEdit Store release pipeline.** Actions run [31297103293](https://github.com/JeffMcClintock/SynthEdit/actions/runs/31297103293) died on `master` at "Build SynthEditStore (Release x64)" on exactly this error, skipping changelog, MSIX signing, bootstrapper and FTP upload — so nothing shipped. **Left as DONE-PENDING-CI rather than DONE:** the local Release link is proof the error is gone, but the CI job goes on to sign and upload, and nobody has watched a green run yet. Whoever sees one first should flip this to DONE. **Also unblocks the link-stage verification C1b and C2 each had to leave open** — both could compile their new TUs but neither could reach SynthEdit2's link. A Debug build of the same project failed on an unrelated `x64\Debug\pch.pch: Invalid argument` intermediate-file error; not investigated, and not this bug. Original finding below. |
| ~~P8~~ | *(was)* | win | **`SynthEdit2` (the WinUI3 app) does not compile from clean.** Not TIDE, and pre-existing — found during C1b and **A/B-confirmed unrelated** (fails identically with the C1b vcxproj change reverted): `SynthEdit2/EditorWindowHelper.cpp(294)` passes `std::unique_ptr<UniversalFactory>` where `se_cl::renderContainerThumbnail` (`EditorScreenshot/ContainerThumbnail.h:35`) now takes `gmpi::api::IUnknown*` — the thumbnail API moved and the WinUI3 caller never caught up. Matters to TIDE because "leave SynthEdit building" is the standing rule and **C1b's link-stage verification of SynthEdit2 is blocked behind it** (the new `ExportAsPlugin.cpp` ClCompile entry compiles clean; the app cannot reach its link step). Same shape as P6 on macOS: SynthEdit's own bug, in SynthEdit's own files — check with Jeff before touching. |
| S1b | TODO | any | **Compile the module scan out — stages (b) and (c); (a) is done.** ~~Blocked on C0~~ — unblocked 2026-08-08. **(a) DONE:** TIDE no longer compiles `SynthEdit2/SynthEditApp.cpp`, which had been putting a second `LoadOrScanModuleData()` call site, a filesystem-watcher thread and the whole licensing surface into the shipping plugin (`SE16` `40b6008ee`). **(b) and (c):** `ScanFolder`, `LoadModuleData`, `Module_Info3` and an imported `dlopen`/`dlsym`/`dlclose` are still in the Release binary. Needs a new `TIDE_NO_EXTERNAL_MODULES` through `EditorLib/CMakeLists.txt` and a real split of `ModuleFactory_Editor.cpp`. **Do not attempt this with an existing flag** — `SE_EXTERNAL_SEM_SUPPORT` guards two lines and removes none of it (measured), and `GMPI_IS_PLATFORM_JUCE` is a trap. **Recommend riding along with C4:** these are the same files C3 and C4 move, so standalone means doing it twice. Accept: constraint 7 verifiable from the Release binary — none of those symbols present. Measurements: addendum B1–B5 of [docs/module-enumeration.md](docs/module-enumeration.md); row history in [docs/s1b-compile-the-scan-out.md](docs/s1b-compile-the-scan-out.md). |
| X4 | WONTFIX | any | **Closed 2026-08-08 by Jeff: leave the `GIT_TAG origin/main` pins alone. Do not re-file this.** `FetchContent` is not how the SynthEdit-family dependencies are meant to be developed against — they change daily and the intended dev path is the `*_FOLDER_OVERRIDE` variables, which are the normal path on a dev box, not an optimisation. CI clones fresh, so `origin/main` there is real. **The thing to watch for instead is a *missing* override** — that asymmetry, not the pin, is what caused X3. **CMake already announces it:** every dependency prints `Using local <X> folder` or `Fetching <X> from github` at configure time. An unexpected "Fetching" on a SynthEdit-family repo means you are building against whatever `main` looked like when the tree was first configured; confirm with `git log -1` in `build/_deps/<dep>-src` before believing your tree. Full reasoning and the original finding: [docs/x4-fetchcontent-pins.md](docs/x4-fetchcontent-pins.md). |
| S5 | TODO | any | **`getFolderInfo` is undefined behaviour in TIDE.** `TideApp::InitInstance` never calls `CSynthEditAppBase::InitInstance`, so `refreshFolderLocations()` never runs and `m_folder_settings` stays empty; `getFolderInfo` (`SE16/SynthEdit2/Application.cpp:167`) then evaluates `m_folder_settings[0]->current_folder` on an empty vector. Reachable via `CSynthEditAppBase::ShortenFilename` (`SE16/SynthEdit2/SynthEditAppBase.cpp:238`). `ResolveFilename` is unaffected. Fix in `Application.cpp` (guard the empty case) — that is EditorLib, so check carve-out ordering before moving it. |
| S9 | TODO | mac | **GATED remainder of S6 — remove the now-dangling `TIDE/Plugins` wiring from `SE_IOS_APP.xcodeproj`.** Filed 2026-08-13 when S6 deleted the files. Three things to remove, all in the **shared, GATED** `SE16/SE_IOS_APP/SE_IOS_APP.xcodeproj/project.pbxproj`, all belonging to the `SeAudioUnitMacOS` target: (a) the Run Script phase at `:2064` (`cp -r "$SRCROOT/TIDE/Plugins" …`); (b) the six `.sem` group/file references (`Oscillator`, `EnveloperAdsr`, `PatchMemory`, `Converters`, `SubControlsXp`, `ControlsXp`, ids `D5F5219525E5F21600E75519` and siblings) — navigator-only, cosmetic, they just show red; (c) **the real ones — the `CpResource` build-file entries for files *inside* those bundles, which are genuine Resources-phase inputs** and are what now errors. **Do not do this without reading S10 first** — if that project is retired rather than revived, this row is moot and the right move is deleting the whole `.xcodeproj`. **Accept:** `SeAudioUnit macOS` builds with no `TIDE/Plugins`-related error (it will still fail on `SE_DSP_CORE` — that is S10, not this). **Size:** minutes, once scope is ruled. **Needs Jeff** — GATED path, and a scheduled run may not edit it. |
| S10 | TODO | mac | **`SE_IOS_APP.xcodeproj` is dead, and this bears directly on M2.** Found 2026-08-13 during S6. **All four targets** (`SE_IOS_APP`, `SE_IOS_AUDIOUNIT`, `SeAppMacOS`, `SeAudioUnitMacOS`) fail to build — measured, each `RC=65` — on **28 references to `SE_DSP_CORE/`**, the pre-split name of the DSP core directory, which no longer exists (it became `SynthEditLib`). The pbxproj was last touched **2022-12-15**; the TIDE bundles inside it, 2021-03-03. **Why it matters beyond tidiness:** [PLAN.md](PLAN.md) calls iOS AUv3 "the constraint that validates the whole design", and **M2** is written as though an iOS project exists to build on. It does not, in any working sense — so M2 is really "author an iOS target", not "get the existing one green", and whoever scopes it should know that before estimating. **Also inside it:** `SE_IOS_APP/TIDE/Resources/database.se.xml` is a 31-entry module database naming the six now-deleted `.sem` bundles by `imbeddedFilename`, and it *is* wired into two Resources build phases. That whole shape — a module database pointing at separately-loadable bundles — is exactly the architecture **constraint 7** rules out, so it should not be revived as-is regardless. **The decision this needs, and it is Jeff's:** revive the project (fix the 28 paths), or retire it (delete the `.xcodeproj` and build the iOS target fresh from the CMake side when M2 comes up). **S9 is moot if the answer is retire.** **Size:** the decision is minutes; either implementation is its own item. GATED path either way. |
| S7 | TODO | any | **Enforce the no-user-skins ruling — PLAN constraint 8, decided 2026-08-07.** The static chain says a TIDE build writes skins to the user's disk today: `CContainer.cpp:97` calls `SkinMgr::Instance()->getSkin(L"default3")` (also `SynthEditDocBase.cpp:386`), and `SkinMgr`'s **constructor** (`SE16/SynthEdit2/SkinMgr.cpp:27-30`) points its folder at `<CommonDocuments>\SynthEdit Projects\skins\`, where `setSkinFolder` (`:47+`) **recursively copies the built-in skins on first use**. So the first container a TIDE instance constructs probably spews the shared skin set over the user's drive — verify at runtime, then make TIDE load its default look from plugin resources only, with no skin folder, scan, or write reachable. `SkinMgr` is EditorLib (C4, GATED) — if the fix cannot be confined to TIDE-side code, do what can be and file the gated remainder naming the exact lines. Overlaps S2's audit; whoever takes either first should check the other's scope. |
| S8 | TODO | any | **TIDE ships modules constraint 2 forbids, and shows SynthEdit licensing copy.** Measured from the macOS Release binary 2026-08-08 (addendum B3 of [docs/module-enumeration.md](docs/module-enumeration.md)): `ug_soundcard_in`, `ug_soundcard_out` and `ug_midi_out` are compiled in *and registered* — each has its `se_static_library_init_*` and `__GLOBAL__sub_I_*` static initialiser — so the module browser offers a **Sound Out** module that opens the soundcard directly, in a plugin whose whole premise is that the DAW owns I/O. `ug_soundcard_out.cpp:10` registers it under Input-Output with the help text *"Sends audio to your speakers… Non-registered SynthEdit limited to 2 output channels"*, which is also wrong for a free unlicensed plugin. Confirms A1's prediction; also confirmed the modern SEM modules are absent (`OscillatorNaive`: zero symbols). Both arms of `initialise_synthedit_modules` are in `SynthEditLib/UgDatabase.cpp:1063-1155`, so there is **no TIDE-side fix** — ~~GATED on C0~~ **unblocked 2026-08-08, C0 approved.** The `SE_EXTRA_STATIC_FILE_CPP` hook (A2) can only *add* modules, not suppress these. Pairs with S1b; whoever takes either should take both. |
| P6 | IN-REVIEW | mac | **Fixed — confirmed 2026-08-14 (macos) on the Xcode generator, which is what the row was waiting for. PR: [#50](https://github.com/JeffMcClintock/TideSynth/pull/50) (docs only — no code change in any repo; the fix had already landed as SynthEdit's own `691270c5d` + `4792f4bf2`).** `cmake -G Xcode` + `--target SynthEditCL` at `SE16` `b3c1efb07`, universal `x86_64;arm64`, Debug: **RC=0**, and the log carries the step Ninja never emits — `CodeSign …/SynthEditCL.app`, `Signing Identity: "Sign to Run Locally"`. `codesign --verify --deep --strict` returns *valid on disk* / *satisfies its Designated Requirement* (336 sealed files, 98 signed `.sem` in `Contents/PlugIns`), and the signed binary runs (`SynthEditCL V1.6.182`). `Contents/MacOS` now holds **only** the executable; `Prefabs`, `fonts`, `skins` and `templates` are all under `Contents/Resources`. **Verification artifact — A/B positive control, same binary, same codesign command, no source edit and no rebuild:** copied the built bundle, moved the four staged dirs back under `Contents/MacOS/Resources`, re-ran codesign → **RC=1** with this row's error string verbatim, naming the same file (`code object is not signed at all` / `In subcomponent: …/Contents/MacOS/Resources/Prefabs/Controls/Button Small2.syntheditprefab`); current layout → **RC=0**. So the staging path is what closes it, not a toolchain difference. `4792f4bf2`'s detritus scrub holds too: 0 `.DS_Store`, no xattrs in the bundle. **Standing rule met, and more strongly than any previous mac run could state it:** all five products build under Xcode and verify — `SynthEditCL`, `SynthEdit_VST3`, `SynthEdit_GMPI`, `TIDE`, `TIDE_VST3`, each RC=0. **Nothing in `SE16` was edited** — `SynthEditCL/CMakeLists.txt` is on neither the ALLOWED nor the GATED list, so GATED by default, and none was needed. **The generalisable lesson: Ninja emits no `codesign` invocation at all, so no Ninja build can confirm or deny a signing bug on mac** — that asymmetry is why this sat open for six days through two RC=0 reports. Evidence: JOURNAL 2026-08-14 macos. Original row follows. **`SynthEditCL` does not build on macOS.** Not TIDE, and pre-existing — verified 2026-08-08 by stashing the S1b change and rebuilding, it fails identically. Compiles and links fine, then `CodeSign` fails: `SynthEditCL.app: code object is not signed at all / In subcomponent: …/SynthEditCL.app/Contents/MacOS/Resources/Prefabs/Controls/Button Small2.syntheditprefab`. The `stage_prefabs_SynthEditCL` build rule stages prefab **data** under `Contents/MacOS/Resources/`; `codesign` treats everything under `MacOS/` as code and refuses. Almost certainly wants `Contents/Resources/`. Matters to TIDE only because "leave SynthEdit, SynthEditCL and TIDE all building" is the standing rule and a macOS run cannot honour it for SynthEditCL today — say so rather than assuming. Lives in `SE16/SynthEditCL/CMakeLists.txt`, which is neither TIDE's nor on the GATED list; treat as SynthEdit's own bug and check with Jeff before touching it.  **2026-08-12 (P7a run) — this looks fixed, but not by me and not confirmed on the failing generator.** `SynthEditCL/CMakeLists.txt:187` now branches on `APPLE` and uses `$<TARGET_BUNDLE_CONTENT_DIR:SynthEditCL>` (i.e. `Contents/`), with a comment naming this row's exact error string; two commits landed for it — `691270c5d` "CL mac bundle stages Resources into Contents/Resources" (2026-08-08) and `4792f4bf2` "CL bundle fails codesign on staged Finder detritus" (2026-08-11, current `master` tip). A fresh **Ninja** build here compiled, linked and staged prefabs into `Contents/Resources/`, RC=0. **That is not proof:** the Ninja generator emits no `codesign` step at all, so it cannot reproduce the failure either way — the app came out unsigned. Confirming this needs an **Xcode**-generator build, which is what Jeff's own `SynthEdit/build` tree uses. Left TODO on purpose. |
| S2 | TODO | any | **Audit every filesystem and cache write** reachable from a TIDE build. Grep for `GetHomeDir`, `AppData`, `%TEMP%`, `~/Library`, `CreateFile`, `fopen`, registry access. Produce `docs/sandbox-audit.md` listing each hit as keep / stub / remove. Constraint 4 in [PLAN.md](PLAN.md) cannot be verified without this. |
| S3 | TODO | any | **Make the removed dialogs genuinely absent.** `TideApp.cpp` currently stubs `doDialogConnectUg`, `doDialogPatchManager` and `doDialogBuildCodeSkeleton` with `assert(false)`. In a release build these silently fall through. Make the code paths that reach them unreachable, or fail loudly in release too. |
| B1 | TODO | any | **CI that builds nothing yet but is correct.** `.github/workflows/build.yml` exists as a skeleton and is expected to fail until C7. Get it to the point where it fails for exactly one honest reason (missing private dependency) rather than for syntax or toolchain errors. |
| D2 | TODO | any | **Place the "TIDE Synth — by SynthEdit Ltd" credit — decided 2026-08-13 by Jeff, following R1(a).** TIDE ships signed as `SynthEdit Limited` (no second signing identity — not affordable), so the credit exists to make that visible on TIDE's own terms rather than only in the OS's Gatekeeper/UAC dialog. Same constraints as D1 govern placement: constraints 1 and 5 rule out a dialog or splash, so it lives in the breadcrumb bar, an about pane, or the installer's credit field — pairs naturally with D1's about-pane work if that lands first. Website (`W1`, already live) can carry it immediately with no constraint conflict. Not v0.1. |
| D1 | TODO | any | **Design the in-plugin donation affordance — design note only, do not build.** [PLAN.md](PLAN.md) "Price and funding" says the plugin should make donating possible without nagging, and two constraints gut the obvious implementations: constraints 1 and 5 rule out a dialog or second window, so it lives in the breadcrumb bar or an about pane or nowhere; constraint 3 puts the whole "open a URL in a browser" approach in doubt, because `browseto.mm`/`openurl.mm` are already listed as removed-or-restricted under AUv3 in [docs/design-notes.md](docs/design-notes.md). **Establish first whether an AUv3 can open a URL at all** — that is a factual question the macOS box can answer and the other two cannot, so if you are not on macOS, say so in the note rather than guessing. Fallbacks worth weighing if it cannot: show the URL as text to be copied, a QR code, or nothing in-plugin at all with the ask living only on the website. Produce `docs/donations.md`. Not v0.1. |
| P3 | TODO | win | **Remove the MFC dependency from the two shared-core files that have one.** **Paths updated 2026-08-13: one of the two has moved.** `CContainer.cpp:8` is now `SynthEditLib/CContainer.cpp:8` — C3 moved it to the **public** repo unaltered, so once [SynthEditLib#5](https://github.com/JeffMcClintock/SynthEditLib/pull/5) merges the hard MFC requirement is public, which is the thing this row was filed to prevent. `MfcDocPresenter.cpp:4` is still `SE16/SynthEdit2/` and moves under **C4**, so the second half is best done before or with C4. Both include `afxres.h` under `#ifdef _WIN32`. That header ships only with Visual Studio's MFC component, so a contributor with VS Build Tools (no MFC) cannot build TIDE at all — see the "MFC trap" section of [docs/building.md](docs/building.md). Both files are on the carve-out list (C3 and C4), so this lands in the public repo as a hard MFC requirement unless it is removed first. Find out what they actually need from `afxres.h` — it is likely only `ID_*`/`IDR_*` resource constants, which could move to the local `resource.h`. Verify SynthEdit and SynthEditCL still build. |
| P5 | TODO | any | **Amended 2026-08-08 by the N1 naming ruling — the target strings changed, and so did the shape of the fix.** The plug-in name is now **TIDE Rack** and the vendor is **TIDE Synth**: VST3 draws exactly the vendor/product distinction the new naming does, so set both fields rather than putting one name in both. Note `TideApp::getVendor4charCode()` returning `"TIDE"` is a **fixed four-character** field — it stays `TIDE` and must not be "fixed" to match a longer name. Whoever takes this should check N1 first; if N1 lands first, this is a subset of it. Original finding: **the plugin does not call itself TIDE.** REAPER lists it as `VST3i: SynthEdit (GMPI)`; "TIDE" appears only in the filename, so `TrackFX_AddByName(tr, "TIDE_VST3", ...)` returns -1 and a user searching their FX browser for "TIDE" finds nothing. `TideApp::getVendor4charCode()` already returns `"TIDE"` — it is the plug-in name and vendor string in the VST3 wrapper that still say SynthEdit. See [docs/state-of-the-prototype.md](docs/state-of-the-prototype.md) §3. |
| E1a | TODO | any | **Not work any linux run can do — and it was the linux NEXT pointer for two days. Corrected in prose 2026-08-13.** *(Left as `any` deliberately: the Platform vocabulary is `any`/`win`/`mac`/`linux` and **has no way to say "any box except linux"**, which is exactly what this row is. Tried `mac/win` and A3's `check-backlog-diff.py` correctly rejected it — only the Status cell may change on an existing row, and that value is outside the documented set anyway. So the constraint lives here in the Item text, where a reader will actually hit it. A one-cell change is Jeff's to make if he wants it.)* The Accept clause below is "one render of both cases **on a second platform**" — second *to linux*, which is the only lane that has ever run. So the linux box is the one machine that is definitionally unable to satisfy it, and the NEXT block's old reasoning ("this is the box that can actually test cross-platform") was backwards. Confirmed on the linux box before falling through to P7c: no wine, no container runtime, no macOS emulation — and **Wine would have been worse than nothing anyway**, since its `msvcrt` routes `sin`/`cos`/`exp` to glibc's libm, i.e. to the exact implementation the linux golden was rendered with, so a Wine render would report near-zero drift and license a tolerance that a real Windows box then fails. **Whichever of mac or win picks this up: the render half is the cheap half** (download the engine, run `tools/render_harness.py`, record two numbers); the judgement half is setting the RMS gate from that data. Original row follows. **The audio harness's two null tolerances disagree about cross-platform float drift.** `render_harness.py` justifies its peak gate as tolerating the ~1 LSB (−90.3 dBFS at 16-bit) that cross-platform rounding can legitimately produce. True of the peak gate; **false of the RMS gate beside it.** Measured during E1: 1 LSB on *every* sample gives RMS −90.3 dBFS, which fails the −100 dBFS gate; solving `rms = sqrt(fraction)` shows the RMS gate tolerates 1-LSB error on at most **~10.7% of samples**. Since only the Linux lane has ever run, this is the most likely cause of a spurious failure the first time mac or Windows renders a case — and it will look like a real regression. **Do not just widen the number**: E1 deliberately left it alone because picking a threshold with zero cross-platform measurements is guessing at the definition of "regression". **Scope** `tools/render_harness.py`. **Accept** one render of both cases on a second platform, its measured RMS and peak residuals recorded, and the tolerances set from that data with the reasoning written down. **Size** one session, but it is blocked in practice on someone running the harness on mac or win — that half is cheap (the engine is a download, not a build). See [docs/e1-verification-harness.md](docs/e1-verification-harness.md) finding (e). |
| U1 | TODO | any | **Target changed 2026-08-13 by the rack-mode pivot (constraint 1) — needs a fresh audit before the old findings below can be trusted.** The gap this row measures is now against rack/panel view as the default top-level UI, not the structure view — [docs/design-notes.md](docs/design-notes.md)'s "The one-view UX" section (and its ASCII mockup) still describes the old structure-view-first model and is itself stale, not yet rewritten. **Do not assume the P2 findings below still apply** — they were measured against the structure view; rack mode is different code, possibly further along, possibly not started. Re-measure against current `TideApp`/`SynthEditSem` state before doing anything else here. Old findings, kept for reference only: as of P2 the editor had no breadcrumb bar, no properties pane on screen (`TideApp::OpenPropertiesBrowser` exists but nothing corresponds to it), the module browser was SynthEdit's two-column browser with no collapse control, and the document canvas was drawn ~440 px in from the top-left of the content area with a dead strip down the right. Measurements and screenshots in [docs/state-of-the-prototype.md](docs/state-of-the-prototype.md) §6 — also pre-pivot, read with the same caution. Probably wants splitting once someone costs it. |

---

## After the carve-out

| ID | Status | Plat | Item |
|---|---|---|---|
| M1 | BLOCKED | mac | AU + AUv3 targets building on macOS. |
| M2 | BLOCKED | mac | iOS AUv3 build; validate under real sandbox rules. This is the constraint that validates the whole design. |
| M3 | BLOCKED | mac | `auval` clean. |
| X1 | BLOCKED | linux | VST3 + CLAP on Linux, GCC 13+. See the Linux toolchain memory for WSL specifics. |
| X2 | BLOCKED | linux | Zero-warning build at `-Wall -Wextra`. |
| V1 | BLOCKED(E2a) | any | Patch state survives host save/reload — the v0.1 acceptance test in [PLAN.md](PLAN.md), **amended 2026-08-13: rack-first, and the acceptance test itself needs the oscillator/envelope/output prefabs E2a builds before it can even be attempted.** The circularity this created against the old E2 (BLOCKED(V1)) is resolved by the split — E2a is unblocked, the rest of E2 stays behind V1 as before. |
| V2 | BLOCKED | any | Host-automatable parameters without a panel view (design note 4 in [docs/design-notes.md](docs/design-notes.md)). **This premise is now stale too — panel view (the rack) is TIDE's default view as of the 2026-08-13 pivot, not something being avoided. Design note 4 needs re-reading against that before this row is trusted; not done here, flagged in passing while splitting E2.** |
| E2a | TODO | any | **Split out of E2, 2026-08-13, at Jeff's direction — the minimal slice V1's acceptance test needs, unblocked.** Build exactly three prefabs: oscillator, envelope, output — each a Container wrapping the real DSP module with its patch points exposed on the panel, per constraint 1's rack model. **Check this before building anything — it may already be a blocker:** S8 measured the current Release binary and found the modern SEM oscillator (`OscillatorNaive`) has **zero symbols** — not compiled in at all. If no suitable oscillator primitive is currently registered, this row cannot produce a real oscillator prefab until S8 (or at least the part of it that gets a usable oscillator primitive registered) lands first. Verify what's actually in the binary before assuming either way. **Also unclear, flagged rather than assumed:** whether placing and cabling prefabs in the rack needs U1's UX work first. U1's original scope was breadcrumb/structure-view navigation for *opening* a Container to edit it — placing and cabling a *closed* prefab in the rack is a different surface (SynthEdit's rack-mode UI itself), and nobody has audited what that UI currently supports. Each prefab needs its own E1 test case. |
| E2 | BLOCKED(V1) | any | **The rest of the first Eurorack module set, beyond E2a's three V1-critical prefabs.** Split 2026-08-13 — see E2a for the part that's unblocked. Everything else here still waits on V1: "no point authoring a fuller module library for a plugin that cannot yet keep its patch across a host save" holds for the broader set even though the minimal V1-critical prefabs couldn't wait for V1 themselves. PLAN "The Eurorack rack", 2026-08-09. Define the naming and I/O conventions for a module Container, then build the rest of a first curated set, each with its own E1 test case. Two dependencies that are not obvious: constraint 7 means the **primitives** a Container is built from must be statically registered — see **S8**, which is about the wrong ones being compiled in today — and a Container is only openable-and-editable if U1's UX can actually navigate into it, which is what the breadcrumb bar is for. Carried over from the superseded `tide-rack` repo, where this was EP-002. |
| E3 | BLOCKED | any | **Curated starter racks** — ready-to-play preset patches shipped with the plugin, treated as a deliverable rather than a demo. PLAN calls this the friendly surface, and it is the Blocks trait the product is explicitly copying. Blocked on **E2**. Two things it needs that nothing else forces: a rack has to survive load without a filesystem path (constraint 3), and it should be the thing a user sees *first*, which is a product decision about default state, not a file format. Carried over from the superseded `tide-rack` repo, where this was EP-003. |

---

## Release & distribution — blocked on V1 (nothing to ship yet)

The plan is [docs/distribution.md](docs/distribution.md). R1 is the only item
with lead time and the only one Jeff can do now; the rest wait for a v0.1 that
passes V1. CI automation additionally waits on C7 — the interim path (build on
each box, `gh release upload`) is in the doc.

| ID | Status | Plat | Item |
|---|---|---|---|
| R1 | RESOLVED | — | **Signing identities and accounts.** **(a) Decided 2026-08-13 by Jeff: TIDE ships under the existing `SynthEdit Limited` identity, deliberately — a second Azure certificate profile is not affordable right now.** So both platforms sign under `SynthEdit Limited`/`SynthEditCertificateProfile` as already configured; no new credential, no R7-style hardening needed for a profile that doesn't exist. Compensating branding decided the same day: **"TIDE Synth — by SynthEdit Ltd"**, placed subtly (about pane, footer, or installer credit — not the plugin name itself, constraints 1 and 5 still rule out anything dialog- or splash-shaped). Placement is unscoped — filed as **D2**. (b) **Confirmed** — Apple Developer ID is `Developer ID Application: SynthEdit Limited (36SNPLRFK3)`, and notarization runs on the app-specific-password route (`APPLE_ID` / `APPLE_ID_PASSWORD` / `APPLE_TEAM_ID`), matching `SynthEdit_cmake_mac.yml:223-244` so the recipe ports without rewriting `notarytool`. An App Store Connect API key would be scoped and revocable where the app-specific password is tied to Jeff's Apple ID and grants far more than notarization — worth switching before anything consumes it, not urgent while nothing does. (c) App Store listing for the iOS AUv3 (needed by M2, not v0.1) — **not started.** (d) **Done early, ahead of C7** — 8 Actions secrets and 4 variables now exist on the public repo: secrets `AZURE_TENANT_ID`, `AZURE_CLIENT_ID`, `AZURE_CLIENT_SECRET`, `APPLE_CERT_P12_BASE64`, `APPLE_CERT_PASSWORD`, `APPLE_ID`, `APPLE_ID_PASSWORD`, `APPLE_TEAM_ID`; variables `AZURE_CODESIGN_ENDPOINT`, `AZURE_CODESIGN_ACCOUNT`, `AZURE_CODESIGN_PROFILE`, `APPLE_SIGNING_IDENTITY`. The non-secret half is deliberately in *variables* — those values are on every binary TIDE ships, and hiding them only makes failures harder to read. Nothing consumes any of them yet. **Two follow-ups, filed as R7.** Detail on the identities, and why the non-secret half is deliberately in variables: [docs/r1-signing-identities.md](docs/r1-signing-identities.md). |
| R7 | TODO | any | **Harden the signing credentials now that they exist (follow-up to R1(d)).** Two things, neither urgent while no workflow consumes them. **(1) The exposure is same-repo branches, not fork PRs.** GitHub withholds secrets from fork-triggered `pull_request` runs, so `build.yml`'s bare `pull_request:` trigger is safe. But it also runs on `push: branches: ["main", "tide/**"]`, and same-repo runs get full secret access — and pushing `tide/**` branches is exactly what the weekly agents do. So today a workflow edit on any agent branch would execute with read access to all 8 credentials, ungated. Fix: create a `release` environment with Jeff as a required reviewer, move the secrets into it (`gh secret set <NAME> --env release`), delete the repo-level copies. Only a job declaring `environment: release` can then reach them, and it pauses for approval. **(2) Drop the two long-lived credentials.** `AZURE_CLIENT_SECRET` → OIDC federated credentials scoped to `repo:JeffMcClintock/TideSynth:ref:refs/tags/*`, which removes the stored secret entirely *and* means even an approved non-tag run cannot sign; `azure/trusted-signing-action@v0` takes the same inputs as the `ArtifactSigning@1` task either way. `APPLE_ID_PASSWORD` → App Store Connect API key (`.p8` base64 + key id + issuer id). Do this before **R5** wires anything up, or R5 will have to be rewritten. |
| R2 | BLOCKED | win | **Windows installer.** Inno Setup, modelled on `SE16/SynthEdit2/installer/SynthEdit2.iss`. Installs `TIDE-Rack.vst3` to `Common Files\VST3`; sign the .vst3 and the installer via Azure Trusted Signing. Also produce `TIDE-Rack-Windows.zip` for users who refuse installers. Installer title and shortcuts use the display form **TIDE Rack**; every filename uses **TIDE-Rack** — see the naming table in [docs/distribution.md](docs/distribution.md). Constant asset names — the version lives in the tag and the version resource, per the doc. |
| R3 | BLOCKED | mac | **macOS pkg.** AU → `Components`, VST3 → `VST3`; Developer ID sign, **notarize and staple** — model on `SE16/SynthEdit_cmake_mac.yml`. AUv3 is deliberately absent: it ships in the container app with M2. |
| R4 | BLOCKED | linux | **Linux tarball.** `TIDE-Rack-Linux.tar.gz` with the VST3 and CLAP bundles and a short `install.sh` targeting `~/.vst3` / `~/.clap`. No signing. |
| R5 | BLOCKED | any | **Release workflow.** Tag `v*` → build + sign every platform → one GitHub Release with constant-name assets + `SHA256SUMS.txt`. Runs on tag push only, never on PRs, so fork PRs never see signing secrets. Needs C7 for public CI builds; until then the same release page is fed by `gh release upload` from the platform boxes. |
| R6 | BLOCKED | any | **Website Downloads section.** Replace the "nothing to download yet" card with static `releases/latest/download/<asset>` permalinks — no JS, no version maintenance, keeps the "no cookies, no scripts, no tracking" footer honest. iOS is a plain text App Store link (Apple's badge image would be the page's first external resource — do not use it). |
**The publisher name TIDE ships under is SynthEdit Limited — decided, not
inherited, as of R1(a) 2026-08-13.** `APPLE_SIGNING_IDENTITY` is
`Developer ID Application: SynthEdit Limited (36SNPLRFK3)`, and
`SynthEditCertificateProfile` almost certainly carries the same subject on
Windows. So a user installing TIDE sees *SynthEdit Limited* in the macOS
Gatekeeper prompt and the Windows UAC dialog — deliberately: a second Azure
certificate profile is not affordable right now, and PR #14's website
stripped every SynthEdit mention so TIDE stands on its own there, which is
why a compensating credit was filed as **D2** — "TIDE Synth — by SynthEdit
Ltd", placed on TIDE's own terms rather than only surfacing in the OS
install dialog.

---

## Done

Landed rows live in **[BACKLOG-DONE.md](BACKLOG-DONE.md)** — moved there by A8,
2026-08-12. Look there for precedent: how a past stage was verified, what a
previous run measured.

**As part of STEP 4:** an `IN-REVIEW` row whose PRs have all merged becomes
`DONE` and moves to that file, with the merge date in the `Done` column. Move
the row verbatim; do not rewrite it on the way out.
