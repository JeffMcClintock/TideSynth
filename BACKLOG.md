# TIDE Synth — Backlog

Ordered. A weekly agent takes the **topmost item that is not blocked and matches
its machine's platform role**, does that one item, and stops.

Status: `TODO` · `DOING` · `DONE` · `BLOCKED` · `NEEDS-JEFF`
Platform: `any` · `win` · `mac` · `linux`

Update this file at the end of every run. An item left in `DOING` with no
matching JOURNAL entry means a previous run died — reset it to `TODO` and note
that in the journal.

---

## Blocked on Jeff — agents must not start these

| ID | Status | Item |
|---|---|---|
| L1 | RESOLVED | **Licence chosen 2026-08-07 by Jeff: ISC**, the same licence as the GMPI and gmpi_ui repos. LICENSE files are committed and pushed in both `TideSynth` (`a58a6f1`) and `SynthEditLib` (`a2143a4` — note `42ce33d` briefly added MIT from a misheard decision; ISC replaced it minutes later, both remain in history). This also completes **C1** and unblocks the "open source" claim, which the holding page now makes. C2–C7 remain blocked on C0 only. |
| C0 | NEEDS-JEFF | **Approve the carve-out plan** in [docs/carve-out.md](docs/carve-out.md), including the staging order and the decision on `SynthEditCL`. Blocks C1–C7. |
| G1 | RESOLVED | **Repo created, and made PUBLIC 2026-08-07 by Jeff** — <https://github.com/JeffMcClintock/TideSynth>. Unblocks the "Source" section of the holding page and makes GitHub Pages available free (see H1). **It also makes L1 urgent rather than theoretical:** TideSynth is now in exactly the state PLAN.md criticises `SynthEditLib` for — public, readable by anyone, with no LICENSE file, therefore all-rights-reserved and legally unusable by anyone. Two repos in that state now, not one. |
| H1 | NEEDS-JEFF | **Hosting decided 2026-08-07 by Jeff: GitHub Pages.** What remains is the one-time go-live, and every step needs repo settings or the registrar, so it stays NEEDS-JEFF: enable Pages (Source: **GitHub Actions**), run the workflow once, set custom domain `tidesynth.com`, add the four apex `A` records + `www` CNAME at the registrar, tick Enforce HTTPS. Exact records and order in the checklist in [docs/hosting.md](docs/hosting.md). The deploy side is already done — [.github/workflows/pages.yml](.github/workflows/pages.yml) publishes `website/` on every push to `main` that touches it; until Pages is enabled it fails with "Get Pages site failed", which means "not enabled yet", not "broken". Shared-host fallback stays documented in the same doc. |
| G3 | RESOLVED | **Fixed 2026-08-07 by Jeff: `gmpi_ui` and `GMPI_Wrappers` are ALLOWED.** [docs/weekly-run-prompt.md](docs/weekly-run-prompt.md) updated — they are shared with SynthEdit and every other GMPI plugin, so keep changes tight, rebuild SynthEditCL too, and stage only your own files (both working copies are routinely dirty with other-platform work). **Each machine's installed task must be reinstalled from that doc** — as with G2, only the machine that made the change carries the new STEP 5. Original finding below. |
| ~~G3~~ | *(was)* | **The run prompt's ALLOWED/GATED split does not mention `gmpi_ui` or `GMPI_Wrappers`.** Both are separate public repos (`JeffMcClintock/gmpi_ui`, `JeffMcClintock/GMPI_Wrappers`) that the TIDE build consumes directly via `GMPI_UI_FOLDER_OVERRIDE` / `GMPI_WRAPPER_FOLDER_OVERRIDE`. They are in neither list, so an agent has no ruling to follow — and TIDE's rendering and windowing bugs land there, not in `SE16`. P4 found this the hard way: the entire fix for a host-killing crash is in those two repos and **nothing in `SE16/SynthEditSem/` is involved at all**, so "do the TIDE-side part" had no TIDE side. Blocks **P4a** and **P4b**. Note `gmpi_ui` is shared with SynthEdit and every other GMPI plugin, so the answer may well be "gated like `SynthEditLib`" — but it needs to be *written down*. See [docs/p4-resize-crash.md](docs/p4-resize-crash.md). |
| G2 | RESOLVED | **The SE16 write-permission conflict in the run prompt** — STEP 5's blanket ban meant no agent could ever write TIDE code, only design notes. **Fixed 2026-08-06** by Jeff: STEP 5 now has an ALLOWED/GATED split — `SE16/SynthEditSem/`, `SE16/TideModules/` and `SE16/SE_IOS_APP/TIDE/` are TIDE's to change; `EditorLib`, `SynthEdit2` and `SynthEditLib` stay behind the C0 gate. See PR #4. **Each machine's installed task must be reinstalled from [docs/weekly-run-prompt.md](docs/weekly-run-prompt.md) — only the macOS box has been updated; Windows and Linux still carry the old STEP 5.** |

---

## Ready now — no dependency on the above

| ID | Status | Plat | Item |
|---|---|---|---|
| P4c | TODO | win | **Reproduce the P4 crash, then confirm P4a/P4b actually fixed it.** P4a and P4b are landed and reasoned from a proven crash dump, but **never verified against the failure** — a rebuilt portable-REAPER harness could not reproduce the crash once, in Release or Debug, even with both fixes disabled (code behaviourally identical to what crashed 3/3 in P2). The lead: P2 reported `MoveWindow` **did not** resize the window and crashed anyway; in the new harness it resizes correctly every time. So the window was in a different state, and that state is likely what produced the bogus `{0,0,2178,32672}` rect. Try: the `%APPDATA%\REAPER` config as it was on 2026-08-06, repeated launches with screenshots and `SetForegroundWindow` in between, a different monitor/DPI layout, or dragging the window edge rather than `MoveWindow`. Table of what was tried in [docs/p4-resize-crash.md](docs/p4-resize-crash.md). Until this is done, treat the resize crash as **fixed-by-reasoning, not fixed-by-test**. |
| S1b | TODO | any | **Compile the scan out: stage 3 of [docs/module-enumeration.md](docs/module-enumeration.md).** **Unblocked — S1a landed 2026-08-07** (`SE16` `d67bdfbab`). Now required rather than optional — [PLAN.md](PLAN.md) constraint 7 makes the fixed module set a non-negotiable, so the scan and cache code must be *absent*, not merely un-called, or S2's sandbox audit can never come back clean. Introduce `TIDE_NO_EXTERNAL_MODULES`, or decouple `SE_EXTERNAL_SEM_SUPPORT` from `GMPI_IS_PLATFORM_JUCE` (`SynthEditLib/modules/shared/xplatform.h:34`) so it is settable on its own. **Read the trap in §6 of the note first:** the two arms of `initialise_synthedit_modules` register different module sets, and neither is the set TIDE wants — the non-JUCE arm includes `ug_soundcard_in`/`ug_soundcard_out`/`ug_midi_out`, which constraint 2 forbids. Blocked behind S1a, and touches shared code, so check carve-out ordering. |
| X3 | DOING | linux | **The Linux `TIDE_VST3.so` exports no `ModuleEntry`/`ModuleExit`, so no Linux VST3 host can load it.** Steinberg's own `validator` rejects it outright: *"The shared library does not export the required 'ModuleEntry' function"*. `nm -D` on the built module shows only `GetPluginFactory` and `InitDll` (the Windows pair); SynthEdit's own `libSynthEdit_VST3.vst3` exports all four. **Not a code defect — a stale dependency.** `SE16/SynthEditSem/CMakeLists.txt` leaves `GMPI_WRAPPER_FOLDER_OVERRIDE` blank, so TIDE `FetchContent`s GMPI_Wrappers from GitHub; the checkout under `build/_deps/gmpi_wrappers-src` is pinned at `032b4d5`, which predates `9a2341d fix(linux) : export ModuleEntry/ModuleExit, and build CLAP wrapper PIC` on that repo's `main`. `GIT_TAG origin/main` does **not** re-fetch on a later configure, so the tree silently keeps building a plugin no host can load. Decide whether TIDE should pin a tag, force a refresh, or use `GMPI_WRAPPER_FOLDER_OVERRIDE` like it already does for `gmpi_ui`. Blocks any Linux runtime testing, including the empirical half of X1 and V1. |
| S5 | TODO | any | **`getFolderInfo` is undefined behaviour in TIDE.** `TideApp::InitInstance` never calls `CSynthEditAppBase::InitInstance`, so `refreshFolderLocations()` never runs and `m_folder_settings` stays empty; `getFolderInfo` (`SE16/SynthEdit2/Application.cpp:167`) then evaluates `m_folder_settings[0]->current_folder` on an empty vector. Reachable via `CSynthEditAppBase::ShortenFilename` (`SE16/SynthEdit2/SynthEditAppBase.cpp:238`). `ResolveFilename` is unaffected. Fix in `Application.cpp` (guard the empty case) — that is EditorLib, so check carve-out ordering before moving it. |
| S6 | TODO | mac | **Delete the dead iOS module artifact.** `SE16/SE_IOS_APP/TIDE/Plugins/` holds six checked-in `.sem` bundles that are all `Mach-O 64-bit bundle x86_64` in macOS bundle layout, installed by a Run Script (`SE_IOS_APP.xcodeproj/project.pbxproj:2064`) that copies them to `.../Contents/` — a macOS-only destination. Nothing there can load on arm64 iOS. It reads as a working iOS module story and is not one. Remove it, or add a README saying what it actually is. See addendum A5 of [docs/module-enumeration.md](docs/module-enumeration.md). **Unblocked** — G2 is resolved and `SE16/SE_IOS_APP/TIDE/` is now ALLOWED. But the Run Script phase that installs it lives in the *shared* `SE_IOS_APP.xcodeproj/project.pbxproj`, which is still GATED — delete the folder, then file the build-phase removal separately rather than editing the project file. |
| S7 | TODO | any | **Enforce the no-user-skins ruling — PLAN constraint 8, decided 2026-08-07.** The static chain says a TIDE build writes skins to the user's disk today: `CContainer.cpp:97` calls `SkinMgr::Instance()->getSkin(L"default3")` (also `SynthEditDocBase.cpp:386`), and `SkinMgr`'s **constructor** (`SE16/SynthEdit2/SkinMgr.cpp:27-30`) points its folder at `<CommonDocuments>\SynthEdit Projects\skins\`, where `setSkinFolder` (`:47+`) **recursively copies the built-in skins on first use**. So the first container a TIDE instance constructs probably spews the shared skin set over the user's drive — verify at runtime, then make TIDE load its default look from plugin resources only, with no skin folder, scan, or write reachable. `SkinMgr` is EditorLib (C4, GATED) — if the fix cannot be confined to TIDE-side code, do what can be and file the gated remainder naming the exact lines. Overlaps S2's audit; whoever takes either first should check the other's scope. |
| S2 | TODO | any | **Audit every filesystem and cache write** reachable from a TIDE build. Grep for `GetHomeDir`, `AppData`, `%TEMP%`, `~/Library`, `CreateFile`, `fopen`, registry access. Produce `docs/sandbox-audit.md` listing each hit as keep / stub / remove. Constraint 4 in [PLAN.md](PLAN.md) cannot be verified without this. |
| S3 | TODO | any | **Make the removed dialogs genuinely absent.** `TideApp.cpp` currently stubs `doDialogConnectUg`, `doDialogPatchManager` and `doDialogBuildCodeSkeleton` with `assert(false)`. In a release build these silently fall through. Make the code paths that reach them unreachable, or fail loudly in release too. |
| B1 | TODO | any | **CI that builds nothing yet but is correct.** `.github/workflows/build.yml` exists as a skeleton and is expected to fail until C7. Get it to the point where it fails for exactly one honest reason (missing private dependency) rather than for syntax or toolchain errors. |
| D1 | TODO | any | **Design the in-plugin donation affordance — design note only, do not build.** [PLAN.md](PLAN.md) "Price and funding" says the plugin should make donating possible without nagging, and two constraints gut the obvious implementations: constraints 1 and 5 rule out a dialog or second window, so it lives in the breadcrumb bar or an about pane or nowhere; constraint 3 puts the whole "open a URL in a browser" approach in doubt, because `browseto.mm`/`openurl.mm` are already listed as removed-or-restricted under AUv3 in [docs/design-notes.md](docs/design-notes.md). **Establish first whether an AUv3 can open a URL at all** — that is a factual question the macOS box can answer and the other two cannot, so if you are not on macOS, say so in the note rather than guessing. Fallbacks worth weighing if it cannot: show the URL as text to be copied, a QR code, or nothing in-plugin at all with the ask living only on the website. Produce `docs/donations.md`. Not v0.1. |
| P3 | TODO | win | **Remove the MFC dependency from the two shared-core files that have one.** `SynthEdit2/CContainer.cpp:8` and `SynthEdit2/MfcDocPresenter.cpp:4` include `afxres.h` under `#ifdef _WIN32`. That header ships only with Visual Studio's MFC component, so a contributor with VS Build Tools (no MFC) cannot build TIDE at all — see the "MFC trap" section of [docs/building.md](docs/building.md). Both files are on the carve-out list (C3 and C4), so this lands in the public repo as a hard MFC requirement unless it is removed first. Find out what they actually need from `afxres.h` — it is likely only `ID_*`/`IDR_*` resource constants, which could move to the local `resource.h`. Verify SynthEdit and SynthEditCL still build. |
| P5 | TODO | any | **The plugin does not call itself TIDE.** REAPER lists it as `VST3i: SynthEdit (GMPI)`; "TIDE" appears only in the filename, so `TrackFX_AddByName(tr, "TIDE_VST3", ...)` returns -1 and a user searching their FX browser for "TIDE" finds nothing. `TideApp::getVendor4charCode()` already returns `"TIDE"` — it is the plug-in name and vendor string in the VST3 wrapper that still say SynthEdit. See [docs/state-of-the-prototype.md](docs/state-of-the-prototype.md) §3. |
| U1 | TODO | any | **Close the gap to the one-view UX in [docs/design-notes.md](docs/design-notes.md).** As of P2 the editor has no breadcrumb bar, no properties pane on screen (`TideApp::OpenPropertiesBrowser` exists but nothing corresponds to it), the module browser is SynthEdit's two-column browser with no collapse control, and the document canvas is drawn ~440 px in from the top-left of the content area with a dead strip down the right. Measurements and screenshots in [docs/state-of-the-prototype.md](docs/state-of-the-prototype.md) §6. Probably wants splitting once someone costs it. |

---

## Carve-out — blocked on C0 (L1 resolved 2026-08-07)

Stages are sequential. Each must leave SynthEdit, SynthEditCL and TIDE all
building before the next starts. See [docs/carve-out.md](docs/carve-out.md).

| ID | Status | Plat | Item |
|---|---|---|---|
| C2 | BLOCKED | win | Move leaf files (`FuzzyMatch.h`, `checkpoint`, `cpu_accumulator`, `FrameRateLogger`, `imbedded_file`, `it_*`) to `SynthEditLib`; repoint `EditorLib/CMakeLists.txt`. |
| C3 | BLOCKED | win | Move the document model (`DocOb`, `CContainer`, `CUG`, `Plug*`, `SynthEditDocBase`, `SynthEditDoc2`). Largest and riskiest stage — split it if it resists. `CContainer.h`'s `friend ExportAsPlugin` declaration moves unchanged. |
| C4 | BLOCKED | win | Move views and browsers (`ModuleBrowser`, `PropertiesBrowser`, `MfcDocPresenter`, `ModuleFactory_Editor`, `SkinMgr`, `ThemeManager`). |
| C5 | BLOCKED | win | Move the app base (`SynthEditAppBase`, `ApplySynthEditConfig`, `SynthRuntime_editor`, `UIoManager`, `IO_base`, `IO_None`). |
| C6 | BLOCKED | any | Move `EditorLib/CMakeLists.txt` itself into `SynthEditLib`. |
| C7 | BLOCKED | any | Repoint TIDE at public `SynthEditLib` only. Prove it with a clean-clone CI build that has no access to the private repo. |

---

## After the carve-out

| ID | Status | Plat | Item |
|---|---|---|---|
| M1 | BLOCKED | mac | AU + AUv3 targets building on macOS. |
| M2 | BLOCKED | mac | iOS AUv3 build; validate under real sandbox rules. This is the constraint that validates the whole design. |
| M3 | BLOCKED | mac | `auval` clean. |
| X1 | BLOCKED | linux | VST3 + CLAP on Linux, GCC 13+. See the Linux toolchain memory for WSL specifics. |
| X2 | BLOCKED | linux | Zero-warning build at `-Wall -Wextra`. |
| V1 | BLOCKED | any | Patch state survives host save/reload — the v0.1 acceptance test in [PLAN.md](PLAN.md). |
| V2 | BLOCKED | any | Host-automatable parameters without a panel view (design note 4 in [docs/design-notes.md](docs/design-notes.md)). |

---

## Release & distribution — blocked on V1 (nothing to ship yet)

The plan is [docs/distribution.md](docs/distribution.md). R1 is the only item
with lead time and the only one Jeff can do now; the rest wait for a v0.1 that
passes V1. CI automation additionally waits on C7 — the interim path (build on
each box, `gh release upload`) is in the doc.

| ID | Status | Plat | Item |
|---|---|---|---|
| R1 | NEEDS-JEFF | — | **Signing identities and accounts.** (a) May TIDE sign under the existing Azure Trusted Signing account (`SynthEditTrustedSigning`, profile `SynthEditCertificateProfile`, `SE16/SynthEdit_store_win.yml:205-207`)? The cert subject is the publisher name users see — decide if TIDE ships under it or gets its own profile. (b) Confirm the Apple Developer ID in `SynthEdit_cmake_mac.yml` may sign TIDE, and that notarization credentials can be issued for CI. (c) App Store listing for the iOS AUv3 (needed by M2, not v0.1). (d) When C7 lands, re-create these as GitHub Actions secrets in the public repo. |
| R2 | BLOCKED | win | **Windows installer.** Inno Setup, modelled on `SE16/SynthEdit2/installer/SynthEdit2.iss`. Installs `TIDE_VST3.vst3` to `Common Files\VST3`; sign the .vst3 and the installer via Azure Trusted Signing. Also produce `TIDE-Windows.zip` for users who refuse installers. Constant asset names — the version lives in the tag and the version resource, per the doc. |
| R3 | BLOCKED | mac | **macOS pkg.** AU → `Components`, VST3 → `VST3`; Developer ID sign, **notarize and staple** — model on `SE16/SynthEdit_cmake_mac.yml`. AUv3 is deliberately absent: it ships in the container app with M2. |
| R4 | BLOCKED | linux | **Linux tarball.** `TIDE-Linux.tar.gz` with the VST3 and CLAP bundles and a short `install.sh` targeting `~/.vst3` / `~/.clap`. No signing. |
| R5 | BLOCKED | any | **Release workflow.** Tag `v*` → build + sign every platform → one GitHub Release with constant-name assets + `SHA256SUMS.txt`. Runs on tag push only, never on PRs, so fork PRs never see signing secrets. Needs C7 for public CI builds; until then the same release page is fed by `gh release upload` from the platform boxes. |
| R6 | BLOCKED | any | **Website Downloads section.** Replace the "nothing to download yet" card with static `releases/latest/download/<asset>` permalinks — no JS, no version maintenance, keeps the "no cookies, no scripts, no tracking" footer honest. iOS is a plain text App Store link (Apple's badge image would be the page's first external resource — do not use it). |

---

## Done

| ID | Done | Plat | Item |
|---|---|---|---|
| S1a | 2026-08-07 | win | **The module scan and cache are gone from TIDE.** Deleted the `semFolder` assignment, S4's `isSemFolderOverridden` flag, and the `LoadOrScanModuleData()` call from `TideApp::InitInstance` — `SE16` commit `d67bdfbab`, TIDE's own file only. **§9 verified, adapted:** `ModulePath` cannot be pointed anywhere in TIDE (`Application.cpp:139` returns the Documents folder for every settings key), so instead of the as-written recipe, the edited build was run with TIDE's cache file deleted — the module browser renders **pixel-identical** (screenshot SHA-256 match) to the scanning build, and a full REAPER run writes **nothing** under `ProgramData\SynthEdit`. Category tree observed: All/Controls/Conversion/Diagnostic/Effects/Experimental/Filters/Flow Control/Input-Output/Logic/Math/MIDI/Modifiers/Old/Special/Waveform. Per-category contents not audited (A1's soundcard-trio check still wants doing — it belongs to S1b, which this unblocks). TIDE, TIDE_VST3, SynthEditCL all build. Also merged the stranded S4 branch first — see the JOURNAL entry. |
| S4 | 2026-08-07 | linux | **TIDE no longer clobbers the installed SynthEdit's module cache.** `TideApp::InitInstance` now sets `BundleInfo::isSemFolderOverridden = true` beside the existing `semFolder` assignment, so `SemCacheName()` restores its `-override-<hash>` suffix and TIDE gets its own cache file instead of reading and writing the desktop app's `Plugin-Cache-16.xml`. One line in `SE16/SynthEditSem/TideApp.cpp` (ALLOWED); nothing shared touched. Verified by linking the **real** `SemCacheName()` out of `libEditorLib.a` and calling it with the flag both ways — `Plugin-Cache-16.xml` before, `Plugin-Cache-16-override-14603581876e07dd.xml` after. TIDE, TIDE_VST3, SynthEditCL and SynthEditWayland all build on Linux, zero warnings. Spun off X3. See the JOURNAL entry for 2026-08-07. |
| C1 | 2026-08-07 | win | **LICENSE added to `SynthEditLib`** per L1's same-day resolution: ISC, byte-identical to gmpi_ui's text, pushed straight to that repo's `main` (`a2143a4`) at Jeff's direction. The only carve-out stage that needed no C0 approval — it moves no code. |
| W1 | 2026-08-07 | win | **tidesynth.com holding page.** Built at [website/index.html](website/index.html) — one self-contained file, no build step, no JavaScript, and **zero external requests** (verified: no `<script>`, `<link>`, `@import`, `<img>` or `<iframe>`; the only URLs are outbound `<a href>` links). Says what TIDE is, says plainly it is free, links the repo, and carries a donation link as a plain `<a href>` with the destination left as `TODO(jeff)` — the last placeholder. Originally shipped without the phrase "open source" (L1 was unresolved); same-day, L1 landed as ISC and the page now says it, linking the LICENSE. Deployment: GitHub Pages, via [.github/workflows/pages.yml](.github/workflows/pages.yml); go-live checklist is **H1**. |
| P4a | 2026-08-07 | win | **Fixed the resize crash in `gmpi_ui`.** `DrawingFrame::reSize` (`backends/DrawingFrameWin.cpp`) now re-reads `d2dDeviceContext` **and** `swapChain` after `SetWindowPos` returns, instead of trusting the check it made before — `SetWindowPos` *sends* `WM_SIZE`/`WM_PAINT`, and those handlers release the device. Also refuses degenerate and over-limit extents (`maxSwapChainDimension = 16384`), and closes the second latent deref where `swapChain` was never checked at all. **Not verified against the original failure — see P4c.** Committed in `gmpi_ui`, not pushed. |
| P4b | 2026-08-07 | win | **`checkSizeConstraint` now honours the VST3 contract.** `GMPI_Wrappers/wrapper/VST3/SEVSTGUIEditorWin.cpp` writes the nearest acceptable size back into the rect instead of returning `kResultFalse` with it untouched, so a host has something to negotiate with. **Not verified against the original failure — see P4c.** Committed in `GMPI_Wrappers`, not pushed. |
| P4 | 2026-08-07 | win | **Diagnosed the editor-resize crash.** Root cause found from P2's minidumps: a time-of-check/time-of-use bug — `DrawingFrame::reSize` verifies its D2D device context, then calls `SetWindowPos`, which synchronously dispatches `WM_SIZE` into a handler that releases the device, then uses the freed pointer. Full stack, locals and reasoning in [docs/p4-resize-crash.md](docs/p4-resize-crash.md). **Not fixed** — both files are in `gmpi_ui`/`GMPI_Wrappers`, outside the run prompt's ALLOWED *and* GATED lists, and nothing in `SE16/SynthEditSem/` is involved. Spun off **P4a**, **P4b** and **G3**. Did land the one in-scope part P4 asked for: Release builds now emit a PDB (`SE16/SynthEditSem/CMakeLists.txt`), verified with TIDE, TIDE_VST3 and SynthEditCL all building at exit 0. See the JOURNAL entry for 2026-08-07. |
| P2 | 2026-08-06 | win | **Load TIDE in a DAW and record what actually happens.** Loaded the P1 Release and Debug builds in a portable REAPER 7.78; it instantiates and opens its editor. Findings in [docs/state-of-the-prototype.md](docs/state-of-the-prototype.md). Spun off P4 (host-killing crash on editor resize), P5 (plugin is not named TIDE) and U1 (one-view UX gaps); added runtime evidence for S1/S2 and V2. See the JOURNAL entry for 2026-08-06. |
| P1 | 2026-08-06 | win | **Verify the prototype still builds.** Clean CMake configure + Release and Debug builds of `TIDE` (GMPI) and `TIDE_VST3`, both exit 0 with zero warnings. Commands and the MFC/`afxres.h` trap recorded in [docs/building.md](docs/building.md). Spun off P3. See the JOURNAL entry for 2026-08-06. |
| S1 | 2026-08-06 | linux | **Design module enumeration without filesystem scanning.** Design note delivered as [docs/module-enumeration.md](docs/module-enumeration.md). Recommends the hybrid: static registry for modules (already the mechanism — the factory constructor registers all ~157 built-ins before any scan runs), bundle-local resources for prefabs. Nothing implemented, per the item. Spun off S1a, S4, S5. |
| S1 | 2026-08-06 | mac | *(duplicate run — macOS took S1 the same day, before the cron stagger took effect.)* Independent analysis reached the same recommendation; the additive findings are folded into the same note as **Addendum A1–A6** rather than landing a competing document. Spun off G2 and S6. See the JOURNAL entry for 2026-08-06. |
