# Backlog — done (archive)

Rows that have landed, newest first. Split out of [BACKLOG.md](BACKLOG.md) by
**A8**, 2026-08-12, when that file reached 76 KB and every run on three machines
was reading all of it to find the handful of rows it could act on.

**Rows here are verbatim** — archiving never rewrites one. A row arrives here
only when its work has *landed*: `IN-REVIEW` means the PRs are open, `DONE`
means they merged. Flipping a row and moving it is a STEP 4 chore, described in
[BACKLOG.md](BACKLOG.md).

This file is history, not queue. An agent picking work reads
[BACKLOG.md](BACKLOG.md); come here for precedent — how a past stage was
verified, what a previous run measured, why a decision went the way it did.

## Done

| ID | Done | Plat | Item |
|---|---|---|---|
| C12a | 2026-08-15 | any | **Landed 2026-08-15 (windows) — [SynthEdit#18](https://github.com/JeffMcClintock/SynthEdit/pull/18) + [TideSynth#60](https://github.com/JeffMcClintock/TideSynth/pull/60), both merged. Originally recorded as: **Done 2026-08-15 (windows) — [SynthEdit#18](https://github.com/JeffMcClintock/SynthEdit/pull/18) carries the four deleted lines and the comment; merging it alone is safe, nothing else depends on it.** 41 → 37 `${EDITOR_DIR}` entries. **All three delisting claims were re-verified rather than taken on trust:** `GuiPin.h` has zero includers across all five repos; `Module_Info_Plugin` is unreachable by two routes, not one — its only construction site is commented out at `ModuleFactory_Editor.cpp:1320` **and** the `switch` there has no `case 3` for its class-type id at all; the two `resource.h` copies are 361 lines each with all **318** `ID_*`/`IDR_*` constants byte-identical, differing only in a comment space and `_APS_NEXT_RESOURCE_VALUE` (210 private, 207 public) — the gap that is **P9**. **The Accept below said 905/905 and that was wrong in a way worth keeping visible:** it also said this removes a TU, and both cannot hold — the measured result is **904/904 RC=0, ctest 92/92**, i.e. the 905 baseline minus exactly `Module_Info_Plugin.cpp.obj`. **904/904 and 92/92 is the baseline for C12b onward**; a later stage reporting 905 has measured a tree without C12a in it. `SynthEdit2.vcxproj`/`.filters` mention none of the four, so no non-CMake build edit was needed, as the scoping run predicted. Original row follows. **Delist the dead and the duplicate — nothing moves.** Delete four lines from `SE16/EditorLib/CMakeLists.txt`: `${EDITOR_DIR}/GuiPin.h`, `${EDITOR_DIR}/resource.h`, `${EDITOR_DIR}/Module_Info_Plugin.cpp`, `${EDITOR_DIR}/Module_Info_Plugin.h`, and update the stage comment at `:31-38` to point at the plan doc. Evidence for each being dead or duplicate is in [docs/c12-remaining-editor-files.md](docs/c12-remaining-editor-files.md); do not take it on trust, the checks are two greps. **Do this one first** — it makes every later stage's counting simpler and it is the only sub-stage that cannot break anything. **Scope** four lines in one file, in `SE16` only. **Accept** 41 → 37 `${EDITOR_DIR}` entries; fresh Ninja tree 905/905 RC=0 and the three suites (58/58, 24/24, 10/10) green. Needs the build, not just a configure: dropping `Module_Info_Plugin.cpp` removes a TU from EditorLib and so is a real link-surface change. **Size** minutes. |
| C5 | 2026-08-14 | win | **Done 2026-08-14 (windows) — [SynthEdit#16](https://github.com/JeffMcClintock/SynthEdit/pull/16) + [SynthEditLib#7](https://github.com/JeffMcClintock/SynthEditLib/pull/7), which must merge together.** Fourteen files moved; C9 finished for its third and last user (`Application.cpp`), proven by a positive control — probe TU prints **0** without the injection and **183** with it, and the define is on 57 build statements, all EditorLib. Verified: fresh Ninja tree **905/905 RC=0** with five products, `dsp_tests`/`synth_ui_tests`/`ui_tests` **58/58, 24/24, 10/10** all RC=0, and **SynthEdit2 (WinUI3) links** RC=0. **First stage to reduce the public repo's dangling-private-include count rather than grow it: 59 → 54** (closed 15, opened 10; both sides read from git refs). No `.vcxproj` or Xcode edit was needed — neither project lists any of the fourteen. **Two things it surfaced, neither fixed here:** C5 does **not** finish the conversion (41 `EDITOR_DIR` entries remain, no stage owns them) → **C12**; and `ApplySynthEditConfig.cpp` gives **C11**'s unruled `SynthEditApp.h` licence gate a **second** public-side call site, so C11 got wider, not just longer. Original row follows. Move the app base (`SynthEditAppBase`, `ApplySynthEditConfig`, `SynthRuntime_editor`, `UIoManager`, `IO_base`, `IO_None`). **`Application.cpp` uses `se_build_number.h` live — see C9.** |
| S2 | 2026-08-14 | any | **Done 2026-08-14 (linux) — [docs/sandbox-audit.md](docs/sandbox-audit.md), PR [#54](https://github.com/JeffMcClintock/TideSynth/pull/54). Constraint 4 is measurably VIOLATED and cannot be marked verified.** **Verification artifact — the file set is derived, not grepped:** TIDE's link closure read off `build.ninja` (`VST3_Wrapper` + `SynthEditLib` + `EditorLib`, and **no** `.sem` module libraries), giving **264 translation units** from `compile_commands.json`, **230** first-party after excluding the VST3 SDK and generated Wayland C, **zero unresolved** paths. **202 categorised hits across 46 files**; then the DWARF compile-unit list from the unstripped `TIDE_VST3.so` separates linked from merely-compiled — **37 of those 46 files are in the binary, 9 are not**. Re-runnable: `python3 tools/sandbox_audit.py --build <tree>`. **The violation is on disk, not inferred: ~12.8 MB across 139 files** — `~/SynthEdit Projects/skins/` (74 files, 724 KB), `~/SynthEdit Projects/Prefabs/` (35, 1.1 MB), `~/.local/share/SynthEdit/` (30, 11 MB, six ~330 KB module caches). **Rulings: 7 remove, 4 stub, the rest keep.** **Most findings attach to rows that already exist rather than creating new ones** — S7 gets its gated remainder named to the line (`SkinMgr.cpp:28-32`, `:48-100`, plus 27 `SkinMgr` symbols and the literal `SynthEdit Projects` measured *in* the TIDE binary); S1b (b)/(c) reproduced on a **second platform** (`ScanFolder`/`LoadModuleData`/`Module_Info3` present, `dlopen`/`dlsym`/`dlclose` imported) **and S1b (a) confirmed genuinely done** (`FileWatcher` — zero symbols, not a linked CU); S8 reproduced on Linux (`ug_soundcard_in` 28, `ug_soundcard_out` 33, `ug_midi_out` 24, `OscillatorNaive` **0**) and gains a fourth module, `ug_wave_recorder`; S3 gains its evidence; C5 inherits three sites. **One genuinely new finding, A2, and it needs a ruling:** `BundleInfo.cpp:343-352` uses `getpwuid` over `getenv("HOME")` with a comment stating it does so *because* getpwuid defeats the sandbox — correct for SynthEdit, contrary to constraint 3 for TIDE, and upstream of both folder-copy findings, so S7 cannot be fixed properly without deciding it. **Measured from the 2026-08-10 build tree, because SE16 no longer configures on linux ([#53](https://github.com/JeffMcClintock/TideSynth/issues/53), filed this run); drift measured rather than assumed — `SynthEditLib`'s source list is unchanged since then and `EditorLib`'s differs only by the two `if(APPLE)` `.mm` files.** **Original item follows.** **Audit every filesystem and cache write** reachable from a TIDE build. Grep for `GetHomeDir`, `AppData`, `%TEMP%`, `~/Library`, `CreateFile`, `fopen`, registry access. Produce `docs/sandbox-audit.md` listing each hit as keep / stub / remove. Constraint 4 in [PLAN.md](PLAN.md) cannot be verified without this. |
| E1a | 2026-08-14 | any | **DONE on the macOS box 2026-08-14 — [#52](https://github.com/JeffMcClintock/TideSynth/pull/52). The answer was "do not widen the RMS gate".** The first cross-platform render in the harness's life: macOS against the Linux goldens, three independent engines (local Release V1.6.175, local Debug V1.6.182, published V1.6.183 — all agreeing, so engine version and build config are ruled out). `voice_midi_note`, whose residual is pure rounding, came back at **RMS −123.1 / peak −90.3 dBFS** — exactly 1 LSB on 0.053% of samples against the ~10.7% the −100 dBFS gate allows, a **200× margin**. Finding (e)'s premise (1 LSB on *every* sample) is simply not what cross-platform drift looks like, so the gates stayed at −100/−86. **What did need fixing was a different class of residual nobody had modelled:** `osc_naive_sine` failed at **RMS −73.5 / peak −68.7 dBFS**, and the residual **grows linearly with render duration** (−96 dBFS in the first 0.1 s block, −69 dBFS in the last, zero best-fit lag) — a 0.15 ppm frequency offset, ≈2.5 ULP at single precision, integrating into phase error. **No fixed dBFS number describes a free-running oscillator**, so the fix is a per-case tolerance override with a written reason that the harness prints and reports, not a wider global gate — widening globally would have to reach −67 dBFS and sail past finding (b)'s reference defect at −80.8 dBFS. Five negative controls plus a new `--selftest`; reasoning and numbers in findings (f) and (g) of [docs/e1-verification-harness.md](docs/e1-verification-harness.md). **Original item follows.** **Not work any linux run can do — and it was the linux NEXT pointer for two days. Corrected in prose 2026-08-13.** *(Left as `any` deliberately: the Platform vocabulary is `any`/`win`/`mac`/`linux` and **has no way to say "any box except linux"**, which is exactly what this row is. Tried `mac/win` and A3's `check-backlog-diff.py` correctly rejected it — only the Status cell may change on an existing row, and that value is outside the documented set anyway. So the constraint lives here in the Item text, where a reader will actually hit it. A one-cell change is Jeff's to make if he wants it.)* The Accept clause below is "one render of both cases **on a second platform**" — second *to linux*, which is the only lane that has ever run. So the linux box is the one machine that is definitionally unable to satisfy it, and the NEXT block's old reasoning ("this is the box that can actually test cross-platform") was backwards. Confirmed on the linux box before falling through to P7c: no wine, no container runtime, no macOS emulation — and **Wine would have been worse than nothing anyway**, since its `msvcrt` routes `sin`/`cos`/`exp` to glibc's libm, i.e. to the exact implementation the linux golden was rendered with, so a Wine render would report near-zero drift and license a tolerance that a real Windows box then fails. **Whichever of mac or win picks this up: the render half is the cheap half** (download the engine, run `tools/render_harness.py`, record two numbers); the judgement half is setting the RMS gate from that data. Original row follows. **The audio harness's two null tolerances disagree about cross-platform float drift.** `render_harness.py` justifies its peak gate as tolerating the ~1 LSB (−90.3 dBFS at 16-bit) that cross-platform rounding can legitimately produce. True of the peak gate; **false of the RMS gate beside it.** Measured during E1: 1 LSB on *every* sample gives RMS −90.3 dBFS, which fails the −100 dBFS gate; solving `rms = sqrt(fraction)` shows the RMS gate tolerates 1-LSB error on at most **~10.7% of samples**. Since only the Linux lane has ever run, this is the most likely cause of a spurious failure the first time mac or Windows renders a case — and it will look like a real regression. **Do not just widen the number**: E1 deliberately left it alone because picking a threshold with zero cross-platform measurements is guessing at the definition of "regression". **Scope** `tools/render_harness.py`. **Accept** one render of both cases on a second platform, its measured RMS and peak residuals recorded, and the tolerances set from that data with the reasoning written down. **Size** one session, but it is blocked in practice on someone running the harness on mac or win — that half is cheap (the engine is a download, not a build). See [docs/e1-verification-harness.md](docs/e1-verification-harness.md) finding (e). |
| A11 | 2026-08-13 | any | **All three boxes confirmed 2026-08-13 — linux, win, then mac. Ready to rotate into BACKLOG-DONE.md.** **SSH remotes bypass A2 entirely, and every check A2 was closed on is blind to it.** Found 2026-08-13 on the linux box, interactive session. A credential helper keyed to `credential.https://github.com.helper` is **never consulted for a `git@github.com:` URL**, so on any repo with an SSH remote a run authenticates with Jeff's key and pushes through his admin bypass. **Both existing guards pass anyway:** STEP 0.7's `gh api user` answers `tide-rack-bot` because `gh`'s API path reads `GH_TOKEN` and never touches git's transport; and the PR-authorship check A2 was flipped DONE on reads `tide-rack-bot` because author/committer come from the four `GIT_*` exports, which are set unconditionally. **Authorship proves authorship, not authentication** — correct `git log`, correct assertion, ruleset bypassed. On linux **8 of 9 repos were SSH** (`SynthEditLib`, `SE16`, `gmpi_ui`, `GMPI_Wrappers`, `GMPI`, `GMPI_Adaptors`, `GMPI-plugins`, `gimpi_ui_tests`); only `TideSynth` was HTTPS, which is the sole reason linux runs looked clean — the one repo they touched was the one repo covered. **Scope:** one `git config` line per box, plus [docs/a2-actor-separation.md](docs/a2-actor-separation.md) and STEP 0.7 in [docs/weekly-run-prompt.md](docs/weekly-run-prompt.md) (both already corrected — setup step 3 and a second assertion). **linux: DONE 2026-08-13. win: DONE 2026-08-13, interactive session with Jeff.** All 22 local repos on the Windows box (not just the 5 in the fleet's usual scope — the full `find C:\SE -name .git` sweep) already used HTTPS; nothing there was actually exposed. Applied the global rewrite anyway, since the acceptance test wants it as a structural safeguard against a future SSH clone, not just a fix for today's state. Full three-way proof run against the private `SynthEdit` over HTTPS: bogus `GH_TOKEN` → `fatal: Authentication failed` (exit 128, proves `gh` is genuinely consulted); real bot token → succeeds, `gh api user` confirms `tide-rack-bot`; no token → succeeds, confirms `JeffMcClintock`. **mac: DONE 2026-08-13** — swept, then fixed by Jeff mid-session and re-verified. The sweep ran (`find ~ -maxdepth 5 -name .git`, **28 repos**): **zero SSH GitHub remotes** — all nine fleet repos and the eight `VST_SDK` submodules are `https://`, same picture as Windows, not linux. **So the macOS A2 evidence is NOT void after all** — `gmpi_ui#3`/`#4` and `GMPI_Wrappers#1`/`#2` were pushed from HTTPS remotes and did authenticate as the bot. The read-only three-way proof also passes here in full (bogus token → `fatal: Authentication failed`; real token → succeeds as `tide-rack-bot`; no token → succeeds as `JeffMcClintock`), so setup steps 1–2 were already correct and only step 3 was missing. Jeff applied it mid-session; re-verified after: assertion 2 prints `git@github.com:`, all nine fleet repos still `https://`, an explicit `git@github.com:…` URL now **rewrites** to `https://…` (the safeguard proven working, not merely absent), and the three-way proof re-run unchanged. **The run halted first, correctly** — STEP 0.7 fires before STEP 1/2, so a scheduled mac run detects this gap but is forbidden from fixing it; it was cleared only because a human was at the keyboard, which an unattended 03:00 run would not have. That residual risk is **A12**, not this row. Evidence and full sweep: JOURNAL 2026-08-13 macos part 1. **Accept:** on each box, `git config --global --get url."https://github.com/".insteadOf` prints `git@github.com:`, and `git -C <repo> ls-remote --get-url origin` starts with `https://` for all nine repos. Prove the wiring read-only, no push needed: against the private `SynthEdit` over HTTPS a bogus `GH_TOKEN` must **fail** auth (that failure is the proof git consults `gh` at all rather than the SSH key or keyring), the real token must succeed, and no token must succeed as Jeff. **Size:** minutes per box; each is its own short session. **Flip DONE only when all three are confirmed.** **Consequence for Jeff, not for runs:** his interactive pushes now resolve through `gh`'s keyring token, which has no `workflow` scope, so a commit touching `.github/workflows/**` is rejected until he runs `gh auth refresh -h github.com -s workflow` once — `SE16` holds nine workflow files and is where it bites. Same scope wall C9(a) already hit from the other side. **Did not bite on Windows** — checked before applying: `gh auth status` there already shows `workflow` in the token scopes, so nothing changed for Jeff's own `.github/workflows/**` edits on that box. **A global rewrite, not per-repo `remote set-url`:** a fresh clone defaults back to SSH and would re-open the hole with no signal. |
| A13 | 2026-08-14 | any | **Done 2026-08-14 (windows) — fixed, and a second defect in the same function found and fixed with it. PR: [#51](https://github.com/JeffMcClintock/TideSynth/pull/51).** **Verification artifact — A/B against GitHub's own renderer, not against a belief about the algorithm:** the contents API with `Accept: application/vnd.github.html` returns each file rendered by GitHub's real markdown pipeline with its anchors intact, and **254 of 254 headings across all 28 markdown files now match, zero mismatches**. Two negative controls prove the A/B discriminates rather than passing vacuously: the old slugger gets **111 headings wrong across 23 files**, and not skipping fenced code gets **34 wrong across 4**. *(Method note for whoever repeats this: the `/markdown` API is **not** an oracle — it renders headings with no `id` at all. Use the contents endpoint.)* **The second defect:** `anchors_of()` never tracked code fences, so nine fenced lines were registered as real anchors — including `JOURNAL.md`'s own template heading `## YYYY-MM-DD — <machine> — <BACKLOG id>` and `#include "it_empty.h"` in `docs/c8-it-empty-header.md`. That is a false *negative*: a link to `#include-it_emptyh` passed. Same both-directions character as the slug bug, same function, so fixed in the same pass. **Also added `--selftest`** — six golden slugs read off GitHub's renderer plus a duplicate/fence case, offline and baked in, so the regression is permanent rather than a thing someone has to remember to re-measure; confirmed discriminating (5 of 6 cases fail when slugify is reverted to the old algorithm, RC=1). Deliberate-break demonstrations all pass: a nonexistent anchor fails (RC=1), a link in the old collapsed form now correctly fails (RC=1) — that is the false negative closing — and the correct GitHub form passes (RC=0). Tree is clean: 186 links, 0 broken, and the single previously-flagged line (`JOURNAL.md:376`) was read and confirmed to be exactly the false positive this row predicted. Original row follows. **`check-links.py`'s slugger disagrees with GitHub's on every em-dash heading — so the link check is red on `main` today, and is wrong in both directions.** Found 2026-08-13 by the P7c run, which hit it as a pre-existing failure and did not cause it. `scripts/check-links.py:44` does `re.sub(r'\s+', '-', s)`, collapsing a **run** of whitespace to ONE hyphen; GitHub emits one hyphen **per space**. Line 43 has already deleted the em-dash, so `## 2026-08-13 — macos — S6 (part 2 of 2)` leaves two spaces where the dash was and the script produces `2026-08-13-macos-s6-part-2-of-2` while GitHub produces `2026-08-13--macos--s6-part-2-of-2`. **Every journal entry heading and most doc headings in this repo use em-dashes**, so this hits all of them. **It is wrong in both directions, which is the part that matters:** a correct link is reported BROKEN (false positive — [JOURNAL.md](JOURNAL.md)'s S6 part-2 cross-reference, the first intra-journal anchor anyone wrote, which is why A3 could honestly claim zero false positives when it landed), and a link written in the script's own single-hyphen form would PASS the check and be **broken on GitHub** (false negative). A3's row is explicit that a noisy lint erodes trust in the other three checks fast, so this is worth fixing before A4 makes any of them a merge gate. **Scope** `scripts/check-links.py` — no workflow edit needed, the script is called by an existing step, so a scheduled run **can** do this one. **Accept** the checker reproduces GitHub's slug for a heading with em-dashes, parentheses and digits; the existing S6 link passes; a deliberately wrong anchor still fails; and the whole tree comes back clean (read every previously-flagged line and confirm each was a false positive, per A10's standard). Worth checking the same line against GitHub's other rules while in there — consecutive spaces, trailing punctuation, and duplicate headings, which get a `-1` suffix. **Size** well under one session. |
| P6 | 2026-08-14 | mac | **Fixed — confirmed 2026-08-14 (macos) on the Xcode generator, which is what the row was waiting for. PR: [#50](https://github.com/JeffMcClintock/TideSynth/pull/50) (docs only — no code change in any repo; the fix had already landed as SynthEdit's own `691270c5d` + `4792f4bf2`).** `cmake -G Xcode` + `--target SynthEditCL` at `SE16` `b3c1efb07`, universal `x86_64;arm64`, Debug: **RC=0**, and the log carries the step Ninja never emits — `CodeSign …/SynthEditCL.app`, `Signing Identity: "Sign to Run Locally"`. `codesign --verify --deep --strict` returns *valid on disk* / *satisfies its Designated Requirement* (336 sealed files, 98 signed `.sem` in `Contents/PlugIns`), and the signed binary runs (`SynthEditCL V1.6.182`). `Contents/MacOS` now holds **only** the executable; `Prefabs`, `fonts`, `skins` and `templates` are all under `Contents/Resources`. **Verification artifact — A/B positive control, same binary, same codesign command, no source edit and no rebuild:** copied the built bundle, moved the four staged dirs back under `Contents/MacOS/Resources`, re-ran codesign → **RC=1** with this row's error string verbatim, naming the same file (`code object is not signed at all` / `In subcomponent: …/Contents/MacOS/Resources/Prefabs/Controls/Button Small2.syntheditprefab`); current layout → **RC=0**. So the staging path is what closes it, not a toolchain difference. `4792f4bf2`'s detritus scrub holds too: 0 `.DS_Store`, no xattrs in the bundle. **Standing rule met, and more strongly than any previous mac run could state it:** all five products build under Xcode and verify — `SynthEditCL`, `SynthEdit_VST3`, `SynthEdit_GMPI`, `TIDE`, `TIDE_VST3`, each RC=0. **Nothing in `SE16` was edited** — `SynthEditCL/CMakeLists.txt` is on neither the ALLOWED nor the GATED list, so GATED by default, and none was needed. **The generalisable lesson: Ninja emits no `codesign` invocation at all, so no Ninja build can confirm or deny a signing bug on mac** — that asymmetry is why this sat open for six days through two RC=0 reports. Evidence: JOURNAL 2026-08-14 macos. Original row follows. **`SynthEditCL` does not build on macOS.** Not TIDE, and pre-existing — verified 2026-08-08 by stashing the S1b change and rebuilding, it fails identically. Compiles and links fine, then `CodeSign` fails: `SynthEditCL.app: code object is not signed at all / In subcomponent: …/SynthEditCL.app/Contents/MacOS/Resources/Prefabs/Controls/Button Small2.syntheditprefab`. The `stage_prefabs_SynthEditCL` build rule stages prefab **data** under `Contents/MacOS/Resources/`; `codesign` treats everything under `MacOS/` as code and refuses. Almost certainly wants `Contents/Resources/`. Matters to TIDE only because "leave SynthEdit, SynthEditCL and TIDE all building" is the standing rule and a macOS run cannot honour it for SynthEditCL today — say so rather than assuming. Lives in `SE16/SynthEditCL/CMakeLists.txt`, which is neither TIDE's nor on the GATED list; treat as SynthEdit's own bug and check with Jeff before touching it.  **2026-08-12 (P7a run) — this looks fixed, but not by me and not confirmed on the failing generator.** `SynthEditCL/CMakeLists.txt:187` now branches on `APPLE` and uses `$<TARGET_BUNDLE_CONTENT_DIR:SynthEditCL>` (i.e. `Contents/`), with a comment naming this row's exact error string; two commits landed for it — `691270c5d` "CL mac bundle stages Resources into Contents/Resources" (2026-08-08) and `4792f4bf2` "CL bundle fails codesign on staged Finder detritus" (2026-08-11, current `master` tip). A fresh **Ninja** build here compiled, linked and staged prefabs into `Contents/Resources/`, RC=0. **That is not proof:** the Ninja generator emits no `codesign` step at all, so it cannot reproduce the failure either way — the app came out unsigned. Confirming this needs an **Xcode**-generator build, which is what Jeff's own `SynthEdit/build` tree uses. Left TODO on purpose. |
| C4 | 2026-08-13 | win | **Done 2026-08-13 (windows) — twelve files moved, C9 implemented, everything rebuilt. PRs: [SynthEdit#15](https://github.com/JeffMcClintock/SynthEdit/pull/15) (deletions + `EditorLib/CMakeLists.txt` + vcxproj), [SynthEditLib#6](https://github.com/JeffMcClintock/SynthEditLib/pull/6) (the twelve files + `se_version.h`), [#49](https://github.com/JeffMcClintock/TideSynth/pull/49) (docs). All three must merge together.** **C3's two checks, run first.** Build files: `EditorLib/CMakeLists.txt` is the only build file that compiles any of the twelve; `SynthEdit2.vcxproj`/`.filters` carry three navigator-only `ClInclude` entries (repointed to the `..\..\SynthEditLib\` form C3 established); `SynthEditCL`/`SynthEditWayland` mention `SkinMgr` in comments only; **all 14 `project.pbxproj` in the repo: zero hits**, so the mac side needed no edit. Relative includes: two of the twelve use `#include "../"`, and only one resolves for real — `SE16/tinyxml2/` **does not exist**, so `ThemeManager.cpp`'s `"../tinyxml2/tinyxml2.h"` already resolves through a search path (`SynthEditLib/modules/se_sdk3_hosting/../tinyXml2/`) and a move cannot affect it; `SE16/se_build_number.h` **does** exist, which is C9. **C9 implemented as ruled (option (c)):** new `SynthEditLib/se_version.h` defaults `SE_APP_BUILD_NUMBER` to 0, `EditorLib/CMakeLists.txt` injects SynthEdit's real value by reading `se_build_number.h` where the three release workflows still grep for it — **no workflow file touched**. Injected rather than owned by the library on purpose: both uses are cache invalidation that tracks the *application* (`SkinMgr` copies skins out of the application's own `Resources` folder), so a library-owned constant would stop SynthEdit invalidating on upgrade — a behaviour regression dressed as a decoupling. **Verification artifact:** fresh scratch Ninja tree **905/905 RC=0**, five products; the six moved TUs verified compiled from `EditorLib.dir\C_\SE\SynthEditLib\`; `dsp_tests` 58/58, `synth_ui_tests` 24/24, `ui_tests` 10/10, all RC=0; **SynthEdit2 (WinUI3) MSBuild Release x64 RC=0** — the first carve-out stage to actually reach that link step, which C1b and C2 both had to leave open behind P8. C9 proven by probe: `se_build_number.h` says 183, `build.ninja` carries `SE_APP_BUILD_NUMBER=183`, a probe TU prints **0** with no injection and **183** with it, and the define appears on **57 build statements, all EditorLib** (PRIVATE holds). No line-ending churn: `core.autocrlf=false` in both repos, all twelve files `cmp`-identical to their originals. **Measured cost, filed as C11:** C4 closes 11 dangling private includes in the public repo and **opens 20** — net 47 → 56, measured from git refs on both sides. 18 of the 20 are headers a later stage already moves; **2 are not**, both in `MfcDocPresenter.cpp`. **Toolchain note:** `MSBuild SynthEditStore.sln` now needs the **VS 2026 (v145)** toolset — under VS 2022 it dies at `build\ZERO_CHECK.vcxproj` with `MSB8020` before reaching `SynthEdit2`, so P8's journal command no longer works as written on this box. Original item follows. Move views and browsers (`ModuleBrowser`, `PropertiesBrowser`, `MfcDocPresenter`, `ModuleFactory_Editor`, `SkinMgr`, `ThemeManager`). **Read C9 first — `ModuleFactory_Editor.cpp` and `SkinMgr.cpp` both use `se_build_number.h` live, and it does not survive the move.** Also run C3's two checks before moving anything: grep every build file for each candidate (C3 found `SynthEditDoc2` compiled by `SynthEdit2.vcxproj` as well as by EditorLib), and grep the candidates for `#include "../`, testing whether each target actually exists at that relative path — most resolve through the search path and are harmless, and the one that resolves for real is the one that breaks. |
| P7c | 2026-08-13 | linux | **Done 2026-08-13 (linux) — reproduced, guarded, tested. PRs: [gmpi_ui#5](https://github.com/JeffMcClintock/gmpi_ui/pull/5) (guard + test + audit), [#48](https://github.com/JeffMcClintock/TideSynth/pull/48) (docs).** **The audit's reading was right in every particular**, including that it would be a heap overflow rather than a null deref. **Verification artifact — A/B on the same harness binary, only the backend source differing: unfixed 3/3 `SIGSEGV` (exit 139), guarded 3/3 exit 0.** Under gdb the unfixed build faults in `encodeDirtyRect` (`CpuEncode.h:269`) from `present()` (`:1332`) with locals `right = 800, bottom = 600` against a 64×48 image — the clip clipping to the stale extents, exactly as predicted — at **row 68**, so rows 48–67 were written outside the surface before it hit an unmapped page. **The row's open question is answered: no.** A client callback *cannot* pump the X loop, and by construction: `ensureImage()` has one caller (`present()`), `present()` has two (`processEvents`/`onTimer`, both host-driven), the backend holds exactly **one** `XNextEvent` with no nested or modal loop (menus, dialogs, text edit and tooltip all share that pump; the portal chooser is async), and `IDrawingHost`/`IInputHost`/`IDialogHost` expose no way to pump, resize, or reach the `Display*`. **Guarded anyway, because of the residual the audit could not close:** a client may call back into the *host* during `measure`/`arrange`/`render`, and a host that re-enters its own run loop reaches `processEvents()` — gmpi_ui can neither prevent nor detect that, and "unreachable today" is a property a future backend change revokes silently. **Two corrections to this row's premises:** (1) `tests/x11_editor_host.cpp` is not in `gmpi_ui` — it is in **`GMPI_Wrappers`**, and it is a full VST3 host, which is the wrong tool: a real plugin client cannot reach the nesting at all. Followed the **P7b** precedent instead — a synthetic client driving the backend directly, no CMake, no VST3 (`gmpi_ui/tests/x11_present_reentrant_resize.cpp` + `run_x11_present_reentrant_test.sh`). (2) **AddressSanitizer is a false-negative machine on this path** — the image is MIT-SHM on a local display, and ASan does not instrument a `shmat()` mapping; the detector is the mapping's own end. That is the *second* time in P7 that ASan was blind to the crash it looks built for, for a different reason each time. **No regression to ordinary painting:** `tests/x11_menu_test.sh` passes in full with the guard in (menu, stock dialog, text edit, key listener, colour picker, tooltip, *"pixels changed by the editor: 18495"* — which is what proves the guard is not firing on normal paints). **Build health:** fresh scratch Ninja tree of `SE16` with `GMPI_UI_FOLDER_OVERRIDE` on this branch — **885/885, RC=0**, five products (`SynthEdit_VST3`, `SynthEdit_GMPI`, `SynthEditCL`, `TIDE.gmpi`, `TIDE_VST3.so`); the log confirms the changed file was compiled into it. Audit: [gmpi_ui/docs/x11-present-extents.md](https://github.com/JeffMcClintock/gmpi_ui/blob/main/docs/x11-present-extents.md), and the closing section of [docs/p7-resize-audit-mac-x11.md](docs/p7-resize-audit-mac-x11.md). Original row follows. **X11 `present()` caches extents across three re-entrant client calls** — the X11 half of P7, which the row always said a Linux run could take alone. `gmpi_ui/backends/DrawingFrameX11.cpp`: `pw`/`ph` are read at `:1262`, `ensureImage(pw, ph)` checks at `:1267`, then `d.client->measure` (`:1290`), `arrange` (`:1293`) and `render` (`:1303`) all re-enter client code, and `:1319` builds the destination surface from `d.image->data` and `bytes_per_line` — re-read, so a replaced image is picked up — but with the **stale** `pw`/`ph`. A nested `present()` at a smaller size would leave `encodeDirtyRect` writing past the new buffer: a heap overflow, not a null deref, so it is a worse outcome than P4's. `reSize` itself (`:932`) is clean, and clamps low (`max(1, …)`) but has no upper bound. **What the audit could not settle from the source, and is the first thing to check:** whether any client callback can pump the X event loop and actually cause a nested `present()`. On current reading it cannot — the host drives `processEvents`, and `invalidateRect` only marks dirty — so this may be unreachable-by-construction, which is a fine answer to record. The macOS box cannot build or run X11. `tests/x11_editor_host.cpp` already attaches an editor and is the place for the probe; copy the liveness discipline from `mac_editor_resize_host.mm`, not the Windows one. Size: one session. |
| S6 | 2026-08-13 | mac | **Done 2026-08-13 (macos) — folder deleted, 26 files / 4.4 MB. PRs: [SynthEdit#13](https://github.com/JeffMcClintock/SynthEdit/pull/13) (the deletion), [#47](https://github.com/JeffMcClintock/TideSynth/pull/47) (docs).** Chose **remove**, not the README alternative: constraint 7 rules out loadable module bundles entirely, so a README would preserve 4.4 MB of a contradiction. **Verified:** all six binaries `Mach-O 64-bit bundle x86_64` with macOS platform load commands, added 2021-02-24→03-03 and untouched since; SynthEdit, SynthEditCL and TIDE all still build (fresh Ninja, **RC=0, 936/936**, five products) and **no `CMakeLists.txt` references `SE_IOS_APP` at all**, so the decoupling is structural. **Two corrections to the row's premises, both material:** (1) the consumer is **`SeAudioUnitMacOS`, a macOS AUv3 app-extension** — *no iOS target references the folder at all*, so this was never wired into an iOS build in the first place; (2) besides the Run Script, **individual files inside the bundles are entries in that target's Resources build phase**, which the row did not know. **Honest A/B:** the target failed before (6 missing `SE_DSP_CORE/*.cpp`) and fails after (7 `CpResource` errors) — nothing went working→broken, but the failure mode *changed*, so this is not an identical-failure A/B. GATED remainder filed as **S9**; the much larger finding that the whole project is dead is **S10**. Original row follows. **Delete the dead iOS module artifact.** `SE16/SE_IOS_APP/TIDE/Plugins/` holds six checked-in `.sem` bundles that are all `Mach-O 64-bit bundle x86_64` in macOS bundle layout, installed by a Run Script (`SE_IOS_APP.xcodeproj/project.pbxproj:2064`) that copies them to `.../Contents/` — a macOS-only destination. Nothing there can load on arm64 iOS. It reads as a working iOS module story and is not one. Remove it, or add a README saying what it actually is. See addendum A5 of [docs/module-enumeration.md](docs/module-enumeration.md). **Unblocked** — G2 is resolved and `SE16/SE_IOS_APP/TIDE/` is now ALLOWED. But the Run Script phase that installs it lives in the *shared* `SE_IOS_APP.xcodeproj/project.pbxproj`, which is still GATED — delete the folder, then file the build-phase removal separately rather than editing the project file. |
| A6 | 2026-08-13 | any | **Live — [issue #44](https://github.com/JeffMcClintock/TideSynth/issues/44), daily rather than weekly, 5 of the 6 listed checks fully implemented; the 6th (missed run windows) is a deliberate, logged substitute, not a silent gap.** [.github/workflows/watchdog.yml](.github/workflows/watchdog.yml) (`schedule: 0 6 * * *` + `workflow_dispatch`) runs `scripts/watchdog-digest.py`, which regenerates the whole digest body from scratch every run — the row's own point, since the hand-maintained table it replaces was wrong within three days, twice. **Every check validated against real data before being trusted, not against synthetic fixtures:** the 72h-stale-DOING detection against a genuinely backdated commit (`git commit --date`); the IN-REVIEW merge check against a real merged PR (SynthEditLib#5) *and* a real closed-not-merged one (opened and closed [TideSynth#42](https://github.com/JeffMcClintock/TideSynth/pull/42) specifically to get one, since this project has never actually abandoned a PR before) — which caught a real bug, `.state` alone reports `closed` for both outcomes, only `.merged` distinguishes them. **Proven live on GitHub Actions across three separate runs on [PR #43](https://github.com/JeffMcClintock/TideSynth/pull/43):** first run created issue #44 with real content (correctly listed the PR itself under "open PRs by age"); second run (a further push) refreshed it in place — zero duplicates; a third run via `workflow_dispatch` after merging to `main` confirmed the final schedule-only trigger set works. **Two scope decisions, both logged rather than silently assumed:** **(a)** cross-repo checks (an IN-REVIEW row's PRs usually live in SynthEditLib/SynthEdit, not TideSynth) use the workflow's default `GITHUB_TOKEN`, which GitHub scopes to TideSynth alone — asked Jeff first rather than adding a credential unilaterally; his call was TideSynth-only for now, with unreachable repos reported inline as "cross-repo unavailable" in the digest itself rather than silently omitted. **(b)** "boxes whose run window passed with no journal entry" is not computable from this repo — each box's actual cron schedule lives in `~/.claude/scheduled-tasks/` on that box's own filesystem, not in git. Substituted with "days since each platform's last journal entry," labelled honestly as an approximation in the digest's own output, not a silent redefinition of the row. Original scope: **Watchdog: a GitHub-cron weekly digest, flag-only.** Runs on GitHub infra so no box needs to be awake. One digest issue per week, refreshed in place, containing: boxes whose run window passed with no journal entry or branch push; DOING rows older than 72h with no open PR (recommend the reset — never commit it; agents-write-main stays an invariant); `tide/**` branches across the five repos with no open or merged PR; IN-REVIEW rows whose PRs all merged; open NEEDS-JEFF/PROPOSED questions with ages; open PRs by age. This digest is also the single "awaiting Jeff" surface — generated, never hand-maintained (the hand-maintained state table was wrong within three days, twice). Scope: one workflow + script (explicitly authorized). |
| A3 | 2026-08-13 | any | **3 of the 4 specified checks are done, live, and proven on real CI — the 4th (bare ID cross-references, distinct from markdown links) is a deliberate, logged gap, not an oversight.** [.github/workflows/lint.yml](.github/workflows/lint.yml) runs on `pull_request`: `scripts/check-links.py` (pre-existing, A8), `scripts/check-journal-prepend.py`, `scripts/check-backlog-diff.py`, `scripts/check-prompt-provenance.py` — new. Each check runs independently (`continue-on-error` per step) so one PR sees all four results; a Summary step fails the job if any did. **The governing invariant for the two new diff checks turned out to be containment, not byte-equality** — BACKLOG-DONE.md's own rule is "archiving never rewrites a row", and checked against real history (A8's own rotation PR, and this session's own status flips) that means the original text is never *lost*, not that the cell is frozen: a status flip routinely elaborates in the same edit. A8's own distillation of 5 rows (N1/A2/R1/S1b/X4) to `docs/<id>.md` pointers correctly still fails the check — real content shrinkage, needs a human — while its 18 clean archives, and every ordinary status flip since, pass. **Verified on real GitHub Actions, not just locally:** [PR #41](https://github.com/JeffMcClintock/TideSynth/pull/41) — a clean PR ran all four green (`links: success`, `journal: success`, `backlog: success`, `provenance: success`); a follow-up commit that edited an existing JOURNAL entry in place with no trace correctly failed naming that exact entry, while the other three checks stayed independently green; reverting made it green again. Merged squash, matching the harness precedent (positive control, then prove the negative). **What's not done, filed as A10:** "BACKLOG/journal ID cross-references resolve" — verifying a bare prose mention like "see C9" actually names a row that exists. Original scope: **Always-green lint checks — the future auto-merge gate.** Scope: `.github/workflows/` (this row explicitly authorizes the workflow edit) plus small scripts. Four checks: JOURNAL diff is pure prepend-after-header; BACKLOG diff touches only status/date cells or adds rows; markdown links and BACKLOG/journal ID cross-references resolve; scheduled-run journal entries carry the `**Prompt:**` provenance line. Accept: all four pass on a clean PR and each fails on a deliberately malformed one (prove the negative, per the harness precedent). Size: one session. These are informational until A4 makes them required. |
| A5 | 2026-08-13 | any | **The mechanism was already implemented (`5799783`, 2026-08-09) but had never actually executed — every push since then hit the pre-C7 suppression, so the issue-filing code path was untested. Proven end-to-end 2026-08-13** with a disposable branch (`tide/_test/a5-issue-loop-verification`, deleted after) carrying a root `CMakeLists.txt` with an immediate `FATAL_ERROR`, which bypasses the suppression (that check is "file exists", not "build succeeds") without costing a real build. First push: [#39](https://github.com/JeffMcClintock/TideSynth/issues/39) (mac) and [#40](https://github.com/JeffMcClintock/TideSynth/issues/40) (linux) filed, correctly labeled, correctly excluding windows (`matrix.platform != 'win'`). Second push: **zero new issues** — both got a `Still failing as of <sha>` comment instead, confirming the dedup path. Both issues closed with the test explained; nothing to leave open. **Original item, kept for the record:** **Make the CI→platform-issue loop actually work — it has never executed.** Rides with B1 (same file). Scope: `.github/workflows/build.yml` (explicitly authorized). The job's token is read-only so `gh issue create` 403s invisibly under `continue-on-error`; zero issues have ever been filed. Add `permissions: issues: write`; search for an existing open issue for branch+platform and comment instead of duplicating; suppress filing while the failure matches the known pre-C7 signature (missing CMakeLists.txt) so the feed stays honest. Accept: one deliberate breakage files exactly one issue, a second run comments on it rather than duplicating, and STEP 1 on the owning platform picks it up. |
| E1b | 2026-08-13 | any | **Installed and verified live — [first run green](https://github.com/JeffMcClintock/TideSynth/actions/runs/31652146304)** on the push that installed it: engine download, package sanity check, both audio test cases rendered and null-tested against golden references, artifacts uploaded. `docs/ci/verify.yml` stays in place unchanged as the staging file for any future revision a scheduled agent proposes, since agents still cannot write `.github/workflows/**`. Original item: **Install the audio-verification CI workflow.** Ready to go at [docs/ci/verify.yml](docs/ci/verify.yml); copy it to `.github/workflows/verify.yml`. **No agent can do this** — STEP 5 forbids writing `.github/workflows/**` and the bot token carries no `workflow` scope, so it is enforced by the credential as well as the prose. Needs no secrets and no engine build: it downloads the published Linux engine from `synthedit.com/release_1_6/`, ~1 minute versus the ~45 an engine build costs. It also runs weekly on a `schedule:`, which is what stops a new engine silently invalidating every golden reference. Note the E1 findings mean CI is the *only* place the module set under test is guaranteed authoritative — see finding (d). |
| A2 | 2026-08-13 | — | **Done on all three boxes — the row's "macOS and Linux: token pending" was stale on both counts, caught 2026-08-13 by cross-checking PR authorship on GitHub rather than trusting the row.** Every commit on every merged PR from all three boxes is authored **and** committed as `tide-rack-bot`, not just opened as the bot with Jeff's identity on the commits: macOS — [gmpi_ui#3](https://github.com/JeffMcClintock/gmpi_ui/pull/3), [GMPI_Wrappers#1](https://github.com/JeffMcClintock/GMPI_Wrappers/pull/1)/[#2](https://github.com/JeffMcClintock/GMPI_Wrappers/pull/2), [gmpi_ui#4](https://github.com/JeffMcClintock/gmpi_ui/pull/4) (2026-08-10 through 2026-08-12, matching the P7/P7a/P7b journal entries under `## macos`); linux — [TideSynth#34](https://github.com/JeffMcClintock/TideSynth/pull/34) (2026-08-11, matching `## linux — E1`); windows — every `tide/win/**` PR this session. That is exactly what STEP 0.7's identity assertion is supposed to prove is happening, verified independently rather than assumed from the row. **`tide-rack-bot` is a collaborator on all five repos, its classic `repo`-scope PAT exists (expires 2026-11-07), and "Agent PRs only" rulesets are active on the four public repos.** Two permanent platform constraints remain, unaffected by this: fine-grained PATs cannot serve a collaborator, and `SE16` cannot have a ruleset at all without GitHub Pro; both are fixed by moving the repos under an organization, which is unforced follow-up, not blocking. Detail (now itself corrected): [docs/a2-actor-separation.md](docs/a2-actor-separation.md). |
| P7b | 2026-08-12 | mac | **Both PRs merged 2026-08-12** — [gmpi_ui#4](https://github.com/JeffMcClintock/gmpi_ui/pull/4) and [TideSynth#37](https://github.com/JeffMcClintock/TideSynth/pull/37); zero open PRs remain in any of the five repos. **Fixed, and no longer latent — it was reproduced first.** A guard now re-checks `backBuffer` after `drawingClient->render()` in `gmpi_ui/backends/DrawingFrameMac.mm`, and a regression test drives it: `gmpi_ui/tests/mac_render_reentrant_resize.mm` + `tests/run_mac_render_test.sh` (no CMake, no VST3, a synthetic client that resizes its own view from inside `render()` — no shipping client does, which is why P7 could only file this). **The audit named the wrong line and that changes where the fix goes:** the faulting call is `context.popAxisAlignedClip()`, *inside* the braces, because `gmpi::cocoa::GraphicsContext` keeps its own copy of the pointer in `cgContext_` and nothing nulls it; the two statements after the block read the member, which `onResize` does null, so they only pass CoreGraphics a NULL. A guard placed after the block — the obvious reading of the old row — would have fixed nothing. **Verification artifact:** A/B on the same test binary, Guard Malloc, **unfixed 3/3 `SIGSEGV`** (exit 139; crash report backtrace `CGContextRestoreGState` ← `popAxisAlignedClip` ← `onRender` ← `drawRect:`, `EXC_BAD_ACCESS`/`KERN_INVALID_ADDRESS`) vs **fixed 3/3 exit 0**, renderer provably live before (2 distinct colours) and drawing again after (3). **AddressSanitizer does not catch this** — the read is inside CoreGraphics, which it does not instrument; measured with a positive control (ASan PASSes the unfixed build, yet flags a hand-written read of the same freed pointer). **No regression to ordinary painting either:** P7's `mac_editor_resize_host`, built standalone and run against the `TIDE_VST3` from this build, **exit 0 3/3**. Correction and the ASan finding recorded in [docs/p7-resize-audit-mac-x11.md](docs/p7-resize-audit-mac-x11.md). PRs: [gmpi_ui#4](https://github.com/JeffMcClintock/gmpi_ui/pull/4) (guard + test) and [#37](https://github.com/JeffMcClintock/TideSynth/pull/37) (docs). |
| A1 | 2026-08-13 | — | **Done — verified live on GitHub, not assumed.** `gh secret list --repo JeffMcClintock/TideSynth` returns nothing at repo level; all 8 (`AZURE_TENANT_ID`, `AZURE_CLIENT_ID`, `AZURE_CLIENT_SECRET`, `APPLE_CERT_P12_BASE64`, `APPLE_CERT_PASSWORD`, `APPLE_ID`, `APPLE_ID_PASSWORD`, `APPLE_TEAM_ID`) live in a `release` environment with a required reviewer (Jeff), so only a job declaring `environment: release` can reach them, and that pauses for approval. **This row had sat at NEEDS-JEFF; the work was already done and nobody had flipped it — caught 2026-08-13 when Jeff checked GitHub directly against a summary that had just repeated the stale row.** Original item, kept for the checklist it documents: **Move the 8 signing secrets into a `release` environment with required review.** Jeff-side, minutes; step-by-step checklist in the review doc. No workflow references them yet, so the move is free. Every other row in this section widens the exposure window until this closes. Default in effect: secrets stay repo-level, reachable by any workflow edit on any `tide/**` branch. Decide-by: before A7 raises cadence or A4 enables any auto-merge. |
| P7a | 2026-08-12 | mac | **Done and verified; the numbers are a product decision and are Jeff's to overrule.** Bounded at **8192 logical points per axis** and **384 MiB of backing bitmap**, set from displays rather than from a graphics API — the widest single Mac display in points is a 6K Pro Display XDR in "more space" at 3840, and a full-screen editor on it at 2x reserves ~265 MiB, so both numbers clear the largest legitimate case with headroom while still biting on every rect the P7 audit exercised. Aspect ratio is preserved when the area budget bites. Clamped in **two** places deliberately: `resizeNativeView` (the wrapper's path) and `initBackingBitmap` (where the memory is actually reserved, and reachable without the wrapper because JUCE sets the view's frame directly). `checkSizeConstraint` now bounds on **both sides** of `measure` and writes the accepted size back, returning `kResultTrue` — the resizable branch was the sharp one, since `measure` returns whatever it is offered and the wrapper was therefore *affirmatively approving* 2178x32672. **Verification artifact — A/B on `GainGui_VST3`, same plugin, both Debug, same machine, "before" built from a worktree at the unmodified sources:** the view adopted `2178x32672`, `16385x600`, `600x16385` and `0x0` verbatim before and adopts `1829x6879`, `8192x600`, `600x8192` and `1x1` after; the `16385x600` paint cost **+253.4 MiB** before and **+128.3 MiB** after, peak resident **612.8 -> 362.5 MiB**. The before column reproduces the audit's +253 MiB figure to within 0.4 MiB — the positive control that harness and machine still match P7. `checkSizeConstraint(0,0,2178,32672)`: GainGui `kResultTrue`/**UNCHANGED** -> `kResultTrue`/**1829x6879**; TIDE_VST3 `kResultFalse`/**UNCHANGED** -> `kResultTrue`/**1829x600**. Negative control: a legitimate `640x480` still returns `kResultTrue` **unchanged**. `mac_editor_resize_host` exit **0, 3/3**, live before and still drawing after. **Builds:** fresh configure with all four local overrides, `ninja` no-target **RC=0** — `SynthEdit_VST3`, `SynthEdit_GMPI`, `TIDE`, `TIDE_VST3`, `SynthEditCL` and the test targets, so this run *can* honour the standing direction after all (see the P6 note). **Both code PRs merged 2026-08-12** at Jeff's direction, in the stated order: [gmpi_ui#3](https://github.com/JeffMcClintock/gmpi_ui/pull/3) then [GMPI_Wrappers#1](https://github.com/JeffMcClintock/GMPI_Wrappers/pull/1) (P7's test, which #2 was stacked on) then [GMPI_Wrappers#2](https://github.com/JeffMcClintock/GMPI_Wrappers/pull/2), which GitHub retargeted to `main` automatically. Re-verified from the merged default branches, not from the branches: fresh configure, rebuild, harness exit **0** with `checkSizeConstraint` reporting `1829 x 6879` adjusted and the view clamping to `1829 x 6879` / `8192 x 600` / `600 x 8192` / `1 x 1`. Row stays IN-REVIEW because **this repo's PR is still open** — it carries this row, the journal and the audit-doc note. **Merge note:** the bot could not merge — `405 At least 1 approving review is required` — so the rulesets hold and the merges were made on Jeff's own credential at his explicit instruction. Original spec below. <br><br> **Original spec:** **Bound the editor extent on macOS, and make `checkSizeConstraint` write back.** From the P7 audit — read [docs/p7-resize-audit-mac-x11.md](docs/p7-resize-audit-mac-x11.md) first, because the obvious fix (copy Windows' 16384 clamp) is wrong here. Windows' number is the hard Direct3D 11 texture limit; CoreGraphics has no equivalent wall — measured, it accepts 2178x32672, 16384x16384 and 65536x600, with a square limit of **131071** and a single-axis limit of **4194303**, because it reserves lazily. So there is no NULL to fall back on at large sizes and `onRender`'s `if(!backBuffer) return;` guard only ever fires at `0 x 0`. The cost is memory: resident size in the hundreds of MiB, one measured paint at 16385x600 costing **+253 MiB**. Any bound is therefore a product decision about how much an editor may reserve, not a technical ceiling — which is why the audit did not pick one. Second half: `SEVSTGUIEditorMac.cpp:79` never writes the rect back (pre-P4b shape), and for a resizable client returns `kResultTrue`, so it *approves* 2178x32672 — a writeback is only meaningful once a bound exists to write back to. Scope: `gmpi_ui/backends/DrawingFrameMac.mm`, `GMPI_Wrappers/wrapper/VST3/SEVSTGUIEditorMac.cpp` (both ALLOWED). Accept: `mac_editor_resize_host` still exits 0, and the `checkSizeConstraint` line reports the rect *adjusted* rather than UNCHANGED. Note the standing direction to rebuild SynthEditCL too — **P6** says it does not build on macOS, so say plainly what was and was not verified. Size: one session. |
| C3 | 2026-08-12 | win | **All three PRs merged 2026-08-12** — [SynthEditLib#5](https://github.com/JeffMcClintock/SynthEditLib/pull/5) and [SynthEdit#11](https://github.com/JeffMcClintock/SynthEdit/pull/11) (the code, merged together as required), plus [TideSynth#38](https://github.com/JeffMcClintock/TideSynth/pull/38) (this coordination row). C4 unblocked. **Verified, Release x64:** `EditorLib.lib`, `SynthEditCL.exe`, `TIDE_VST3.vst3` and `SynthEdit2.exe` all build; `ctest` **92/92**; positive control — hiding `SynthEditLib/DocOb.cpp` fails the build naming that exact path, restoring it builds clean. **26 of 27 files byte-identical** (SHA-256, line endings normalised); the 27th, `SynthEditDocBase.cpp`, loses one dead line. mac/iOS/linux **unverified** — not buildable here. **Three findings for the stages after this one:** **(a)** the C2 "nothing outside EditorLib compiles it" test caught exactly one file — `SynthEditDoc2`, also compiled by `SynthEdit2.vcxproj` by a `SynthEdit2/`-relative path; run that grep at C4/C5 rather than assuming. **(b)** `se_build_number.h` blocks C4 and C5 — filed as **C9**. **(c)** root-vs-subfolder was decided as **root**, deliberately; the re-home is filed as **C10**. Original scope: move the document model (`DocOb`, `CContainer`, `CUG`, `Plug*`, `SynthEditDocBase`, `SynthEditDoc2`). Largest and riskiest stage — split it if it resists. `CContainer.h`'s `friend ExportAsPlugin` declaration moves unchanged. **Three things C2 learned that apply here, from the JOURNAL entry for 2026-08-08:** **(a) decide root-vs-subfolder deliberately.** C2 put its 16 files in `SynthEditLib`'s *root* because root is already an include dir in all three build systems, so the move cost zero include-path edits — a good trade for 16 files and a worse one for 120. Re-homing C2's files later is a `git mv`, so this is still open. **(b) grep the candidates for `#include "../`** before moving them. `checkpoint.h`'s `"../tinyXml2/tinyxml2.h"` never resolved relative to the file at all (`SE16/tinyXml2/` does not exist) — it was matching through the search path, so it gave no warning in either direction. **(c) re-check for `file(GLOB)`** in `SynthEditLib` (there was none at C2). A glob over the destination would put the moved files in `SynthEditLib`'s own target *as well as* EditorLib's, and that surfaces as duplicate symbols at link, far from the cause. |
| A8 | 2026-08-12 | any | **Journal rotation and backlog grooming.** Monthly rotation to `JOURNAL-YYYY-MM.md` with the live file holding the template + current month (runs read "the last four entries", which must keep working); move RESOLVED/DONE rows to an archive section or file; distill any row over ~10 lines into `docs/<id>.md` with the row pointing at it. The journal grew 143KB in four days at 1× cadence; A7 doubles the rate. Accept: JOURNAL.md under 30KB after rotation, no broken links (A3's checker is the proof). May later become a cloud Routine (Jeff's call — public repo only, GitHub-proxy credentials, SE16 never in cloud). **Done 2026-08-12 in [#36](https://github.com/JeffMcClintock/TideSynth/pull/36):** `JOURNAL.md` 192,010 B → just under 30 KiB (34 entries to `JOURNAL-2026-08.md`), `BACKLOG.md` 76,304 → 45,003 B (24 rows to `BACKLOG-DONE.md`), N1/A2/R1/S1b/X4 distilled to `docs/<id>.md`, and `scripts/check-links.py` added — 164 links, 0 broken, exit 1 on a deliberate break. The rotation rule now lives in `JOURNAL.md` itself. **The size target and the four-entry floor conflict and the floor wins**, so the file sits just under 30 KiB rather than under a decimal 30,000. |
| E1 | 2026-08-12 | any | **Bring the audio verification harness in from the superseded `tide-rack` repo** — PLAN "The Eurorack rack" ruling, 2026-08-09. **That repo is now ARCHIVED and read-only, and there is no local copy on any box** (`git clone https://github.com/JeffMcClintock/tide-rack.git` to read it). Do not try to commit anything there; everything below is a one-way port. It works and is the only part of that repo worth keeping: `tools/render_harness.py` (stdlib only, no pip step) drives headless SynthEditCL over `--script`, renders each case in `tests/cases/*.json` to a WAV, and gates the result three ways — render succeeded, **signal present** (peak > −90 dBFS), and null-test against a checked-in golden WAV. Two golden references exist (a naive-oscillator sine and a full MIDI voice) and the Linux CI job in `.github/workflows/verify.yml` downloads the published engine from `synthedit.com/release_1_6/` rather than building it, so it needs no secrets and no engine build. **Port the reasoning with the code, not just the files** — three findings cost a day each to learn and are invisible from the source: **(a)** `-factorysemsfolder` must be an **absolute** path. Relative works for the editor factory and silently fails for the DSP loader — every command still reports `"ok":true`, `==== MISSING MODULES ====` goes to stdout, and you get a well-formed WAV of digital silence. Hence the general rule: **assert on the signal, never on the exit code alone.** **(b)** The null test needs **both** an RMS gate (≤ −100 dBFS, catches broad regressions) and a **peak-sample-difference** gate (≤ −86 dBFS, catches localized ones). A measured 3-LSB nudge across 200 of 96,000 samples yields an RMS residual of only −107.6 dBFS — through any sane RMS tolerance while being an obvious audible defect. Do not drop the peak gate. **(c)** Same-platform renders were **bit-exact** across runs *and* between a Release g++-14 build and a Debug g++-13.3 build of the same engine version, so references survive compiler and build-config changes; the tolerances are for cross-platform float drift, which is still untested because only the Linux lane ever ran. Two open ends that came with it: the harness has **never executed on a real GitHub runner**, and `--update-refs` is deliberately manual — a reference is a claim about how something *should* sound, and seeding one without listening locks in a bug. Decide where it lands relative to the existing `tests/` gtest harness in `SE16` before copying anything. **DONE 2026-08-11 (linux), PR [#34](https://github.com/JeffMcClintock/TideSynth/pull/34).** Landed here, not in `SE16/tests/` — that path is GATED by default and this is an end-to-end Python test driving a published binary, not a gtest unit. Files: `tools/render_harness.py`, `tests/cases/`, `tests/references/`, `tests/README.md`. Reasoning ported in [docs/e1-verification-harness.md](docs/e1-verification-harness.md), including two NEW findings **(d)** `--modules` is not authoritative on a dev box and **(e)** the two null tolerances disagree about float drift. **Verified:** 2/2 cases pass byte-identical to the goldens against engine V1.6.178 — which also extends finding (c) to engine *versions*. Gates positive-controlled in both directions. Two spin-offs: **E1a** (the tolerance defect) and **E1b** (install the CI workflow — an agent may not). |
| P7 | 2026-08-12 | mac | **All PRs merged 2026-08-12 — the audit and its regression test are on the default branches.** [TideSynth#31](https://github.com/JeffMcClintock/TideSynth/pull/31) merged 2026-08-11 and [GMPI_Wrappers#1](https://github.com/JeffMcClintock/GMPI_Wrappers/pull/1) merged 2026-08-12, at Jeff's direction in the P7a session. The row below is as it stood at IN-REVIEW.  **Audited; the crash does not exist on macOS, and the ported test proves the path was genuinely exercised.** Full findings in [docs/p7-resize-audit-mac-x11.md](docs/p7-resize-audit-mac-x11.md). **Verdict:** the Cocoa resize path has no liveness check to go stale — `onResize` ([DrawingFrameMac.mm:434](#)) releases the backing bitmap and stops; reallocation is lazy in `onRender`, where the check and the use are adjacent. `SEVSTGUIEditorMac::onSize` uses nothing after `resizeNativeView`, so the Windows "code after the re-entrant call" has no counterpart. X11's `reSize` ([DrawingFrameX11.cpp:932](#)) is likewise safe — it calls `XResizeWindow` last, and X requests queue rather than dispatch. **What was found instead:** no upper bound on extent in either backend, and — measured, not assumed — `CGBitmapContextCreate` does *not* refuse these sizes (accepts 2178x32672; square limit 131071), so the macOS consequence is memory, not a null deref: a single paint at 16385x600 cost **+253 MiB**. `checkSizeConstraint` is the pre-P4b code and never writes the rect back; for a *resizable* client it returns `kResultTrue`, i.e. actively approves 2178x32672. Two latent TOCTOUs recorded: `onRender` uses `backBuffer` after `drawingClient->render()` without re-checking, and X11 `present()` caches `pw`/`ph` across three re-entrant client calls. **Test:** `GMPI_Wrappers/tests/mac_editor_resize_host.mm`, exit 0 **3/3** on GainGui_VST3 and 1/1 on TIDE_VST3 Release, provably live first (liveness A+B) and still drawing after. Deliberately **no** behavioural change to `gmpi_ui`/`SEVSTGUIEditorMac.cpp` — reasons in the doc; the four follow-ups are P7a–P7d. PRs: [GMPI_Wrappers#1](https://github.com/JeffMcClintock/GMPI_Wrappers/pull/1) (test + CMake, additive) and this repo's P7 PR (audit doc + backlog + journal). Independent — the wrappers PR changes no shipped code. |
| C8 | 2026-08-11 | any | **Audited 2026-08-11 on the macOS box — the "or find out why it exists" half is done; only the deliberate keep-or-delete remains, and it is gated. Full evidence in [docs/c8-it-empty-header.md](docs/c8-it-empty-header.md).** **Recommendation: delete.** The header is a 2002 MFC-era `EmptyIterator` template — creation date read out of its ClassWizard guard UUID (`E50CDB53-05FA-11D6`, a v1 UUID whose timestamp decodes to 2002-01-10), so it predates every repo it sits in. Three dead-code passes each left it one degree more orphaned: `27f28b54e` (2022-03-03) took the last live instantiation `it_visual_ob_list_empty : EmptyIterator<CVisualOb, it_visual_ob>`; `176c6c26f` (2025-01-24) took the archived V1 copies; `671457fc5` (2026-04-13) took `SynthEdit2/it_empty.cpp`, **the last includer anywhere** — a file whose body was entirely commented out, so its only live line was `#include "it_empty.h"`. C2 then moved it because it was on `EditorLib/CMakeLists.txt:74`, and a CMake source list is an inventory, not a dependency graph. **Verified this run, not carried over:** zero `#include` in 8 repos (tracked *and* working trees); no `file(GLOB)` anywhere in `SynthEditLib`; **no `install()`/`export()`/`PUBLIC_HEADER` rules at all**, so "public API surface" overstates it — visible, but not exported and not built (`SynthEditLib`'s own CMake lists none of C2's 16 files yet; that waits on **C6**); no `.vcxproj`/`.filters`/`.pbxproj` reference. No build was run, deliberately: a header with zero includers cannot affect a TU, so the grep is a proof the build could only fail to contradict. **What stopped the deletion:** both files are GATED — `SynthEditLib/it_empty.h` and `SE16/EditorLib/CMakeLists.txt:74` — and STEP 5's exception covers C1–C7 only, which C8 is not. The prompt's remedy ("file the gated part as its own BACKLOG item") is already spent: C8 *is* that item. Filed as a `PROPOSED:` entry in [docs/decisions.md](docs/decisions.md). ~~Default in effect:~~ the dead header stays in the public repo; no functional cost, but every reader of the newly-public `SynthEditLib` meets a misleading orphan whose own comment names a class it does not contain. ~~Decide-by:~~ before **C6** moves `EditorLib/CMakeLists.txt` into `SynthEditLib` — after that this stops being a two-repo change. **Execution once ruled:** delete the file, drop `EditorLib/CMakeLists.txt:74`; the two must go together. **RULED 2026-08-11 by Jeff — option (b), the `PROPOSED:` entry merged unedited in [#32](https://github.com/JeffMcClintock/TideSynth/pull/32) and "go" given in session; the two struck lines above are the escalation it answered, kept for the record.** The GATED exception was **not** widened — see [docs/decisions.md](docs/decisions.md), so this is not a precedent for a later non-C1–C7 item reaching across. **Executed the same day:** [SynthEditLib#4](https://github.com/JeffMcClintock/SynthEditLib/pull/4) deletes the header, [SynthEdit#10](https://github.com/JeffMcClintock/SynthEdit/pull/10) drops `EditorLib/CMakeLists.txt:74`. **They must merge together** — either alone leaves a source list naming a file that does not exist, which CMake tolerates for headers and so fails silently, exactly the mechanism that let the orphan survive three dead-code passes. Flip to DONE when both have merged. |
| G4 | 2026-08-09 | mac | **This box now runs the bootstrap — it fetches `docs/weekly-run-prompt.md` from `origin/main` on every run instead of a frozen copy, and macOS was the last box that did not.** Task `tidesynth-weekly-macos` (no hyphen after `tide`, like Linux and unlike Windows' `tide-synth-weekly-windows`); file `~/.claude/scheduled-tasks/tidesynth-weekly-macos/SKILL.md`; 127 frozen lines replaced by the 42-line bootstrap. `list_scheduled_tasks` showed exactly one task on this box, so there was no risk of the duplicate the item warns about, and cron was left untouched at `0 2 * * 6` — verified after the write that `cronExpression`, `enabled` and `nextRunAt` (2026-08-14) were all unchanged, since only `prompt` was passed. **Verified, not assumed:** all three STEP 0 commands pasted verbatim from `/` — an unrelated cwd — while the tree was parked on `tide/mac/G4-bootstrap-task`: `fetch` exit 0, `show` returns 314 lines, `rev-parse --short origin/main:docs/weekly-run-prompt.md` returns `f0f60a8`. Both of G5's documented false alarms were present and correct in the installed text (the prose "as STEP 4 requires"; the literal `{MACHINE}`/`{PLATFORM}`/`{REPO}` in the substitution sentence). **The frozen copy this replaced dated to 2026-08-06 14:51** and was missing G3, the CRLF guidance, STEP 4's prompt-sha line and all of STEP 5's two-end-states rule — and its STEP 2 would have sent this run at **P7**, whose entire fix lives in `gmpi_ui`/`GMPI_Wrappers`, which that text does not list as ALLOWED: the run would have refused its own item. Took effect next run, so this run stopped here, as the item said. **All three boxes now fetch;** `docs/agent-setup.md`'s state table is closed. |
| G5 | 2026-08-09 | linux | **This box now runs the bootstrap — it fetches `docs/weekly-run-prompt.md` from `origin/main` on every run instead of a frozen copy.** Task `tidesynth-weekly-linux` (no hyphen after `tide`, unlike Windows; file `~/.claude/scheduled-tasks/tidesynth-weekly-linux/SKILL.md`) went from a 180-line frozen copy to the 36-line bootstrap. **Correction to the record while doing it:** that copy was *not* "as originally installed" and did *not* predate PR #4, as [docs/agent-setup.md](docs/agent-setup.md) claimed — it had the ALLOWED/GATED split and the push-the-DOING-mark procedure, and dates to 2026-08-06 17:55, the same generation as macOS's. Cron untouched (`0 2 * * 0`). **Verified, not assumed:** all three STEP 0 commands run verbatim from an unrelated cwd while the tree is parked on a non-`main` branch — `fetch` exit 0, `show` returns 314 lines, `rev-parse --short origin/main:docs/weekly-run-prompt.md` returns `f0f60a8`. **The staleness it removes was real and this run measured it:** the frozen copy was missing G3, the CRLF guidance, STEP 4's prompt-sha line and all of STEP 5's two-end-states rule, and its own STEP 2 would have sent this run at `X4` — closed WONTFIX a day earlier — because a frozen prompt cannot know the queue moved. Three verification traps recorded in [docs/agent-setup.md](docs/agent-setup.md) for **G4** to avoid. Takes effect next run, so this run stopped here, as the item said. |
| C2 | 2026-08-08 | win | **Carve-out stage 2 landed — the leaf files are in the public repo and `SE16/EditorLib/` now holds nothing but its `CMakeLists.txt`.** Sixteen files moved to the **root** of `SynthEditLib`: `checkpoint.{cpp,h}`, `cpu_accumulator.{cpp,h}`, `FrameRateLogger.{cpp,h}`, `imbedded_file.{cpp,h}`, `it_doc_ob.{cpp,h}`, `it_doc_ob_recursive.{cpp,h}`, `it_empty.h`, `it_plug_destinations.{cpp,h}` from `SynthEdit2/`, plus `FuzzyMatch.h` from `EditorLib/`. Root chosen because it is already an include dir in all three build systems, so **zero include-path edits anywhere** — but that trade gets worse at 120 files, so C3 should decide root-vs-subfolder deliberately rather than inherit it. **One content change:** `checkpoint.h`'s `#include "../tinyXml2/tinyxml2.h"` → `"modules/tinyXml2/tinyxml2.h"`; the old form never resolved relative to the file (`SE16/tinyXml2/` does not exist) and was matching through the search path. **Hand-maintained projects:** only `FuzzyMatch.h` had references outside EditorLib's CMake — `SynthEdit2.vcxproj`, its `.filters`, and the SynthEditMac `project.pbxproj` — all repointed. **Verified on Windows:** full Release build of the CMake tree exits 0 with no warnings (SynthEditLib, EditorLib, EditorScreenshot, SynthEditCL, SynthEdit_GMPI, SynthEdit_VST3, TIDE, TIDE_VST3); `ctest` 92/92; no old copy left on disk anywhere in `SE16`; SynthEdit2 (WinUI3) reaches **P8** and only P8, having freshly compiled `FindDialog.xaml.obj` — the TU that includes `FuzzyMatch.h` — from the new location. **Mac is edit-verified only** (both xcconfigs already list the SynthEditLib root, so `FindWindowController.mm` resolves; the mac box should confirm, as it should for C1b). Spun off **C8**; added C2's findings to the **C3** and **C7** rows. `SE16` `d933e5e03`, `SynthEditLib` `6e49dbf`, both on branch `tide/win/C2-leaf-files` — **merging the TideSynth PR does not land either.** |
| C1b | 2026-08-08 | win | **The export code is off `EditorLib`'s source list — the commercial boundary now holds by construction, on every platform's build system.** `ExportAsPlugin.{cpp,h}` removed from `EditorLib/CMakeLists.txt` (the entries formerly at :113-114 are now an explanatory comment), and every app that calls `ExportAsPlugin` compiles the `.cpp` itself, per the `SynthEditApp.cpp` precedent: `SynthEdit2.vcxproj` (ClCompile + the VST3 SDK include path it now needs), the `SynthEditMac` Xcode project (four-coordinate pbxproj addition cloning the `SynthEditApp.cpp` quartet), `SynthEditCL/CMakeLists.txt`, and the deprecated `SynthEditJuce/CMakeLists.txt`. Non-callers (TIDE, EditorScreenshot, SynthEditWayland, tests) untouched — recon of every consumer first, then **adversarially reviewed before landing**, which caught a real one: both mac xcconfigs pinned `VST3_SDK` to the January 3.7.14 CPM hash, inert until `ExportAsPlugin.cpp` became the first Xcode TU to include a VST3 header — shipped un-bumped, C1b itself would have broken clean-mac builds. Both bumped to the 3.8.0 hash (`2df5ae7c…`) in the same commit, with tracking comments. **Verified on Windows:** EditorLib, SynthEditCL, TIDE, TIDE_VST3 all exit 0 zero warnings; `dumpbin` shows no `?ExportAsPlugin@@` in `EditorLib.lib` (control positive); SynthEditCL compiles its own `ExportAsPlugin.obj` and links; TIDE_VST3 still export-free; the vcxproj entry produced `ExportAsPlugin.obj`. **Not verified:** SynthEdit2's link stage (blocked behind pre-existing **P8**) and everything mac (edit-verified only — the mac box should build SynthEdit before trusting it). `SE16` `f313fe37e`. C2 may now proceed. |
| P4c | 2026-08-08 | win | **The resize crash is fixed-by-test, not just fixed-by-reasoning.** Reproduced on demand and A/B'd: **crashes 3/3 with the fixes reverted, survives 3/3 with them in**, same fault frames as the original minidump (`DrawingFrame::reSize` ← `SEVSTGUIEditorWin::onSize`, `mov rax,[rcx]` with rcx = 0). Chasing REAPER's state — what P4c was written to do — was the wrong problem: the dump already recorded the input, so the harness applies it directly. Landed as `GMPI_Wrappers/tests/win_editor_resize_host.cpp`, a Win32 VST3 host that attaches the editor and calls `onSize({0,0,2178,32672})`; build with `-DGMPI_WRAPPERS_BUILD_TESTS=ON`. It refuses to report a pass it has not earned — the D2D device is created lazily and an unfixed `reSize` returns early when there is none, so a benign resize must be *observed to take effect* first or the test exits 3, INCONCLUSIVE. **P2's "MoveWindow does not resize the window" lead was a red herring** — `attached()` makes a child window and `reSize` moves *that*; P2 measured the parent. P4a is verified as the crash fix; P4b is verified as a contract fix (`checkSizeConstraint` now returns `kResultTrue` with the rect rewritten to 2178×600 instead of `kResultFalse` untouched) but cannot be crash-tested, because the crash path bypasses it. X11/macOS resize paths still unaudited. Full write-up in [docs/p4-resize-crash.md](docs/p4-resize-crash.md). |
| X3 | 2026-08-08 | linux | **The Linux `TIDE_VST3.so` now exports `ModuleEntry`/`ModuleExit`, and Steinberg's `validator` loads it — 47 tests passed, 0 failed** (it previously rejected the module outright). Root cause was the stale dependency X3 described, and the fix is one line in `SE16/SynthEditSem/CMakeLists.txt` (ALLOWED): `FetchContent`'s `GIT_TAG origin/main` → the explicit sha `e6a4541`. **`origin/main` never re-fetches** — it is a remote-tracking ref that already resolves in the cached clone, so CMake's update step sees "up to date" and the checkout freezes at the first configure forever; an explicit sha is absent from the clone, so it does fetch (verified: `_deps` moved `032b4d5` → `e6a4541`). Pin chosen over force-refresh or `GMPI_WRAPPER_FOLDER_OVERRIDE` because it is reproducible for B1/C7's public CI and it is *visible in the file*. The bump also carries **P4b**'s `checkSizeConstraint` fix to the win/mac boxes. TIDE, TIDE_VST3, SynthEditCL, SynthEditWayland and SynthEdit_VST3 all build on Linux, no TIDE warnings. Unblocks the empirical half of **X1** (CLAP is still genuinely missing from `FORMATS_LIST`). Spun off **X4**. **Code is in `SE16` `23bee68ce`, branch `tide/linux/X3-vst3-moduleentry` — merging the TideSynth PR does not land it.** See the JOURNAL entry for 2026-08-08. |
| S1a | 2026-08-07 | win | **The module scan and cache are gone from TIDE.** Deleted the `semFolder` assignment, S4's `isSemFolderOverridden` flag, and the `LoadOrScanModuleData()` call from `TideApp::InitInstance` — `SE16` commit `d67bdfbab`, TIDE's own file only. **§9 verified, adapted:** `ModulePath` cannot be pointed anywhere in TIDE (`Application.cpp:139` returns the Documents folder for every settings key), so instead of the as-written recipe, the edited build was run with TIDE's cache file deleted — the module browser renders **pixel-identical** (screenshot SHA-256 match) to the scanning build, and a full REAPER run writes **nothing** under `ProgramData\SynthEdit`. Category tree observed: All/Controls/Conversion/Diagnostic/Effects/Experimental/Filters/Flow Control/Input-Output/Logic/Math/MIDI/Modifiers/Old/Special/Waveform. Per-category contents not audited (A1's soundcard-trio check still wants doing — it belongs to S1b, which this unblocks). TIDE, TIDE_VST3, SynthEditCL all build. Also merged the stranded S4 branch first — see the JOURNAL entry. |
| S4 | 2026-08-07 | linux | **TIDE no longer clobbers the installed SynthEdit's module cache.** `TideApp::InitInstance` now sets `BundleInfo::isSemFolderOverridden = true` beside the existing `semFolder` assignment, so `SemCacheName()` restores its `-override-<hash>` suffix and TIDE gets its own cache file instead of reading and writing the desktop app's `Plugin-Cache-16.xml`. One line in `SE16/SynthEditSem/TideApp.cpp` (ALLOWED); nothing shared touched. Verified by linking the **real** `SemCacheName()` out of `libEditorLib.a` and calling it with the flag both ways — `Plugin-Cache-16.xml` before, `Plugin-Cache-16-override-14603581876e07dd.xml` after. TIDE, TIDE_VST3, SynthEditCL and SynthEditWayland all build on Linux, zero warnings. Spun off X3. See the JOURNAL entry for 2026-08-07. |
| C1 | 2026-08-07 | win | **LICENSE added to `SynthEditLib`** per L1's same-day resolution: ISC, byte-identical to gmpi_ui's text, pushed straight to that repo's `main` (`a2143a4`) at Jeff's direction. The only carve-out stage that needed no C0 approval — it moves no code. |
| W1 | 2026-08-07 | win | **tidesynth.com holding page.** Built at [website/index.html](website/index.html) — one self-contained file, no build step, no JavaScript, and **zero external requests** (verified: no `<script>`, `<link>`, `@import`, `<img>` or `<iframe>`; the only URLs are outbound `<a href>` links). Says what TIDE is, says plainly it is free, links the repo, and carries a donation link as a plain `<a href>` with the destination left as `TODO(jeff)` — the last placeholder. Originally shipped without the phrase "open source" (L1 was unresolved); same-day, L1 landed as ISC and the page now says it, linking the LICENSE. Deployment: GitHub Pages, via [.github/workflows/pages.yml](.github/workflows/pages.yml); go-live checklist is **H1**. |
| P4a | 2026-08-07 | win | **Fixed the resize crash in `gmpi_ui`.** `DrawingFrame::reSize` (`backends/DrawingFrameWin.cpp`) now re-reads `d2dDeviceContext` **and** `swapChain` after `SetWindowPos` returns, instead of trusting the check it made before — `SetWindowPos` *sends* `WM_SIZE`/`WM_PAINT`, and those handlers release the device. Also refuses degenerate and over-limit extents (`maxSwapChainDimension = 16384`), and closes the second latent deref where `swapChain` was never checked at all. **Not verified against the original failure — see P4c.** Committed in `gmpi_ui`, not pushed. |
| P4b | 2026-08-07 | win | **`checkSizeConstraint` now honours the VST3 contract.** `GMPI_Wrappers/wrapper/VST3/SEVSTGUIEditorWin.cpp` writes the nearest acceptable size back into the rect instead of returning `kResultFalse` with it untouched, so a host has something to negotiate with. **Not verified against the original failure — see P4c.** Committed in `GMPI_Wrappers`, not pushed. |
| P4 | 2026-08-07 | win | **Diagnosed the editor-resize crash.** Root cause found from P2's minidumps: a time-of-check/time-of-use bug — `DrawingFrame::reSize` verifies its D2D device context, then calls `SetWindowPos`, which synchronously dispatches `WM_SIZE` into a handler that releases the device, then uses the freed pointer. Full stack, locals and reasoning in [docs/p4-resize-crash.md](docs/p4-resize-crash.md). **Not fixed** — both files are in `gmpi_ui`/`GMPI_Wrappers`, outside the run prompt's ALLOWED *and* GATED lists, and nothing in `SE16/SynthEditSem/` is involved. Spun off **P4a**, **P4b** and **G3**. Did land the one in-scope part P4 asked for: Release builds now emit a PDB (`SE16/SynthEditSem/CMakeLists.txt`), verified with TIDE, TIDE_VST3 and SynthEditCL all building at exit 0. See the JOURNAL entry for 2026-08-07. |
| P2 | 2026-08-06 | win | **Load TIDE in a DAW and record what actually happens.** Loaded the P1 Release and Debug builds in a portable REAPER 7.78; it instantiates and opens its editor. Findings in [docs/state-of-the-prototype.md](docs/state-of-the-prototype.md). Spun off P4 (host-killing crash on editor resize), P5 (plugin is not named TIDE) and U1 (one-view UX gaps); added runtime evidence for S1/S2 and V2. See the JOURNAL entry for 2026-08-06. |
| P1 | 2026-08-06 | win | **Verify the prototype still builds.** Clean CMake configure + Release and Debug builds of `TIDE` (GMPI) and `TIDE_VST3`, both exit 0 with zero warnings. Commands and the MFC/`afxres.h` trap recorded in [docs/building.md](docs/building.md). Spun off P3. See the JOURNAL entry for 2026-08-06. |
| S1 | 2026-08-06 | linux | **Design module enumeration without filesystem scanning.** Design note delivered as [docs/module-enumeration.md](docs/module-enumeration.md). Recommends the hybrid: static registry for modules (already the mechanism — the factory constructor registers all ~157 built-ins before any scan runs), bundle-local resources for prefabs. Nothing implemented, per the item. Spun off S1a, S4, S5. |
| S1 | 2026-08-06 | mac | *(duplicate run — macOS took S1 the same day, before the cron stagger took effect.)* Independent analysis reached the same recommendation; the additive findings are folded into the same note as **Addendum A1–A6** rather than landing a competing document. Spun off G2 and S6. See the JOURNAL entry for 2026-08-06. |

---

## Resolved — questions that were blocked on Jeff

Moved verbatim from [BACKLOG.md](BACKLOG.md) by A8, 2026-08-12. Every row in
that section was `RESOLVED`, so a section headed "agents must not start these"
held nothing an agent could be stopped from starting. The rulings themselves are
indexed in [docs/decisions.md](docs/decisions.md); these rows carry the working
detail behind them.

| ID | Status | Item |
|---|---|---|
| L1 | RESOLVED | **Licence chosen 2026-08-07 by Jeff: ISC**, the same licence as the GMPI and gmpi_ui repos. LICENSE files are committed and pushed in both `TideSynth` (`a58a6f1`) and `SynthEditLib` (`a2143a4` — note `42ce33d` briefly added MIT from a misheard decision; ISC replaced it minutes later, both remain in history). This also completes **C1** and unblocks the "open source" claim, which the holding page now makes. C2–C7 were blocked on C0 alone, and C0 was approved 2026-08-08. |
| C0 | RESOLVED | **Carve-out APPROVED 2026-08-08 by Jeff**, with one standing direction: **keep as much `ExportAsPlugin` code private as practical.** C2–C7 are now TODO rather than BLOCKED, and so are S1b's remaining stages and S8. Two consequences of the direction, both recorded in [docs/carve-out.md](docs/carve-out.md): **(1)** `SynthEditCL` **stays private** — `SynthEditCL/main.cpp:1230` calls `ExportAsPlugin` directly, so a public CLI means publishing the export code or splitting the tool, and TIDE embeds patches rather than exporting them so it needs neither. That closes the second open question. **(2)** The plan as written would have **published the export implementation** — `ExportAsPlugin.{cpp,h}` are on `EditorLib/CMakeLists.txt:113-114` unconditionally, and the plan moves everything on that list, so following it literally leaks the one thing the carve-out exists to hold back. Filed as **C1b**, which must run before C2. Verified harmless to TIDE first: `x TIDE_VST3!*ExportAsPlugin*` returns nothing against the Release build while a control query returns hits, so the linker never pulls the object — the boundary already holds at link time, by accident rather than design. **Repo naming answered the same day: keep `SynthEditLib`** — no rename when the editor moves in. **That closes every open question on the carve-out plan:** C0 approved, licence ISC, `SynthEditCL` private, name unchanged. Nothing on it waits on a decision now, only on the work. |
| G1 | RESOLVED | **Repo created, and made PUBLIC 2026-08-07 by Jeff** — <https://github.com/JeffMcClintock/TideSynth>. Unblocks the "Source" section of the holding page and makes GitHub Pages available free (see H1). **It also makes L1 urgent rather than theoretical:** TideSynth is now in exactly the state PLAN.md criticises `SynthEditLib` for — public, readable by anyone, with no LICENSE file, therefore all-rights-reserved and legally unusable by anyone. Two repos in that state now, not one. |
| H1 | RESOLVED | **tidesynth.com went live 2026-08-08.** `https://tidesynth.com` serves the holding page over a Let's Encrypt cert (SANs cover apex + `www`, auto-renewing), `www` 301s to the apex, and HTTP 301s to HTTPS on all four GitHub edge IPs. Done in an interactive session with Jeff: Pages enabled (Actions source), workflow run, DNS repointed at the registrar, custom domain set, Enforce HTTPS ticked. **The old site was already dead** — the apex pointed at `redirector.servers.netregistry.net`, which served a frameset embedding an Azure static-website endpoint that no longer resolves. **Live email on the domain was preserved** (MX + smtp/imap/pop/pop3/webmail on `nsserver.net.nz`, verified intact on all three nameservers afterwards). Two checklist errors corrected in [docs/hosting.md](docs/hosting.md): the Domainz zone editor is at `/~/dns` and is unreachable from the domain's product page, and the portal's "Export Zone" button 500s — so the zone backup was transcribed by hand into [docs/dns-zone-tidesynth.com.txt](docs/dns-zone-tidesynth.com.txt). `website/CNAME` landed in PR #21 and survived a redeploy. See the JOURNAL entry for 2026-08-08. |
| G3 | RESOLVED | **Fixed 2026-08-07 by Jeff: `gmpi_ui` and `GMPI_Wrappers` are ALLOWED.** [docs/weekly-run-prompt.md](docs/weekly-run-prompt.md) updated — they are shared with SynthEdit and every other GMPI plugin, so keep changes tight, rebuild SynthEditCL too, and stage only your own files (both working copies are routinely dirty with other-platform work). **Each machine's installed task must be reinstalled from that doc** — as with G2, only the machine that made the change carries the new STEP 5. Original finding below. |
| ~~G3~~ | *(was)* | **The run prompt's ALLOWED/GATED split does not mention `gmpi_ui` or `GMPI_Wrappers`.** Both are separate public repos (`JeffMcClintock/gmpi_ui`, `JeffMcClintock/GMPI_Wrappers`) that the TIDE build consumes directly via `GMPI_UI_FOLDER_OVERRIDE` / `GMPI_WRAPPER_FOLDER_OVERRIDE`. They are in neither list, so an agent has no ruling to follow — and TIDE's rendering and windowing bugs land there, not in `SE16`. P4 found this the hard way: the entire fix for a host-killing crash is in those two repos and **nothing in `SE16/SynthEditSem/` is involved at all**, so "do the TIDE-side part" had no TIDE side. Blocks **P4a** and **P4b**. Note `gmpi_ui` is shared with SynthEdit and every other GMPI plugin, so the answer may well be "gated like `SynthEditLib`" — but it needs to be *written down*. See [docs/p4-resize-crash.md](docs/p4-resize-crash.md). |
| G2 | RESOLVED | **The SE16 write-permission conflict in the run prompt** — STEP 5's blanket ban meant no agent could ever write TIDE code, only design notes. **Fixed 2026-08-06** by Jeff: STEP 5 now has an ALLOWED/GATED split — `SE16/SynthEditSem/`, `SE16/TideModules/` and `SE16/SE_IOS_APP/TIDE/` are TIDE's to change; `EditorLib`, `SynthEdit2` and `SynthEditLib` stay behind the C0 gate. See PR #4. **Each machine's installed task must be reinstalled from [docs/weekly-run-prompt.md](docs/weekly-run-prompt.md) — only the macOS box has been updated; Windows and Linux still carry the old STEP 5.** |
